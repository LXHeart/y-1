node_modules
dist
server/dist
# Gradle 本地缓存（GRADLE_USER_HOME 指到仓库内时的 caches/daemon/wrapper 等）
.gradle-local/
# 凭据：忽略所有 .env 变体（历史上 .env.docker / .env.local-db 因只忽略 .env 而被提交）
# 仅 *.example 模板入库，且模板内不得写真值
.env
.env.*
!.env.example
!.env.docker.example
*.pem
*.p12
*.jks
*storage-state*.json
.DS_Store
coverage
playwright-report
test-artifacts

# Production release metadata is generated locally and may contain image references.
.release-state/

# Claude Code session worktrees + plans (local only)
.claude/worktrees/
.claude/plans/

# Codex / Superpowers isolated development worktrees (local only)
.worktrees/

# ZCode 会话产物（计划草稿等），不入库
.zcode/

# Playwright MCP 会话产物（快照/截图/console 日志），不入库
.playwright-mcp/
tmp/

# 任务书浏览器实测产物（截图/会话与造数凭证 json/一次性验收脚本），留磁盘留档不入库
/bt-shots-*/
/dbg-*.png
/verify-*.mjs
/smoke-*.mjs
/snap-*.mjs

# 生产库迁移前备份（含真实数据与凭据，绝不入库；deploy/backups/ 仅留本地磁盘）
deploy/backups/
/smoke-shots/
/smoke-shots-65/
/shots-71/
/shots-72/
/shots-73/
/shots-74/
/shots-74e/
/shots-75/
/shots-final/
