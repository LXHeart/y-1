# 草场项目速览（AI 会话笔记）

> 这是一份**给 AI 会话快速恢复项目理解**的速览笔记，不是项目正式文档。
> **权威真相源仍是 `docs/` 下的设计文档**，尤其是《草场开发进度与续接指南.md》第四节（当前 backlog 的唯一权威）。
> 本笔记只做"速览 + 指针"，不替代上述文档；若与权威文档冲突，以权威文档为准。
> 创建背景：一次完整阅读了项目文档与代码之后整理。
> 机器可读状态源：`docs/status.yaml`；CI 通过 `npm run docs:status` 校验关键状态与覆盖率门槛。

---

## 0. 这是什么项目

**草场（Grassland）**：商家 × 推荐官的**种草推广任务撮合平台**，内置 AI 内容创作工具 + 到店核销消费闭环。
前身是"全功能营销工具库"（短视频提取 / 文章 / 图片评价 / 视频制作等 AI 工具），现整合为草场的 AI 内容创作中心。

**两条 Sandbox 闭环均已跑通**：① 任务撮合（发任务→报名→预留→履约→核验→确认→争议→结算→钱包）
② 到店消费（推广→套餐→支付→核销→分账/退款→评价）。**真实资金被 D-01 门禁阻塞，禁止上线。**

---

## 1. 技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue 3.5 + Vite + TS + vue-router + Pinia（12 页面 / 27 composable / 188 文件）|
| 后端 | Java 25 + Spring Boot 4.1 + WebFlux/R2DBC + Gradle Kotlin DSL |
| 后端规模 | 6 服务 + 3 共享库 + database-bootstrap，**783 源文件 / 317 测试 / 112 Flyway 迁移** |
| 异步 | Kafka(KRaft) + Transactional Outbox/Inbox + Temporal 1.37 Saga |
| 存储 | PostgreSQL（database-per-service 逻辑隔离）+ Redis + MinIO/S3 |
| 加密/媒体 | platform-crypto（KEK 信封加密）+ Java Playwright + FFmpeg |

> **Express/Node 后端已完全移除**（JBE-01~08）。Node 仅用于：前端构建/测试、E2E seed、覆盖率/密钥扫描，外加 intelligence 容器内 Java Playwright 上游要求的 driver。**不存在 Node HTTP 服务或领域 Worker。**

---

## 2. 六服务职责（+ 真实代码量）

| 服务(:端口) | 迁移 | 核心职责 |
|---|---|---|
| `edge-bff`(:8080) | — | 唯一公网入口、RouteManifest、Cookie/Session 解析、签发身份断言、移动端 access token 验签、SSE/Multipart/Range 零聚合透传。**fail-closed 404，无 Node 回退** |
| `identity-service`(:8082) | V31 | 账号/会话/活动身份/组织/门店/成员/权限审核/KYB(OCR+信封加密)/邀请/通知中心/邮件outbox/RBAC(7角色)/移动端token。**32 controller，最大身份域** |
| `marketplace-service`(:8083) | V30 | 任务版本/报名/履约凭证/Verification v2/商家确认/核销/消费订单/声誉Lv1-5/确定性推荐匹配与邀请/运营处置/分析归因。**5 个 Temporal saga** |
| `finance-service`(:8084) | V13 | 双录账本/escrow/积分/provider operation/Webhook Inbox/对账/推荐官钱包/提现 |
| `trust-service`(:8085) | V12 | 争议/7人审判/平票重开/上诉/客服MFA终审/等级投影/风控信号/证据脱敏。**1 个 Temporal saga** |
| `intelligence-service`(:8086) | V26 | AI 控制面(Run/BYOK/平台模型)/抖音B站媒体/创作上下文快照/素材库/创作助手/草稿/视频生成。**33 controller，最大服务** |

**4 个共享库**：`platform-storage`(S3)、`platform-identity-assertion`(断言+access token，被5服务依赖)、`platform-crypto`(KEK 信封，KYB/BYOK 共用 `CRYPTO_KEK_BASE64`)、`platform-http`（服务间 WebClient 连接池、超时和响应体治理）。

---

## 3. 关键设计约束（易踩坑，亲自读代码确认）

1. **Edge fail-closed**：`edge.default-upstream: fail-closed`，未登记/方法不匹配/flag=false 一律 404，**无 Express 回退**
2. **身份断言 keyring**：edge 按 target upstream 用**各服务一把独立钥**签发用户断言（5 audience），移动端 access token 用 kid + previous-keys 三阶段轮换；replay 经 Redis `SET NX + TTL`
3. **内部端点 `/internal/*`**：不在 `/api` 树下，拒绝任一 `X-Forwarded-*`/`Forwarded`（返回 404）；按 issuer/audience 分离断言；通用 `INTERNAL_API_KEY` 已删
4. **金融不可变**：双录 `journal/posting`（借贷合计为零、Finalized 不可改、只能追加 Reversal、余额可由 Posting 重建）
5. **积分闭环**：`consume`→`CreditCharge` 句柄→上游失败 `refund()`，退款键 `refund:<operationId>` 幂等，**用户 abort 不退**（内容已流出）
6. **创作上下文快照冻结**（Slice 16–21，PRD §4.12）：任务进入创作时冻结版本/规则/素材/AI 配置，**DB trigger 拒绝 UPDATE**，所有生成链路绑定同一 snapshot ID 审计；BYOK 只存元数据不存明文
7. **配置不篡改历史**：task_version / package_version / platform_model_config_history / content_asset_version 等不可变快照表
8. **identity 集中授权清单**：`IdentityAuthorizationManifest` 构建期覆盖全部 Controller 映射并 fail-closed 校验 admin/internal/mobile/public 策略；组织与门店的资源级权限仍由 `OrgAuthorization`/`StoreAuthorization` 执行
9. **请求/响应字段不对称坑**（typecheck 抓不到）：请求 `type` / 响应 `identityType`；`materials` 请求收对象 / 响应回字符串。写前端前先逐字读后端契约
10. **轮询/异步中间态**：accept/confirm 是 202，前端必须轮询到终态（accepted/compensated/settled/held）；轮询期间别发额外探测请求（会干扰时序）

---

## 4. 路由速查（哪个 `/api` 路径去哪个服务）

- **→ identity**：`/api/auth/*`(8) · `/api/me` · `/api/organizations` · `/api/recommenders` · `/api/admin/{users,adjust-credits,permission-requests,kyb-requests,recommender-requests}`
- **→ marketplace**：`/api/tasks`（含 `/api/tasks/{id}/recommendations*` 确定性推荐与邀请） · `/api/analytics` · `/api/v2`(消费新契约) · `/api/ops` · `/api/reputation` · `/api/admin/{commerce,analytics,tasks,reputation,reputation-config}`
- **→ finance**：`/api/finance` · `/api/credits` · `/api/admin/finance`
- **→ trust**：`/api/trust` · `/api/admin/trust`
- **→ intelligence**（最多）：`/api/{ai,settings,homepage,media,content-assets,creation-assistant,creation-drafts,creation-contexts,intelligence,comedy-generation}` · `/api/article-generation/*` · `/api/video-production/*` · `/api/video-recreation/*` · `/api/image-analysis/*` · `/api/{douyin,bilibili}/*` · `/api/admin/{ai,content-assets}`

> 真相源：`platform-java/services/edge-bff/src/main/resources/application.yml`（514 行 RouteManifest，每条带 `EDGE_ROUTE_*` flag）。

---

## 5. 跨服务调用拓扑（15 条内部 WebClient，不经 edge）

- identity → finance（admin 调账代理/合规导出与清理）、→ intelligence（KYB 媒体/合规检查与清理）、→ marketplace/trust（合规检查与清理）
- marketplace → finance（escrow/reconciliation/commerce）、→ trust（dispute/resolution）、→ intelligence（verification/media）、→ identity（store-authorizations）
- trust → marketplace（dispute-authorization/reputation）、→ identity（memberships）、→ finance（decision）
- intelligence → finance `/internal/credits`（**积分闭环，refund 键 refund:<consumeId>**）、→ marketplace（ai-entitlement/creation-context）、→ identity（store-authorizations）

> 全部走签名服务断言；`/internal/*` 拒绝 X-Forwarded-*。

---

## 6. 当前状态（2026-08-19）

- **已完成 Sandbox 闭环**：认证/身份/组织/门店/KYB OCR/邀请/通知/RBAC、任务全生命周期、撮合 Saga、Verification v2、争议完整审判、推荐官钱包/分账/提现、声誉等级、消费者 Web MVP、AI 创作中心全工具、AdminView 全域 + OpsConsole
- **当前纯代码主队列**：稳定编号 #22–#45 已全部完成；下一阶段是需要真实 provider、PSP/合规方案或生产环境配合的联调与演练
- **🔴 头号阻塞**：D-01（真实 PSP/存管/合规）—— 真实资金禁止上线

---

## 7. 尚未开发与未生产化项（四类，性质不同）

### A. 纯代码缺口（当前主队列）
- **APP / 微信小程序**：当前只有 Web 和后端移动端 token 协议，原生客户端、原生扫码和端侧 E2E 尚未开发

### B. 已完成的近期增强
- **#40 搜索与报表导出**：任务大厅/商家任务/任务审核、个人/商家/授权/公共素材及公共素材审核、后台用户均支持最长 100 字符的关键词搜索；PostgreSQL `pg_trgm` 表达式索引覆盖任务、素材和用户。商家订单、钱包流水、月账单和商家/后台经营分析均支持安全 CSV 与真实 XLSX 导出，单次最多 10,000 行
- **#38 个人数据合规**：账号级异步 ZIP（Identity JSON + Finance CSV）、短期签名下载、五域注销前置检查、180 天可配置保留期、租约批量清理 Worker、账号注销与不可变 PII 生命周期审计已完成
- **#42 门店媒体库**：门头、环境、菜单/价目表、宣传视频、公开媒体聚合和短时下载 URL 已完成
- **#43 高级图片/视频创作**：裁剪调色、抠图、字幕时间轴、BGM 建议、模板和封面工作流已完成
- **#44 创作结果增强追踪**：创作产物、提示词/模型/素材关系和版本追踪已完成
- **#45 AI 矩阵与安全增强**：公共素材 AI 批量生产、重复度/低原创度和行业 overlay 已完成

### C. 代码框架已完成，卡外部渠道、真实数据或合规
- **真实支付/退款/分账/提现**：Sandbox 账本和 provider 抽象已完成，真实 PSP、签名、Webhook、存管、清结算和合规主体未接入（D-01）
- **真实平台履约指标**：Verification v2 和人工复核已完成，但缺小红书、抖音、B 站、大众点评等官方指标事实源
- **视频真实渠道**：Seedance/MiniMax adapter、回调验签、归档和计价框架已完成，缺真实凭据、公网回调和生产对账演练
- **真实营销归因**：provider Campaign 绑定、HMAC Webhook（曝光/互动/转化/退款）、幂等归因、经营建议、持久化告警与 CSV 导出已完成；仍需部署侧 provider 凭据、真实流量校准和 finance 对账
- **生产 AI 金额结算**：积分包 Sandbox 闭环已完成，按实际 usage 做 credits↔cents 差额结算仍依赖真实 Finance/PSP 方案

### D. 生产基础设施与运行演练
- TLS/LB、外部 HA Kafka/Temporal/MinIO、Canary、真实 Alertmanager/OTel 投递、灾备和容量演练
- SOPS 安全物化和凭据轮换证据门禁已具备；仍需接入真实 Secret Manager、完成供应商侧吊销、滚动/蓝绿发布和回退演练
- registry digest、cosign 签名、SPDX attestation，以及真实故障注入后的 RTO/RPO、数据和资金一致性演练
- Legacy 冻结表 Phase B 清理（收尾项，非核心业务阻塞）

> **状态汇总**：#22–#45 已全部完成。#33 语音转写与 Embedding/语义检索已于 2026-08-19 以可替换 Sandbox Provider 闭环交付，真实 Provider 接入属外部依赖项。推荐官确定性匹配、朋友圈工作流、消费者 Web D-07 也已完成。PRD 明确“不做”的系统自动打标、平均曝光、按粉丝量筛选等不计入缺口。

---

## 8. 常用命令

```bash
# 启动后端栈（默认含 Edge + 5 Java 服务 + 基础设施）
docker compose up -d

# 前端开发（:5173 代理 /api → edge-bff :8081）
npm run dev:client

# 构建 Java 服务（容器外构建 bootJar）
cd platform-java && ./gradlew :services:edge-bff:bootJar

# E2E seed（本地 postgres）
npm run e2e:seed           # 幂等；--reset 先清空

# 测试 / 类型检查 / 构建
npm run test && npm run typecheck && npm run build
```

---

## 9. 阅读路径（本次已精读，新会话可按需重读）

**核心文档**（`docs/`）：
- 草场系统技术总体设计（HLD-v0.1）— 架构基线，1129 行
- 草场产品需求文档（PRD v1.4）— 产品定义，1230 行
- 草场Java微服务技术架构与渐进迁移方案 — 迁移蓝图，575 行
- **草场开发进度与续接指南** — ⭐ 上下文锚点，1521 行，**第四节是当前 backlog 权威**
- ADR：README + D-01(部分采纳,根) / D-02 / D-06 / D-07 / D-11（已采纳）
- 草场旧API兼容契约矩阵、移动端刷新token认证方案设计、生产发布与灾备运行手册、生产Kafka与Temporal部署方案

**代码**（已全扫）：前端 `src/`（188 文件）、`platform-java/`（783 源/317 测试/112 迁移）、`scripts`+`deploy`+`contracts`+`docker-compose`+`nginx.conf`。

---

## 10. 续接入口（新会话怎么开）

1. 先读 `CLAUDE.md`（架构约定，自动加载）+ 本速览
2. 再读 `docs/草场开发进度与续接指南.md` **第四节"当前未完成开发与生产门禁"**（唯一权威 backlog）
3. 确认 `git status` / `git log -1` / 与 origin/main 同步
4. 从第四节"当前开放项速览"挑一个真实剩余项，或按用户指示

**每轮约定**（项目踩坑总结）：
- 先把后端契约逐字读准再写前端（字段不对称 typecheck 抓不到）
- 做完在真浏览器实测一遍（每轮都会抓到单测覆盖不到的集成缺陷）
- 轮询/异步中间态只读 DOM，别发额外探测请求
- 完成后更新续接指南第四节 + 本速览，区分 commit / push / 真实验证
