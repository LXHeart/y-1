# 任务书 #92 卡 C-06 完成报告

## 实现结果

- 卡号：C-06（助手、运行记录、错误恢复与成本提示）
- 完成状态：VERIFIED
- 行为变化：
  - `AiRunHistoryPanel`：可选 `draftId`/`associatedRunIds` props——runs API 无 draft 维度，按草稿 `runIds` 客户端过滤关联记录并在标题显示「关联项目 {id 截断}」；无关联时保持既有全量形态（面板可独立使用）。
  - 成本摘要（§6.6/AC-502）：记录数 + 消耗合计（分）+ token 入/出合计；字段缺失显示「token 消耗：暂无数据」，不伪造 0。
  - 失败行出口（REQ-006）：「重试」「继续编辑」按钮——重试以 `retry-run` 事件携带完整 run 载荷（runId 供工作流沿用既有幂等键，不重复扣费）；`continue-edit` 回编辑面。`AiCreationCenter` 按能力派发到对应创作面（video_generation→视频工坊、image_generation→图片生成、其余→开始创作）。
  - 加载 epoch 守卫（快速切换项目取消旧订阅语义：乱序响应丢弃）；权限撤销（401/403）只显示错误文案、不渲染任何运行详情。
  - `CreationAssistantPanel`：`draftId` prop 显示「关联项目 {id 截断}」标识；建议回写维持既有语义（`queueSave` 只更新草稿编辑器字段并触发自动保存，不触发生成——AC-501 既有行为，新增用例锁定零生成端点调用）。
  - lib 增会话级 `currentProjectId`（仅展示关联；不参与权限/计费判断，§0.2.7），C-03 continue 时置入。
  - 日志脱敏：面板与 lib 不输出 prompt、密钥或资源 URL（无新增 console 输出）。
- 未完成项：无。

## 文件与范围

| 文件 | 变更 | 白名单对应 |
|---|---|---|
| `src/components/AiRunHistoryPanel.vue`（+测试） | 关联过滤/成本摘要/失败出口/epoch（103→155 行） | §9.1 共享面板 |
| `src/components/CreationAssistantPanel.vue`（+测试） | draftId 关联标识（+8 行） | §9.1 共享面板 |
| `src/lib/creation-workspace.ts` | `currentProjectId` 会话态 | §9.1 lib |
| `src/views/ai-center/AiCreationCenter.vue` | 面板接线 + 失败出口派发（964 行=基线） | §9.1 |
| `scripts/acceptance/shot-92-c06.mjs`、`docs/任务书/evidence/92/**` | 截图脚本与证据 | §8.8 |

既有工作区改动未触碰。

## 验证证据

| 项 | 工作目录 | 命令 | 退出码 | 结果 |
|---|---|---|---|---|
| V-008 | 仓库根 | `npm run test -- src/components/CreationAssistantPanel.test.ts src/components/AiRunHistoryPanel.test.ts` | 0 | 22 用例全过 |
| 回归 | 仓库根 | `npm run test -- src/views/ai-center/AiCreationCenter.test.ts` | 0 | 47 用例全过 |
| typecheck / 体积门禁 | 仓库根 | `npm run typecheck` / `node scripts/check-file-size.mjs` | 0 / 0 | AiCreationCenter 964=基线，其余 ≤800 |
| 双主题截图 | 仓库根 | `node scripts/acceptance/shot-92-c06.mjs`（dev + 路由拦截 fixture） | 0 | 4/4 断言过 |

- TC-C06-001：助手关联标识（截断态）+ 改写后零生成端点调用（AC-501）。
- TC-C06-002：失败行双出口、重试载荷带 runId、刷新后状态更新、token 摘要与合计（AC-502）。
- TC-C06-003：项目关联过滤（1 条/3 分，不含未关联 7 分）、权限撤销 403 隐藏详情。
- 截图自查（analyze_image 双主题）：摘要行/操作列/按钮清晰，无溢出截断。
- 证据绝对路径：`/Users/LXH/claude/y-1/docs/任务书/evidence/92/`（v008.log、c06-runs-{light,dark}.png、c06-assistant-{light,dark}.png、c06-screenshot.log）。

## 偏差说明

1. 「按 draftId 过滤」实现为「按草稿 `runIds` 客户端过滤」：runs API（`/api/ai/runs`）无 draft 维度字段，服务端关联需改控制面契约（范围外、§6 未覆盖）；`draftId` 仅作展示标识。其余无偏差。

## 未解决问题

无。
