# 任务书 #92 卡 C-03 完成报告

## 实现结果

- 卡号：C-03（最近项目列表与继续创作）
- 完成状态：VERIFIED
- 行为变化：
  - AI 创作中心导航新增「最近项目」页签（个人模式全量板块第二位；platform 模式过滤逻辑天然不露出）。
  - 最近项目列表：服务端权威（`GET /api/creation-drafts?limit=20&status=active`，updatedAt DESC, id DESC），展示能力标签、标题、来源（workspace.sourceLabel 优先，缺省 ID 截断态）、相对时间、状态徽标；空态引导、加载态、错误态带重试。
  - 继续创作：先 GET 最新草稿（404 → 从列表移除不泄露原因），置入模块级 `pendingContinue` 交接，再按能力派发——article/moments 走 `router.push({ name, query: { draft } })`，video/image 切换到视频工坊/图片生成板块；epoch 守卫保证快速点击只执行最后一次导航；只导航不自动生成。
  - 删除（归档）：二次确认（5s 武装超时）→ POST archive → 立即从列表移除 → 撤销条（8s，aria-live）一次撤销（GET 最新 + PUT 回原状态，不新增接口）；他端已归档（404）→ 刷新列表对齐。
  - 列表函数 epoch 守卫：乱序响应以最后一次为准（取消旧请求语义）。
- 未完成项：无。

## 文件与范围

| 文件 | 变更 | 白名单对应 |
|---|---|---|
| `src/types/creation.ts` | 新建：CreationProject/CreationWorkspacePayload/能力与状态枚举 | §9.1 `src/types/creation.ts`（C-02/C-03 需要时新建） |
| `src/lib/creation-workspace.ts` | 新建：`useCreationWorkspace`（列表/读取/归档/撤销 + `pendingContinue` 交接）、`useCreationSourceContext`（C-01 逻辑迁入）、`parseCreationSourceQuery`/`CREATION_CAPABILITIES`（自 router.ts 迁入并收敛） | §9.1 `src/lib/creation-workspace.ts` |
| `src/views/ai-center/creation/RecentProjectsPanel.vue` | 新建：列表/空态/错误态/二次确认/撤销条组件 | §9.1 `src/views/ai-center/creation/**` |
| `src/views/ai-center/creation/useHotTopicSource.ts` | 新建：热点来源取数与业务流自视图外迁（行为零变更） | §9.1 `src/views/ai-center/creation/**` |
| `src/views/ai-center/AiCreationCenter.vue` | 接线最近项目板块与继续创作派发；C-01 来源上下文与热点块改用 composable | §9.1 `AiCreationCenter.vue` |
| `src/views/ai-center/components/AiCenterNavigation.vue` | `AI_CENTER_SECTIONS`/`AiCenterSection` 增 `recent` | §9.1 `AiCenterNavigation.vue` |
| `src/ai/router.ts` | 解析函数改自 lib re-export（导入面不变） | §9.1 `src/ai/router.ts` |
| `src/views/ai-center/AiCreationCenter.test.ts` | vue-router mock 增 push；既有 tab 断言随契约更新（9 板块/9 次登录请求）；新增 TC-C03-001/002/003 | V-005 指定测试文件 |
| `shot-92-c03.mjs`、`docs/任务书/evidence/92/**` | 截图脚本与证据 | §8.8/§9.1 |

既有工作区改动（#93 批次等）未触碰。

## 验证证据

| 项 | 工作目录 | 命令 | 退出码 | 结果 |
|---|---|---|---|---|
| V-005 | 仓库根 | `npm run test -- src/views/ai-center/AiCreationCenter.test.ts` | 0 | 47 用例全过 |
| typecheck | 仓库根 | `npm run typecheck` | 0 | — |
| 回归（非必需） | 仓库根 | `npm run test -- src/ai/AiAppLayout.test.ts src/App.test.ts src/composables/useCreationDraft.test.ts` | 0 | 39 用例全过（真实路由/既有草稿链路） |
| 体积门禁 | 仓库根 | `node scripts/check-file-size.mjs` | 0 | `AiCreationCenter.vue` 954 ≤ 豁免基线 964，其余 .vue 全部 ≤800 |
| 双主题截图 | 仓库根 | `node shot-92-c03.mjs`（dev + Playwright 路由拦截 fixture） | 0 | 8/8 断言过 |

- TC-C03-001：三条降序、能力/来源/状态徽标/标题断言。
- TC-C03-002：视频→工坊板块 + `pendingContinue` 置入；文章→路由带 `draft` 参；404→移除列表项。
- TC-C03-003：空态引导；二次确认武装→归档→列表移除→撤销条→撤销（PUT 载荷含 expectedVersion=4 与原状态）→恢复；他端 404→刷新对齐且无撤销条。
- 截图自查（analyze_image 双主题）：无截断/重叠/溢出/横向滚动，层级间距协调；meta 灰字与能力 chip 对比度中等但可读（非阻断，token 按规范使用）。
- 证据绝对路径：`/Users/LXH/claude/y-1/docs/任务书/evidence/92/`（v005.log、c03-recent-{light,dark}.png、c03-recent-undo-{light,dark}.png、c03-recent-empty-{light,dark}.png、c03-screenshot.log）。

## 偏差说明

1. **AiCreationCenter 外迁两块既有逻辑**（来源上下文 C-01 块 → `lib/creation-workspace.ts`；热点来源块 → `creation/useHotTopicSource.ts`）：非本卡功能，但该文件在豁免清单「只减不增」红线内（基线 964 行），且 AGENTS.md 组件分层规约要求取数与业务流进域 composable——不外迁则任何本任务改动都会直接违反体积门禁（V-012 lint 必挂）。外迁为零行为变更（既有 44 用例未改断言全绿佐证）。行数 1036→954。
2. **`RecentProjectsPanel` 落位 `creation/` 目录**：白名单前端新文件唯一开放子树；语义上最近项目是创作工作区的项目索引，与 C-04/C-05 创作视图同域。
3. 既有用例的两处断言更新（tab 清单加「最近项目」、登录请求计数 8→9）：导航契约按本书 §8.2 新增入口的直接连坐，非放宽。
4. 其余无偏差。

## 未解决问题

无。
