# 草场 #105F：数字人工作台 阶段五-形象录制与素材交接

| 项目 | 内容 |
| --- | --- |
| 版本/日期 | 2.1 / 2026-09-22 |
| 文档状态 | IMPLEMENTED（2026-09-24）：v2.1规格；各卡按 AUTO_CHAIN 落地并实跑 TC/V；真实第三方渲染/实机仍单独门禁（REAL_NOT_RUN） |
| 实施/用例 | IMPLEMENTED / 本阶段 TC 全 PASS（本地真实 DB/容器/浏览器，证据 test-artifacts/task-105/F/）；真实 provider 项 NOT_RUN |
| 卡/用例 | 5张任务卡，20组Given/When/Then用例；组内边界需参数化 |
| 基线 | 草场f2976aaeb329ee17e9e08a8997bffaabde78d3ac + 当前#103/#104既有修改；OpenTalking固定8c739a5a6f114daf71aeace832a668c3ad60f536 |
| 前置 | 105E S1本地闭环通过；105D音视频输出hook与费用桥可用；#104媒体引用/清理修复验证 |
| 方式 | AUTO_CHAIN：卡级通过自动进入本阶段下一卡；不启子代理，不自动越到未指定的下一本或生产开放 |
| 入口 | [#105总纲](草场任务书-105-数字人工作台开发计划.md) · [PRD](../产品/数字人工作台需求文档.md) · [共享契约](草场任务书-105-数字人工作台共享契约.md) |

## 0. 开始与完成协议

本书供编码模型直接执行。先读本节、§9、§13、§14，再读当前卡列明的共享K节、源码和测试。CARD PASS才推进；已有真实服务授权在同范围内持续有效，不逐卡重复申请。普通本地依赖、Fake、合成数据库与浏览器验证可直接进行。
先保存`git status --short`和当前diff；不得清空工作区或覆盖已有#103/#104修改。行号漂移以本卡真实符号定位；需要改变业务契约/归属/资金规则才停止受影响卡，不因发现未提交文件而全部停工。所有新增接口都应拒绝未知字段，无密码/密钥进入证据。
完成必须同时有实现、非零用例、卡级命令退出0、适用边界/视觉检查、范围检查和对话交接。NOT_RUN不是PASS；真实条件不足只阻塞相应real/beta门禁，保留Fake可继续的明确路径。

## 1. 本阶段目标与范围

实现个人图片形象、独立带声音录制、字幕和幂等入库，录制失败不破坏实时对话或误删素材。

| 需求 | 负责卡 | 验收组 |
| --- | --- | --- |
| DH-R03 | C105F-01、C105F-04、C105F-05 | TC105F-01-01～TC105F-01-04、TC105F-04-01～TC105F-04-04、TC105F-05-01～TC105F-05-04 |
| DH-R09 | C105F-02 | TC105F-02-01～TC105F-02-04 |
| DH-R10 | C105F-02 | TC105F-02-01～TC105F-02-04 |
| DH-R12 | C105F-03、C105F-05 | TC105F-03-01～TC105F-03-04、TC105F-05-01～TC105F-05-04 |
| DH-R13 | C105F-02、C105F-04、C105F-05 | TC105F-02-01～TC105F-02-04、TC105F-04-01～TC105F-04-04、TC105F-05-01～TC105F-05-04 |
| DH-R14 | C105F-03、C105F-04、C105F-05 | TC105F-03-01～TC105F-03-04、TC105F-04-01～TC105F-04-04、TC105F-05-01～TC105F-05-04 |
| DH-R15 | C105F-03 | TC105F-03-01～TC105F-03-04 |
| DH-R16 | C105F-01 | TC105F-01-01～TC105F-01-04 |
| DH-R17 | C105F-01、C105F-02、C105F-03、C105F-05 | TC105F-01-01～TC105F-01-04、TC105F-02-01～TC105F-02-04、TC105F-03-01～TC105F-03-04、TC105F-05-01～TC105F-05-04 |
| DH-R19 | C105F-04、C105F-05 | TC105F-04-01～TC105F-04-04、TC105F-05-01～TC105F-05-04 |

范围外：组织共享、知识库/记忆、对外客服/直播、克隆真人声音、摄像头驱动、任意模型URL、自动交易工具；不顺手改文章/视频工具或前序缺陷。未被本卡白名单列出的文件一律只读。

## 2. 仓库上下文与只读基线

前端Vue/TypeScript，AI独立入口`src/ai/router.ts`及`AiAppLayout.vue`；治理台`src/ops/admin/adminTabs.ts`；Java控制域在intelligence-service，Edge转发；Python实时域为本系列新增目录。现有鉴权、预算、媒体、生命周期只作定点复用。
必读根AGENTS.md与`docs/架构/目录结构.md`。涉及用户/AI UI读根DESIGN.md，治理UI读src/ops/DESIGN.md，共享组件默认根规范。每卡源码摘录是2026-09-21快照，仅作定位；真实实现优先核对签名和上下文。

## 3. 已定技术决策

> 2026-09-22 v2.1修订：K14是模型来源与扩展的统一约束。本地指应用开发、媒体处理和Fake测试，不包括任何模型推理服务；上游源码中的local后端仅是研究事实，不是实施选项。A旧Fake记录保留，v2.1修订复验已通过，可按本阶段前置推进。

Java拥有owner/会话/费用/清理事实；Python持有每会话Binding与媒体，无长期供应商key；Vue只有同源控制资格。首期STT为按键提交后内存整段转写；LLM采用受控计量流；TTS采用24000Hz PCM桥且沿既有平台资助模式；RTC只下行音视频。打断关闭旧peer并重建，租约独立回收。
全部模型均来自第三方服务，通过现有模型控制面使用与扩展；包括STT、LLM、TTS、数字人渲染及涉及模型的形象预处理，禁止本地或自托管模型部署。新增渲染能力沿现有模型/凭据/受信端点/价表链路扩展，不另建模型配置体系，不用env或renderer-profile保存供应商地址、模型名及key。Fake仅用于隔离测试，全链外网请求为0。详见共享K14；配置默认关闭，S1内测、S2个人试点分别验收。

## 4. 目标行为与状态

全局状态迁移见共享K04；本阶段只实现各卡明确触点，不增同义state。session、turn、invocation、settlement、recording、cleanup互相独立：会话ended不表示费用已结或文件已删。失联回收≤45秒、正常结束P95≤5秒，都是待实现/实测目标。

## 5. 业务不变量

首期owner固定个人，orgId不由浏览器提交。账号+epoch变化使旧异步结果无效。每动作requestId创建一次；同键异payload409，同键重放先owner再receipt。费用unknown不填0、不自动重放provider。原始mic不入磁盘、素材库、日志；未同意文本仅有界易失缓冲。

## 6. 接口、字段与内部签名

公开API01～40见共享K03，内部15端点与NDJSON见K07.1～K07.3，管理6端点见K10。金额单位cents、时长ms、UTC时间、JS安全整数、可null/必填及错误码见K01。类型不得用any/无约束map逃避合同；当前卡精确签名见§11。

## 7. 数据、事务与迁移

共享K05是DDL合同，B核心迁移一次创建B/C/D/G核心表；F另建媒体表。执行时按intelligence最大Flyway整数+1替换唯一NEW_DYNAMIC路径中的NEXT，先记录实际文件名，禁止修改V86/V87及其他历史SQL。锁顺序account gate→session→turn/recording→invocation/operation→media；不持事务等待网络。

## 8. UI与用户可观察状态

本阶段涉及UI。共享K11固定布局/组件与设备行为；只用现有token及gl-field/gl-btn-primary/EmptyState/GlModal。新增token必须明暗成对且先补定义（若本卡无style.css写权限，优先现有token；确需新增须修订本卡白名单）。每页loading/empty/error/submitting/disabled/result齐全；1440×900和390×844×明暗截图实际打开检查，另320px、200%缩放、键盘focus。Vue≤800行，composable承载业务，URL独立。

## 9. 写入白名单与禁止项

下面是阶段全局上限；每卡仍只可改其§11表内文件，取两者交集。v2.1新增的§9.1修订表同时补充阶段与指定卡白名单，按表中责任卡执行；其它卡不能借用。NEW仅首创，MODIFY_AFTER表明前卡已创建，不得重建覆盖。只读锚点不是写权限。测试产物可写test-artifacts/task-105/F及卡内指定`test-artifacts/task-105/`子目录（Git忽略），不能放根目录或提交用户内容。

| 完整仓库相对路径 | 动作 | 用途 |
| --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/resources/db/migration/NEXT__digital_human_media.sql` | NEW_DYNAMIC | 按max+1新增，补K05媒体表 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarService.java` | NEW | 归属/授权/真实图片检查 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarController.java` | NEW | DTO/异步状态 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanMediaRepository.java` | NEW | 引用/唯一约束 |
| `platform-realtime/digital-human/src/grassland_dh/avatar.py` | NEW | sandbox路径/单人检查/manifest |
| `platform-realtime/digital-human/tests/test_avatar.py` | NEW | 坏图/多脸/路径/缓存 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/contracts/resource-lifecycle.registry.json` | MODIFY | 新增表/派生对象handler |
| `platform-realtime/digital-human/src/grassland_dh/routes_internal.py` | MODIFY_AFTER_C105D-05 | 扩展F内部命令，沿D mTLS |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanInternalController.java` | MODIFY_AFTER_C105D-02 | 只扩已持久media命令回执 |
| `platform-realtime/digital-human/src/grassland_dh/app.py` | MODIFY_AFTER_C105A-03 | 只internal surface |
| `platform-realtime/digital-human/src/grassland_dh/recording.py` | NEW | 输出AV分支、每段独立文件 |
| `platform-realtime/digital-human/src/grassland_dh/subtitles.py` | NEW | 实际输出段对齐 |
| `platform-realtime/digital-human/tests/test_recording.py` | NEW | ffprobe真实音轨/时长 |
| `platform-realtime/digital-human/tests/test_subtitles.py` | NEW | 中断/零点/UTF8 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingService.java` | NEW | API33～35、幂等与能力限制 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingController.java` | NEW | 只操作本人录制 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-realtime/digital-human/src/grassland_dh/media.py` | MODIFY_AFTER_C105D-05 | 输出AV分支接新recording，不改输入mic路径 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactService.java` | NEW | 受控文件与资产挂载 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactProbe.java` | NEW | 容器/codec/时长/摘要/非空验证 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/media/MediaReferenceRepository.java` | MODIFY | 必要新增引用守卫，保留#104 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/contentlibrary/ContentAssetRepository.java` | MODIFY | 优先只读复用；只加save所需稳定查找 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSaveGcRaceIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `src/views/digital-human/components/DigitalHumanAvatarUpload.vue` | NEW | 文件/授权/处理进度 |
| `src/views/digital-human/components/DigitalHumanRecording.vue` | NEW | 显式提示/partial/保存 |
| `src/views/digital-human/composables/useDigitalHumanRecording.ts` | NEW | start/stop/poll/save/download |
| `src/views/digital-human/composables/useDigitalHumanAvatar.ts` | NEW | upload/finalize/process |
| `src/views/digital-human/composables/useDigitalHumanRecording.test.ts` | NEW | 幂等/换号/重试 |
| `src/views/digital-human/components/DigitalHumanRecording.test.ts` | NEW | 不可用/部分/失败/提交中 |
| `src/views/digital-human/DigitalHumanWorkbench.vue` | MODIFY_AFTER_C105E-01 | 接新增组件，不堆流程 |
| `src/style.css` | MODIFY | 复用现有token/gl类；必要数字人布局与新增token明暗成对，不重写全局主题 |
| `tests/e2e/digital-human-recording.spec.ts` | NEW | 图片→录制→MP4/SRT→库 |
| `platform-realtime/digital-human/tests/test_media_cleanup.py` | NEW | 24h/重启/删除失败 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanMediaLifecycleIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/e2e/fixtures/digital-human.ts` | MODIFY_AFTER_C105E-06 | 合成脸/音频与ffprobe检查 |

禁止：改根依赖/lock（除本书明确列入）、改历史迁移、给旧大视图增业务逻辑、修改组织BYOK/取消退款口径、持久化grant/key/原音频、更新既有生产开关、全仓格式化、删除失败测试、杜撰测试PASS。共用契约/PRD只读；发现实质冲突先按§13给具体修订建议。

### 9.1 v2.1修订补充白名单与执行要求

本表同时属于阶段与所列卡的写入上限；变更仅为K14落实，保留现有其它能力语义。

| 责任卡 | 完整仓库相对路径 | 动作/责任 |
|---|---|---|
| C105F-01 | `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRenderProvider.java` | MODIFY_AFTER_C105D-05：第三方形象检测/准备/查询/删除协议 |
| C105F-01 | `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRenderService.java` | MODIFY_AFTER_C105D-05：按owner/冻结配置处理远端形象，记录清理句柄 |

C105F-01通过既有Java AvatarService编排第三方准备，再调用runtime无模型图片规范化；TC105F-01-01/02/04加入远端0脸/多脸、配置撤销、异步迟到、额外收费不支持及远端删除重试。相关持久列随本阶段原媒体迁移创建，源文件解码不触发本地模型；原DigitalHumanAvatarIT必须覆盖这些分支。

## 10. 卡顺序、依赖与估算

| 卡 | 目标 | 依赖 | 估计人日 | 初始状态 |
| --- | --- | --- | --- | --- |
|  C105F-01 | 自有图片授权、预处理与派生登记 | C105E-06 | 3～5 | VERIFIED（V105F-01 | 自有图片授权、预处理与派生登记 | C105E-01 exit0；证据 test-artifacts/task-105/F/） |
|  C105F-02 | 输出AV录制与分段时钟 | C105F-01 | 4～6 | VERIFIED（V105F-02 | 输出AV录制与分段时钟 | C105F-01 exit0；证据 test-artifacts/task-105/F/） |
|  C105F-03 | 下载、校验与幂等素材保存 | C105F-02 | 3～5 | VERIFIED（V105F-03 | 下载、校验与幂等素材保存 | C105F-01 exit0；证据 test-artifacts/task-105/F/） |
|  C105F-04 | 图片、录制与素材交接UI | C105F-03 | 2～4 | VERIFIED（V105F-04 | 图片、录制与素材交接UI | C105F-01 exit0；证据 test-artifacts/task-105/F/） |
|  C105F-05 | S2媒体闭环与隐私回归 | C105F-04 | 2～3 | VERIFIED（V105F-05 | S2媒体闭环与隐私回归 | C105F-01 exit0；证据 test-artifacts/task-105/F/） |

人日是包含实现/测试/调试的计划区间，不是模型运行时长或日历承诺；第三方服务开通/服务条款核验及实机等待不计。A04(Fake)只要求隔离Fake与缺口清单；H03(conditional-beta)只约束真实开放。

## 11. 逐卡执行规格

### C105F-01 自有图片授权、预处理与派生登记

需求：DH-R03、DH-R16、DH-R17。依赖：C105E-06。必读共享：K02、K03、K05、K09、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/resources/db/migration/NEXT__digital_human_media.sql` | NEW_DYNAMIC | `dh_avatar/recording/cleanup/asset_attachment` | 按max+1新增，补K05媒体表 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarService.java` | NEW | `create/get/delete` | 归属/授权/真实图片检查 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarController.java` | NEW | `API30～32` | DTO/异步状态 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanMediaRepository.java` | NEW | `avatar/recording/cleanup SQL` | 引用/唯一约束 |
| `platform-realtime/digital-human/src/grassland_dh/avatar.py` | NEW | `prepare_avatar` | sandbox路径/单人检查/manifest |
| `platform-realtime/digital-human/tests/test_avatar.py` | NEW | `avatar validation` | 坏图/多脸/路径/缓存 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarIT.java` | NEW | `DigitalHumanAvatarIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/contracts/resource-lifecycle.registry.json` | MODIFY | `media DH resources` | 新增表/派生对象handler |
| `platform-realtime/digital-human/src/grassland_dh/routes_internal.py` | MODIFY_AFTER_C105D-05 | `avatar prepare/artifacts/delete` | 扩展F内部命令，沿D mTLS |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanInternalController.java` | MODIFY_AFTER_C105D-02 | `INTERNAL11 artifact callback` | 只扩已持久media命令回执 |
| `platform-realtime/digital-human/src/grassland_dh/app.py` | MODIFY_AFTER_C105A-03 | `avatar controlled route registration` | 只internal surface |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/media/MediaController.java`，`class MediaController`，快照行57起。先核实真实上下文：

```text
@ConditionalOnProperty(prefix = "object-storage", name = "enabled", havingValue = "true")
public class MediaController {

	private static final Logger log = LoggerFactory.getLogger(MediaController.class);
	private static final com.fasterxml.jackson.databind.ObjectMapper MODERATION_MAPPER = new com.fasterxml.jackson.databind.ObjectMapper();
	private static final Set<String> ALLOWED_MIME_TYPES = Set.of("image/jpeg", "image/png", "image/webp", "image/gif",
			"video/mp4", "video/quicktime", "video/webm", "audio/mpeg", "audio/mp4", "audio/wav", "audio/x-wav",
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `create(actor,UUID mediaId,String rightsVersion,boolean rightsAccepted,UUID requestId): Mono<AvatarItem>。`
- `prepare_avatar(source: bytes, avatar_id: str, revision: int, root: Path) -> AvatarManifest；manifest固定relative keys/sha256/size/contentType，拒绝外部路径。`

**严格执行步骤**：

1. 获取现有media上传ticket+finalize结果，source_media owner必须本人、personal、可用且审核允许；content-type与扩展名都不作为解码证明。
2. 限制10MiB、4096²、JPEG/PNG、单帧；真实解码防炸弹，0脸或多人返dh_image_rejected，不自动换脸/抠人。
3. 先登记avatar+每个派生对象清理handle再写bundle/cache；预处理仅调用现有控制面引用的第三方renderer能力（含人脸检测/形象准备），禁止本地检测/驱动模型；本地只做解码、尺寸和格式检查，不执行用户输入shell。供应商不提供无额外计费且可清理的形象准备时不批准customAvatar能力，另立扩展不得隐式多扣费用。
4. 处理失败保failed/errorCode，无可用catalog项；原media生命周期仍由原库决定，不随失败擅自删他人/shared引用。
5. 用户删avatar有活动会话引用409，无活动则revoked立即拒新建、后台逐派生清；过期授权同处理。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105F-01-01 合法图片

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R16、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarIT.java`；测试名称包含`tc105f_01_01`；跨语言同TC可各有子断言 |
| Given | 本人合成人像/声明版本正确 |
| When | 上传finalize→avatar处理 |
| Then | ready可选、manifest逐key登记 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-01-01、V105F-01-02；`test-artifacts/task-105/F/C105F-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-01-02 坏图/多脸/超限

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R16、DH-R17 |
| 实现位置 | `platform-realtime/digital-human/tests/test_avatar.py`；测试名称包含`tc105f_01_02`；跨语言同TC可各有子断言 |
| Given | 伪mime、炸弹尺寸、10MiB+1、0/2脸 |
| When | 处理 |
| Then | 拒绝且无可运行bundle |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-01-01、V105F-01-02；`test-artifacts/task-105/F/C105F-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-01-03 越权/任意路径

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R16、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAvatarIT.java`；测试名称包含`tc105f_01_03`；跨语言同TC可各有子断言 |
| Given | B mediaId、URL、../字段 |
| When | 创建 |
| Then | 404/422，无访问/落盘到外部 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-01-01、V105F-01-02；`test-artifacts/task-105/F/C105F-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-01-04 部分派生失败

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R16、DH-R17 |
| 实现位置 | `platform-realtime/digital-human/tests/test_avatar.py`；测试名称包含`tc105f_01_04`；跨语言同TC可各有子断言 |
| Given | 写第二个cache失败 |
| When | 恢复worker/删除 |
| Then | 可追踪清理全部已写对象，不误删原shared media |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-01-01、V105F-01-02；`test-artifacts/task-105/F/C105F-01/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105F-02 输出AV录制与分段时钟

需求：DH-R09、DH-R10、DH-R13、DH-R17。依赖：C105F-01。必读共享：K04、K07、K09、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-realtime/digital-human/src/grassland_dh/recording.py` | NEW | `RecordingBranch/SegmentWriter` | 输出AV分支、每段独立文件 |
| `platform-realtime/digital-human/src/grassland_dh/subtitles.py` | NEW | `build_srt` | 实际输出段对齐 |
| `platform-realtime/digital-human/tests/test_recording.py` | NEW | `AV/overflow/interruption` | ffprobe真实音轨/时长 |
| `platform-realtime/digital-human/tests/test_subtitles.py` | NEW | `cue bounds` | 中断/零点/UTF8 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingService.java` | NEW | `start/stop/status` | API33～35、幂等与能力限制 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingController.java` | NEW | `API33～36` | 只操作本人录制 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingIT.java` | NEW | `DigitalHumanRecordingIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-realtime/digital-human/src/grassland_dh/media.py` | MODIFY_AFTER_C105D-05 | `recording branch integration` | 输出AV分支接新recording，不改输入mic路径 |
| `platform-realtime/digital-human/src/grassland_dh/routes_internal.py` | MODIFY_AFTER_C105D-05 | `recording start/stop command` | 扩INTERNAL02判别payload |

**只读源码锚点**：`docs/任务书/草场任务书-105-数字人工作台共享契约.md`，`## K09 `，快照行351起。先核实真实上下文：

```text

## K09 形象、录制、保存与删除

上传复用现有media票据/finalize，不修改原上传全局阈值；dh avatar追加真实decode、像素、单人、许可检查；单人模型检测由现有配置的第三方服务完成，本地只做格式解码。预处理在独立job，key只能由owner/avatar/revision生成，manifest列全部源/派生object引用与sha256，不接受用户提供本机路径/zip。缓存可重建不代表可漏清理。

录制通过ProgramOutputManager新增独立recording branch，连续ProgramClock；采集输出音频与视频，永不接用户mic。start绑定recordingId和startProgramMs，0点=该段起点；先验支持再创建。录制队列max2秒媒体，超限停止该段并标partial=true、dh_recording_overflow，不阻塞对话；无可解码音画失败。并发录制/停止通过DB幂等控制，不调用上游会清空session目录的start。

```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `RecordingBranch.start(recording_id: str,start_program_ms:int) -> Awaitable[None]；stop(reason:str) -> Awaitable[RecordingManifest]；输入仅ProgramAudio/ProgramVideo。`
- `build_srt(segments:list[SpeechSegment],start_ms:int,end_ms:int) -> bytes；序号从1，时间非负且裁剪，不包含用户mic字幕。`

**严格执行步骤**：

1. 只绑定ProgramOutputManager的输出分支，禁止将mic接入录制；不用原生只视频export_flashtalk_recording，独立recordingId避免覆盖。
2. 起点来自program连续钟，画面与音频都按PTS映射，同步录制TTS输出；多轮中断不reset段时钟。
3. ffmpeg encode H264/yuv420p+AAC/48000/faststart，限制5分钟/200MiB；验证构建具备codec且许可证清单登记，无法编码明确failed。
4. 分支队列2秒满则停止录制partial并提示，不阻塞对话；空帧/无音轨不能保存，静音AAC轨仍是音轨，但必须验证测试里有非零输出样本。
5. stop/end/lease过期尽力封装、kill/wait encoder，记录partial与cleanup；两段文件独立，start第二段不能清第一段。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105F-02-01 有声音真MP4

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R09、DH-R10、DH-R13、DH-R17 |
| 实现位置 | `platform-realtime/digital-human/tests/test_recording.py`；测试名称包含`tc105f_02_01`；跨语言同TC可各有子断言 |
| Given | 彩色帧+1kHz输出音频、mic另有2kHz |
| When | 录制5秒并ffprobe/解码 |
| Then | H264+AAC、含1kHz无2kHz、时间同步 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-02-01、V105F-02-02；`test-artifacts/task-105/F/C105F-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-02-02 两段不中覆盖

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R09、DH-R10、DH-R13、DH-R17 |
| 实现位置 | `platform-realtime/digital-human/tests/test_recording.py`；测试名称包含`tc105f_02_02`；跨语言同TC可各有子断言 |
| Given | 同session先后两段 |
| When | 停止并下载第一段 |
| Then | 首段hash不变、各自0点/manifest |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-02-01、V105F-02-02；`test-artifacts/task-105/F/C105F-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-02-03 中断/溢出

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R09、DH-R10、DH-R13、DH-R17 |
| 实现位置 | `platform-realtime/digital-human/tests/test_recording.py`；测试名称包含`tc105f_02_03`；跨语言同TC可各有子断言 |
| Given | 旧turn取消/录制队列超2秒 |
| When | 继续对话 |
| Then | 旧未发内容丢，录制partial真实，对话不被队列阻塞 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-02-01、V105F-02-02；`test-artifacts/task-105/F/C105F-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-02-04 边界与进程退出

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R09、DH-R10、DH-R13、DH-R17 |
| 实现位置 | `platform-realtime/digital-human/tests/test_recording.py`；测试名称包含`tc105f_02_04`；跨语言同TC可各有子断言 |
| Given | 300秒/200MiB/encoder崩溃 |
| When | 强制结束 |
| Then | 有界退出，合法部分标partial或failed，孤儿文件可清 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-02-01、V105F-02-02；`test-artifacts/task-105/F/C105F-02/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105F-03 下载、校验与幂等素材保存

需求：DH-R12、DH-R14、DH-R15、DH-R17。依赖：C105F-02。必读共享：K03、K05、K09、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactService.java` | NEW | `validate/download/save` | 受控文件与资产挂载 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactProbe.java` | NEW | `probe` | 容器/codec/时长/摘要/非空验证 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRecordingController.java` | MODIFY_AFTER_C105F-02 | `API36/37` | 范围下载/保存状态 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/media/MediaReferenceRepository.java` | MODIFY | `digital-human reference/claim integration` | 必要新增引用守卫，保留#104 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/contentlibrary/ContentAssetRepository.java` | MODIFY | `create existing method invocation` | 优先只读复用；只加save所需稳定查找 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactIT.java` | NEW | `DigitalHumanArtifactIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSaveGcRaceIT.java` | NEW | `DigitalHumanSaveGcRaceIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/media/MediaReferenceRepository.java`，`claimCleanup(`，快照行149起。先核实真实上下文：

```text
    /** cleanup 专用：对到期候选取得/刷新 deleting 所有权；stale deleting 可重试；注销引用护栏在 claim 时重验。 */
    public Mono<MediaReference> claimCleanup(UUID id) {
        return transactions.transactional(lockForLifecycle(id).flatMap(ignored -> db.sql("""
                UPDATE media_reference SET status='deleting', updated_at=now()
                WHERE id=CAST(:id AS uuid) AND status IN ('pending', 'active', 'finalizing', 'deleting')
                  AND NOT EXISTS (SELECT 1 FROM media_kyb_retention r
                                  WHERE r.media_reference_id=media_reference.id AND r.released_at IS NULL
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `save(actor,UUID recordingId,UUID requestId,String title,boolean includeSubtitles): Mono<Operation>；原assetId可回读，不能重新上传扣quota。`
- `probe(Path controlledFile): ArtifactInfo；blocking ffprobe只在boundedElastic、固定args、timeout，不拼用户shell。`

**严格执行步骤**：

1. 先检查recording owner/ready/not expired、费用状态如实展示不据pending伪成功；下载签名或流都限制本人，Range校验单区间，非法416不泄内部路径。
2. 读取真实ffprobe及sha256验证H264/AAC/时长±200ms、尺寸档位/size；无声轨或损坏MP4失败，不靠拓展名入库。
3. operation先持久确定assetId与目标object handle，按既有media quota/ownership锁上传校验后创建content_asset个人video；失败重试同id，避免旧20MB用户上传接口挡住受控200MiB生成产物。
4. SRT保存时作为附属media挂到asset，记录asset与subtitle_media_id引用；session/recording删除不撤已保存附件引用，asset删后GC回收。首期不新增SRT素材category。
5. 保存与GC争用同一media引用/claim协议；清理先抢到则save失败不挂已删对象，save先挂载则GC保留，不能用先查后写窗口。
6. F迁移创建dh_asset_attachment，save同事务登记字幕附件；MediaReferenceRepository在生命周期锁后重验活跃asset主引用/附件引用。G定期发现asset已删才释放，不能靠recording存在与否清理。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105F-03-01 产物验证

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R12、DH-R14、DH-R15、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactIT.java`；测试名称包含`tc105f_03_01`；跨语言同TC可各有子断言 |
| Given | 有效/截断/无音轨/错时长四文件 |
| When | save |
| Then | 只有合格文件生成可播放个人asset |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-03-01；`test-artifacts/task-105/F/C105F-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-03-02 重试与响应丢失

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R12、DH-R14、DH-R15、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactIT.java`；测试名称包含`tc105f_03_02`；跨语言同TC可各有子断言 |
| Given | save完成后丢HTTP响应 |
| When | 同键并发重试 |
| Then | 同assetId，quota/对象/资产各一次 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-03-01；`test-artifacts/task-105/F/C105F-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-03-03 GC竞争

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R12、DH-R14、DH-R15、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSaveGcRaceIT.java`；测试名称包含`tc105f_03_03`；跨语言同TC可各有子断言 |
| Given | save与cleanup双事务受控屏障 |
| When | 分别先后提交 |
| Then | 无悬空asset、无误删已挂载对象 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-03-01；`test-artifacts/task-105/F/C105F-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-03-04 下载与过期

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R12、DH-R14、DH-R15、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanArtifactIT.java`；测试名称包含`tc105f_03_04`；跨语言同TC可各有子断言 |
| Given | B下载A、Range越界、过24h临时 |
| When | 请求 |
| Then | 404/416/410正确，已存asset仍走原库生命周期 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-03-01；`test-artifacts/task-105/F/C105F-03/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105F-04 图片、录制与素材交接UI

需求：DH-R03、DH-R13、DH-R14、DH-R19。依赖：C105F-03。必读共享：K02、K03、K09、K11、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `src/views/digital-human/components/DigitalHumanAvatarUpload.vue` | NEW | `upload/rights` | 文件/授权/处理进度 |
| `src/views/digital-human/components/DigitalHumanRecording.vue` | NEW | `recording actions` | 显式提示/partial/保存 |
| `src/views/digital-human/composables/useDigitalHumanRecording.ts` | NEW | `recording state` | start/stop/poll/save/download |
| `src/views/digital-human/composables/useDigitalHumanAvatar.ts` | NEW | `avatar flow` | upload/finalize/process |
| `src/views/digital-human/composables/useDigitalHumanRecording.test.ts` | NEW | `recording flow tests` | 幂等/换号/重试 |
| `src/views/digital-human/components/DigitalHumanRecording.test.ts` | NEW | `record states` | 不可用/部分/失败/提交中 |
| `src/views/digital-human/DigitalHumanWorkbench.vue` | MODIFY_AFTER_C105E-01 | `assembly only` | 接新增组件，不堆流程 |
| `src/style.css` | MODIFY | `DH reusable styles only` | 复用现有token/gl类；必要数字人布局与新增token明暗成对，不重写全局主题 |

**只读源码锚点**：`src/components/GlModal.vue`，`defineProps`，快照行24起。先核实真实上下文：

```text
/** Shared modal shell; business decisions stay with the caller. */
const props = withDefaults(defineProps<{
  title: string
  wide?: boolean
  scroll?: boolean
  /** Keep a pending/unsaved flow open on Escape or backdrop clicks. */
  persistent?: boolean
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `useDigitalHumanRecording(api,account): {recording:ShallowRef<Recording|null>;start:()=>Promise<void>;stop:()=>Promise<void>;save:(title:string,includeSubtitles:boolean)=>Promise<void>;download:(artifact:"mp4"|"srt")=>Promise<void>;dispose:()=>void}。`

**严格执行步骤**：

1. 上传前明确rightsAccepted且用户选择文件；不默认勾选、不请求camera；进度只显示真实传输/处理状态无虚百分比。
2. 录制按钮只在能力声明可用显示可操作；开始确认说明只录数字人输出，录制中持续醒目标志、时长与停止动作，颜色+文字双提示。
3. 停止→finalizing轮询使用请求完成后1秒串行timeout，最多90次，失败停手动刷新；隐藏/换号终止读轮询，晚回包不复活。
4. ready显示预览、partial标识、有效期、保存title/includeSubtitles；saving锁保存按钮但不锁结束会话，重试沿requestId。
5. 成功assetId提供已有素材库链接/选择，不自动新建分镜或跳任务；重新进入在history取记录，不复制正文到画布。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105F-04-01 上传处理

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R13、DH-R14、DH-R19 |
| 实现位置 | `src/views/digital-human/components/DigitalHumanRecording.test.ts`；测试名称包含`tc105f_04_01`；跨语言同TC可各有子断言 |
| Given | 合法/超限/无授权图片 |
| When | 操作 |
| Then | 未确认不提交，失败保配置且不可用 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-04-01、V105F-04-02；`test-artifacts/task-105/F/C105F-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-04-02 录制状态链

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R13、DH-R14、DH-R19 |
| 实现位置 | `src/views/digital-human/composables/useDigitalHumanRecording.test.ts`；测试名称包含`tc105f_04_02`；跨语言同TC可各有子断言 |
| Given | ready→recording→finalizing→ready→saving→saved |
| When | 点击及连点 |
| Then | 一次start/asset，标识连续且有停止动作 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-04-01、V105F-04-02；`test-artifacts/task-105/F/C105F-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-04-03 partial/失败

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R13、DH-R14、DH-R19 |
| 实现位置 | `src/views/digital-human/components/DigitalHumanRecording.test.ts`；测试名称包含`tc105f_04_03`；跨语言同TC可各有子断言 |
| Given | 网络断开或encoder失败 |
| When | 显示结果 |
| Then | 不冒充完整视频；可用部分明确提示，坏产物不可保存 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-04-01、V105F-04-02；`test-artifacts/task-105/F/C105F-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-04-04 轮询卸载

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R13、DH-R14、DH-R19 |
| 实现位置 | `src/views/digital-human/composables/useDigitalHumanRecording.test.ts`；测试名称包含`tc105f_04_04`；跨语言同TC可各有子断言 |
| Given | saving查询在途后换号 |
| When | 迟到回复 |
| Then | 无旧状态/音视频泄漏、不恢复timer |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-04-01、V105F-04-02；`test-artifacts/task-105/F/C105F-04/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105F-05 S2媒体闭环与隐私回归

需求：DH-R03、DH-R12、DH-R13、DH-R14、DH-R17、DH-R19。依赖：C105F-04。必读共享：K09、K12、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `tests/e2e/digital-human-recording.spec.ts` | NEW | `recording full flow` | 图片→录制→MP4/SRT→库 |
| `platform-realtime/digital-human/tests/test_media_cleanup.py` | NEW | `file lifecycle` | 24h/重启/删除失败 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanMediaLifecycleIT.java` | NEW | `DigitalHumanMediaLifecycleIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/e2e/fixtures/digital-human.ts` | MODIFY_AFTER_C105E-06 | `media fixtures` | 合成脸/音频与ffprobe检查 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataObjectCleanup.java`，`class PersonalDataObjectCleanup`，快照行24起。先核实真实上下文：

```text
@Component
public class PersonalDataObjectCleanup {

	private static final Logger log = LoggerFactory.getLogger(PersonalDataObjectCleanup.class);
	private static final int BATCH = 100;
	private static final int MAX_OBJECT_ATTEMPTS = 8;

```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `本卡不新增公开函数；测试必须读取服务端真实对象/下载字节与数据库结果，不能只判断toast。`

**严格执行步骤**：

1. 从真实AI路由上传合成头像、建会话、录两段、打断、结束、下载、save，在现有素材库重新播放同asset。
2. 检查输出音频频谱不含mic标记，SRT时长/UTF8/取消部分、文件SHA与库记录一致。
3. 推进24h、runtime重启、孤儿目录、删除失败重试；cleanup证据必须逐对象，不能只看DB state=deleted。
4. 保存与删除/授权撤销交错保护已存asset；图片下架后新会话拒绝、活动会话按策略终止。
5. 明暗两主题×两视口查看新增图片/录制状态；三引擎不支持AAC时明确平台兼容缺口，不能回退静音格式称完成。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105F-05-01 完整素材链

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R12、DH-R13、DH-R14、DH-R17、DH-R19 |
| 实现位置 | `tests/e2e/digital-human-recording.spec.ts`；测试名称包含`tc105f_05_01`；跨语言同TC可各有子断言 |
| Given | 合成头像+Fake正常声画 |
| When | 浏览器录制→save→素材库播放 |
| Then | 实际有声MP4与正确SRT，owner与hash对应 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-05-01、V105F-05-02、V105F-05-03；`test-artifacts/task-105/F/C105F-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-05-02 24h/重启清理

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R12、DH-R13、DH-R14、DH-R17、DH-R19 |
| 实现位置 | `platform-realtime/digital-human/tests/test_media_cleanup.py`；测试名称包含`tc105f_05_02`；跨语言同TC可各有子断言 |
| Given | 临时段与已存asset各一份 |
| When | 时间推进与重启扫描 |
| Then | 临时删除，已存保留；失败有重试证据 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-05-01、V105F-05-02、V105F-05-03；`test-artifacts/task-105/F/C105F-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-05-03 录制隐私

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R12、DH-R13、DH-R14、DH-R17、DH-R19 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanMediaLifecycleIT.java`；测试名称包含`tc105f_05_03`；跨语言同TC可各有子断言 |
| Given | mic含独立合成标记 |
| When | 检查文件/日志/备份 |
| Then | mic不在产物或持久buffer |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-05-01、V105F-05-02、V105F-05-03；`test-artifacts/task-105/F/C105F-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105F-05-04 功能撤销

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R12、DH-R13、DH-R14、DH-R17、DH-R19 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanMediaLifecycleIT.java`；测试名称包含`tc105f_05_04`；跨语言同TC可各有子断言 |
| Given | 上传后授权撤回 |
| When | 新建/活动/导出 |
| Then | 新建拒绝，活动停止，新导出按撤销策略拒绝 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105F-05-01、V105F-05-02、V105F-05-03；`test-artifacts/task-105/F/C105F-05/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

## 12. 验收命令、边界矩阵与阶段出口

所有命令均为**实施后执行合同**，本次写任务书没有运行业务测试。工作目录“仓库根”指`/Users/LXH/claude/y-1`。每条记录开始结束、退出码、实际测试数量、报告路径；0 tests/全部SKIPPED/缓存未执行不能当本次PASS。

### V105F-01-01 Java/intelligence-service（C105F-01）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-01/V105F-01-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanAvatarIT' --rerun-tasks --no-build-cache
```

### V105F-01-02 Python（C105F-01）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-01/V105F-01-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_avatar.py -q
```

### V105F-02-01 Java/intelligence-service（C105F-02）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-02/V105F-02-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanRecordingIT' --rerun-tasks --no-build-cache
```

### V105F-02-02 Python（C105F-02）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-02/V105F-02-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_recording.py platform-realtime/digital-human/tests/test_subtitles.py -q
```

### V105F-03-01 Java/intelligence-service（C105F-03）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-03/V105F-03-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanArtifactIT' --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanSaveGcRaceIT' --rerun-tasks --no-build-cache
```

### V105F-04-01 Vitest（C105F-04）

目录：`仓库根`；shell：bash。前提/影响：当前npm lock与Node工具链；新增测试必须被vitest include覆盖。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-04/V105F-04-01.log`及工具原生报告。

```bash
npm run test -- src/views/digital-human/composables/useDigitalHumanRecording.test.ts src/views/digital-human/components/DigitalHumanRecording.test.ts
```

### V105F-04-02 类型（C105F-04）

目录：`仓库根`；shell：bash。前提/影响：不运行远程服务。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-04/V105F-04-02.log`及工具原生报告。

```bash
npm run typecheck
```

### V105F-05-01 Java/intelligence-service（C105F-05）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-05/V105F-05-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanMediaLifecycleIT' --rerun-tasks --no-build-cache
```

### V105F-05-02 Python（C105F-05）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-05/V105F-05-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_media_cleanup.py -q
```

### V105F-05-03 S2录制浏览器（C105F-05）

目录：`仓库根`；shell：bash。前提/影响：先有E06新增opt-in扩展点；隔离栈与合成媒体。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/F/C105F-05/V105F-05-03.log`及工具原生报告。

```bash
DH_E2E=1 E2E_SPECS=tests/e2e/digital-human-recording.spec.ts E2E_ENGINES="chromium firefox webkit" E2E_WORKERS=1 bash scripts/ci-e2e.sh
```

### 阶段公共门禁

```bash
npm run typecheck
npm run lint
npm run build
npm run quality:lifecycle
npm run docs:links
npm run docs:status
npm run security:secrets
git diff --check
```

`quality:lifecycle`从B建表开始必需；A阶段仅既有基线回归，不要求不存在的DH表。UI阶段追加适用DESIGN lint和明暗截图自查；设计工具失败/真实设备缺失均记录具体范围。Java修改阶段先执行本卡定向再受影响service的`:check`；H02汇总跨域门禁，不能将本书全完成代替下一阶段。
### E01～E22边界追踪

这是整条个人版链路的精确责任矩阵；专项卡未到达时标DEPENDENCY_PENDING，不宣称当前阶段已通过它。当前卡对应情形仍在其四组TC中参数化实现；不相关场景无需在纯锁文件/纯部署卡造UI。

| 编号 | 情形 | 固定输入与预期 | 专项责任/用例 |
| --- | --- | --- | --- |
| E01 | 空输入 | text="  "、persona=""、requestId缺失；422且零派发 | C105B-03 / TC105B-03-02 |
| E02 | 超长输入 | text2001/persona4001码点、SDP65537bytes；422/413 | C105B-03 / TC105B-03-02 |
| E03 | 重复提交 | 同request同体/异体各并发2次；原资源或409 | C105B-03 / TC105B-03-03 |
| E04 | 网络失败 | HTTP接受后断连；只查原键，不换经济键 | C105D-01 / TC105D-01-02 |
| E05 | 服务端错误 | provider500/半流、runtime不可达；受控code与pending | C105D-03 / TC105D-03-02 |
| E06 | 未登录 | 无cookie且无内部身份；401、零业务写 | C105B-02 / TC105B-02-02 |
| E07 | 无权限 | 非platform_admin调用管理端；403 | C105G-03 / TC105G-03-01 |
| E08 | 数据为空 | 无角色/历史/voice/catalog；空态、不能点开始 | C105E-02 / TC105E-02-03 |
| E09 | 数据过期 | grant29999/30000ms、preflight60秒、内容结束10分钟 | C105D-02 / TC105D-02-03 |
| E10 | 页面刷新 | 现有会话刷新、建立新controller后明确接管 | C105C-02 / TC105C-02-01 |
| E11 | 用户快速切换 | A→B→A，同账号epoch已变仍拒旧回包 | C105E-03 / TC105E-03-02 |
| E12 | 组件卸载在途 | 权限对话晚返回/流未完成；stop tracks并清timer | C105E-04 / TC105E-04-02 |
| E13 | 缺值与非法枚举 | null/缺字段/空白/tone=unknown/多余orgId | C105A-02 / TC105A-02-03 |
| E14 | 数值长度边界 | 0/1/max/max+1、2^53、负金额、小数sample | C105A-02 / TC105A-02-03 |
| E15 | 并发乱序 | 两个控制器接管、旧epoch/旧seq迟到 | C105C-02 / TC105C-02-01 |
| E16 | 已提交响应丢失 | asset save完成丢响应；原id重试一次资产 | C105F-03 / TC105F-03-02 |
| E17 | 跨主体 | B访问A会话/avatar/media、组织上下文不能越权 | C105G-01 / TC105G-01-02 |
| E18 | 中途撤权删除 | 六窗口撤权/删除墓碑；晚结果不得复活 | C105G-05 / TC105G-05-03 |
| E19 | 旧配置/旧缓存 | expectedVersion/catalogVersion失效、旧core升级 | C105H-02 / TC105H-02-02 |
| E20 | 部分成功补偿失败 | 原run结算503、录制partial、事件重复 | C105D-06 / TC105D-06-01 |
| E21 | 时区/金额 | UTC日切与[from,to)、cents整数、冻结价表 | C105G-01 / TC105G-01-01 |
| E22 | 大文件列表长文案 | 10MiB+1头像、200MiB录制、101条limit、长中文布局 | C105F-01 / TC105F-01-02 |

阶段出口：本书本地必需TC/V与范围门禁通过、前置合同无漂移、所有新增资源有handler/test、下阶段能使用已验证真实接口；条件真实缺口列明。不得将FAKE_PASS写成REAL_QUALIFIED。

## 13. 何时阻塞，何时继续

明确阻塞当前依赖路径：公开字段/权限/资金口径矛盾；现有改动与本卡无法共存；必需前卡失败；已批准模型仍无法满足核心协议且需要换方案。报告必须含卡号、准确文件/符号、最小复现、预期/实际、已排查项与最小所需决定。
可继续：行号/空白变化、正常实现细节、本地依赖安装、已有服务复用、Fake测试；第三方服务配置/服务条款证据/测试额度缺少时，只停止真实实验和开放，不阻塞明确无依赖的本地开发。禁止无限重试、假数据冒充真实或降低门槛。

## 14. 对话交接与执行提示词

逐卡在对话给出：卡号及状态；新增/修改文件；TC PASS/FAIL/NOT_RUN与命令/退出码/报告路径；前置既有失败；越界检查；下一卡。无需另建完成报告。用户明确要求运行手册与机器验收证据的文件已在白名单中，按卡更新即可。
可将下面提示词连同本书交给执行模型：

```text
执行 docs/任务书/草场任务书-105F-数字人工作台-阶段五-形象录制与素材交接.md，从 C105F-01 开始，采用 AUTO_CHAIN。
先核对当前卡前置，再读 AGENTS.md、适用 DESIGN.md、任务书第0/9/13/14节和共享契约K14及该卡明确引用的其余K节。
只修改本卡与全局白名单的交集，保留现有工作区修改，不实现未定义的扩展。
每卡完成后真实运行列出的TC/V并汇报，再按依赖自动推进本阶段，不为普通本地步骤逐次等确认。
真实条件不具备如实标NOT_RUN，继续本书中无依赖的本地工作；不能自动部署、打开生产开关或把Fake当真实验收。
业务或资金决策冲突时给出精确阻塞点，不自己换方案。
```
