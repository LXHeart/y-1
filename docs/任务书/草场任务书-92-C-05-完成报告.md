# 任务书 #92 卡 C-05 完成报告

## 实现结果

- 卡号：C-05（视频恢复、结果资产化与任务回跳）
- 完成状态：VERIFIED
- 行为变化：
  - 视频工坊（`VideoStudioView`，= C-03 continue 派发的 video 能力落点）接入 `useWorkspaceAutosave`：capability 'video'，子区即步骤白名单（templates/subtitles/bgm/cover）；保存模板筛选、字幕来源、BGM 输入、封面配置（来源/比例/标题/副标题/排版/AI 提示词）等结构化文本；本地视频帧/音频文件不落库（D-04）；保存徽标挂 tab 行右侧。
  - 恢复（AC-401）：`pendingContinue` 交接 → 子区步骤、封面字段、任务来源回填。
  - 结果资产化（AC-402）：「存入素材库」补齐既有上传链（`uploadContentAssetFile` 三步上传 + `createContentAsset` 登记——与素材库面板同链路），成功后资产 ID 去重追加进 draft `resultAssetIds`（≤20）并立即落库；同一封面重复点击幂等（不再上传/登记/追加）；tab 行显示「已存 N 项结果素材」chip（点击不可用态交素材库面板呈现，保留继续编辑）。
  - 任务回跳（AC-403）：恢复的 draft 带任务来源（workspace.source.taskId）时显示「回到任务」→ `jumpToGrassland('/?task=<id>')`（草场工作台任务详情深链，#77 既有 `?task=` 语义）；只带 id 不携带详情，跳转失败停留本页按钮复位可重试。
  - `useWorkspaceAutosave` 增可选 `collectResultAssetIds`（C-04 三视图不受影响）；`saveProject` 载荷带 `resultAssetIds`。
- 未完成项：视频六段管线（VideoProductionView，草场侧）的分镜/成片 runId 关联——该视图不在本书锚点（V-07 指定 VideoStudioView）与白名单内，runId 写入归 C-06 运行关联线评估（见 C-06 报告）。

## 文件与范围

| 文件 | 变更 | 白名单对应 |
|---|---|---|
| `src/views/ai-center/components/VideoStudioView.vue` | 工作区接线 + 结果资产化 + 任务回跳（615→723 行 ≤800） | V-007 指定视图 |
| `src/views/ai-center/components/VideoStudioView.test.ts` | 新建（V-007 指定新增）：4 用例 | V-007 |
| `src/views/ai-center/creation/useWorkspaceAutosave.ts` | 增 `collectResultAssetIds` 可选项 | §9.1 `creation/**` |
| `src/lib/creation-workspace.ts` | `saveProject` 载荷增 `resultAssetIds` | §9.1 lib |
| `scripts/acceptance/shot-92-c05.mjs`、`docs/任务书/evidence/92/**` | 截图脚本与证据 | §8.8 |

MediaLibraryPanel 复用其上传/登记链路（未改面板本身）。既有工作区改动未触碰。

## 验证证据

| 项 | 工作目录 | 命令 | 退出码 | 结果 |
|---|---|---|---|---|
| V-007 | 仓库根 | `npm run test -- src/views/ai-center/components/VideoStudioView.test.ts` | 0 | 4 用例全过 |
| 跨卡回归 | 仓库根 | `npm run test -- src/views/ai-center/AiCreationCenter.test.ts src/views/ai-center/components/ImageGenerationStudio.test.ts src/views/ai-center/creation/useWorkspaceAutosave.test.ts` | 0 | 61 用例全过 |
| typecheck / 体积门禁 | 仓库根 | `npm run typecheck` / `node scripts/check-file-size.mjs` | 0 / 0 | VideoStudioView 723 ≤800 |
| 双主题截图 | 仓库根 | `node scripts/acceptance/shot-92-c05.mjs`（dev + 路由拦截 fixture + 真实文件上传） | 0 | 4/4 断言过 |

- TC-C05-001：恢复（cover 子区、AI 提示词字段、任务来源、pendingContinue 消费）。
- TC-C05-002：上传+登记载荷断言、`resultAssetIds:['asset-1']` 随 PUT 提交（去重）、chip 计数、重复点击零副作用（幂等）。
- TC-C05-003：`/?task=task-9` 跳转断言 + 拒绝时按钮复位可重试、不泄露详情。
- TC-C05-004：无任务来源无回跳按钮；存量结果素材 chip 计数（过期/无权态由素材库面板负责呈现，chip 非链接不误导）。
- 截图自查（analyze_image 双主题）：chip/徽标清晰、画布与控制区无重叠溢出。
- 证据绝对路径：`/Users/LXH/claude/y-1/docs/任务书/evidence/92/`（v007.log、c05-video-cover-{light,dark}.png、c05-video-result-{light,dark}.png、c05-screenshot.log）。

## 偏差说明

1. 「分镜/成片 runId」锚点差异：本书设想视频工作区含分镜与成片管线，实际 V-007 锚点（视频工坊）是模板/字幕/BGM/封面四子区工具台；六段成片管线在 `VideoProductionView`（草场侧共享视图，不在白名单与 V-007 目标内）。本卡按锚点视图交付恢复/资产化/回跳，runId 关联由 C-06 统一处理。其余无偏差。

## 未解决问题

无。
