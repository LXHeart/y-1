# 任务书 #98 交付报告：推广链接、数据来源与经营度量（评审第三批）

> 任务书：草场任务书-98-推广链接、数据来源与经营度量.md v1.0.0 ｜ 报告日期：2026-09-09
> 审查与收口执行：主程（ZCode）｜基线：a7e4239b + C98-01~04 已提交（c0fce1d4…436f2ea7）
> 总状态：**六卡全部 VERIFIED**（C98-06 整栈 e2e 于本地真实栈实跑通过，环境注记见 §5）

## 0. 收口概要（本报告做了什么）

任务书进入时：C98-01~04 已由前会话实现并提交；C98-05 代码在工作区未提交、未验收；C98-06 未开始。
本次审查发现并处理的问题：

| # | 发现 | 处置 |
|---|---|---|
| 1 | `MerchantCreditIT.tc98_018` 软排序用例顺序敏感：feed `limit=10` 被类内其他用例残余任务挤占，`olderTask` 落榜（首跑实锤 `olderIdx=-1`） | 用例加标题标记 + `q=` 收敛 feed 作用域，单类复跑与全量复跑均绿（92ba2e27） |
| 2 | C98-04 提交漏带 `docker-compose.yml` 的 `EDGE_ROUTE_MERCHANT_CREDIT_MARKETPLACE` 部署接线 | 补提交（92ba2e27） |
| 3 | `OpsCommercePanel` 金额手写 `¥${(cents/100).toFixed(2)}`，违反 §8「复用 money.ts」 | 改用 `formatYuan`（千分位），c1f9cc78 |
| 4 | C98-05 双主题截图缺失 | visual-fixture 扩 `ops` surface（`[data-app="ops"]` 作用域）+ shot 脚本，4 张入库 |
| 5 | C98-06 未实现 | 新写整栈 spec + 单引擎变体脚本 + ci-e2e 透传，本地真实栈实跑 2/2 通过（7a36f11b） |
| 6 | 首跑 Java 全量时 vitest 并发互相争 CPU 出现 7 处超时型假失败 | 排除争用后干净复跑 1591/1591 全绿；失败均为负载 flake，与改动无关 |

**未触碰**：工作区内另一会话的设计系统 v2 改动（AGENTS.md / DESIGN.md / src/ops/DESIGN.md / docs/原型 / 审查修复任务书）原样保留，未纳入本任务任何提交。

## 1. 各卡状态与证据

### C98-01 不透明推广链接（rlid）——VERIFIED（前会话 c0fce1d4）
V60 `referral_link`/`referral_touch`；发放/列表/本人失效端点；下单收 `referralLinkId` 服务端解析（链接级失效 422 可解释、无归因可重试）；分享卡不再拼裸账号 ID；旧参数兼容。IT 覆盖 TC98-001~005。截图 `user-sharecard-rlid-{active,ended}-{dark,light}.png`、`user-commerce-rlid-locked-*`。

### C98-02 归因窗口与可解释归因——VERIFIED（前会话 9a5ea9e3）
触达落行（未登录也记/订单时兜底）、7 天 last-touch、窗口外 422、多链接后触达胜出、attribution-explain 三端同构、归因事实行随订单落库、治理台 rlid 生命周期查询。IT 覆盖 TC98-006~010。截图 `user-commerce-attribution-explain-*`。

### C98-03 粉丝数据来源标注——VERIFIED（前会话 fe96f891）
identity V50 存量回填（幂等可重放+列存在守卫）、SocialAccount 扩展 source/collectedAt（verified/platform_fact 构造器拒绝）、保存时点服务端落定、双端「自报 · 采集于」标注。IT 覆盖 TC98-011~014。截图 `user-reputation-badge-social-*`。

### C98-04 商家信用与软排序——VERIFIED（前会话 436f2ea7 + 本次 92ba2e27 补漏）
四指标读时派生、三档标签+样本不足中性态、`GET /api/merchants/{orgId}/credit`、任务详情/feed 内嵌摘要、同分软排序（页内同刻组重排不动游标）、edge 登记（application.yml + compose 接线 + 门禁测试）。IT 覆盖 TC98-015~019（含本次确定性修复）。截图 `user-taskdetail-merchant-credit-{good,insufficient}-*`。

### C98-05 经营看板与异常订单暂扣——VERIFIED（本次 4901b6ea 后端 + c1f9cc78 前端）
- **V61 `ops_order_hold`**：`UNIQUE(order_id, rule) WHERE status IN ('flagged','held')` partial unique 幂等（V55/V59 同款惯例）；IF NOT EXISTS 全量（重放红线）。
- **三规则看门狗**（`OpsOrderHoldFlagger` 照 C97-03 dispatcher 范式，2 分钟轮询，阈值全 env 可配）：`referral_refund_rate`（同推荐官近窗退款率≥watch）、`appeal_burst`（同推荐官被归因申诉集中度）、`rlid_order_burst`（同 rlid 短窗订单激增）。只写 flagged，**永不碰钱**。
- **人工动作**：confirm（flagged→held + 默认 72h 处理期限，重复处理 409）、release（必填解除说明，held→released）、dismiss（flagged→dismissed）；overdue 视图。
- **结算闸**：`CommerceService.attemptSplit` 执行前 `hasActiveHold`（仅 held；flagged 不影响结算）；解除后下轮扫描自然恢复。
- **看板**：五指标（归因销售额/退款净核销/待结算/已结算/净佣金）各带 source/window/note——归因销售额明示「不宣称增量收益（评审 §10 红线）」。
- **前端**：`OpsCommercePanel`（指标卡+来源脚注、flagged/held 分组、确认/驳回/解除、期限倒计时与超时态），admin_tabs `ops-commerce`（finance 组，platform_admin/cs/finance/risk 可见，23→24 签）；金额复用 `formatYuan`。
- IT：`OpsOrderHoldIT` 6 用例（TC98-020~024 + 驳回/超时/403）；vitest `OpsCommercePanel.test.ts` 3 用例 + `AdminView.test.ts` 可见性改造。
- 截图：`ops-commerce-dashboard-{flagged,held}-{dark,light}.png`。

### C98-06 真实栈整栈集成验收——VERIFIED（本次 7a36f11b，本地真实栈实跑）
- **spec**：`tests/e2e/task98-full-chain.spec.ts`（serial 两用例）：
  - 用例 1（AC-98-25/28/29）：资金型任务全链 发布（审稿/期限）→ 报名（UI）→ 接受（escrow 冻结）→ 草稿送审（自动核验）→ 商家验收 confirm → 观察期 → 结算 settledAt 落账，无人工改库；三端待办回归（推荐官报名/履约中/已完成、商家已接受、消费者订单态）；四域守恒：组织余额 Δ=−bounty（escrow 即扣、capture 后不变）、推荐官钱包入账=bounty+声誉加成（`commissionBonusBpsAtAccept` 快照按 `CommissionBonusPolicy` floor 口径）、声誉 completedCount 递增。
  - 用例 2（AC-98-26/27/29）：rlid 归因链（发放→触达→下单→attribution-explain 五字段）、退款边界两口径（结算前 200/refunded/全额、结算后 409 拒退 + 订单回显 `refundBlockedReason="settled_no_refund"`）、订单域守恒（三方分配=实付）+ commerce 分账后组织/推荐官钱包 Δ 精确等于分配额。
  - AC-98-30：`dualThemeShot`（`E2E_SHOT_DIR` 控制落盘）产出 `e2e98-{recommender-settled,consumer-orders}-{dark,light}.png`。
- **CI 变体**：`scripts/acceptance/ci-e2e-98-only.sh`（E2E_ENGINES=chromium + E2E_SPECS 限定 + `MARKETPLACE_COMMERCE_SPLIT_COOLDOWN_SECONDS_OVERRIDE=15` + 截图目录）；`scripts/ci-e2e.sh` 增 `E2E_SPECS`/`E2E_SHOT_DIR` 透传（缺省空=全量，行为不变）——**白名单偏差**：scripts/ 根不在 §9.1 白名单，此项为变体落地所必需的最小改动（2 行、向后兼容），特此披露。
- **commerce-order-flow.spec.ts** rlid 化 + 三方分配守恒断言（前会话改动，随本批提交）。

## 2. 实跑命令与结果（C98-06，本地 macOS + Docker 真实栈）

本地跑通采用的拉栈方式（`scripts/local/e2e-98-local.sh` + `e2e-98-resume.sh`，git-ignored）：与 ci 变体唯一差异 = 分步拉栈耐心等健康——本地与开发栈并跑时 intelligence/trust 启动常超 compose 150s 依赖健康窗（两次标准变体实跑均卡此处）；播种/平台 AI 配置/断言环境逐行提取自 ci-e2e.sh 原文。

| 步骤 | 结果 |
|---|---|
| bootJar×7 + 全镜像重建 | 成功（镜像含 C98-01~05 全部代码） |
| 分步拉栈（infra→5 Java 服务耐心等健康→全栈） | 成功 |
| e2e:seed:auth + e2e-seed + 平台 AI 三件套 | 成功 |
| `npx playwright test --project=chromium tests/e2e/task98-full-chain.spec.ts` | **2 passed (1.9m)**，退出码 0 |

**实跑暴露并修复的四个 spec 缺陷**（均已修入 7a36f11b）：① playwright API 默认 10s 超时不够（任务创建触发审核采样）→ context 30s；② `contentUrl` 用 xiaohongshu 主页对 HEAD 恒 404（实测）→ `link_reachability=failed` 挂起结算，换稳定 200 的 `https://example.com/`（弱网超时也只 inconclusive，capture 闸只挡 failed）；③ 报名列表分页信封 `{items}` 未解包；④ 种子无独立消费者账号 → 复用 judge1（与既有 commerce spec 同款）。

**退款边界口径注记**：结算后退款实测先撞状态守卫（redeemed 态「当前订单状态不可退款」）而非「已结算」文案守卫——两者都属 #97 拍板的结算后拒；结算事实由 `splitCompletedAt` + `refundBlockedReason="settled_no_refund"` 机器可读断言钉死，不空过。

## 3. 门禁（V98-01~05）

| 命令 | 结果 |
|---|---|
| V98-01 `npm run test -- --run` | 178 文件 1591 测试全绿（退出码 0） |
| V98-02 `npm run typecheck && npm run lint && npm run build` | 全绿（file-size 门禁通过；仅既有 >500 行 WARN） |
| V98-03 `./gradlew :services:marketplace-service:test :services:identity-service:test --max-workers=2`（JDK 25） | BUILD SUCCESSFUL，两服务 0 失败（含修复后 MerchantCreditIT 与新 OpsOrderHoldIT） |
| V98-04 `npx @google/design.md lint DESIGN.md` / `src/ops/DESIGN.md` | 均退出码 0 |
| V98-05 `git diff --check && npm run security:secrets` | 通过（3057 文件扫描） |
| V98-06 整栈 e2e（chromium 单引擎） | 本地真实栈 2/2 通过（§2）；CI 常规跑由 `scripts/acceptance/ci-e2e-98-only.sh` 承接 |

## 4. 本批提交

| commit | 内容 |
|---|---|
| 92ba2e27 | C98-04 补漏（compose 接线）+ MerchantCreditIT 确定性修复 |
| 4901b6ea | C98-05 后端（V61/三规则/人工动作/结算闸/看板/edge 登记） |
| c1f9cc78 | C98-05 前端（OpsCommercePanel + 接线 + 测试） |
| 7a36f11b | C98-06（整栈 spec + ci-e2e 透传 + 变体脚本 + commerce spec rlid 化） |
| （本报告） | 交付报告 + evidence/98 截图 22 张入库 + 书状态收口 |

## 5. 偏差与遗留

1. **scripts/ci-e2e.sh 透传改动**超出 §9.1 白名单（见 §1 C98-06 披露）——2 行向后兼容，为变体脚本必需。
2. **e2e 三引擎矩阵未跑**：按任务书本意走单引擎变体（chromium），firefox/webkit 留给 CI 全量；本地已证 spec 逻辑正确。
3. **收口时工作区存在另一会话的设计系统 v2 未提交改动**（AGENTS.md/DESIGN.md/src/ops/DESIGN.md/docs/README.md/docs/原型/审查修复 01-04 任务书）——与本任务无关，未纳入提交，原样保留。
4. **contentUrl 语义**：e2e 用 example.com 而非真实平台链接（xiaohongshu 对 HEAD 恒 404 会让核验 failed 挂起结算）；真实平台链接的核验语义由 IT 层（mock LinkReachabilityChecker）覆盖。

## 6. 截图清单（docs/任务书/evidence/98/，共 22 张）

C98-01/03/04（前会话 14 张）+ C98-05（ops-commerce-dashboard-{flagged,held}-{dark,light}）+ C98-06 e2e（e2e98-recommender-settled-{dark,light}、e2e98-consumer-orders-{dark,light}）。视口 1440×900（e2e 截图 fullPage）。
