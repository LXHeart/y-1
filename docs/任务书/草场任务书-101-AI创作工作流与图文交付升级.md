# 开发规格：AI 创作工作流与图文交付升级（草场任务书 #101）

> 模板依据：任务书模板 2.4.1；规格版本：1.2；创建日期：2026-09-12；本地复核日期：2026-09-14。
> 目标仓库：`/Users/LXH/claude/y-1`；核验分支：`main`，本次不创建开发分支。
> 原始代码／覆盖率基线：`7d5797756e3c0fb6df0e921637213eede384900e`；复核对象：`main@a08101e0` 加当前工作区修改，文件指纹见 §14.1。
> 上游研究基线：`JimLiu/baoyu-skills@1567581c26ec29f4216c6e6835415bf30343b0e3`。
> 文档状态：`IMPLEMENTATION_REVIEWED`；代码状态：`VERIFIED`（仅本地）；本地验收：`LOCAL_PASS`；真实模型／公众号：`NOT_RUN`；生产启用：`NOT_RUN`。
> 任务卡：24 张，`C101-01`～`C101-24`；执行方式：`AUTO_CHAIN`，按卡号串行；起始卡：`C101-01`。
> 规格作者：Codex；执行者：接手本任务的开发者／编码模型；卡级与最终集成责任人：执行本任务的主程。真实渠道验收由主程与所选公众号账号持有人共同完成。

**用途**：本文件把[结合研究](../reviews/baoyu-skills-ai-creation-integration-2026-09-12.md)转成实施合同，覆盖目标、契约、迁移、UI、分卡、测试和发布。24 卡实现已按用户后续指派完成本地复核与修正；§2 保留规格写作时的历史基线，当前验证结果以 §14.1 为准。本地通过不构成真实付费出图、授权公众号验收或生产上线的完成记录。

## 0. 执行协议

### 0.1 阅读与推进

1. 开始编码前读根 `AGENTS.md`、本节、§2、§9、§13、§14；读当前卡明确引用的契约和现有源码。UI 卡额外读适用 `DESIGN.md`。
2. `AUTO_CHAIN` 表示当前卡全部验收通过后进入下一卡，不要求逐卡询问。它不要求创建新任务或启动子代理。
3. 不将“卡代码写完”“本地验证通过”“真实渠道验证通过”“生产已开放”混为一个状态。每个里程碑按 §12 单独判定。
4. 事实标为 `FACT`；本文选定的目标方案标为 `DECISION/NEW`；合成请求、样稿和账号标为 `FIXTURE`。字段名和枚举属于契约，不是可自行替换的示例。
5. 同名源码有新实现时先核对语义，保留他人工作。行号变化不阻塞；需要改变目标行为、权限、数据模型、计费口径或写入范围时按 §13处理。
6. 不以本文替代系统、开发者和当前用户指令。本文不授权生产迁移、真实账号操作或未被用户授权的付费调用。

### 0.2 完成定义

每卡须同时满足：写入范围符合 §9；依赖交付实际存在；正向、边界、失败和并发验收通过；相关测试确实被发现并执行；无未解释的类型／构建错误；UI 截图和键盘检查完成；按 §14 在对话记录命令、退出码、证据和剩余限制。

卡状态使用 `NOT_STARTED / IN_PROGRESS / BLOCKED / IMPLEMENTED / VERIFIED`。`IMPLEMENTED` 不能替代 `VERIFIED`；必须验证未执行时写 `NOT_RUN`。默认不新建完成报告或交付总结文件，测试工具生成的产物按规定保留。

## 1. 目标、范围与交付

### 1.1 一句话目标

让用户在现有 AI 创作中心中，从真实资料或已有原稿出发，完成可编辑、可恢复、可追溯的图文策划、图片制作、文章排版和实际文件交付，并可将确认过的公众号文章存入自己绑定账号的草稿箱。

### 1.2 三个里程碑

| 里程碑 | 卡 | 可独立使用的结果 | 开放条件 |
|---|---|---|---|
| M1 图卡生产 | 01～13 | 原稿导入、改编建议、推荐式图卡计划、参考图生成、候选采用、恢复和现有交付面板贯通 | 本地集成通过；真实参考图模型验收通过后才开放该模型的参考模式 |
| M2 文章与文件交付 | 14～18 | 段落配图、封面、保留原文的排版预览、Markdown／TXT／HTML／ZIP | M1 + 文件真实性、文字一致性和媒体权限验收 |
| M3 公众号草稿与收口 | 19～24 | 自有公众号绑定、上传与草稿写入、结果核实、迁移和最终回归 | M2 + 真实授权账号草稿写入／回读验收；生产启用另有授权 |

三阶段均在 #101 的代码范围内。M1/M2 可以先交付；M3 缺少真实账号时继续完成本地模拟验收，保留“真实渠道未验收”，不能宣称整项完成。

### 1.3 需求追踪

| REQ | 必须交付的行为 | 负责卡 | 主验收 |
|---|---|---|---|
| R101-01 | 有版本的场景模板目录，保留旧平台矩阵与路由 | 01、03 | AC101-01、03 |
| R101-02 | 粘贴／导入 UTF-8 原稿，完整保存并定位段落 | 02、03 | AC101-02、03 |
| R101-03 | create/adapt/format 真正分流，format 不自动改写 | 03、04、16、17 | AC101-03、04、16、17 |
| R101-04 | 改编／标题摘要建议先预览，显式应用到指定草稿版本 | 04 | AC101-04 |
| R101-05 | 图卡推荐策略、逐页目的、原文依据和布局可编辑 | 05、06 | AC101-05、06 |
| R101-06 | 模型协议区分通用参考、人物参考和文字约束 | 07 | AC101-07 |
| R101-07 | 每张图片的运行、结果和结算可恢复；同操作不重复调用 | 08、10 | AC101-08、10 |
| R101-08 | 封面参考链、部分成功、取消、单图重做和候选采用 | 09、10、11 | AC101-09、10、11 |
| R101-09 | 工作区只保留稳定引用，旧草稿和旧接口仍可用 | 02、06、12、24 | AC101-12、24 |
| R101-10 | 文章封面和正文图片与段落绑定，共享视觉偏好 | 14、15 | AC101-14、15 |
| R101-11 | 排版输出可预览且文字、来源和媒体可核对 | 16、17 | AC101-16、17 |
| R101-12 | 指定版本导出真实文件，不把 JSON 清单当作图片包 | 18 | AC101-18 |
| R101-13 | 自有公众号凭据加密、校验、轮换、断开且不泄露 | 19、20 | AC101-19、20 |
| R101-14 | 公众号只写草稿箱，返回草稿 ID 并回读核实 | 21、22 | AC101-21、22 |
| R101-15 | 超时结果未知不盲目重发；可凭真实草稿 ID 核实 | 21、22 | AC101-21、22 |
| R101-16 | 任务快照、素材权限、预算、BYOK、安全检查不被新流程绕过 | 04～12、14、18～23 | AC101-23 |
| R101-17 | 两创作入口与治理设置均符合设计、移动和键盘要求 | 03、06、07、10～13、15、17、18、20、22、23 | AC101-23 |
| R101-18 | 分批开关、升级／回退、审计、证据与质量门禁齐全 | 01、13、23、24 | AC101-24 |

### 1.4 明确不做

- 不恢复独立信息图／SVG 创作品类。历史[任务书 #55](草场任务书-55-信息图生成.md)文首明确记录“品类取消”；其遗留 migration 不代表功能仍有效。
- 图卡继续内嵌文章／图文流程，不恢复 #54 已撤销的独立图卡入口和额外制作方式层。
- 不做通用 skill 上传／执行市场，不执行用户提交的脚本、`SKILL.md` 或任意 HTTP 工具。
- 不接入非官方 Gemini Web、X Cookie 接口、本机个人 Chrome 登录态或微信群摘要。
- 不新增 X／微博／YouTube 发布平台，不做翻译、漫画、PPT、自动检索、通用网页采集、一稿多平台自动分发。
- 公众号首版只支持单篇 `article_type=news` 草稿；不做公开发布、群发、定时发布、平台效果统计或公众号多成员协作授权。
- 不改视频镜头、合成、TTS、计价和 #100 画布。现有视频封面工具仅复用已登记的封面预设；生成工具不自动替换视频结果。
- 不做原生富文本编辑器／协同编辑器／像素级文字覆盖修图。字图一体仍为现有图卡默认制作方式。

### 1.5 不许顺手修

不全站清偿 UI token、不批量替换旧提示词、不修无关财务／任务／身份问题、不改老 Flyway 文件、不重排现有任务书、不更新真实 `.env`、不升级全仓依赖。仅命中本卡路径和行为所必需的修正可实施。

### 1.6 用户与入口

- 任意正常登录用户可管理自己的创作草稿、来源、计划、候选及自有公众号连接；游客继续使用现有试用流程，本任务不增加游客付费能力。
- 独立 AI 应用：`ai.html` → `src/ai/router.ts` → 既有创作视图。
- 工作台任务创作：保留用户端内嵌入口、来源锁定、接受时快照和组织预算归属。
- 公众号连接首版归登录账号所有；不从 URL 或请求体接受连接的 owner/org。商家账号也管理自己的连接，组织共享连接另立范围。

NEW studio的原稿、文本建议、视觉计划、制作和排版只接受`capability=article`、`contentForm=graphic`且平台在小红书／抖音／公众号／知乎中的草稿；服务端也验证此范围。其他项目继续旧流程与旧manifest导出。共享DeliveryPanel新增可选`studioExportEnabled:boolean`（默认false），仅符合该范围的宿主传true；不能给视频／朋友圈／点评悄悄打开新文章导出。

## 2. 仓库上下文与核验基线

### 2.1 已确认事实

| FACT | 当前符号／文件 | 必须保持或补齐的点 |
|---|---|---|
| 三入口、两套设计 | 根 `DESIGN.md`；`src/ops/DESIGN.md`；`src/ai/router.ts` | 不新增第三套设计、不漏验任务入口 |
| 平台与来源 | `src/config/ai-platform-capabilities.ts:resolveWorkflow`；`src/types/ai-creation.ts` | 九平台、五类来源；reference 目前进入视频分析 |
| 工作区上限 | `creationassistant/CreationWorkspace.java` | JSON 64 KiB，resultRefs/runIds 各最多 20；大正文、提示词、候选历史不得堆入 workspace |
| 保存语义 | `CreationDraftService.save`；`useCreationDraft.savePatch` | 整行保存 + expectedVersion；不能假设 PUT 是部分字段 PATCH |
| 共享写队列 | `src/lib/creation-draft-session.ts`、`useWorkspaceAutosave.ts` | 800ms 防抖、账号 epoch、冲突保稿；新服务端应用动作需接入这一队列 |
| 原文加工字段 | `CreationBriefEditor.vue`；`CreationBriefInput` | 模式已有，缺少完整原稿输入和真正的非生成路径 |
| 图卡 | `useCardSeries.ts`；`CardSeriesService`；`CardSeriesPrompts` | 稳定 cardId、1～9 张、角色、文字风格锚、单卡生成与操作记录已有 |
| 旧操作读取 | `CardSeriesController.operation` | Controller 已有 GET，但 Edge 现有图卡登记需补读取方法覆盖 |
| 参考图片 | `ImageGenerationClient.generate` | MiniMax 支持人物参考；普通兼容路径忽略实际图片，只做描述增强 |
| 写作模板 | `CreationStyleSkillCategory` | 只有标题套路／体裁／文风，不能冒充完整工作流定义 |
| 配图 | `ArticleImagePrompts.recommendation`、`ImagePlacement` | 固定推荐 3～4 张、位置为字符串，需补稳定段落定位 |
| 导出 | `CreationDraftExportService`；`src/lib/creation-export.ts` | 当前仅 bundle-manifest；已按版本生成授权媒体下载项 |
| 图片执行 | `IndependentImageGenerationService`、`TaskImageGenerationService` | 用 `AiExecutionService.prepareMediaExecution`；目前每次内部生成随机 operationId，需要面向可恢复任务的明确操作绑定 |
| 财务口径 | `CreditFeature`、`AiExecutionService` | 文本走现有功能键；图片 feature=null 按预算 cents 记成本，不新建用户图片积分收费规则 |
| 后端工具 | intelligence `build.gradle.kts` | 已有 jsoup 1.21.1、Java Playwright 1.55.0、Temporal 1.37.0、Java 图像和 ZIP 标准库 |
| 迁移 | intelligence migration 目录 | 当前最高 V76；本书预占 V77～V83，仅 intelligence 新增迁移 |

本表 Java 包根为 `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/`。精确可写文件见 §11；以上缩写只用于定位，不授予目录级写权限。

### 2.2 版本、已改文件与测试事实

- 当前 Node `v22.22.3`、npm `10.9.8`；Gradle wrapper `9.6.1`；项目 Java toolchain 25、Spring Boot 4.1.0、Vue 3.5、Vite 7、TypeScript 5.9。Java 实际运行时及 Docker 在开工时按 §12 重新探测。
- 写作前已有改动：`docs/reviews/README.md` 已修改，结合研究文档未跟踪。它们来自上一轮研究，保留；本次增加规格及索引，并补充研究中发现的历史撤销边界。
- 本轮仅编写文档，不跑业务测试、数据库迁移和真实 AI／公众号操作。本文的新测试全部由对应卡实现后运行，作者未执行的项目一律是 `NOT_RUN`。
- 根规范适用于用户／AI 页和个人 BYOK 设置；治理台凭据／平台模型控件按 `src/ops/DESIGN.md`。两规范均为 2026-09-09 的 grassland 系工作台规范，旧模板的风格名称不沿用。

### 2.3 写作期间的并行工作与后续合入

写作期间HEAD推进到`86b763c41e0f81847793408120fa1b4d1469bb2d`，另有MinIO镜像来源修复、视频测试及[#102画布复盘修复规格](草场任务书-102-创作画布复盘修复与真实闭环验收.md)。本书保留原业务核验基线，不覆盖这些并行改动，也不把#100原完成声明当成当前测试证明。

#101与#102共享CreationDraftService/Repository、FrozenTextExecutionService、creation-export.ts及DeliveryPanel。实施时同一文件串行合入，并在开卡前读实际已合入版本：复用已有原子草稿写入口；保留#102的画布锁和权限规则；新增观察回调不改已修复文本执行语义；DeliveryPanel若已有`beforeExport():Promise<number|false>`，必须先等待该钩子并使用其返回的已保存版本，false时不下载，再执行本书格式分派。未提供钩子的旧宿主保持原行为。

两书的计划表不同：#101只锁本书visual plan，不能把其draft→visual-plan顺序套到#102的canvas agent plan；调用共享草稿方法时遵循已定义的公共draft锁边界，不反向获取另一领域的plan锁。#102不新增Flyway版本，本书V77～V83没有编号冲突。无需等待整个视频修复批次才能做图文，但共享方法若出现实质契约差异，先按§13解决受影响卡，不覆盖前序修复。

## 3. 唯一技术决策

| DECISION | 方案 |
|---|---|
| D01 架构 | Vue＋Java intelligence；提炼上游方法和模板，不安装整个仓库到生产、不新增 Node/Bun HTTP 后端 |
| D02 模板 | 新 `contracts/creation-recipes.v1.json` 与 `contracts/creation-visual-presets.v1.json`；前后端共享合法ID和本地化视觉预设，公开目录不带内部prompt；版本固定，旧版本可读，停用版本不接受新生成 |
| D03 原稿 | PostgreSQL 保存不可变来源文档及块定位；workspace 仅存引用。UTF-8 TXT／Markdown／粘贴文字；不自动请求其中 URL |
| D04 模式 | create 继续既有生成；adapt 产生可预览改编建议；format 用确定性渲染，自动改标题、缩写和摘要生成均关闭 |
| D05 计划 | 服务端保存结构化视觉计划，支持单套推荐及显式再生成其他策略；1 次选择只执行 1 次策划，不默认出三套 |
| D06 图像协议 | 新增受控 provider 名 `openai-image` 表示原生图片生成／编辑协议。普通 openai-compatible 保持旧行为；MiniMax 人物参考不升级为通用风格参考 |
| D07 生成 | 新版图卡／文章图片使用可恢复异步视觉任务；沿用 card_series_operation 并增加子项表，不新建另一套 ai_run/计费记录 |
| D08 编排 | 使用已有 Temporal 基础设施增加专用视觉 task queue；业务与状态仍在领域服务和数据库；不改视频 workflow |
| D09 幂等 | 父操作 requestId + 输入 hash；每个图像子项固定 executionOperationId。外部结果未知不自动重调模型或公众号 draft/add |
| D10 成品 | 生成原图、目标画幅衍生图、采用结果分开；历史候选不自动成为交付，只有用户采用后更新 resultRefs |
| D11 渲染 | Java CommonMark AST＋既有 jsoup＋固定内联样式；不接受模型生成的任意 HTML/SVG，不执行页面脚本 |
| D12 依赖 | 仅 intelligence 新增 `org.commonmark:commonmark:0.24.0`、`org.commonmark:commonmark-ext-gfm-tables:0.24.0`；两项在 C101-02 登记和解析验证。前端无新增依赖，既有依赖不升级 |
| D13 文件处理 | Java ImageIO／BufferedImage 做 PNG/JPEG 检验、等比补边与压缩，ZipOutputStream 打包；阻塞工作在有界工作线程执行 |
| D14 微信 | 自有 AppID/AppSecret，经现有 EnvelopeEncryption 加密；服务端直接调用官方 API；不使用个人浏览器／SSH 跳板／Cookie 方案 |
| D15 发布边界 | 首版只存单篇文章草稿和回读；不自动公开发布。无法判定 draft/add 结果时需要真实草稿 ID 核实，不承诺外部接口 exactly-once |
| D16 计费 | 所有 AI 消费复用既有执行环。图片预算成本、用户积分与外部 BYOK 费用分开表达；不新增定价、不把预算成本当现金扣款 |
| D17 开关 | 新功能默认关闭；旧路由与旧工作区仍可用。关写入口后保留已创建结果的核实和读取能力，按 §7.6 排空再回退 |
| D18 规范 | 按根 AGENTS 分层、SFC 800 行门禁、两主题 token 和两款自托管字体；不复制上游 UI 样式或字体 CDN |

依赖版本是本规格选择的目标，不表示已经安装。依赖解析失败时停止受影响的来源解析／渲染卡并记录原因；不得临时换成手写 Markdown 正则解析器或升级整个工具链。

## 4. 用户流程与状态

### 4.1 场景入口

在“开始创作”的现有平台／形式区域旁提供“从已有内容开始”，打开内嵌来源区；不新增顶级导航。四个模板：

| recipeId | 用户名称 | 适用平台／形式 | 模式 | 进入位置 |
|---|---|---|---|---|
| `social-card-series` | 攻略／知识图卡 | 小红书、抖音 graphic | create、adapt | 文章页图卡区 |
| `article-visuals` | 文章封面与配图 | 公众号、知乎 graphic | create、adapt、format | 文章页配图区；format 可加图但不改文字 |
| `article-format` | 原稿排版 | 公众号、知乎 graphic | format | 原稿编辑＋排版预览 |
| `cover-only` | 单张封面 | 上述四平台 graphic；既有视频封面工具可读取预设 | create、adapt、format | 现有配图／封面区域，不修改视频制作流程 |

模板版本均从 `1.0.0` 开始。未知组合返回不可用原因，不静默切换平台。大众点评和朋友圈保留原有专用流程；不得把普通图卡模板当成真实体验证明。

图片默认画幅按“用户显式targetAspect → 当前platform-format-rules.imageSpec.aspect → recipe.defaultAspect”解析。social-card-series兜底3:4，article-visuals为16:9，cover-only为2.35:1；article-format的defaultAspect=null、minItems=maxItems=0、supportedStrategies=[]，只走排版API，不允许提交视觉计划API。因此小红书与抖音图卡仍分别沿用其3:4和9:16建议。

视觉预设文件完整迁入现有12个style、8个layout、3个palette、6个preset及旧textLayout字段，保留所有ID和文案。顶层为version/styles/layouts/palettes/presets；元素字段沿用card-series-templates.ts接口。palette新增backgroundHex仅用于生成交付图补边：macaron=`#F5F0E8`、warm=`#FFECD2`、neon=`#1A1025`，属于内容数据，不作为应用UI颜色。前端原常量和查询函数改为从此契约导出，服务端通过processResources加载，禁止再维护一份手工Java样式表。

### 4.2 主流程

1. 保留已锁定的任务、门店、平台与形式，选择本次加工方式。
2. 输入主题，或粘贴／选择原稿；检查可用事实、身份及已有素材。
3. 导入后进入正文编辑。format 跳过标题、大纲和正文生成；adapt 允许直接拆卡或提出全文改编建议。
4. 选择模板，系统提出一套视觉计划。逐页展示目的、对应原文、关键文字、布局、插图说明和画幅。
5. 保存计划并确认版本；查看费用估算与模型参考能力。默认“先生成封面”，也可明确选择“生成整套”。
6. 展示每项真实阶段和候选，允许比较、单项重做、采用；采用后才更新交付媒体。
7. 完成正文与媒体联合预览，核对声明和发布字段，保存指定版本。
8. 下载文件；已连接且具备权限的公众号账号可存入草稿箱并回读结果。

### 4.3 状态字典

| 对象 | 状态／标记 | 含义与允许动作 |
|---|---|---|
| 原稿输入 UI | empty、editing、saving、saved、error | saving 禁重复导入，error 保留原文；已创建文档不可变，重新导入创建新 ID |
| 文本建议 | preparing、ready、failed、unknown、applied | ready 可看差异并应用；unknown 只读取确认，不自动再生成 |
| 视觉计划 | preparing、ready、failed、unknown；另有 revision、confirmedRevision、stale | ready 可编辑；修改使 confirmedRevision=null；stale 不允许生成或采用 |
| 视觉父任务 | queued、running、succeeded、partial、failed、cancelled、unknown | 成功／部分成功可查看候选；unknown 有待核实子项，不当成普通失败 |
| 视觉子项 | waiting_anchor、queued、prepared、dispatching、generated_unsettled、succeeded、failed、cancelled、unknown | dispatching 后无确定结果不得重新派发；generated_unsettled 只恢复结算，不重生成 |
| 导出记录 | building、ready、failed | ready 可重新签发下载链接；过期文件可按相同不可变快照重建，不调用 AI |
| 公众号连接 | unverified、active、invalid、disconnected | 校验成功后 active；断开后不能新建同步，已存在同步仍可读 |
| 公众号同步 | preparing、uploading、submitting、verifying、succeeded、failed、unknown、cancelled | succeeded 必须有草稿 ID 且回读一致；unknown 只核实，不盲重发 |

`CreationProjectStatus` 与 `CreationRunState` 现有枚举保持不变。细粒度任务状态由专用 API 返回；workspace 只写这些对象的引用及必要摘要，不能添加新枚举值绕过旧校验。

### 4.4 关键迁移

- 计划编辑成功：revision+1，清除旧确认；已生成候选保留但不自动迁移到新计划。
- 草稿正文／平台／brief 相关事实／准备绘制的封面标题变化：当前内容摘要与计划基线不同，stale=true；只改导航用草稿名称或发布描述不使视觉计划过期。任何变化均不自动重生成图片。
- 生成封面成功：先保存原始媒体与结算结果，再允许后续图片使用该 artifact；用户另选封面不触发后续重做。
- 子项已全部结束：若存在 unknown 则父任务 unknown；否则全部成功为 succeeded，成功与失败／取消混合为 partial，无成功且全取消为 cancelled，其余为 failed。
- 取消：未派发项 cancelled；已发出的供应商调用继续核实结果与结算，不承诺立即停止外部计算或自动退回所有成本。
- 关闭／KeepAlive 失活：停止当前视图轮询与 UI 更新，保存本地编辑；后台视觉任务按已接受操作继续。账号 epoch 改变后丢弃所有旧响应、订阅和缓存。
- 微信 submitting 响应丢失：unknown；拿到 media_id 后进入 verifying；验证失败不显示“已存草稿”。

## 5. 业务规则与不变量

### 5.1 输入上限（NEW）

本表是产品和服务保护上限，不宣称是外部平台的全部限制。当前 Brief 的既有约束继续生效。

| 输入 | 范围／默认 | 超限或非法处理 |
|---|---|---|
| 新原稿 | 非空，最多 30,000 Unicode code point 且 UTF-8 ≤128 KiB；只接受 TXT／MD 文本 | 400 STUDIO_LIMIT_EXCEEDED；不截断、不先覆盖旧稿 |
| 原稿块 | 顶层段落／标题／列表／引用／表格／代码块，最多 500；块 ID 由服务端生成 | 400；不把嵌套块重复计为原文 |
| AI 输入选择 | 最多 200 个不重复块，合计最多 8,000 code point | 400，并要求用户选择处理范围；未选择部分保留且显示未覆盖 |
| 图卡／插图 | 图卡 1～9；文章插图 1～8（含封面）；cover-only 恰好 1 | 400；不沿用上游 10 张作为新增限制 |
| 图卡文字 | title ≤60、bullets 0～5 条且每条 ≤80、caption ≤500、purpose ≤200、illustration ≤1,000 code point | 400；数字和专有名词不得通过静默截短适配 |
| 样式 | styleId/layoutId/paletteId 取本地既有目录；计划的每项可覆盖 layoutId | 未知 ID 400；旧合法 ID 保留 |
| 引用媒体 | PNG／JPEG，先读header并以long校验宽×高，再解码；单张 ≤10 MiB 且 ≤25MP；原生图片编辑实际发送参考图 ≤5 MiB | 无法满足时先做可检查的确定性压缩；超限400，不在完整解码超大图后才检查、不丢弃参考图继续生成 |
| 结果与工作区 | 现有 resultRefs/runIds 上限 20、workspace 64 KiB 不变 | 候选历史放服务器；不裁掉工作区字段以求保存成功 |
| ZIP | 最多 20 媒体项；源文件合计及最终包分别 ≤100 MiB | 400，列出超限项；不返回缺图但标完整的包 |
| 列表 | limit 默认 20、范围 1～50，cursor 为不透明 keyset | 非法 cursor 400；无结果 items=[]、nextCursor=null |
| 公众号正文 | 渲染 HTML UTF-8 ≤1 MiB；title 1～64 字，summary ≤120 字，author ≤32 字 | 按本地上限拒绝，不自动删改；若账号 API 更严格则失败并展示可行动原因 |
| 公众号图片 | 初版最多 20 张，正文上传产物每张 ≤1 MiB，封面 ≤2 MiB；均 JPEG／PNG | 先按 §6.8 压缩验证；达不到则返回对应图片错误 |
| 请求 ID | UUID；旧图卡接口原 requestId≤128 字规则不变 | 新 API 缺失／非法 400；旧 API 保持既有可选语义 |

新 code point 计数不修改历史接口采用 UTF-16 长度的规则；前后端新 DTO 使用相同计数方法和合成边界样例。正文只统一 CRLF/CR 为 LF；不做 NFKC、数字替换、全角半角转换或自动删除空格。

### 5.2 来源、事实与任务

1. 新来源 doc 归属 draft 的 owner，导入不改变 `sourceType/taskId/taskVersion/storeId/contextSnapshotId`。`inputKind` 只说明文字输入形式，不能代替任务来源。
2. 任何任务声明存在时，生成前按现有 `GraphicTaskCreationContext`／`ArticleCreationContext` 核实冻结快照和平台。缺少快照或字段互相冲突时拒绝，不静默降为独立调用。
3. 素材在选择、计划确认、worker 读取、采用、导出和渠道上传时重新核对归属、授权、删除／隔离状态。只保存 ID，不信任浏览器给出的签名 URL／文件路径／owner。
4. 原稿中的链接先作为来源或文本保存；不自动抓取，也不自动下载远程图片。内嵌图片需用户映射到已授权素材。
5. 仅已确认的用户经历可写成第一人称；价格、日期、地址和引用有明确来源。上游示例的“90%”“99%”等数字不得作为默认事实。

### 5.3 原文排版与改编

- format 的正文源为已经保存的当前草稿版本；渲染不调用 LLM、不替换标题、不生成摘要、不改词句。缺少发布元数据时提供手工填写或单独的“生成标题／摘要建议”。
- Markdown AST 可改变 HTML 结构，但解码后的文本 token 序列、数字和引用内容必须保持；换行和标签变化按规范化比较，代码块内容逐字保留。
- 原始 HTML 块作为转义文本显示并给出提示；不执行／直接注入。外链保持受控链接，远程图片显示待绑定占位，不进行网络访问。
- adapt 的 AI 结果存为 TextProposal，展示原文／候选差异与变更摘要。应用前验证 draftVersion 和正文 hash，一次原子写入正文及适用元数据；旧稿仍在历史版本中。
- 图卡拆解允许压缩表达，但来源段落与关键事实可追溯。不能把“摘要拆卡”标成“原文不变”。

### 5.4 计划、生成与采用

- 推荐结果必须符合请求的平台／形式、页数、合法样式和来源块集合；未知 ID、重复 cardId、无来源支撑的关键文字、模型返回可执行标记均不能直接采用。
- 一套计划共享 style/palette，每项独立 layout；position 是展示顺序，itemId/cardId 是身份，拖动或删除不重新编号身份。
- 计划确认锁定 revision、contentHash、recipeVersion、材料引用和执行能力指纹；新输入或模型配置变化后重新估算，不自动发起付费调用。
- `reference-image` 仅在原生参考协议可用时开放。`prompt-only` 明确显示使用文字风格约束。MiniMax `character` 参考只服务人物一致，不能冒充整体构图参考。
- 默认先生成封面；生成整套时首项成功后再做后续项，首项失败则后续 waiting_anchor 项停止派发。后续并发最多 2，仍服从现有模型并发门禁。
- 已成功候选作为不可变 artifact 保存。单项重做建立新 attempt 和新操作；只重做请求选中的项，不覆盖其他结果。
- 采用操作仅替换所选 item 对应的已采用引用，其余已授权结果保留；每项最多一个当前采用结果。采用不生成新图、不重新结算。

### 5.5 费用与未知结果

| 动作 | 现有计费入口 | 规则 |
|---|---|---|
| 原稿导入／保存、模板选择、编辑计划、format、采用、导出、渠道同步 | 无 AI 调用 | 不扣 AI 积分，不生成 AI run |
| 改编正文 | ARTICLE_GENERATION | 平台／BYOK 按既有策略；完整文稿只算一次本次调用 |
| 标题摘要建议、文章配图策划 | CREATION_ASSISTANT | 一次请求一个建议／计划；不在后台循环优化 |
| 图卡策划 | CARD_SERIES_PLAN | 一套方案一次调用；用户再选其他策略才有新的调用 |
| 图片制作 | image_generation，feature=null | 保留当前 cents 预算与成本记录，不新增用户图片积分扣费规则 |
| 既有内容深检 | 既有安全服务 | 不旁路既有计费／平台资助规则，不把安全检查假装为免费生图 |

费用估算必须区分 `userCredits` 与 `platformBudgetCents`；BYOK 的外部费用不能显示为“完全免费”。估算失败时不伪造 0 元，生成按钮保持可解释的未就绪状态。

**重试不变量**：外部请求尚未发送可恢复派发；已收到确定失败可让用户重做；已发送但没有可核实结果为 unknown，不能按新 requestId 自动重发。图片已保存但结算未确认时，只恢复既有结算，不再调用图片供应商。公众号 `draft/add` 同理；所有失败补偿沿用现有执行／补偿服务，禁止其他服务直写 finance 表。

### 5.6 并发、时限与资源

- 普通 JSON 读写客户端 30s；策划／改编 120s 客户端上限、模型调用 90s；渲染／导出 120s；图片 API 单项 180s；视觉 workflow 总时限 30min。
- 视觉租约 240s，每 30s heartbeat；租约过期仅回读状态并核实，不据此自动重发 dispatching 项。
- 编辑防抖 800ms；同一 draft 与同一 plan 各一条保存队列。新 UI 通过共享 draft session 执行服务端应用后的 adopt，不创建第二个草稿写计时器。
- 非付费 GET 自动重试最多两次，间隔 1s／2s；401/403/404/409 不自动重试。写请求不自动换键；重复 key 的读取不计算新调用。
- 视觉任务轮询：2s，达到 60s 后 5s；隐藏／失活暂停，激活立即读取，持续 30min 后停止并提供“刷新状态”。未知状态不靠无限轮询隐藏。
- ZIP／图像处理同时最多 2 个工作线程任务／实例，队列 20，超出返回 429；WebFlux event loop 不做 ImageIO、压缩或文件 I/O。
- 源文档、plan、quote、job、artifact 的 UUID 与 account/draft 组合校验；不因 UUID 难猜就省掉 owner 检查。

## 6. 接口、类型与执行契约

### 6.1 通用 wire 规则

所有 NEW JSON 端点沿用 `{success:true,data}`／`{success:false,error,code?}`；HTTP 状态由 ResponseEntity 正确设置。身份沿用 `y1.sid` → Edge → 内部断言 → `IntelligenceCallerResolver`，不接受请求体的 owner、organizationId、provider URL 或密钥作为生成身份。

所有有副作用的创建／应用命令包含 `requestId`。服务端先核验 owner 与语法，再使用 `(owner, operationKind, requestId)` 唯一记录和规范化 JSON SHA-256 校验相同输入。同键同输入返回原结果，同键异参 409。JSON hash 按键排序，保留数组顺序，缺省值先按 DTO 归一；不将标题、图片 URL 或当前时间当幂等键。

首次回包丢失、客户端尚不知道资源ID时，用完全相同payload和requestId重放创建命令取得原ID；不能把requestId当成服务端资源ID。处理顺序为鉴权／基本语法 → 读取本人已有操作并比较hash → 返回已有结果 → 仅新操作继续当前业务校验与写开关检查；原操作回读不因quote后来过期而重新派发。资源已删或已无读取权限仍拒绝。此原键恢复不授权创建第二次逻辑操作。

NEW DTO 拒绝未知字段；字段表中 `?` 表示可省略，`null` 表示允许显式空值。未声明 nullable 的字段显式 null 为 400。旧 DTO 的宽容解析规则不批量改变。表中 UUID 均传 JSON string，版本为正整数，hash 为 64 位小写十六进制，时间为 UTC ISO-8601。

NEW字段校验采用独立StudioRequestValidator：先对JsonNode检查真实JSON类型，再构造DTO。禁止把浮点数截成版本整数、把数字转成文本、把字符串true转成布尔；不修改全局Jackson设置影响旧客户端。绑定/secret字段的解析错误只返回字段名和原因，不回显原值。

### 6.2 共享类型（NEW，声明）

落点：`src/types/creation-studio.ts`；Java DTO 使用同名字段和相同枚举。完整类型由本节和 §6.3 对象字段表共同定义，不能自行增加状态。导入现有类型不改变其原契约。

```ts
import type { CreationProject, CreationResultRef } from './creation'

export type RecipeId = 'social-card-series' | 'article-visuals' | 'article-format' | 'cover-only'
export type VisualStrategy = 'story' | 'information' | 'visual'
export type TargetAspect = '3:4' | '9:16' | '1:1' | '16:9' | '2.35:1'
export type ConsistencyMode = 'reference-image' | 'prompt-only'
export type OperationState = 'preparing' | 'ready' | 'failed' | 'unknown'
export interface RecipeRef { id: RecipeId; version: string }
export interface SourceRef { id: string; contentHash: string }
export interface PlanRef { id: string; revision: number }
export interface StudioError { code: string; message: string }
export interface SourceBlock {
  id: string; kind: 'heading' | 'paragraph' | 'list' | 'quote' | 'table' | 'code';
  position: number; startCodePoint: number; endCodePoint: number;
  text: string; textHash: string;
}
export interface VisualStyle { styleId: string; layoutId: string; paletteId: string }
export interface VisualPlanItem {
  itemId: string; cardId: string; position: number;
  role: 'cover' | 'content' | 'summary' | 'illustration';
  title: string; bullets: string[]; caption: string; purpose: string;
  illustration: string; sourceBlockIds: string[]; criticalText: string[];
  layoutId: string; targetAspect: TargetAspect;
  placement: { afterBlockId: string } | null;
  inputMediaRef: CreationResultRef | null;
}
export interface VisualPlanDocument {
  recipe: RecipeRef; strategy: VisualStrategy; style: VisualStyle;
  items: VisualPlanItem[]; explanation: string; uncoveredBlockIds: string[];
}
export interface VisualPlan {
  id: string; draftId: string; status: OperationState; revision: number;
  confirmedRevision: number | null; source: SourceRef; baseDraftVersion: number;
  baseContentHash: string; stale: boolean; document: VisualPlanDocument | null;
  runId: string | null; error: StudioError | null; createdAt: string;
}
export type VisualItemState = 'waiting_anchor' | 'queued' | 'prepared' | 'dispatching'
  | 'generated_unsettled' | 'succeeded' | 'failed' | 'cancelled' | 'unknown'
export interface VisualArtifact {
  id: string; itemId: string; attemptId: string; plan: PlanRef;
  originalMediaRef: CreationResultRef; deliveryMediaRef: CreationResultRef;
  runId: string; width: number; height: number; contentHash: string;
  anchorArtifactId: string | null; createdAt: string;
}
export interface VisualJobItem {
  attemptId: string; itemId: string; position: number; state: VisualItemState;
  runId: string | null; artifact: VisualArtifact | null; error: StudioError | null;
}
export interface VisualJob {
  id: string; requestId: string; draftId: string; plan: PlanRef;
  state: 'queued' | 'running' | 'succeeded' | 'partial' | 'failed' | 'cancelled' | 'unknown';
  version: number; items: VisualJobItem[]; cancelRequested: boolean;
  quoteId: string; createdAt: string; updatedAt: string;
}
export interface StudioApplyResult {
  project: CreationProject; appliedVersion: number; alreadyApplied: boolean;
}
export interface StudioWorkspaceRefs {
  schemaVersion: 1; recipe: RecipeRef | null; sourceDocumentId: string | null;
  visualPlan: PlanRef | null; activeVisualJobId: string | null;
  lastProposalId: string | null; renderTheme: 'standard' | 'compact';
}
export interface StudioPage<T> { items: T[]; nextCursor: string | null }
```

`workspace.inputs.studio` 只保存 StudioWorkspaceRefs。新计划和候选不再写进旧 `workspace.inputs.cards`；旧 cards 继续按旧 reader 读取，用户首次选择新版策划时才创建新计划并保留旧结果。未知 `studio.schemaVersion` 只读恢复且禁止覆盖。source／plan／job ID 不能只做格式校验，必须验证本人、本草稿及引用关系。

当前采用的交付图进入resultRefs，原图／未采用候选只在artifact与job历史中保留。新版runIds由服务端按当前采用结果、当前计划和当前建议计算去重摘要；完整历史留在ai_run/lineage/job，不靠无限追加workspace数组。超过20项必须报错，不取数组头尾假装保存成功；未迁入新版的旧稿不重写其runIds。

### 6.3 其余对象字段表（NEW）

本表是完整字段集；标记“继承”的对象含被继承对象全部字段，不包含未列出的自由 JSON。每类 DTO 放到其领域包内，前端在 `creation-studio.ts` 声明对应接口。

| 对象 | 全部字段与类型 | 规则 |
|---|---|---|
| RecipeDefinition | id:RecipeId；version:string；label:string；enabled:boolean；platformIds:AiPlatformId[]；contentForms:AiContentFormId[]；processingModes:CreationProcessingMode[]；minItems/maxItems:number；defaultAspect:TargetAspect/null；supportedStrategies:VisualStrategy[] | 复用 `ai-creation.ts` 与 `creation.ts` 的平台／形式／模式类型；article-format无图片参数；公开响应不返回prompt |
| SourceDocument | id/draftId:UUID；schemaVersion:1；kind:plain-text/markdown/draft-content；title/rawText/normalizedMarkdown/contentHash:string；blocks:SourceBlock[]；sourceRefs:CreationSourceRef[]；warnings:string[]；createdAt:时间 | sourceRefs 复用现有类型，最多 20；正文不可变 |
| TextProposal | id/draftId/requestId:UUID；action:adapt-body/suggest-metadata；status:OperationState/applied；baseDraftVersion:number；baseContentHash:string；source:SourceRef；result:TextProposalResult/null；runId:UUID/null；safety:SafetyReport/null；appliedDraftVersion:number/null；error:StudioError/null；createdAt/expiresAt:时间 | SafetyReport 复用 `content-safety.ts`；结果有效期 30min，仅影响应用，不删除历史 |
| TextProposalResult | title/body/summary:string/null；changes:string[]；sourceBlockIds:UUID[] | changes≤20，每条≤300；正文≤30,000 字；未请求生成的字段必须为 null |
| ImageCapabilities | protocol:openai-image/minimax-character/legacy-generation；referenceKinds:(image/character)[]；maxReferences/maxReferenceBytes:number；generationSizes:string[]；providerLabel/modelLabel/configurationFingerprint:string；available:boolean；unavailableReason:string/null | 来自服务端实际路由和冻结配置，不接受浏览器自报支持能力 |
| VisualQuote | id:UUID；plan:PlanRef；selectedItemIds:UUID[]；imageCalls:number；consistencyMode:ConsistencyMode；anchorArtifactId:UUID/null；userCredits/platformBudgetCents:number；billingSource:platform/personal-byok/org-byok；pricingVersion/configurationFingerprint:string；expiresAt:时间；warnings:string[] | 有效期 120s；userCredits 与预算成本不混用；不冻结资金 |
| RenderPreview | draftId:UUID；version:number；renderVersion/contentHash/html/text:string；warnings:string[]；unresolvedMediaIds:string[] | html 为服务端确定性且净化后的内容片段；预览链接临时生成 |
| StudioExportFile | exportId:UUID；filename/contentType/sha256/url:string；sizeBytes:number；expiresAt:时间 | url 是短时授权下载地址，不落 workspace 或日志 |
| StudioExportResult | draftId:UUID；version:number；format:markdown/text/wechat-html/bundle-zip；file:StudioExportFile；missingItems:string[] | 新格式缺必需媒体时失败，不把缺图写成 ready；旧 manifest 结构保持原样 |
| WechatAccount | id:UUID；displayName/appId:string；state:unverified/active/invalid/disconnected；version:number；verifiedAt:时间/null；error:StudioError/null | 响应永不含 AppSecret、token、密文或 maskedSecret；appId 可显示用于辨认账号 |
| WechatDraftSync | id/requestId/accountId/draftId:UUID；draftVersion/version:number；state:preparing/uploading/submitting/verifying/succeeded/failed/unknown/cancelled；externalDraftMediaId:string/null；payloadHash:string；error:StudioError/null；createdAt:时间；verifiedAt:时间/null | accountId 指草场内连接 ID，不是登录身份；完整内容快照不从列表返回 |
| WechatDraftCandidate | externalDraftMediaId/title:string；updatedAt:时间；contentMatches:boolean | 只显示当前连接且与本同步相关的候选，匹配不能只比较标题 |

### 6.4 HTTP 清单

NEW 基础前缀 `/api/creation-studio`；公众号前缀 `/api/creation-channels/wechat`。所有接口登录且 owner 校验；适用任务时另校验冻结来源。请求／响应均为 JSON，除既有 SSE 和实际文件下载。字段中的默认值写在括号中；没有默认值的字段必填。

| API | 方法与路径 | 请求字段 | 成功 data／状态 |
|---|---|---|---|
| API101-01 | GET /api/creation-studio/recipes | query platform、contentForm、processingMode，均可省略（返回可见集合） | `{items:RecipeDefinition[]}`，200 |
| API101-02 | GET /api/creation-studio/drafts/{draftId}/capabilities | path draftId | `{image:ImageCapabilities, studioEnabled:boolean, wechatEnabled:boolean}`，200 |
| API101-03 | POST /api/creation-studio/sources | requestId、draftId、expectedDraftVersion、kind；title?（空）；kind 为 plain-text/markdown 时 text 必填、sourceRefs?（[]）；draft-content 禁止 text | SourceDocument，首次 201、重放 200 |
| API101-04 | GET /api/creation-studio/sources/{id} | path id | SourceDocument，200 |
| API101-05 | POST /api/creation-studio/text-proposals | requestId、draftId、expectedDraftVersion、action、source:SourceRef、selectedBlockIds；instructions?（空，≤2,000） | TextProposal，ready 200、同键 preparing 202 |
| API101-06 | GET /api/creation-studio/text-proposals/{id} | path id | TextProposal，200 |
| API101-07 | POST /api/creation-studio/text-proposals/{id}/apply | requestId、expectedDraftVersion、fields:(title/body/summary)[] | StudioApplyResult，200 |
| API101-08 | POST /api/creation-studio/visual-plans | requestId、draftId、expectedDraftVersion、recipe:RecipeRef、source:SourceRef、selectedBlockIds；strategy?（自动推荐）；itemCount?（按§6.5）；style?:部分VisualStyle；targetAspect?（模板默认） | VisualPlan，ready 200、同键 preparing 202 |
| API101-09 | GET /api/creation-studio/visual-plans/{id} | revision?（当前）；历史版本只读 | VisualPlan，200 |
| API101-10 | PATCH /api/creation-studio/visual-plans/{id} | requestId、expectedRevision、document:VisualPlanDocument | 新 revision 的 VisualPlan，200 |
| API101-11 | POST /api/creation-studio/visual-plans/{id}/confirm | requestId、draftId、expectedDraftVersion、expectedRevision、sourceContentHash | VisualPlan，200 |
| API101-12 | POST /api/creation-studio/visual-plans/{id}/estimate | requestId、expectedRevision、selectedItemIds、consistencyMode；anchorArtifactId? | VisualQuote，200；没有模型调用／资金预留 |
| API101-13 | POST /api/creation-studio/visual-jobs | requestId、plan:PlanRef、quoteId、selectedItemIds、consistencyMode；anchorArtifactId?；acknowledgedUnknownAttemptIds?（[]） | VisualJob，首次／进行中 202，终态重放 200 |
| API101-14 | GET /api/creation-studio/visual-jobs/{id} | path id | VisualJob，200 |
| API101-15 | GET /api/creation-studio/visual-jobs | draftId、limit?（20）、cursor? | `StudioPage<VisualJob>`，200 |
| API101-16 | POST /api/creation-studio/visual-jobs/{id}/cancel | requestId、expectedVersion | VisualJob，200；只请求取消，不假报全部停止 |
| API101-17 | POST /api/creation-studio/visual-plans/{id}/adopt | requestId、draftId、expectedDraftVersion、expectedPlanRevision、selections:[{itemId,artifactId}] | StudioApplyResult，200 |
| API101-18 | POST /api/creation-studio/render-previews | draftId、version、theme:standard/compact；includeTitle?（false）、citeExternalLinks?（false） | RenderPreview，200；纯计算，不落 AI run |
| API101-19 | POST /api/creation-drafts/{id}/exports（扩展） | version?（旧格式缺省当前）；format?（bundle-manifest）；新格式 requestId 必填且 version 必填；theme?（standard）、includeTitle?（false）、citeExternalLinks?（false） | 旧格式保持 CreationExportResult；新格式返回 StudioExportResult；首次 200、同键 building 202 返回 `{exportId,state:'building'}` |
| API101-20 | GET /api/creation-studio/exports/{id} | path id | ready 为 StudioExportResult；building/failed 返回 `{exportId,state,error:StudioError或null}`，200 |
| API101-21 | GET /api/creation-channels/wechat/accounts | limit?（20）、cursor? | `StudioPage<WechatAccount>`，200 |
| API101-22 | POST /api/creation-channels/wechat/accounts | requestId、displayName、appId、appSecret | WechatAccount，201／同键 200；仅加密保存，不自动联网 |
| API101-23 | POST /api/creation-channels/wechat/accounts/{id}/verify | requestId、expectedVersion | WechatAccount，200；仅测试 API 凭据，不写内容 |
| API101-24 | POST /api/creation-channels/wechat/accounts/{id}/rotate | requestId、expectedVersion、appSecret | WechatAccount，200；失效 token 缓存，回 unverified |
| API101-25 | POST /api/creation-channels/wechat/accounts/{id}/disconnect | requestId、expectedVersion | WechatAccount，200；重放幂等 |
| API101-26 | POST /api/creation-channels/wechat/draft-syncs | requestId、accountId、expectedAccountVersion、draftId、draftVersion、exportId；author?（空）、contentSourceUrl?（空）；needOpenComment/onlyFansCanComment:0或1 | WechatDraftSync，首次／进行中 202，终态重放 200 |
| API101-27 | GET /api/creation-channels/wechat/draft-syncs/{id} | path id | WechatDraftSync，200 |
| API101-28 | GET /api/creation-channels/wechat/draft-syncs | draftId、limit?（20）、cursor? | `StudioPage<WechatDraftSync>`，200 |
| API101-29 | GET /api/creation-channels/wechat/draft-syncs/{id}/candidates | path id；无自由 URL／查询内容 | `{items:WechatDraftCandidate[],searchedCount:number,hasMore:boolean}`，200 |
| API101-30 | POST /api/creation-channels/wechat/draft-syncs/{id}/reconcile | requestId、expectedVersion、externalDraftMediaId（1～256 字） | WechatDraftSync，200；只回读，不调用 draft/add |
| API101-31 | POST /api/creation-channels/wechat/draft-syncs/{id}/cancel | requestId、expectedVersion | WechatDraftSync，200；submitting 后不能证明取消时返回 unknown |

API101-03 只创建不可变来源，不隐式改写 draft；前端随后通过共享 draft session 保存正文／studio 引用，成功才显示“已导入”。保存冲突时已创建来源可重放读取，用户原文仍保留。draft-content 由服务端读取指定版本的当前正文，不能由浏览器提交另一份文本冒充。

API101-05/08 为有界同步策划，先落 preparing 记录；断线／进程中断时读取 ai_run 并标记 unknown，不承诺继续执行。重复 preparing 不再次请求模型。GET 只读结果；超时检查只改变本地状态，不触发新的付费动作。

API101-07/17 在同一事务中校验版本、构造完整草稿、保存历史、写新草稿和应用结果；不能绕过 CreationDraftService 的校验。重放返回已应用版本；客户端不把旧版本结果覆盖新编辑，必要时重新读取当前草稿。

API101-19 普通新导出在 120s 内完成；同键并发请求返回 building 以供读取。building 超过 150s 无租约／文件结果后标 failed，同键重试可重新构建这一非付费产物。旧 bundle-manifest 不受新增 requestId 必填规则影响。

### 6.5 计划与草稿应用的详细语义

**来源解析**：rawText保留解码后的用户原输入；normalizedMarkdown统一LF，plain-text分支仅转义Markdown控制字符以保持字面呈现，markdown分支保留原标记。CommonMark启用source spans，顶层节点跨度对应normalizedMarkdown的code point区间`[start,end)`，不是UTF-16索引或rawText字节偏移；嵌套列表仍属于一个顶层块。块ID在不可变source内稳定，重新导入产生新document和块ID。contentHash对完整normalizedMarkdown做SHA-256；正文不因解析告警丢失。导入到编辑器使用normalizedMarkdown，纯文本导出再由AST恢复字面文本。

**来源选择**：策划当前正文前，由 API101-03 的 draft-content 分支冻结当前正文，图卡计划绑定这份文档。最初导入的原文仍作为来源保留。若用户修改正文，旧计划的 baseContentHash 不再匹配，必须重建或重新确认新计划，不能把旧块 ID 挪到新段落。

baseContentHash是服务端对“当前正文LF版本、articleTitle、contentMode、questionText/questionRef、platform/contentForm、影响生成的Brief字段”的规范化JSON摘要；不包含导航草稿title、发布描述、下载URL、UI主题或保存时间。source.contentHash只描述来源正文，两者不能混用。任务字段额外服从冻结快照，不通过这两个hash代替权限。

**推荐缺省**：strategy 未指定时，存在用户确认经历且明确选择体验型内容才选 story；选有至少 3 个既有素材且文字≤1,200 字可选 visual；其余 information。itemCount 未指定时：图卡为 `min(6,max(2,1+ceil(非空块数/2)))`；文章配图为 `min(6,max(1,标题块数+1))`；封面为 1。这些是可修改推荐，不能更改用户显式页数。

**计划输出**：social-card-series 第一项 role=cover，后续为 content/summary；article-visuals 第一项 cover，后续 illustration 且 placement.afterBlockId 必须存在；cover-only 只有 cover。所有 item 的 sourceBlockIds 必须属于本次选择；criticalText 逐字出现在至少一个关联来源块中。未知字段、重复身份、超限、错平台、错角色或无效引用，返回 STUDIO_INVALID_PLAN，不用第二次隐形 LLM 调用“修复 JSON”。

**计划编辑**：客户端提交完整 VisualPlanDocument，服务端逐项校验；PATCH 创建不可变 revision 快照并推进 current revision。revision 改变清除 confirmedRevision；来源、模板 id/version 不允许在 PATCH 中切换，切换必须新建计划。删除封面时要求用户指定新的合法封面；position 重新连续排列，itemId/cardId 不变。

**计划确认**：只确认当前 revision，不增加内容 revision；记录 confirmedRevision、确认时 draftVersion、contentHash 和 actor。保存、确认及任务创建共享计划行锁，不能生成半更新计划。

**估算**：按请求 selectedItemIds 算本次实际图片调用数；若整组 reference-image 需要生成封面，封面必须显式包含在选择和估算中。只生成后续页时 anchorArtifactId 必须指向同计划版本已成功且用户选定的封面。估算包含目标尺寸、引用方式、价目和执行配置指纹；创建任务时再次核对，120s 过期或配置漂移返回 STUDIO_QUOTE_EXPIRED，不悄悄提价或换模型。

quote的120s只约束新任务接受时间，已经接受的job不因quote到期取消。worker按冻结指纹继续检查凭据／权限和现有执行门禁，不重新读一份新价格替代快照。

**正文建议应用**：adapt-body 仅允许 body/title，suggest-metadata 仅允许 title/summary；用户可以选择其中部分非 null 字段。apply 锁草稿并校验 expectedDraftVersion、baseContentHash、来源和有效期，调用同一草稿持久化路径，旧内容写版本历史。同请求再次调用只返回原 appliedVersion，不再追加版本。

proposal.title映射草稿articleTitle，不修改导航用title；summary映射workspace.delivery.summary。知乎answer模式的title建议必须为null，申请应用title返回400；body改编保持questionText/questionRef不变。apply要求当前version同时等于expectedDraftVersion与建议的baseDraftVersion，避免建议覆盖期间发生的人工编辑。

**采用图片**：API101-17 每项必须属于当前计划 revision 且 state=succeeded；同 item 不允许多选。使用当前合法的 deliveryMediaRef 更新 workspace.resultRefs 与 delivery 的对应项；保留未涉及的素材。卡片 cover 更新 coverRef，正文图片保留段落绑定。人工已编辑的标题、正文、摘要和分享配文不被图片采用覆盖。

PlanItem角色与既有结果角色显式映射：cover→cover，content/summary→card，illustration→body；不把illustration/content写成旧CreationResultRef中不存在的角色。

**共享保存队列（NEW 签名）**：`useWorkspaceAutosave` 增加 `runExternalMutation<T>(action:(expectedVersion:number)=>Promise<{project:CreationProject; value:T}>):Promise<T|null>` 和只读状态 `externalMutationBusy`。它先 flush，失败／conflict 就不调用 action；执行期间禁当前草稿编辑；成功用现有 session.adopt 接收服务器版本后恢复 UI；乱序／账号变更不 adopt；无响应先读当前草稿再决定恢复，不盲重放一次 apply。此动作与现有 800ms 队列互斥。

### 6.6 图像协议、持久执行与参考链

新增 `PlatformProviderNames.OPENAI_IMAGE = "openai-image"`，同步 ALL/PATTERN/MESSAGE、凭据选择器和设置帮助。平台与 BYOK 只允许将该 provider 用于 image_generation；绑定 text/video 等能力返回 400。旧 provider 名不迁移、不自动改协议。

平台校验必须针对credentialId解析后的实际provider，不能只检查body.provider；凭据覆盖请求字段的既有规则继续生效。新协议不增加免费的探测式生图，设置帮助和capabilities读取不调用供应商生成接口。

| 协议 | 实际调用 | 引用语义 | 上限／行为 |
|---|---|---|---|
| openai-image | 无参考时 `/images/generations`；有参考时 Multipart `/images/edits` | 通用图像参考 | 首版 1 张 PNG/JPEG，≤5 MiB；显式带 image 文件、model、prompt、size、n=1；生成尺寸仅 1024x1024、1024x1536、1536x1024 |
| 既有 minimax | 既有 `/image_generation` | `subject_reference.type=character` | 保留现有第一张合规人物参考逻辑；通用 reference-image 图卡模式拒绝 |
| 其他现有协议 | 既有生成路径 | prompt-only | 维持旧能力，不因模型名含 image 就推断支持参考图 |

Multipart 的 boundary 由 WebClient 构建，不能手拼；API key 来自既有服务端解密与路由。新增协议仍走现有受信 origin、BYOK 全量 DNS 校验和固定地址连接。实际模型若不接受协议参数，返回受控错误；不删掉 reference 后再请求一次。`openai-image` 只声明调用协议，模型可用性和出图质量必须通过 V-LIVE-IMAGE 证明。

**运行拆分（NEW 内部契约）**：增加 `ImageExecutionObserver`，声明 `Mono<Void> prepared(UUID runId)`、`Mono<Void> generated(UUID runId, UUID mediaId)`。为现有独立／任务图片服务增加接受固定 `executionOperationId` 与 observer 的重载；原方法保留并按旧方式生成 ID。observer 由视觉任务持久层实现，禁止将它变为任意回调脚本。

1. 父任务唯一占位并冻结完整计划、selectedItemIds、quote、配置／价格指纹。每项 attemptId 唯一，executionOperationId 由父任务 ID＋itemId＋attemptId 确定性生成。
2. prepareMediaExecution 完成后，先将 runId、executionOperationId、输入摘要写入子项；`prepared()` 成功后才能发送外部请求。登记失败走既有失败处理，供应商调用数为 0。
3. 外部请求发送前持久化 dispatching；拿到图后按固定 objectKey 保存原始媒体，调用 generated() 持久化 mediaId，再进入既有成功结算。
4. 结算完成才标 succeeded；生成图已保存但结算尚未完成标 generated_unsettled，重放仅读取 run 和恢复既有结算流程。不得再次生图或另造扣退流水。
5. 进程崩溃在 dispatching 且无可确认产物时标 unknown。没有供应商受理证据查询能力时保持 unknown；用户可在明确告知可能再次计费后主动重做，该次 API101-13 必须携带旧 unknown attempt ID 的 acknowledgedUnknownAttemptIds；后台不能代填。
6. API／Temporal 重放先读持久状态。activity 超时或 worker 重启都不能当作“请求肯定没发送”。业务模型调用不采用 Temporal 默认自动重试；最多一次派发由数据库阶段守卫决定。

**Temporal**：新 workflowId=`creation-visual-{父操作UUID}`，task queue=`intelligence-creation-visual`，同 ID 拒绝重复工作流。启动失败留下 queued 行，由每 10s 扫描、批次 20 的收养器补起同 ID；不重新创建父操作。activity 只读取领域服务结果，不能直接扣费；现有视频 workflow、queue 和参数保持。

**图片顺序**：整组第一张成功并完成持久化后，最多并发 2 张后续项；reference-image 将原始封面媒体作为实际参考。保存每项 promptHash、anchorArtifactId、配置指纹；完整最终 prompt 仅存在受 owner 保护的执行记录，不返回公共列表。首项失败时后续停止派发；不能用其他项目的旧图自动顶替。

### 6.7 画幅、渲染与导出

| targetAspect | 衍生图尺寸 | 默认处理 |
|---|---|---|
| 3:4 | 1080×1440 | 等比缩放并补边，保留全部文字 |
| 9:16 | 1080×1920 | 同上 |
| 1:1 | 1080×1080 | 同上 |
| 16:9 | 1920×1080 | 同上 |
| 2.35:1 | 1410×600 | 同上，仅作为封面输出选择 |

供应商原图与衍生图分别保存；UI 先预览结果，不能把原图尺寸显示成目标尺寸。补边颜色从已选择 palette 的定义读取，属于内容数据；不在 UI 组件新增硬编码色值。首版不提供自动裁掉文字的 cover 裁切，也不覆盖生成图上的文字。原图与交付图相同时允许复用同一个媒体 ID，引用数组仍须去重。

渲染支持标题、段落、列表、引用、粗体、斜体、链接、GFM 表格、代码块和绑定图片。数学公式、Mermaid、PlantUML、原始 HTML 与复杂插件首版不执行；按文字／代码保留并列出提示。两个排版主题 standard/compact 使用同一 AST 和内容，差别只有 token 化的段距、标题层次及边距。

HTML 只输出受控元素 `section,h1,h2,h3,h4,p,span,strong,em,ul,ol,li,blockquote,table,thead,tbody,tr,th,td,pre,code,a,img,br,hr`；属性仅允许渲染器生成的必要项。用 jsoup safelist 再校验，移除事件、脚本、iframe、style 标签、任意用户 CSS、data: 和 javascript:。inline style 只由服务端主题枚举生成。浏览器预览再经现有 DOMPurify；不将原文交给裸 v-html。

图片语法 `![说明](media:<UUID>)` 仅为内部文档引用形式；解析为本草稿授权媒体后才可渲染。外部 URL 图片进入 unresolved 提示；不能由导出服务自动下载。普通 HTTP(S) 引用链接允许输出，可选 citeExternalLinks 转为编号引用；转为引用只改变链接呈现，原文文本仍保留。

新文件格式要求：

- markdown：正文与合法 Markdown 元数据；媒体引用改为包内相对路径，单文件有外置资源时明确说明，不能残留 `media:` 私有协议。
- text：保留正文逻辑顺序、链接和引用文本；不是发布描述的截断版本。
- wechat-html：可导入的完整 HTML；本地包使用相对图片路径，公众号同步阶段才替换为微信已上传 URL。单 HTML 下载对必需图片采用受控资源打包规则，见下面 ZIP 清单。
- bundle-zip：至少包含 `README.txt`、`manifest.json`、`article.md`、`article.txt`、`article.html`、`publication.json`、`sources.json` 和有序 `images/01-cover.png`、`images/02-content.png`；扩展名与真实编码一致，没有图片则 images 目录可省略。

wechat-html 和 markdown 在存在绑定媒体时，文件下载的包装为 ZIP（contentType=application/zip，主文件分别为 article.html/article.md，附 images/）；无媒体时可直接下载该文本文件。format 字段表示主内容格式，返回 filename/contentType 准确描述实际包装。完整 bundle-zip 始终提供以上所有文字文件；publication.json 含标题／摘要／话题／声明和平台，不包含内部凭据／prompt／签名地址。

导出使用指定 draftVersion 的不可变快照和当前仍有效的媒体权限；不能混合当前标题与历史正文。文件名过滤路径穿越与控制字符，压缩包条目只能来自服务端命名，禁止用户相对路径。输出 objectKey 由 exportId 确定，sha256 和实际 sizeBytes 从写出的文件计算。

下载 URL 有效期 900s；导出文件保存 7 天，到期按同版本重新构建。权限撤销后不再签发新链接；已经签发的链接仍可能在 900s 内可用，不能声称能立即撤销。缺失／隔离／无法解码的必需媒体令新导出失败并指出项；旧 manifest 的逐项 unavailable 语义保持。

### 6.8 公众号连接与草稿协议

**连接字段**：displayName 1～80 字；appId 匹配 `wx` 加 16 位十六进制；appSecret 为 16～512 位可见字符，不允许换行／控制字符。服务端用 EnvelopeEncryption 加密保存；缺少加密设施时 503，不保存明文。首版同一 appId 仅有一个未 disconnected 连接，避免两个 owner 同时持有同一发布账号。

校验连接是用户单独点击的操作：调用官方 token 接口确认凭据；成功只说明 API 凭据可用，不声称所有内容发布权限均已验证。绑定／轮换后为 unverified，校验失败为 invalid，保留连接供修正。断开清除可用凭据和 token 缓存并取消未提交任务，不删除外部草稿。

生产出站仅允许 HTTPS `api.weixin.qq.com` 和固定 API 路径；不接受用户设置 host、代理或端口，不跟随重定向，不在启动时访问外网。测试通过依赖注入使用 WireMock，而不是给生产接口增加任意 baseUrl 参数。

token 缓存在既有 Redis，key 使用内部连接 ID＋凭据版本；值用 EnvelopeEncryption 加密，TTL=`max(1,expires_in-120)`秒，最长 7,000s。SET NX 锁 TTL30s 控制多实例刷新，释放锁要校验持有者。Redis 不可用时渠道操作返回 503，不影响本地编辑／导出。日志不得打印含 access_token 的完整 URL。

**上传与草稿装配**：

1. API101-26 验证连接 active、凭据版本、草稿 owner、platform=wechat-official、所选 exportId 对应同一 draftVersion 且主格式包含可用 HTML；声明／元数据／封面齐全。
2. 冻结 title、正文 HTML、summary、author、content_source_url、评论选项、封面和图片顺序。记录规范化 payloadHash；同账号＋draftVersion＋payloadHash 有进行中／unknown／succeeded 记录时返回该记录，不创建第二个草稿。
3. 解码并处理图片：优先保持 PNG；需要压缩时按 JPEG quality 90、80、70 与宽度 2048、1600、1280、1024 的固定阶梯尝试，达到限制即停止；透明图需要转 JPEG 时使用选定排版背景，产物保存可核对，不静默丢图。
4. 正文图片调用 `/cgi-bin/media/uploadimg`，封面调用 `/cgi-bin/material/add_material?type=image`；映射表按连接＋凭据版本＋内容 hash＋用途缓存。两种 media_id／URL 语义不得混用。
5. 仅在所有必需素材已准备且再次核验权限／凭据版本后，持久化 submitting，再调用 `/cgi-bin/draft/add` 一次。payload 的 articles 长度=1，article_type=news；title/content/thumb_media_id 必填；digest、author、content_source_url 有值才带；评论两个字段明确传 0/1。
6. 收到 media_id 先持久化再调用 `/cgi-bin/draft/get`；回读文章 title、content、digest、封面和图片顺序与冻结快照一致才 succeeded。

每次微信请求连接超时 5s、响应超时 30s；每个同步总时限 10min。上传和只读请求最多重试 2 次（1s／2s）；draft/add 不因 HTTP 超时、断线、5xx 或解析失败自动重试。已收到确定“token 失效且未创建”的业务错误时可刷新一次 token 后重试，其他错误按表返回。同步使用专用 Temporal workflow，业务状态与 dispatch 标记在数据库；activity 不复制默认重试副作用。

**未知结果核实**：API101-29 通过 `/cgi-bin/draft/batchget` 按20条一页最多读取100条，只在用户点击核实时执行；单次搜索总截止20s，分页请求使用5s响应上限，截止后返回已检查条数且hasMore=true，不把未查完写成“确定不存在”。同sync相同版本缓存30s。匹配检查正文规范化token、标题、摘要、图片引用和封面，不能只看标题。用户选择候选后API101-30再draft/get核实；未找到或多份内容相同，保持unknown，不自动draft/add。人工提供media_id走同样owner/连接/内容比对，不能仅凭字符串标成功。

回读中的 HTML 可以被平台规范化；比对采用受控 DOM token 序列与已保存上传映射，不比较无意义的属性顺序，也不能忽略文本、数字和图片位置。平台对内容做实质改写时返回 STUDIO_CHANNEL_CONTENT_MISMATCH，保持记录可核对。

### 6.9 统一错误与调用方动作

| HTTP／NEW code | 条件 | 调用方行为／副作用 |
|---|---|---|
| 400 STUDIO_INVALID_INPUT | 类型、空白、枚举、重复 ID、未知字段非法 | 定位字段，保留输入；不调用模型 |
| 400 STUDIO_LIMIT_EXCEEDED | 字数、文件、图片数、列表或字节超限 | 显示具体上限，不截断 |
| 400 STUDIO_UNSUPPORTED_FORMAT | 文件编码、媒体格式、Markdown 功能不支持且无法保留 | 保留原稿，提供受支持输入说明 |
| 401／沿用鉴权响应 | 未登录／会话过期 | 复用登录，账号切换后清旧缓存 |
| 403／沿用已有执行拒绝 | BYOK 组织策略／功能权限拒绝 | 显示服务端原因，不改为独立模式 |
| 404 STUDIO_NOT_FOUND | 不存在、软删或不属于本人 | 不泄露其他 owner／对象存在性 |
| 404 STUDIO_DISABLED | 创建开关关闭或 Edge 未放行 | 保留旧路径；本地来源／草稿内容不删除 |
| 409 STUDIO_VERSION_CONFLICT | draft/plan/account/job 版本过期 | 保留本地，读取远端后由用户选择；不自动覆盖 |
| 409 STUDIO_OPERATION_CONFLICT | 同 requestId 异参 | 不产生新副作用；恢复原请求或新建明确操作 |
| 409 STUDIO_RESOURCE_LOCKED | 草稿归档、计划准备中、重复冲突任务 | 显示可读状态，禁止当前写入 |
| 409 STUDIO_PLAN_STALE | 正文／来源／确认版本不匹配 | 提示重新核对，不自动生图 |
| 409 STUDIO_QUOTE_EXPIRED | quote 到期、模型／价格／选项漂移 | 刷新估算，重新确认制作范围 |
| 409 STUDIO_REFERENCE_UNSUPPORTED | 请求通用参考但当前协议不支持 | 明确能力；用户可选择 prompt-only 并重新估算 |
| 409 STUDIO_ANCHOR_REQUIRED | 后续页缺有效封面引用 | 引导先生成／选择封面，不自动找旧图 |
| 409 STUDIO_MEDIA_UNAVAILABLE | 素材删除、隔离、授权撤销或失效 | 指明受影响项；不转成付费生成补缺 |
| 409 STUDIO_UNKNOWN_OUTCOME | 外部请求结果未知 | 查询／核实；图像主动重做需确认，微信不得盲重发 |
| 409 STUDIO_CHANNEL_CONTENT_MISMATCH | 外部草稿与快照不一致 | 显示核对失败，不置 succeeded |
| 409 STUDIO_CHANNEL_ACCOUNT_BOUND | appId 已存在未断开的绑定 | 不返回另一 owner 身份或凭据 |
| 402／沿用计费错误 | 积分或预算不足 | 不生成本地成功结果，不绕过执行环 |
| 422 STUDIO_CHANNEL_ACCOUNT_INVALID | 连接未验证／已断开／凭据失效 | 到连接设置修正和校验 |
| 429 STUDIO_BUSY | 本地有界队列／速率超限 | 保留输入，允许稍后原键重试 |
| 502 STUDIO_INVALID_PLAN | 模型输出违约 | 保留运行和错误，不执行计划 |
| 502 STUDIO_PROVIDER_FAILED | 模型／外部 API 明确失败 | 脱敏原因；按确定性失败规则处理 |
| 503 STUDIO_DEPENDENCY_UNAVAILABLE | 存储、Redis、Temporal、加密设施或模型缺失 | 明确缺项；没有成功假数据 |
| 504 STUDIO_TIMEOUT | 确定未成功的时限失败 | 保留输入和记录；结果不确定时使用 unknown 状态 |
| 无响应 | 客户端断网／超时 | 无服务端 code；读原操作，禁止自动换键 |

旧 API 原有 code 和 message 保持；新增条件才使用以上 code。底层供应商原文、堆栈、密钥和完整 URL 不直接返回用户。

### 6.10 Edge、SSE 与媒体

- `/api/creation-studio` 注册 GET/POST/PATCH 前缀，独立 `EDGE_ROUTE_CREATION_STUDIO_INTELLIGENCE`，上游 intelligence；未知叶子仍由 Controller 404，未登记方法 404。既有 `/api/creation-drafts/{id}/exports` 继续原路由，新格式另受服务端写开关约束。
- `/api/creation-channels/wechat` 注册 GET/POST 前缀，独立 `EDGE_ROUTE_CREATION_WECHAT_INTELLIGENCE`；DELETE/PUT/PATCH 等未用方法不放行。
- 补 `/api/card-series/operations` 的 GET 前缀，沿用 `EDGE_ROUTE_CARD_SERIES_INTELLIGENCE`；不扩大成 `/api/**`。
- 本任务不改变旧 POST SSE 的帧序、Content-Type、`[DONE]`、错误尾帧和取消语义；NEW 策划接口是 JSON＋操作读取，不用原 SSE 客户端强行解包。
- 实际文件下载通过已有对象存储短期授权，遵守真实 Content-Type／文件名／大小；新 URL 不带站内 cookie/header。已有媒体 Range／206 不修改。
- source 文件在浏览器按 UTF-8 严格解码后通过 JSON 提交；不增加任意文件上传后执行的入口。现有素材上传继续用 `putToPresignedUrl`。

## 7. 数据模型、迁移与生命周期

### 7.1 迁移责任

| 迁移（NEW） | 唯一 DDL 责任卡 | 内容 |
|---|---|---|
| V77__creation_source_document.sql | 02 | 不可变源文档、请求幂等与草稿引用索引 |
| V78__creation_text_proposal.sql | 04 | 文本建议、运行关联、应用结果 |
| V79__creation_visual_plan.sql | 05 | 视觉计划、不可变 revision、quote、应用幂等 |
| V80__creation_visual_execution.sql | 08 | 扩展 card_series_operation，增加子项、artifact、运行关联和恢复字段 |
| V81__creation_export_artifact.sql | 18 | 导出快照、文件、构建状态与清理时间 |
| V82__creation_wechat_account.sql | 19 | 账号级公众号连接、加密凭据、版本和校验状态 |
| V83__creation_wechat_draft_sync.sql | 21 | 同步任务、冻结 payload、上传映射、派发与核实状态 |
| V84__creation_studio_integrity.sql | 24 | 复核补丁：导出构建租约、公众号连接唯一性、上传映射保留与数据 CHECK；不修改 V1～V83 checksum |

开工重新查版本，若编号已占用但语义无冲突，可只顺延本书的新 migration 号并同步测试及本表；这是已允许的编号适配，不需为纯编号变化重复询问。不得改动 V1～V76 或其他服务历史 migration。新表不建跨服务 FK，逻辑关系由服务验证；唯一键、CHECK 和索引必须落数据库。

### 7.2 表与字段（NEW）

除注明外，id 为 UUID PK，时间列 timestamptz，owner_account_id 为 text。JSONB 必须经 DTO 校验后存储；表中 request_hash/内容 hash 为 char(64)，版本为正整数。

| 表 | 必需字段 | 约束与索引 |
|---|---|---|
| creation_source_document | id、owner_account_id、draft_id、request_id、request_hash、kind、schema_version、title、raw_text、normalized_markdown、content_hash、blocks_json、source_refs_json、warnings_json、created_at | UNIQUE(owner,request_id)；索引(owner,draft,created_at DESC,id DESC)；kind/schema/字节上限校验 |
| creation_text_proposal | id、owner_account_id、draft_id、request_id、request_hash、action、status、base_draft_version、base_content_hash、source_document_id、selected_blocks_json、input_snapshot_json、prompt_ciphertext、prompt_hash、result_json、run_id、error_code、created_at、expires_at、applied_draft_version、apply_request_id、apply_result_json | UNIQUE(owner,request_id)；apply_request_id 唯一但可空；status/action CHECK；owner+draft 最近索引 |
| creation_visual_plan | id、owner_account_id、draft_id、request_id、request_hash、source_document_id、source_content_hash、base_draft_version、base_content_hash、recipe_id、recipe_version、upstream_commit、input_snapshot_json、prompt_ciphertext、prompt_hash、status、current_revision、confirmed_revision、run_id、error_code、created_at、updated_at | UNIQUE(owner,request_id)；status CHECK；owner+draft 最近索引 |
| creation_visual_plan_revision | plan_id、revision、document_json、document_hash、created_at | PK(plan_id,revision)；不可 UPDATE 旧 revision |
| creation_visual_quote | id、owner_account_id、plan_id、plan_revision、request_id、request_hash、quote_json、created_at、expires_at | UNIQUE(owner,request_id)；plan/revision 和 expires_at 索引；JSON 不保存明文凭据 |
| creation_studio_apply | id、owner_account_id、request_id、request_hash、kind、resource_id、applied_draft_version、result_json、created_at | UNIQUE(owner,kind,request_id)；apply 与草稿历史／写入同事务 |
| card_series_operation（扩展） | 保留原列；加 api_version(default 1)、job_kind、draft_id、plan_id、plan_revision、quote_id、snapshot_json、job_version(default 1)、cancel_requested(default false)、workflow_id、dispatch_state、settlement_state | 旧行和旧 status 不改；v2 单独服务校验；索引 api_version+dispatch_state+updated_at；已有 owner/request 唯一键继续使用 |
| creation_visual_item | id=attemptId、operation_id、item_id、position、state、execution_operation_id、run_id、input_hash、prompt_ciphertext、anchor_artifact_id、original_media_id、artifact_id、error_code、claim_token、claimed_until、heartbeat_at、created_at、updated_at | UNIQUE(operation_id,item_id)；UNIQUE(execution_operation_id)；state CHECK；可领取状态／租约索引 |
| creation_visual_artifact | id、owner_account_id、draft_id、plan_id、plan_revision、item_id、attempt_id、run_id、original_media_id、delivery_media_id、target_aspect、width、height、content_hash、anchor_artifact_id、created_at | UNIQUE(attempt_id)；索引(owner,draft,plan_id,item_id,created_at)；宽高>0 |
| creation_export_artifact | id、owner_account_id、draft_id、draft_version、request_id、request_hash、format、render_version、options_json、snapshot_json、state、object_key、filename、content_type、size_bytes、sha256、error_code、created_at、expires_at | UNIQUE(owner,request_id)；快照可重建；state/format CHECK；state/过期清理索引 |
| creation_wechat_account | id、owner_account_id、display_name、app_id、encrypted_secret、key_version、state、version、verified_at、error_code、created_at、updated_at、disconnected_at | app_id 在未断开行部分唯一；owner 最近索引；断开后 encrypted_secret 清除，不删历史身份 |
| creation_wechat_draft_sync | id、owner_account_id、account_id、account_version、draft_id、draft_version、export_id、request_id、request_hash、payload_hash、snapshot_ciphertext、upload_map_json、state、version、external_media_id、submit_started_at、cancel_requested、error_code、verified_at、workflow_id、created_at、updated_at | UNIQUE(owner,request_id)；连接+版本+payload_hash 对进行中/unknown/succeeded 行做部分唯一；owner+draft 最近索引；state CHECK |

`prompt_ciphertext` 和微信冻结正文使用既有 EnvelopeEncryption；元数据／内容 hash 可明文。source 和草稿正文沿用业务存储保护，不额外宣称端到端加密。没有 KEK 时拒绝需要加密记录的制作／渠道动作；source 导入及基础排版仍可用。

### 7.3 事务与并发

1. 生成操作占位与 item 创建一个事务；原子确定谁拥有派发权。HTTP 200/202 只能在记录提交后返回。
2. 计划写 revision、推进指针、清确认在一个事务。采用和正文应用锁 draft 行，再验证 plan/source；锁顺序固定 draft → plan → apply，避免交叉死锁。
3. 外部 HTTP／对象存储／Temporal 调用在数据库事务外。先持久化请求意图，后调用，再 CAS 写回结果；不能长时间持锁等网络。
4. 对父操作同键重放先校验本人，然后比较请求摘要；不得因为参数相同就让另一账号读到结果。
5. 图像 run 绑定在外部派发前落库；结果媒体在结算前落库。首次返回丢失、服务重启和 Temporal 重放都从状态继续，不从函数头重新扣费。
6. 草稿应用返回完整项目；冲突不追加版本、不改变结果选择。已有草稿整行保存逻辑不能把其他新字段清空。

### 7.4 旧数据与删除

- 新增列可空或有安全缺省，旧 v1 图卡记录按旧接口原样读取。新版 Controller 不把缺字段旧行当成可继续执行的 v2 job。
- 旧草稿仍可打开、编辑和导出 manifest；原有成功图卡继续可查看。切到新版需显式创建 source 和 plan，保留旧媒体，不自动重新收费。
- `workspace.inputs.studio.schemaVersion=1` 与整个 workspace.schemaVersion=1 分别校验；视频／朋友圈／点评的 workspace 不注入空 studio，不改变其 hash 或保存频率。
- 草稿软删后所有新资源拒绝 owner API 读取，queued 项取消；运行中的外部请求只收尾持久化／结算，不把结果挂给别的草稿。
- 来源、计划、已采用 artifact 随可恢复草稿历史保留；不因 30min quote/建议过期删除原稿。候选原图至少保留 90 天，已采用结果沿用永久媒体策略；未采用候选清理前需核对草稿历史与 anchor 依赖。
- 导出文件 7 天清理，仅删导出副本；已有媒体和 AI run 不随导出过期删除。微信同步记录至少 90 天，断开连接不删除远端内容。

### 7.5 升级与回退演练

在隔离 PostgreSQL 验证：空库到 V84；V76 有草稿、旧图卡操作、视频画布数据时升级；重跑 migration 幂等语句；约束拒绝非法状态；升级后旧 HTTP／旧前端 fixture 正常。既有数据 row count、内容 hash、媒体 ID、旧迁移 checksum 必须保持。

回退使用开关和旧代码，不执行 DROP 新表／列。旧代码忽略新列，已写 studio 的草稿由新读侧保留查看；若旧前端无法理解，明确只读而非用空对象覆盖。生产回退前先停止新写并处理运行中任务；没有排空证据不能直接停 worker。

### 7.6 配置登记

| 配置 | 缺省 | 作用 |
|---|---|---|
| EDGE_ROUTE_CREATION_STUDIO_INTELLIGENCE | false | 新 studio GET/POST/PATCH 路由总闸 |
| EDGE_ROUTE_CREATION_WECHAT_INTELLIGENCE | false | 微信 GET/POST 路由总闸 |
| CREATION_STUDIO_WRITES_ENABLED | false | 服务端拒绝新 source/建议/plan/job/采用/新导出写入；读取既有记录仍允许 |
| CREATION_VISUAL_WORKER_ENABLED | false | 不领新视觉 item；已有网络请求允许持久化与结算收尾 |
| CREATION_WECHAT_WRITES_ENABLED | false | 拒绝新连接变更／同步提交；已存记录读取、结果核实及安全断开仍允许 |
| CREATION_WECHAT_WORKER_ENABLED | false | 不派发新上传／draft/add；已提交结果继续核实 |

服务端映射在 `application.yml` 的 `creation.studio`／`creation.wechat`；Edge 映射在对应 application.yml。`.env.example`、`.env.docker.example`、`docker-compose.yml` 和 `docker-compose.production.yml` 同步模板透传，禁止写入真实密钥。增加 Temporal queue 配置，沿用现有 Temporal target／namespace，禁止新起独立编排基础设施。

开放顺序：迁移 → 后端及 worker（写开关关闭）→ Edge 读路径 → 前端 → 本地门禁 → 经授权小范围开启写入。关闭顺序：关业务写开关 → 查询并排空／核实在途操作 → 关 worker → 最后关 Edge。C101-01 负责配置登记，C101-10/21 只接已登记开关，避免多卡写配置漂移。

## 8. UI 实现规格

### 8.1 页面与组件职责

| 页面／区域 | 目标结构 | 主动作与辅助动作 |
|---|---|---|
| 创作中心 | 保留平台／形式／来源；增加简短“从已有内容开始”和四模板入口 | 继续创作；模板只预填，不另造平台导航 |
| 原稿输入 | 模式、粘贴／选 TXT/MD、来源说明、字数／范围、原稿预览 | 导入并编辑；错误留在输入旁 |
| 正文 | 沿用 ContentStage；按来源显示“改编建议”“只排版”“制作图卡” | 已有内容可以直接检查／制作，不强迫经过标题大纲 |
| 视觉计划 | 顶部策略及整组风格；下方每页内容、来源、布局、画幅；一套推荐 | 保存计划、确认计划；其他方案作为次要动作 |
| 图片制作 | 左侧项目／条目，右侧选中项候选与大图；真实阶段与错误 | 先生成封面／生成所选、重做本项、采用本项 |
| 文章配图 | 封面及段落插图和原文位置联合显示 | 选择素材或生成，改文字不自动生图 |
| 排版预览 | 原稿与预览双栏，standard/compact 两主题，缺图和元数据提示 | 保存、下载；生成摘要作为独立付费建议 |
| 交付面板 | 标题／正文或发布描述／摘要／话题／声明／采用媒体 | 下载文件；条件齐全时存公众号草稿 |
| 公众号连接 | 复用 GlModal；账号列表、绑定、验证、轮换、断开 | 显示 AppID 用于辨认；密钥仅输入不回显 |
| 草稿同步 | 明确目标账号、当前草稿版本、预览和同步历史 | 存入草稿箱；unknown 显示核实，不显示重发成功 |

### 8.2 交互细节

- 所有生成／应用／同步按钮在请求中禁用；表单保留原值，状态文案不改变按钮宽度。模型不支持参考时展示具体原因，不让用户点击后才静默降级。
- source 文件读取失败保留此前正文。一次只处理一个导入，不因第二次文件选择尚未完成就覆盖第一份成功结果；以输入 revision 和账号 epoch 丢弃迟到返回。
- source/plan 切换可能使正在编辑内容失去上下文时，用现有 GlModal 说明影响；取消切换则完全保留编辑。已经保存的旧产物不删除。
- 卡片提供上移／下移按钮，拖拽只是增强；键盘可完成排序、编辑、确认、生成、采用和导出。排序不能误触生图。
- 候选大图显示“原图／交付画幅”切换；删除候选或替换采用引用不自动删掉素材库原始媒体。
- 计划编辑期间显示未保存状态；生成按钮必须先 flush 计划和 draft。服务器 apply 期间锁本草稿编辑，网络结果未知时优先核实，不清空用户输入。
- 公众号动作前显示目标账号名／AppID、草稿版本、标题、封面、摘要、正文预览、评论选项；点击“存入草稿箱”仅授权这次对应快照。默认评论两个开关均为 0，用户可开启。
- 同步结果文案：“已存入草稿箱”“正在核实草稿”“结果待核实”“未能存入草稿箱”；不出现“已发布”或虚构公开链接。
- 关闭设置弹窗清理内存明文 AppSecret；不进 localStorage、Pinia 持久状态、URL、workspace、日志或截图。验证错误不能回显秘密。

### 8.3 设计和响应式

遵守 DESIGN：`<768px` 单栏，`768～1023px` 平板，`≥1024px` 双栏，宽屏受现有 layout token 限制。移动端计划与预览用页内切换，固定动作栏不得遮挡最后一行内容、输入法和焦点；320px 宽度不得出现整页横向滚动，表格只能局部滚动。

应用 UI 只用既有 token；必要 token 先在 `src/style.css` 两主题成对定义并与 DESIGN 对齐。原图、交付图和导出文件的内容配色是内容数据，不随用户切换 UI 明暗主题自动改色或重新生成。导出样式不引入第三方字体 CDN。

复用 GlModal、EmptyState、WorkspaceSaveBadge、SafetyFindingsPanel 和已有按钮／字段类；不在单个 SFC 里重造基本控件。`AiCreationCenter.vue` 豁免上限只减不增，新增编排先抽独立 composable；五大视图继续纯装配。

### 8.4 状态与无障碍矩阵

每个新面板必须有：初始／加载／空结果／输入错误／服务错误／提交中／成功／无权限／功能关闭／旧数据只读状态。错误有可见文本和 aria-describedby；状态更新 aria-live=polite，不持续播报轮询次数；模态框限制焦点、Escape 关闭、关闭后回到触发按钮；媒体预览有文字说明。

普通文字对比度≥4.5:1，必要控件边界和焦点≥3:1；点击区域按既有40px／44px规则。prefers-reduced-motion 时取消位移动画。耗时任务可离开页面，不用不可关闭的 loading 遮罩阻止其他工作。

### 8.5 截图清单

| SC 编号 | 页面／状态 | 入口与视口 | 负责卡 |
|---|---|---|---|
| SC101-01 | 原稿输入、长文、非法文件、format 直达 | 用户任务／独立AI；1440×900、390×844、320×740；亮暗 | 03 |
| SC101-02 | 计划推荐、逐页编辑、缺来源、未保存冲突 | 用户任务／独立AI；桌面和390移动；亮暗 | 06 |
| SC101-03 | openai-image 凭据／模型选择、个人／组织 BYOK 帮助 | 治理台及AI设置；桌面和390；亮暗 | 07 |
| SC101-04 | 封面等待、部分成功、unknown、候选采用 | 两创作入口；桌面和390；亮暗 | 11、12 |
| SC101-05 | 最近项目恢复、旧图卡仍可读、已采用结果 | 两创作入口；桌面和390；亮暗 | 12、13 |
| SC101-06 | 文章配图、段落变更、封面预设 | 两创作入口及现有视频封面工具；桌面和390；亮暗 | 15 |
| SC101-07 | 排版双主题、表格／代码、外图未绑定 | 两创作入口；桌面和390；亮暗 | 17 |
| SC101-08 | 文件导出进行中、缺图、下载完成 | 两创作入口；桌面和390；亮暗 | 18 |
| SC101-09 | 连接绑定、校验失败、轮换、断开 | AI设置／交付弹窗；桌面和390；亮暗，秘密遮盖 | 20 |
| SC101-10 | 微信预览、草稿成功、结果待核实、候选选择 | 两创作入口；桌面和390；亮暗 | 22 |
| SC101-11 | 全链路键盘焦点、模块关闭、账号切换 | 三入口受影响面；桌面和390；亮暗 | 23、24 |

每个 SC 至少保存主状态的亮暗各一张，错误／加载等补充状态也需覆盖对应两主题；键盘检查记录焦点顺序和恢复结果。截图写 `test-artifacts/task-101/ui/SC编号-入口-状态-主题-宽度.png`，不能用设计稿或伪造页面替代浏览器结果。

## 9. 写入范围与工程约束

### 9.1 文件白名单

§11 各卡精确路径表中标为“新增／修改”的文件并集，构成本任务全局写入白名单。卡只写自己列出的子集；只读锚点不授予写权限。各卡共享同一文件时严格串行并保留前卡成果，不按文首旧基线覆盖。目录名称不代表目录下任意文件均可改。

额外允许维护本文、`docs/任务书/README.md`、`docs/文档总索引.md` 与本次研究文件的关联／边界说明。不得把 `docs/status.yaml` 的 #22～51 范围扩大成 #101 已完成声明。

禁止写入：真实 `.env`／密钥／Cookie；既有 V1～V76；finance、identity、marketplace、trust 的业务和数据表；既有视频生产／画布核心流程；三 HTML 防闪脚本与 CSP；未列出的依赖／lockfile／CI 配置。首次列出的 CommonMark 两依赖之外没有第三方依赖新增权限。

生成物允许在 `test-artifacts/task-101/`、`coverage/`、`playwright-report/`、`test-artifacts/playwright/` 及 Gradle `build/`；不作为业务源码提交。未登记的临时分析脚本仅放 `scripts/local/`，不作为产品运行依赖。真实截图不得包含用户正文、秘密或仍有效签名 URL。

### 9.2 适用约束

2026-09-14 复核修订：为修复已实现流程的契约、共享保存互斥和生命周期问题，C101-23 另允许修改 `src/composables/useCreationDraft.ts` 及其已有测试，新增 `src/lib/creation-studio-http.ts`、`src/views/article/composables/useArticleStudio.ts` 及同目录针对性测试。前者复用现有请求和账号会话，后者承接文章视图的业务装配；不增加产品能力、供应商、收费规则或生产权限。

本轮用户已要求补完并修正发现的问题，以下必要辅助文件一并登记到 C101-23/24；其余卡的原有精确白名单继续适用：

| 精确补充路径 | 限定用途 |
|---|---|
| `src/components/GlModal.vue`、`src/components/GlModal.test.ts` | 共享弹窗分层 Escape、焦点循环／恢复、背景 inert；保留旧宿主默认行为和既有测试 |
| `src/views/article/composables/useArticleUrlState.ts`、`src/views/article/composables/useArticleUrlState.test.ts` | 将待写入地址栏的稳定 ID 立即提供给恢复逻辑，防止新旧图片任务循环切换 |
| `src/views/article/composables/useStudioLifecycle.test.ts` | 页面失活、重新激活和迟到响应回归 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/StudioCommandStore.java` | 同资源命令幂等与事务互斥 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/StudioCursor.java` | 受校验的分页游标 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatContentVerifier.java` | 公众号回读时核对文字、图片顺序和封面 |
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V84__creation_studio_integrity.sql` | 只向前追加本任务的数据完整性修正 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/StudioTestFiles.java` | 本任务真实文件字节测试夹具 |
| `scripts/acceptance/creation-studio-server.mjs` | 独立三入口 UI fixture 宿主，不加载 .env、不回退真实 API |
| `tests/deployment/canvas-e2e-fixture.contract.test.ts` | 仅将两项假凭据加上明确 test 标记，使既有秘密扫描正确识别；不改画布业务或断言 |

| 约束 | 卡 | 必须遵守 |
|---|---|---|
| UI／分层 | 03、06、07、10～13、15、17、18、20、22～24 | 两套设计路由、共享组件、token、亮暗、移动键盘、SFC上限 |
| HTTP／入口 | 01～24 | 原 JSON/SSE/媒体契约、Edge fail-closed、账号 epoch、独立 origin |
| Java／响应式 | 后端各卡 | Java25，Mono.defer 延迟副作用；不可在 WebFlux event loop block；activity 仅在其工作线程边界使用既有阻塞惯例 |
| 数据 | 02、04、05、08、09、12、18、19、21、24 | 单域事务、唯一键、版本锁、迁移不重号、真实PostgreSQL测试 |
| AI／预算 | 04、05、07～15、23 | 既有执行环、冻结路由、受信出站、用量／补偿、安全检查；不手写积分／现金扣退 |
| 内容与外部输入 | 02～06、11、14、16～23 | 不运行HTML／SVG／脚本；明确来源、媒体权限与变更范围 |
| 质量 | 全部 | 每卡针对性检查，里程碑集成；保持现有覆盖率门槛，不删测试／降断言换绿灯 |
| 交付 | 全部 | 对话报告事实，NOT_RUN 如实记；未获授权不做真实付费／生产操作 |

## 10. 执行总表与交接

全部按编号串行；原因是共享 DTO、来源／计划、图片执行器、视图和测试基座持续演进。可以只读并行研究，但本规格不要求多代理并发写入。

下表 `VERIFIED` 仅表示 §12.6 的本地验收通过；M1 的真实参考图质量与 M3 的真实公众号草稿仍为 `NOT_RUN`，不能据此对外开放。

| 卡 | 交付 | 前置关键成果 | 对应需求 | 状态 |
|---|---|---|---|---|
| C101-01 | 目录、DTO、Edge和开关 | 当前源码 | R101-01、18 | VERIFIED |
| C101-02 | 原稿存储与块解析 | 01 类型／开关 | R101-02、09 | VERIFIED |
| C101-03 | 原稿入口与三模式分流 | 02 来源API | R101-01～03、17 | VERIFIED |
| C101-04 | 可预览并原子应用的改编建议 | 02 来源；03 保存入口 | R101-03、04、16 | VERIFIED |
| C101-05 | 服务端视觉计划与版本 | 02 来源；04 上下文／应用惯例 | R101-05、16 | VERIFIED |
| C101-06 | 计划编辑／确认 UI | 05 计划API | R101-05、09、17 | VERIFIED |
| C101-07 | 原生图片协议与能力探测 | 01 DTO；05 模板需求 | R101-06、16、17 | VERIFIED |
| C101-08 | 图像运行／结果持久绑定 | 07 协议；既有执行环 | R101-07、16 | VERIFIED |
| C101-09 | 画幅衍生图与永久结果 | 08 原图与运行绑定 | R101-08、16 | VERIFIED |
| C101-10 | 异步视觉任务与参考链 | 05计划；08执行；09artifact | R101-07、08、16 | VERIFIED |
| C101-11 | 状态、重做与候选界面 | 10 job API与09artifact | R101-08、17 | VERIFIED |
| C101-12 | 采用、交付与旧项目兼容 | 06计划；11artifact | R101-09、16、17 | VERIFIED |
| C101-13 | M1联合验收 | 01～12 | R101-01～09、16～18 | VERIFIED |
| C101-14 | 文章视觉策划与段落绑定 | 05、11、12 | R101-10、16 | VERIFIED |
| C101-15 | 文章配图与封面交互 | 14计划；11候选 | R101-10、17 | VERIFIED |
| C101-16 | 确定性 Markdown/HTML 渲染 | 02解析；12交付 | R101-03、11 | VERIFIED |
| C101-17 | 排版预览与文字保留 | 16渲染API | R101-03、11、17 | VERIFIED |
| C101-18 | 实际文件与ZIP导出 | 11媒体；16渲染；17预览 | R101-12、16、17 | VERIFIED |
| C101-19 | 公众号连接和加密管理 | 01开关；既有加密/Redis | R101-13、16 | VERIFIED |
| C101-20 | 公众号连接设置 UI | 19连接API | R101-13、17 | VERIFIED |
| C101-21 | 草稿上传、派发、核实 | 18导出；19连接 | R101-14～16 | VERIFIED |
| C101-22 | 微信预览、同步与历史 | 20设置；21同步API | R101-14、15、17 | VERIFIED |
| C101-23 | 安全、组合流程与三入口回归 | 01～22 | R101-16、17 | VERIFIED |
| C101-24 | 迁移、质量、发布与最终交接 | 23联合验收 | R101-09、18 | VERIFIED |

每卡交接记录：实际源文件／符号、接口样例、migration、对应 TC/V、测试结果、可恢复操作 ID（脱敏）及未完成外部验收。卡间公共契约只在 §6/§7 维护一份；新增行为先修订规格版本再实现。

## 11. 开发任务卡

本节所有卡共同开始检查：核对依赖已通过；记录当前未提交文件；按真实源码核对锚点；记录针对性基线结果；按 §9 检查白名单。所有卡共同结束要求：完成 AC、TC、命令和适用 SC，并按 §14 对话交接。命令工作目录：npm 在仓库根，Gradle 在 `platform-java/`，运行前准备 Java25。下列测试路径／测试类为本卡明确新增或修改的目标，不代表当前已经存在或通过。

### C101-01：建立模板目录、DTO 与功能路由

**输入／输出**：输入当前基线与§3/§6；输出四模板结构、完整共享DTO、关闭状态下不影响旧入口的路由和配置。对应 R101-01、18；执行／验收责任按文首。

**必读锚点**：`PlatformCreationRuleCatalog` 的契约加载；`RouteProperties`、Edge application.yml 的 method/exact/flag；`AI_PLATFORM_DEFINITIONS`。API101-01、§7.6 是本卡契约，其他接口尚未实现时保持404，不返回占位成功。

| 精确写入路径 | 操作与限定 |
|---|---|
| `contracts/creation-recipes.v1.json` | 新增；四模板与版本、适用性；先启用 social-card-series，其余由后续负责卡启用 |
| `contracts/creation-visual-presets.v1.json` | 新增；迁入现有视觉预设与受控补边颜色，前后端单源 |
| `src/constants/card-series-templates.ts` | 修改；从新契约导出原常量与查询函数，保留全部旧ID／签名 |
| `src/constants/card-series-templates.test.ts` | 修改；验证旧预设兼容及背景数据，不重造组件快照测试 |
| `src/types/creation-studio.ts` | 新增；§6声明 |
| `src/config/creation-recipes.ts` | 新增；目录读取、合法组合选择；不修改九平台ID |
| `src/config/creation-recipes.test.ts` | 新增；目录与禁用组合测试 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationRecipeCatalog.java` | 新增；版本、ID及适用性校验 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationVisualPresetCatalog.java` | 新增；视觉ID、描述、palette与布局的服务端加载 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationStudioProperties.java` | 新增；§7.6开关绑定 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationRecipeController.java` | 新增；API101-01 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/StudioRequestValidator.java` | 新增；NEW JsonNode字段类型、未知字段、枚举、整数、null与长度校验，后续Controller复用 |
| `platform-java/services/intelligence-service/build.gradle.kts` | 修改；仅增加新contract的processResources复制；依赖由02卡负责 |
| `platform-java/services/intelligence-service/src/main/resources/application.yml` | 修改；登记服务开关和两个新task queue：intelligence-creation-visual、intelligence-wechat-draft |
| `platform-java/services/edge-bff/src/main/resources/application.yml` | 修改；新studio/微信方法路由；补旧图卡operation GET |
| `.env.example` | 修改；新开关模板，不含凭据 |
| `.env.docker.example` | 修改；新开关模板，不含凭据 |
| `docker-compose.yml` | 修改；新配置透传 |
| `docker-compose.production.yml` | 修改；新配置透传，默认不启用业务 |
| `tests/deployment/creation-studio.contract.test.ts` | 新增；路由／默认值／配置透传契约 |
| `platform-java/services/edge-bff/src/test/java/com/grassland/edge/proxy/JavaRouteManifestGateTest.java` | 修改；增加新方法和flag关闭负向用例 |

**做法**：先定义目录和DTO；在服务端加载同一JSON；实现GET过滤与旧组合兼容；登记开关并验证关闭状态；补方法、未知路径、非法平台和目录版本测试。`resolveRecipe(id,version,platform,form,mode)` 必须返回唯一合法定义或受控400，不能回退到第一个模板。

**AC101-01**：Given 开关关闭／开启两套配置，When 访问新旧路由和非法方法，Then 新路由按开关受控、旧平台正常、未实现端点404、禁用模板不可生成。

**边界／验收**：TC101-001～004、V-EDGE；UI截图N/A（无新增可见页面）。命令：`npm run test -- src/config/creation-recipes.test.ts src/constants/card-series-templates.test.ts tests/deployment/creation-studio.contract.test.ts`；`./gradlew :services:edge-bff:test --tests 'com.grassland.edge.proxy.JavaRouteManifestGateTest'`。禁止新增另一套平台矩阵或更改旧fallback语义。

### C101-02：保存原稿与不可变段落引用

**输入／输出**：依赖01；交付API101-03/04、V77、可恢复的原稿和真实段落范围。R101-02、09。只读参考 `CreationDraftService.loadOwned`、`CreationWorkspace.parse`、现有 jsoup 依赖。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/build.gradle.kts` | 修改；仅新增D12两个固定版本依赖 |
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V77__creation_source_document.sql` | 新增；§7表与索引 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/source/SourceDocument.java` | 新增；完整源文档／块记录 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/source/SourceDocumentParser.java` | 新增；AST、source spans、code point转换和hash |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/source/SourceDocumentRepository.java` | 新增；owner/request唯一保存和读取 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/source/SourceDocumentService.java` | 新增；create/load，读取真实草稿和版本 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/source/SourceDocumentController.java` | 新增；API101-03/04 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationStudioReferenceValidator.java` | 新增；草稿studio引用owner/draft验证；本卡只开放source，其余非null引用在负责卡实现前拒绝 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationDraftService.java` | 修改；创建／保存时调用新引用验证，不能只校验UUID形状 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationWorkspace.java` | 修改；仅新增studio引用schema校验，保持64KiB |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/SourceDocumentIT.java` | 新增；导入、归属、边界和无网络访问测试 |
| `tests/fixtures/creation-studio/source-samples.json` | 新增；纯合成中文／emoji／表格／代码／恶意HTML／超长样稿 |

**函数与步骤**：`SourceDocumentParser.ParsedSource parse(UUID documentId,String kind,String text)`为纯计算，不抓URL；ParsedSource是Parser内嵌record，字段为rawText、normalizedMarkdown、contentHash、blocks、warnings，与§6对应字段同类型。块ID由documentId和块序号确定。service.create接收API101-03完整DTO及Caller，先校验草稿owner/version，再解析、原子insert/replay；draft-content只读取服务端正文；load仅owner且草稿未删可读。跨度必须能切回normalizedMarkdown，重复段落有不同块ID；直接PUT伪造其他草稿source引用也必须拒绝。依赖解析失败不回退手写正则。

**AC101-02**：Given 含中文、重复段落和代码的原稿，When 导入、断线原键重试并读取，Then 只有一份source，完整原文一致、跨度正确、不改草稿、不访问外链；其他账号404。

**边界／验收**：TC101-005～010、V-MIGRATION；UI N/A。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.SourceDocumentIT'`。未执行迁移或真实PostgreSQL用例不能把本卡记为VERIFIED。

### C101-03：打通原稿入口与加工方式

**输入／输出**：依赖02 API；输出从原稿直接进入正文／图卡的用户路径。R101-01～03、17。必读 `resolveWorkflow`、`useArticleCreation`、`useArticleWorkspace`、根DESIGN，§4/§8。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/ai-center/components/CreationSourceEntry.vue` | 新增；来源／模式入口，复用既有平台和锁定上下文 |
| `src/views/ai-center/creation/useCreationRecipeEntry.ts` | 新增；场景handoff构造，不伪造任务快照 |
| `src/views/ai-center/AiCreationCenter.vue` | 修改；装配新入口；先抽逻辑保证豁免行数只减不增 |
| `src/views/article/components/SourceDocumentInput.vue` | 新增；TXT/MD/粘贴和明确长度范围 |
| `src/views/article/composables/useSourceDocument.ts` | 新增；读取、导入、响应epoch和状态 |
| `src/views/article/ArticleCreationView.vue` | 修改；仅装配与入口分派 |
| `src/composables/useArticleCreation.ts` | 修改；importContent行为，不自动生成标题／正文 |
| `src/views/article/composables/useArticleWorkspace.ts` | 修改；保存source与recipe引用；原文放草稿content |
| `src/types/ai-creation.ts` | 修改；增加可选recipe输入引用；保留既有联合类型和source语义 |
| `src/views/article/components/SourceDocumentInput.test.ts` | 新增；文件、模式和保稿测试 |
| `src/views/ai-center/AiCreationCenter.test.ts` | 修改；模板预填和任务锁定回归 |
| `src/views/article/ArticleCreationView.test.ts` | 修改；原稿直达且未发标题／大纲请求的断言 |

**步骤**：浏览器严格UTF-8解码；先创建source，再通过共享队列保存完整草稿；成功后进入正文。format只允许人工编辑／检查／后续排版，不触发生成；adapt显示后续建议入口。切换来源保留已有草稿和任务约束，模板不可用不静默跳视频。新字段只用于预填，后端仍核验。

**AC101-03**：Given 已有原稿或任务带入内容，When 选format并导入，Then 原文进入正文、来源锁保留、标题/大纲/正文生成请求均为0；文件失败和409不清空此前编辑。

**边界／验收**：TC101-011～014、SC101-01。命令：`npm run test -- src/views/article/components/SourceDocumentInput.test.ts src/views/article/ArticleCreationView.test.ts src/views/ai-center/AiCreationCenter.test.ts`，再 `npm run typecheck`。M1只提供正文保留路径，HTML预览由17卡启用，不能显示未实现预览的成功状态。

### C101-04：改编建议、差异预览与原子应用

**输入／输出**：依赖02来源和03入口；输出API101-05～07、V78与安全的应用动作。R101-03、04、16。只读锚点 `ArticlePrompts`、`CreationBriefInput`、`FrozenTextExecutionService`、`CreationDraftService.save`、账号epoch。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V78__creation_text_proposal.sql` | 新增；文本建议表 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationStudioContextService.java` | 新增；服务端草稿／任务／Brief读取和上下文摘要 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/text/TextProposalService.java` | 新增；准备、解析、读取与应用 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/text/TextProposalRepository.java` | 新增；幂等、运行和应用结果事务 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/text/TextProposalController.java` | 新增；API101-05～07及其DTO |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationStudioReferenceValidator.java` | 修改；增lastProposalId归属验证，保留source规则 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/run/FrozenTextExecutionService.java` | 修改；onPrepared异步回调重载，在模型调用前持久化runId；旧调用语义保持 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationDraftService.java` | 修改；抽复用同事务完整写路径，不能新建旁路save |
| `src/views/article/components/TextProposalPanel.vue` | 新增；原文／候选差异与字段选择 |
| `src/views/article/composables/useTextProposal.ts` | 新增；API读取与请求幂等 |
| `src/views/ai-center/creation/useWorkspaceAutosave.ts` | 修改；§6.5 runExternalMutation与busy状态 |
| `src/views/article/ArticleCreationView.vue` | 修改；装配建议面板 |
| `src/views/article/components/TextProposalPanel.test.ts` | 新增；无隐式应用、错误／过期行为 |
| `src/views/ai-center/creation/useWorkspaceAutosave.test.ts` | 修改；外部写与原保存队列互斥／乱序 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/TextProposalIT.java` | 新增；TC与一次调用／一次应用证明 |

**函数与步骤**：`prepare` 先claim再走现有文本执行；onPrepared为 `Function<UUID,Mono<Void>>`，在准备run后、请求模型前await，失败不调用模型；普通独立／任务执行各加对应重载，原参数与返回类型保持。建议结果仅字段白名单；`apply`校验过期、源hash和版本后一次写历史／草稿／应用记录。前端用runExternalMutation，不直接改session.draft冒充保存。

**AC101-04**：Given 同一建议被两个窗口同时应用，When 使用同一／不同请求键，Then 只产生一次合法版本变化，另一方重放或409；用户取消预览原稿完全不变，format不会后台发改写。

**边界／验收**：TC101-015～019。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.TextProposalIT'`；`npm run test -- src/views/article/components/TextProposalPanel.test.ts src/views/ai-center/creation/useWorkspaceAutosave.test.ts`。UI并入SC101-01/02；禁止让提示词覆盖冻结任务约束。

### C101-05：服务端视觉计划、修订与费用估算

**输入／输出**：依赖02/04；输出API101-08～12、V79、结构化计划与原始来源对应关系。R101-05、16。必读§5.4、§6.5、`CardSeriesPrompts.systemPlan`、现有规则与品牌上下文。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V79__creation_visual_plan.sql` | 新增；计划、revision、quote、apply表 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlan.java` | 新增；计划与item记录 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanRepository.java` | 新增；版本、owner、确认与不可变快照 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanService.java` | 新增；计划生成、严格解析、更新与确认 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanPrompts.java` | 新增；从上游方法本地化，删除夸大事实和运行命令 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanController.java` | 新增；API101-08～12 DTO/wire |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualQuoteService.java` | 新增；当前可用协议的估算，不扣费；07后读新能力目录 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationStudioReferenceValidator.java` | 修改；增plan/revision所属草稿验证，不信任客户端版本引用 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/VisualPlanIT.java` | 新增；计划／版本／原文覆盖测试 |

**步骤**：完整保存最终组装prompt的受保护记录；只发送用户选定范围；把模型JSON转为受控PlanDocument；验证所有来源／身份／版式后落revision1；PATCH保存新revision；confirm做版本和hash核验；estimate只调用既有路由／价目读取，不prepareMediaExecution。本卡对旧协议只能报价prompt-only，不能假报原生参考支持。

**AC101-05**：Given 含数字的原稿和指定5张卡，When 生成、编辑、确认及重复估算，Then 恰好5项且来源可查、错误数字被拒绝、编辑清确认、估算没有AI调用／扣费、旧revision仍可读。

**边界／验收**：TC101-020～025。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.VisualPlanIT'`。UI N/A；禁止把模型返回的HTML、工具调用或任意字段写进计划。

### C101-06：计划编辑、来源核对与确认界面

**输入／输出**：依赖05；交付逐页计划编辑、保存队列、来源查看和确认。R101-05、09、17。锚点 `CardSeriesPanel.vue`、`useCardSeries`、GlModal；读§6.5/§8。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/article/components/VisualPlanEditor.vue` | 新增；策略、共享风格和逐项编辑 |
| `src/views/article/components/VisualPlanItemEditor.vue` | 新增；条目、来源片段、排序按钮与关键文字核对 |
| `src/views/article/composables/useVisualPlan.ts` | 新增；每plan单队列、revision、确认与stale |
| `src/views/article/components/CardSeriesPanel.vue` | 修改；装配新版与旧版兼容分支，不删除旧结果 |
| `src/views/article/composables/useArticleWorkspace.ts` | 修改；studio引用序列化与恢复 |
| `src/views/article/ArticleCreationView.vue` | 修改；纯装配 |
| `src/views/article/components/VisualPlanEditor.test.ts` | 新增；编辑／键盘／冲突行为 |
| `src/views/article/composables/useVisualPlan.test.ts` | 新增；保存、快速切换、迟到响应 |

**步骤**：一套推荐默认展开；选择其他策略时说明会新建计划；每页来源可定位；顺序变动不变ID；保存成功后才能确认；现有样式预设不改ID。计划编辑产生自己的800ms队列，只有studio引用变化才写草稿，禁止每次轮询写workspace。

**AC101-06**：Given 已确认的计划，When 改第3页并快速切换草稿，Then 改动按正确plan保存、确认被清除、迟到响应不覆盖新草稿，键盘能完成排序与确认。

**边界／验收**：TC101-026～029、SC101-02。命令：`npm run test -- src/views/article/components/VisualPlanEditor.test.ts src/views/article/composables/useVisualPlan.test.ts src/views/article/components/CardSeriesPanel.test.ts`；`npm run typecheck`。生成按钮对未保存／stale计划不可用。

### C101-07：原生图片协议和参考能力

**输入／输出**：依赖01/05；输出openai-image注册、原生Multipart引用、API101-02和准确能力提示。R101-06、16、17；必读§6.6、当前ImageGenerationClient、BYOK保存校验以及根／ops设计。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/controlplane/PlatformProviderNames.java` | 修改；添加新协议到单一值集，旧值保持 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/controlplane/PlatformModelConfigController.java` | 修改；新协议仅image_generation校验 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/byok/AiProviderKeyController.java` | 修改；同上，保留旧BYOK约束 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/byok/AiOrgProviderKeyController.java` | 修改；组织BYOK同一协议校验，不改组织权限 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/ImageProtocolPolicy.java` | 新增；协议能力和合法输入单源定义 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/ImageGenerationClient.java` | 修改；无参考／有参考两实际调用分支 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/ArticleImageService.java` | 修改；原生参考直接传图片，不额外自动发一次图像描述AI调用 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/ImageCapabilitiesController.java` | 新增；API101-02，读取本人草稿的实际路由 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualQuoteService.java` | 修改；使用新能力和真实尺寸映射 |
| `src/components/AiPlatformCredentialsPanel.vue` | 修改；增协议选项与含义说明，不重做治理面板 |
| `src/components/AiProviderKeysPanel.vue` | 修改；增图片协议帮助与能力约束 |
| `src/components/AiOrgProviderKeysPanel.vue` | 修改；同上 |
| `src/components/AiPlatformCredentialsPanel.test.ts` | 修改；同步合法选项及保留旧选项 |
| `src/components/AiProviderKeysPanel.test.ts` | 修改；个人设置回归 |
| `src/components/AiOrgProviderKeysPanel.test.ts` | 修改；组织设置回归 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/articleimage/ImageProtocolIT.java` | 新增；原生Multipart、旧端点和SSRF／能力负向 |

**步骤**：先补协议／能力测试；原生有参考必须发送真实文件并验证model/prompt/size/n；不支持时生成前拒绝；保存、运行、任务冻结三处保持同一provider值。不能由一个模型名或URL子串将旧协议自动升级。平台控制面原有provider约束无DB枚举新增需求，仍以开工核对为准。

**AC101-07**：Given 三种协议配置，When 请求通用参考、人物参考及无参考，Then 只有符合协议的路径被调用，原生请求含图片字节，旧兼容路径不发生新调用，text能力不能绑定openai-image。

**验收**：TC101-030～034、SC101-03。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.articleimage.ImageProtocolIT'`；`npm run test -- src/components/AiPlatformCredentialsPanel.test.ts src/components/AiProviderKeysPanel.test.ts src/components/AiOrgProviderKeysPanel.test.ts`。V-LIVE-IMAGE 不可用时不妨碍本地代码测试，但不能开放未验证模型参考模式。

### C101-08：图像操作、运行、原图与结算持久绑定

**输入／输出**：依赖07；交付V80、ImageExecutionObserver、固定操作ID和原图已保存／结算待恢复的可核实状态。R101-07、16；读§6.6/§7.3和既有AiExecutionService成功／失败／补偿路径。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V80__creation_visual_execution.sql` | 新增；§7.2父操作扩展、item和artifact表 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/ImageExecutionObserver.java` | 新增；prepared/generated接口 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/IndependentImageGenerationService.java` | 修改；固定executionOperationId和observer重载；旧方法保持 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/TaskImageGenerationService.java` | 修改；同上，保留冻结模型和价目 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/ArticleImageService.java` | 修改；原图按操作确定性key保存，返回可靠mediaId |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/articleimage/GeneratedImageResponse.java` | 修改；兼容增加可选mediaId；旧构造／wire不删字段 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/run/AiExecutionService.java` | 修改；仅增加从已持久run恢复媒体结算的入口；复用原预算／补偿，不改变费率和旧状态语义 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/run/AiRunRepository.java` | 修改；增owner+operation安全回读所需查询 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/cardseries/CardSeriesOperationRepository.java` | 修改；v2占位、快照和版本；旧操作读写保持 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualItemRepository.java` | 新增；租约、run/媒体绑定和CAS状态写 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualExecutionBridge.java` | 新增；单项prepare/execute/reconcile编排 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/VisualExecutionIT.java` | 新增；故障注入、重复派发、运行与预算恢复 |

**步骤**：外部调用前持久run；原图用操作确定性key上传并登记owner媒体；外部成功与原图可读后才结算。对“原图存好、结算失败”只重放结算；对“已派发、结果未知”禁止后台重新prepare和派发。parent/item的unknown描述外部结果，不能仅因旧ai_run处于failed就推断供应商未收费，审计同时展示实际run状态。

**AC101-08**：Given 在prepare后、上传后、结算前分别注入崩溃，When 重放同一操作，Then 可恢复的阶段继续、供应商调用次数不增加、原图和run对应唯一、unknown不自动付费重做，预算与流水无手工修补。

**验收**：TC101-035～041。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.VisualExecutionIT'`。UI N/A。禁止为重放重新UUID.randomUUID产生另一笔执行。

### C101-09：生成交付画幅并保存可靠 artifact

**输入／输出**：依赖08的原图media/run；输出VisualArtifact与目标尺寸衍生图，供10卡使用。R101-08、16；读§6.7和现有媒体归属／对象存储适配。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualArtifact.java` | 新增；不可变artifact记录 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualArtifactRepository.java` | 新增；attempt唯一与owner读取 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualArtifactService.java` | 新增；读取原图、生成衍生图、登记引用和hash |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationImageProcessor.java` | 新增；有界ImageIO、解码上限、补边和压缩 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationArtifactCleanupWorker.java` | 新增；7天导出／90天候选清理规则基础，保留采用和anchor依赖 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/VisualArtifactIT.java` | 新增；尺寸／透明／格式伪装／永久引用／清理保护 |

**步骤**：先检查字节和解码像素；处理放有界工作线程；按targetAspect输出真实尺寸；保存原图与衍生图的独立ID及hash；同attempt重复调用读已存在artifact，不再生成／登记重复素材。衍生失败保留原图和已结算run，只重做确定性图像处理。

**AC101-09**：Given 一张1024×1536原图，When 请求3:4交付并重复处理，Then 得到1080×1440完整内容的PNG／JPEG，原图未改、引用不重复；损坏／超像素图片被拒绝。

**验收**：TC101-042～045。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.VisualArtifactIT'`。UI截图N/A，实际图像尺寸和像素内容检查保留到test-artifacts。

### C101-10：异步视觉任务、封面依赖与恢复

**输入／输出**：依赖05、08、09；输出API101-13～16、受控Temporal编排与参考链。R101-07、08、16；读§6.6/§7、VideoWorkflowStarter的启动模式，不能复制视频业务状态。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualJobService.java` | 新增；创建、读取、取消、派发、父状态汇总 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualJobController.java` | 新增；API101-13～16及DTO |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/CreationStudioReferenceValidator.java` | 修改；增activeVisualJobId所属草稿／计划验证 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualExecutionBridge.java` | 修改；接artifact后处理与参考链，不改08不变量 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/cardseries/CardSeriesOperationRepository.java` | 修改；v2列表、取消与父状态CAS |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/CreationVisualWorkflow.java` | 新增；run/cancel协议 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/CreationVisualWorkflowImpl.java` | 新增；队列、首图依赖、上限2并发 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/CreationVisualActivities.java` | 新增；有界activity声明 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/CreationVisualActivitiesImpl.java` | 新增；薄壳调用领域服务，外部副作用不自动重试 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/CreationVisualWorkflowStarter.java` | 新增；确定性workflowId与安全启动 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/CreationVisualAdoptionSweeper.java` | 新增；10s/20条收养queued任务，不重复创建 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/VisualJobIT.java` | 新增；API、owner、取消、quote和部分成功 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/CreationVisualWorkflowTest.java` | 新增；内存Temporal重放、首图依赖与崩溃 |

**步骤**：quote、source、plan、权限校验后一个事务claim父项和子项；异步启动失败由同ID收养。首图成功后将其真实媒体传入后续每项；不同item并发最多2。cancel只阻止新派发；刷新只读数据库。主动重做unknown需请求显式包含旧attempt确认，不在服务端推定同意。

**AC101-10**：Given 6张图计划，When 封面失败／中途取消／重启／某页失败，Then 依赖正确、成功页保留、父状态准确、无重复计费或自动替图，后台工作不依赖页面保持打开。

**验收**：TC101-046～051。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.VisualJobIT' --tests 'com.grassland.intelligence.creationstudio.CreationVisualWorkflowTest'`。UI N/A；通过测试必须核对mock调用次数和实际DB，不只看HTTP202。

### C101-11：制作进度、候选比较与单项重做

**输入／输出**：依赖10/09；输出用户可理解的真实状态、原图／交付图预览及采用选择事件。R101-08、17；读§4.3、§5.6、§8与既有lightbox。采用写入由12卡完成，本卡不假报已保存。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/article/composables/useVisualJob.ts` | 新增；创建、幂等重试、轮询、暂停／恢复和取消 |
| `src/views/article/components/VisualProductionPanel.vue` | 新增；进度、生成范围与费用确认 |
| `src/views/article/components/VisualCandidateCard.vue` | 新增；候选比较与原图／衍生图预览 |
| `src/views/article/components/CardSeriesPanel.vue` | 修改；装配新版制作面，保留legacy分支 |
| `src/views/article/ArticleCreationView.vue` | 修改；纯装配 |
| `src/views/article/composables/useVisualJob.test.ts` | 新增；轮询、账号／页面切换、未知结果 |
| `src/views/article/components/VisualProductionPanel.test.ts` | 新增；部分成功、费用、重做和选择事件 |

**步骤**：先确认计划和quote再发job；每项显示完成／待封面／失败／待核实；原键恢复不出现“再次扣费”的模糊重试。用户主动重做时新key且显式范围；未知结果重做需逐项告知并确认。选择候选只emit item/artifact ID，父层未确认成功前不显示“已采用”。

**AC101-11**：Given 第3张失败其余成功，When 点击重做第3张并离开再返回，Then 请求只含第3项，其他候选保留，恢复同一job；账号切换后旧结果不会显示给新账号。

**验收**：TC101-052～055、SC101-04。命令：`npm run test -- src/views/article/composables/useVisualJob.test.ts src/views/article/components/VisualProductionPanel.test.ts`；`npm run typecheck`。不得用假进度百分比替代已完成项目数。

### C101-12：采用结果、最近项目与旧草稿兼容

**输入／输出**：依赖06、09～11；输出API101-17、可靠交付引用和刷新恢复。R101-09、16、17；读CreationResultReferences、useArticleWorkspace、RecentProjectsPanel与§6.5/§7.4。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualAdoptionService.java` | 新增；owner／revision／artifact校验和原子采用 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanController.java` | 修改；API101-17 wire |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationDraftService.java` | 修改；采用复用完整写事务 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationResultReferences.java` | 修改；新artifact引用可用性与归属，不放宽旧校验 |
| `src/views/article/composables/useArticleWorkspace.ts` | 修改；studio只存引用，恢复已采用媒体与plan |
| `src/views/article/ArticleCreationView.vue` | 修改；接采用事件与runExternalMutation |
| `src/views/ai-center/creation/RecentProjectsPanel.vue` | 修改；来源／制作摘要与继续入口，无新增项目存储 |
| `src/views/ai-center/components/DeliveryPanel.vue` | 修改；已采用项显示与正确媒体预期 |
| `src/composables/useCardSeries.ts` | 修改；仅兼容旧结果与修正陈旧注释，不移除v1方法 |
| `src/views/ai-center/creation/workspace-adapters.test.ts` | 修改；新旧工作区恢复回归 |
| `src/views/ai-center/components/DeliveryPanel.test.ts` | 修改；未采用不算完成，部分成功明确 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/VisualAdoptionIT.java` | 新增；并发／引用／版本／实际采用数据库断言 |

**步骤**：采用前flush草稿；服务端按当前revision读取artifact，不相信客户端媒体字段；完整更新resultRefs和coverRef，不覆盖人工文案。最近项目读取同一草稿服务。旧cards和未识别studio schema只读保留，不能reset。关闭新写开关仍能通过已保存引用查看交付素材。

**AC101-12**：Given 新旧草稿各一份，When 采用第2页新候选、手改配文并刷新，Then 媒体顺序／封面／文案均正确恢复，旧草稿可用，重复采用只产生一次版本变更，不新增模型调用。

**验收**：TC101-056～060、SC101-04/05。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.VisualAdoptionIT'`；`npm run test -- src/views/ai-center/creation/workspace-adapters.test.ts src/views/ai-center/components/DeliveryPanel.test.ts`。禁止用TTL URL充当长期结果ID。

### C101-13：验证 M1 图卡完整流程

**输入／输出**：依赖01～12；交付从入口到交付的本地E2E和M1证据，不增加产品功能。R101-01～09、16～18；读§12的fixture、边界矩阵与现场验收分层。

| 精确写入路径 | 操作与限定 |
|---|---|
| `tests/e2e/creation-studio.spec.ts` | 新增；M1用户端／AI端全流程 |
| `tests/e2e/fixtures/creation-studio.ts` | 新增；隔离合成账号、草稿／来源／媒体fixture |
| `scripts/acceptance/verify-creation-studio.mjs` | 新增；可重复UI／文件核验脚本，phase参数仅m1/m2/m3 |
| `tests/deployment/creation-studio.contract.test.ts` | 修改；扩充两入口、旧深链与feature关闭回归 |

**步骤**：真实Edge验证登录、来源／草稿和无付费读写；UI网络fixture覆盖6张图的慢、错、unknown和交互状态。后端Java集成测试独立用PostgreSQL/WireMock跑完整生产链，检查DB、调用次数及媒体字节，不能把UI mock当作这部分证据。分别制造第3张失败、原键重试、主动重做、刷新、采用、改配文；任务快照／禁止借用素材和旧草稿各一条路径。脚本不自动发现或使用真实密钥。

**AC101-13**：Given 小红书独立稿与抖音任务稿，When 从原稿到采用交付完整操作，Then 来源／计划／run／artifact／草稿版本可串联，断线不丢成功项，旧入口和旧图卡可用。

**验收**：TC101-061～064、SC101-01～05、V-M1。命令：`npm run e2e -- tests/e2e/creation-studio.spec.ts --project=chromium --grep M1`；`node scripts/acceptance/verify-creation-studio.mjs --phase m1`。真实模型证据按V-LIVE-IMAGE另记；未授权／未配置时保持该外部门槛NOT_RUN，继续14～24不依赖它的本地工作，不把M1记为已对外开放。

### C101-14：文章封面和段落配图策划

**输入／输出**：依赖05/12；输出article-visuals和cover-only的服务端计划、段落绑定与共享风格。R101-10、16；读取ArticleImagePrompts和ImagePlacement作为旧行为参考，不用其模糊位置覆盖新契约。

| 精确写入路径 | 操作与限定 |
|---|---|
| `contracts/creation-recipes.v1.json` | 修改；启用article-visuals和cover-only，范围保持§4.1 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/ArticleVisualPlanAdapter.java` | 新增；文章位置、目的、图形语义和封面结构 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanPrompts.java` | 修改；增文章／封面模板，图卡回归不变 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/plan/VisualPlanService.java` | 修改；按recipe分派，统一严格解析 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/visual/VisualAdoptionService.java` | 修改；持久段落位置与cover，仍由单一采用事务写 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/ArticleVisualPlanIT.java` | 新增；文章／回答／封面及段落变化测试 |

**步骤**：根据标题与段落推荐1～8图而非固定3～4；每个插图给出purpose/afterBlockId/关键文字；封面图与正文图角色分开。format只加图片计划，不改正文；知乎answer保留问题与开头语义。正文变化令相关计划stale，必须重新核对位置。

**AC101-14**：Given 带步骤和数字的公众号稿／知乎回答，When 请求配图，Then 每图能定位真实段落和解释目的，封面恰好一个，回答问题不被改成文章标题；正文改变后不隐式重生成。

**验收**：TC101-065～068。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.ArticleVisualPlanIT' --tests 'com.grassland.intelligence.creationstudio.VisualPlanIT'`。UI N/A；不新增独立信息图路由或SVG渲染器。

### C101-15：文章配图与封面交互

**输入／输出**：依赖14和11候选组件；输出文章联动预览、封面选项与可复用预设。R101-10、17；读根DESIGN、ArticleImageSlots、VideoStudioView的封面子区，§8。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/article/components/ArticleVisualPanel.vue` | 新增；段落与图片计划联合视图 |
| `src/views/article/components/ArticleImageSlots.vue` | 修改；接新版计划；旧推荐入口可回退 |
| `src/views/article/components/CoverRecipeOptions.vue` | 新增；精简封面预设／画幅，复用现有选择控件 |
| `src/views/article/ArticleCreationView.vue` | 修改；纯装配，正文和结果始终同项目 |
| `src/views/ai-center/components/VideoStudioView.vue` | 修改；仅复用CoverRecipeOptions的描述预设，不改视频执行、计费、来源或生成端点 |
| `src/views/article/composables/useArticleWorkspace.ts` | 修改；已采用插图与段落引用恢复 |
| `src/views/article/components/ArticleVisualPanel.test.ts` | 新增；位置、选图、正文改动和错误状态 |
| `src/views/ai-center/components/VideoStudioView.test.ts` | 修改；旧视频封面流程回归 |

**步骤**：源段落旁显示插图用途与绑定；现有素材可替换生成项；候选用同一VisualProductionPanel。封面高级选项仅有本书已定义的风格／布局／配色／画幅，默认折叠，不擅自增加未入DTO的字体／情绪等维度。VideoStudioView只获得可复制的预设描述，不调用未支持的studio草稿能力，不自动替换已有视频封面。

**AC101-15**：Given 文章第3节有插图，When 修改正文并重新核对位置、采用另一图，Then 预览和保存后的引用一致；视频工具只是预设增强，不启动新视频任务。

**验收**：TC101-069～071、SC101-06。命令：`npm run test -- src/views/article/components/ArticleVisualPanel.test.ts src/views/ai-center/components/VideoStudioView.test.ts`；`npm run typecheck`。视图超行数需在本卡列明的组件中拆装配，不扩大重构范围。

### C101-16：确定性排版与受控 HTML

**输入／输出**：依赖02 AST和12采用结果；输出API101-18、两个排版主题及文字一致性检查。R101-03、11；读§6.7和现有jsoup/DOMPurify边界。

| 精确写入路径 | 操作与限定 |
|---|---|
| `contracts/creation-recipes.v1.json` | 修改；启用article-format |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationDocumentRenderer.java` | 新增；受控AST→HTML/text/Markdown与文字token比对 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationRenderTheme.java` | 新增；standard/compact固定样式枚举，无外链字体 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationRenderService.java` | 新增；版本读取、媒体绑定与预览 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationRenderController.java` | 新增；API101-18 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/CreationRenderIT.java` | 新增；文字、代码、表格、HTML攻击、缺媒体测试 |
| `docs/架构/AI创作第三方资源说明.md` | 新增；记录上游commit、本地化范围、MIT和实际依赖许可；不搬未使用代码 |
| `docs/架构/目录结构.md` | 修改；仅增加上述说明文档引用，确保可发现 |

**步骤**：用同一AST构建输出；文本保留检查先于标ready；原HTML转义、外部图提示绑定；链接按请求是否转引用；主题只改样式。绑定位置过期时给出警告并阻止含这些必需图片的完整导出。旧稿无精确位置的图片允许按原有顺序附于文后，必须在预览标明“未绑定段落”，用户可随后编辑绑定。

**AC101-16**：Given 含金额、日期、引用、表格和代码的定稿，When 两主题渲染及注入攻击，Then 正文token相同、代码逐字一致、没有脚本/外部资源抓取，缺图不伪装完整，AI调用数为0。

**验收**：TC101-072～077。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.CreationRenderIT'`。UI N/A；不引入Bun、Node业务服务或高危任意HTML执行。

### C101-17：原稿排版预览与元数据编辑

**输入／输出**：依赖16，复用04建议与12保存；输出format完整路径和文章联合预览。R101-03、11、17；读§8、ContentStage、DeliveryPanel。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/article/components/ArticleFormatPanel.vue` | 新增；原稿／预览、主题、缺图与引用说明 |
| `src/views/article/composables/useArticleRender.ts` | 新增；按版本请求、取消和迟到响应隔离 |
| `src/views/article/components/ContentStage.vue` | 修改；增显式排版入口和模式适配，修正不再适用的固定步骤文案 |
| `src/views/article/ArticleCreationView.vue` | 修改；纯装配 |
| `src/views/ai-center/components/DeliveryPanel.vue` | 修改；元数据与正文分离，摘要建议是独立动作 |
| `src/views/article/components/ArticleFormatPanel.test.ts` | 新增；no-AI、主题、sanitizer及移动交互 |
| `src/views/article/composables/useArticleRender.test.ts` | 新增；草稿版本／响应乱序／未保存状态 |

**步骤**：渲染前flush当前草稿；只读预览经DOMPurify，再插入限定内容区；组件样式不影响页面外部。切主题不改正文和版本；改变includeTitle/cite选项仅影响本次输出。需要标题摘要时用户单独请求TextProposal，禁止切主题触发LLM。

**AC101-17**：Given format定稿，When 改主题、预览、关闭返回并刷新，Then 正文未改、来源保留、无新增AI run，元数据手改不被下一次预览覆盖。

**验收**：TC101-078～080、SC101-07。命令：`npm run test -- src/views/article/components/ArticleFormatPanel.test.ts src/views/article/composables/useArticleRender.test.ts`；`npm run typecheck`。未知或失败响应不能渲染未经净化的原HTML。

### C101-18：交付真实文件与图片包

**输入／输出**：依赖09、12、16、17；交付API101-19/20、V81、真实下载与M2验收。R101-12、16、17；读§6.7、CreationDraftExportService、对象存储和现有creation-export.ts。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V81__creation_export_artifact.sql` | 新增；导出表 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationDraftExportService.java` | 修改；保持旧manifest，分派新格式 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationassistant/CreationDraftController.java` | 修改；扩展ExportRequest，不改旧默认 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationExportService.java` | 新增；快照、文件装配、ZIP、恢复签名 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationExportRepository.java` | 新增；幂等与状态 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationExportController.java` | 新增；API101-20 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationArtifactCleanupWorker.java` | 修改；接导出7天生命周期 |
| `src/lib/creation-export.ts` | 修改；新类型／下载行为，保留旧exportCreationDraft兼容 |
| `src/views/ai-center/components/DeliveryPanel.vue` | 修改；格式选择、building/错误/实际下载 |
| `src/views/ai-center/components/DeliveryPanel.test.ts` | 修改；ZIP/HTML/正文／旧manifest回归 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/CreationExportIT.java` | 新增；解压／hash／媒体字节／权限／历史版本 |
| `tests/e2e/creation-studio.spec.ts` | 修改；增M2路径 |
| `scripts/acceptance/verify-creation-studio.mjs` | 修改；实现phase=m2实际文件验证 |

**步骤**：只按请求version读取不可变快照；收集权限合格媒体、生成真实文本文件、ZIP条目和manifest；计算实际hash/字节；先成功写对象再ready。重复导出只重建文件，不生成图片／正文。新格式缺必需项失败；旧manifest仍可逐项unavailable。浏览器按实际contentType和filename下载。

**AC101-18**：Given 历史v3正文和当前v4标题不同，When 导出v3 ZIP并解压，Then 所有字段、图片顺序和来源均属于v3，文件真实可打开，无有效签名地址写入永久manifest；重复下载无AI调用。

**验收**：TC101-081～086、SC101-08、V-M2。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.CreationExportIT'`；`npm run test -- src/views/ai-center/components/DeliveryPanel.test.ts`；`npm run e2e -- tests/e2e/creation-studio.spec.ts --project=chromium --grep M2`；`node scripts/acceptance/verify-creation-studio.mjs --phase m2`。

### C101-19：公众号连接、加密和 token 管理

**输入／输出**：依赖01；交付API101-21～25、V82、固定目的地API客户端与token管理。R101-13、16。读取EnvelopeEncryption、既有Redis与IntelligenceCallerResolver；依§6.8，不接入第三方本机发布脚本。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V82__creation_wechat_account.sql` | 新增；连接表、appId部分唯一键 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatProperties.java` | 新增；绑定01已登记开关，不新增可自定义host |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatAccountRepository.java` | 新增；归属、版本、绑定/断开CAS |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatAccountService.java` | 新增；加密、校验、轮换与断开 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatAccountController.java` | 新增；API101-21～25及DTO |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatApiClient.java` | 新增；固定API、TLS、超时、脱敏错误 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatTokenService.java` | 新增；Redis加密缓存、刷新互斥、版本失效 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/WechatAccountIT.java` | 新增；加密、owner、并发、刷新、无密钥日志、禁用测试 |

**步骤**：连接创建只加密保存；verify按显式动作访问token接口；本地测试固定WireMock，通过DI替换客户端，不暴露公网配置参数。旋转使原token缓存失效、版本+1；断开清密文、禁新写，但不删除同步记录。相同owner重新绑定自己的disconnected连接时恢复同一连接ID并版本+1；另一owner已绑定相同appId则409，不盗取其记录。

**AC101-19**：Given 两账号及同一AppID，When 并发绑定、读取、轮换和断开，Then 只有一个有效owner，响应／日志无secret/token，旧版本请求409，Redis故障只影响渠道功能。

**验收**：TC101-087～092。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.WechatAccountIT'`。UI N/A；禁止在作者或执行者未获授权时取真实公众号凭据验证。

### C101-20：公众号账号设置与安全输入

**输入／输出**：依赖19；交付可访问的连接管理和发布前账号选择。R101-13、17；读GlModal、AiGovernanceSection、根DESIGN、§8.2。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/ai-center/components/WechatAccountPanel.vue` | 新增；账号列表／绑定／验证／轮换／断开 |
| `src/views/ai-center/creation/useWechatAccounts.ts` | 新增；owner API、版本、错误与epoch |
| `src/views/ai-center/components/AiGovernanceSection.vue` | 修改；在现有设置区挂账号管理，不新增主导航 |
| `src/views/ai-center/components/DeliveryPanel.vue` | 修改；打开相同账号管理组件／选择连接 |
| `src/views/ai-center/components/WechatAccountPanel.test.ts` | 新增；字段、关闭清secret、版本冲突、键盘 |
| `src/views/ai-center/creation/useWechatAccounts.test.ts` | 新增；迟到响应与账号切换 |

**步骤**：使用password类型输入且不做持久缓存；用户可以核对账号名与AppID；校验和保存分开。断开用明确说明，取消则无请求；轮换后提示需要再验证。没有连接时可以继续编辑和导出文件，不能把连接弹窗当作整个创作流程的必经步骤。

**AC101-20**：Given 尚未连接公众号，When 保存连接、校验失败、关闭弹窗再打开，Then 非秘密表单按明确状态显示，AppSecret为空；失败不阻止下载文件，键盘焦点正确返回。

**验收**：TC101-093～095、SC101-09。命令：`npm run test -- src/views/ai-center/components/WechatAccountPanel.test.ts src/views/ai-center/creation/useWechatAccounts.test.ts`；`npm run typecheck`。截图必须使用合成数据并遮盖敏感输入。

### C101-21：公众号上传、草稿写入与核实

**输入／输出**：依赖18/19；交付API101-26～31、V83、专用workflow与无盲重发的核实。R101-14～16；读§6.8、导出快照、已有Temporal启动惯例。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/main/resources/db/migration/V83__creation_wechat_draft_sync.sql` | 新增；同步表、payload唯一性、派发记录 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatDraftSyncRepository.java` | 新增；状态、上传映射、CAS和分页 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatDraftSyncService.java` | 新增；创建／上传／提交／回读／取消／候选／核实 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatDraftSyncController.java` | 新增；API101-26～31及DTO |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/wechat/WechatApiClient.java` | 修改；增固定uploadimg/add_material/draft/add/get/batchget端点 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/creationstudio/render/CreationImageProcessor.java` | 修改；按微信用途限制压缩，保留可查衍生图 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/WechatDraftWorkflow.java` | 新增；同步run/cancel声明 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/WechatDraftWorkflowImpl.java` | 新增；专用queue和状态驱动，无自动重复draft/add |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/WechatDraftActivities.java` | 新增；领域动作声明 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/WechatDraftActivitiesImpl.java` | 新增；薄壳，持久派发守卫 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/WechatDraftWorkflowStarter.java` | 新增；workflowId=`wechat-draft-{syncId}`，同ID幂等启动 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/orchestration/WechatDraftAdoptionSweeper.java` | 新增；10s/20条补起未启动同步；不重新创建同步 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/WechatDraftSyncIT.java` | 新增；官方形状模拟、超时／重复／核实／内容不一致 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/WechatDraftWorkflowTest.java` | 新增；内存Temporal重放、派发边界和取消 |

**步骤**：锁owner+连接版本和导出snapshot，持久payload；图片用途分别上传；submitting持久后仅发一次draft/add；media_id先保存再回读；逐字段核实后成功。worker起不来留待同ID收养。unknown时通过候选搜索或明确media_id核实，API101-30不触发上传／draft/add。连接轮换可使用同一owner已重新验证的凭据只读核实，但不能继续旧版本未发出的写请求。

**AC101-21**：Given 相同快照并发两次提交且第一次draft/add响应丢失，When 重放／重启／查候选／核实，Then draft/add最多一次自动派发，记录先unknown、确认一致后succeeded，错误media_id不能使记录成功，未调用公开发布接口。

**验收**：TC101-096～104。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.WechatDraftSyncIT' --tests 'com.grassland.intelligence.creationstudio.WechatDraftWorkflowTest'`。真实渠道验收按V-LIVE-WECHAT，模拟通过不得冒充真实成功。

### C101-22：微信预览、同步状态与结果核实界面

**输入／输出**：依赖20/21；交付指定账号／版本预览、同步历史、unknown核实。R101-14、15、17；读§8.1/8.2，复用DeliveryPanel与GlModal。

| 精确写入路径 | 操作与限定 |
|---|---|
| `src/views/ai-center/components/WechatDraftPreview.vue` | 新增；账号、版本、成品及评论选项确认 |
| `src/views/ai-center/components/WechatDraftSyncPanel.vue` | 新增；状态、历史、候选核实与取消 |
| `src/views/ai-center/creation/useWechatDraftSync.ts` | 新增；版本／请求ID、轮询、未知结果读取 |
| `src/views/ai-center/components/DeliveryPanel.vue` | 修改；接预览、提交及已有输出快照 |
| `src/views/ai-center/components/WechatDraftSyncPanel.test.ts` | 新增；真实状态文案、重复点击和核实分支 |
| `src/views/ai-center/creation/useWechatDraftSync.test.ts` | 新增；断线、迟到响应、连接轮换 |
| `tests/e2e/creation-studio.spec.ts` | 修改；增M3模拟发布草稿流程 |
| `scripts/acceptance/verify-creation-studio.mjs` | 修改；实现phase=m3模拟／显式授权现场模式区分 |

**步骤**：未保存草稿先flush并重新导出指定版本；预览展示这份快照，不混用正在编辑的内容。点击后保留syncId；刷新读回同任务；unknown提供“核实草稿”，不默认勾选或重复提交。候选显示账号、标题和更新时间供判断；内容比对结果由服务端给出。

**AC101-22**：Given 公众号同步进行中，When 用户改本地正文、刷新并核实原同步，Then 原同步仍引用原版本，新稿不被覆盖；成功只显示已存入草稿箱，外部公开链接没有被编造。

**验收**：TC101-105～108、SC101-10。命令：`npm run test -- src/views/ai-center/components/WechatDraftSyncPanel.test.ts src/views/ai-center/creation/useWechatDraftSync.test.ts`；`npm run e2e -- tests/e2e/creation-studio.spec.ts --project=chromium --grep M3`。默认验收脚本只走隔离模拟服务；真实模式必须显式传入获授权连接ID和用途，禁止自动选第一个真实账号。

### C101-23：安全、组合流程和三入口回归

**输入／输出**：依赖01～22；交付跨层异常、权限、竞态和完整UI验收证据。R101-16、17。只增加验收和已登记路径上的必要修复；发现需新写文件先按§13修订卡，不能借“集成修复”开放全仓写权限。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/CreationStudioSecurityIT.java` | 新增；跨账号、快照、权限撤销、输入污染测试 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/CreationStudioIntegrationIT.java` | 新增；来源→计划→生成→采用→导出→同步组合 |
| `tests/e2e/creation-studio.spec.ts` | 修改；全矩阵、账号切换、旧入口、键盘／移动 |
| `tests/e2e/fixtures/creation-studio.ts` | 修改；完整恶意／竞态fixture |
| `tests/deployment/creation-studio.contract.test.ts` | 修改；三入口、路由off、错方法、生产默认值 |
| `scripts/acceptance/verify-creation-studio.mjs` | 修改；完整SC清单及下载核对 |

**步骤**：从用户可操作路径发起任务；在保存、生成、采用、导出、上传前分别撤销权限／删除媒体；跑多客户端同键和异键竞争；验证未出现错误成功、额外调用或泄密。检查新provider设置对旧文本/视频配置无破坏。探索性测试每模块至少一次15min会话，记录确认的问题，不凑缺陷数量。

**AC101-23**：Given 两账号、任务／独立两来源和三入口，When 混合异常与正常流程，Then 权限及版本边界一致，能通过键盘和移动完成关键动作；无未关闭P0/P1缺陷。

**验收**：TC101-109～116、SC101-01～11、V-E2E、V-EDGE。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.CreationStudioSecurityIT' --tests 'com.grassland.intelligence.creationstudio.CreationStudioIntegrationIT'`；`npm run e2e -- tests/e2e/creation-studio.spec.ts`（三引擎）。所有已有核心创作测试保留，不删断言换绿灯。

### C101-24：迁移演练、质量门禁与最终交接

**输入／输出**：依赖23；交付V77～V84演练、开关回退、最终质量和明确上线边界。R101-09、18。读§7.5/7.6、§12/§14、现有Gradle与CI门槛。

| 精确写入路径 | 操作与限定 |
|---|---|
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/creationstudio/CreationStudioMigrationIT.java` | 新增；空库、V76升级、重放、旧数据与回退读侧 |
| `tests/deployment/creation-studio.contract.test.ts` | 修改；排空／开关／错误配置的最终契约 |
| `scripts/acceptance/verify-creation-studio.mjs` | 修改；门禁结果和脱敏证据汇总，不生成独立完成报告 |
| `docs/任务书/草场任务书-101-AI创作工作流与图文交付升级.md` | 修改；仅维护已确认的规格修订与实际边界，不伪造卡完成 |
| `docs/任务书/README.md` | 修改；更新真实验收范围／交接索引 |
| `docs/文档总索引.md` | 修改；更新可发现入口，区分本地、渠道、生产状态 |

**步骤**：验证数据库新旧双读；升级不改旧checksum、草稿／媒体hash；关写、排空、停worker、关Edge按顺序演练；执行最终FE/Java覆盖率和三入口构建；复核无秘密／无越界文件；登记实际模型与公众号外部验收状态。没有外部授权不部署、不绑定、不收费，只完成本地可做工作并列出准确未验项。

**AC101-24**：Given V76存量数据和若干在途v2任务，When 升级、排空并关闭新功能，Then 旧数据和入口保持，已提交请求有可核实归宿，恢复开关可继续读已存结果；最终证据可定位到相同代码基线。

**验收**：TC101-117～120、V-FINAL、V-MIGRATION、SC101-11。命令：`./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.creationstudio.CreationStudioMigrationIT'`，并执行§12.5所有适用最终门禁。不能因尚未部署而删除新表；不能把未执行真实渠道验收写成PASS。

## 12. 测试、证据与最终验收

### 12.1 测试数据、风险和分层

合成fixture至少包括：A/B两账号；A的一份独立小红书稿、一份带真实测试快照的抖音任务稿、一份公众号长稿、一份知乎回答；A可用／不可用／已撤权媒体；B私有媒体；两个模拟公众号连接；V76旧草稿及旧图卡操作。数据通过既有隔离seed或测试Repository创建，使用UUID和`.invalid`域名，不采用生产ID。

source-samples.json包括：中文数字与金额、emoji、重复段落、表格、链接、长代码、原始HTML、控制字符、超长边界。图像fixture用程序生成的PNG/JPEG和破损字节，能校验真实宽高／hash；不下载用户私有媒体。

| 风险 | 严重度×概率×检出难度（设计初评，1～5） | 测试深度 |
|---|---|---|
| 重复调用／扣费／外部草稿重复 | 5×4×4=80 | 每个崩溃边界、并发与重放；检查DB及mock调用次数 |
| 跨账号／任务素材越权 | 5×3×4=60 | 所有owner API、引用链、执行中撤销、服务端负向 |
| 原稿丢失／错误版本覆盖 | 5×4×3=60 | 多端竞态、刷新、长文、hash和历史快照 |
| HTML、URL或凭据进入不可信路径 | 5×3×3=45 | 恶意输入、日志审查、固定目的地、净化与资源限制 |
| 画幅、文件名、部分成品错误 | 3×4×2=24 | 实际解码／解压、对比、缺项、过期重取 |
| 布局／键盘／移动端受损 | 3×3×2=18 | 双主题浏览器、焦点、窄屏、长文案和状态矩阵 |

验证分四层：纯函数/组件；Spring+PostgreSQL+WireMock集成；真实Edge的无付费API与浏览器UI；经单独授权的真实供应商／公众号。浏览器UI允许明确的网络fixture驱动慢、错、unknown状态；它不能作为真实计费、供应商或公众号结果证据。后端完整生产链必须在Java IT中通过真实数据库、文件和模拟上游验证。现场验收再核对真实输出与账号，三类证据不得混报。

### 12.2 具体用例目录

每条均按“前置＋动作→可观察结果”执行。REQ对应关系以§1.3和本行主卡为双向映射；自动化宿主为主卡明确列出的测试文件，跨层复核由23/24卡承担。P0/P1必须全部通过；无未批准的跳过。

| TC | 主卡／REQ | 前置与动作 | 必须断言的结果 | 优先级 |
|---|---|---|---|---|
| TC101-001 | 01/R101-01 | 登录后读取四模板，过滤平台／模式 | 合法集合准确；无prompt或密钥字段 | P1 |
| TC101-002 | 01/R101-18 | 开关false、错方法、未知叶子 | 404；旧路由仍可用 | P0 |
| TC101-003 | 01/R101-01 | 请求未知版本／非法组合 | 400；不降级到另一个模板 | P1 |
| TC101-004 | 01/R101-18 | 经过Edge读取旧图卡operations，切flag | 开启可到Controller、关闭404、身份保持 | P1 |
| TC101-005 | 02/R101-02 | 中文／emoji／重复段落导入再读取 | 原文、hash、block跨度正确；ID互异 | P0 |
| TC101-006 | 02/R101-02 | 0／1／30,000／30,001 code point、UTF-8大小边界 | 空／超限拒绝；合法完整保存 | P1 |
| TC101-007 | 02/R101-02 | 同requestId并发同文与异文 | 一份source；异参409 | P0 |
| TC101-008 | 02/R101-16 | B读A source，或A引用B draft | 404，无内容泄露 | P0 |
| TC101-009 | 02/R101-02 | markdown含外链图、HTML和脚本 | 不发网络、不执行、原文可查 | P0 |
| TC101-010 | 02/R101-09 | draft-content指定过期版本／伪造text | 409／400；不能冒充服务器正文 | P0 |
| TC101-011 | 03/R101-03 | format导入后进入正文 | 标题／大纲／正文生成调用0 | P0 |
| TC101-012 | 03/R101-02 | 非UTF-8／损坏文件／导入失败 | 旧正文和输入保留，错误可定位 | P1 |
| TC101-013 | 03/R101-01 | 工作台任务锁定后选另一模板平台 | 不改平台／来源／快照；不合法选择禁用 | P0 |
| TC101-014 | 03/R101-17 | 快速选文件A/B、账号切换／刷新 | 最后有效意图生效，旧响应不覆盖 | P0 |
| TC101-015 | 04/R101-04 | 同建议请求并发／断线重放 | 一次模型调用、一份建议、run可追踪 | P0 |
| TC101-016 | 04/R101-04 | 预览改编后取消 | 原稿／版本不变 | P0 |
| TC101-017 | 04/R101-04 | 两窗口应用同建议、版本相同／不同 | 一次写入或409，历史保留 | P0 |
| TC101-018 | 04/R101-03 | format生成metadata只选summary应用 | 正文和原有标题不变 | P0 |
| TC101-019 | 04/R101-16 | 建议超时、过期、源变更、模型编造字段 | unknown／拒绝应用；不暗中重生成 | P0 |
| TC101-020 | 05/R101-05 | 指定5图和信息策略 | 恰好5个合法稳定项，来源可定位 | P1 |
| TC101-021 | 05/R101-05 | 无显式数量，来源块数变化 | 缺省算法一致且不超模板上限 | P1 |
| TC101-022 | 05/R101-16 | 模型返回假blockId／重复itemId／恶意字段 | STUDIO_INVALID_PLAN，无后续图片调用 | P0 |
| TC101-023 | 05/R101-05 | 改第3页、再读旧revision | 新revision清确认，旧revision不可变 | P0 |
| TC101-024 | 05/R101-05 | confirm与PATCH并发 | 行锁顺序一致，无半更新确认 | P0 |
| TC101-025 | 05/R101-16 | 重复estimate／余额不足配置 | 无run或扣费，价格／能力异常不伪造0 | P0 |
| TC101-026 | 06/R101-05 | 键盘修改／排序／删除计划项 | ID不变，位置连续，封面规则有效 | P1 |
| TC101-027 | 06/R101-09 | 保存中收到旧revision响应 | 不覆盖后续编辑，flush后才确认 | P0 |
| TC101-028 | 06/R101-17 | 空、错误、stale、部分来源未覆盖 | 说明可见，生成禁用或明确覆盖范围 | P1 |
| TC101-029 | 06/R101-17 | KeepAlive失活／卸载／账号变更 | 无遗留计时器和跨账号状态 | P0 |
| TC101-030 | 07/R101-06 | openai-image含1张参考图 | Multipart实际图片字节、size/model/n正确 | P0 |
| TC101-031 | 07/R101-06 | legacy请求通用参考 | 生成前拒绝，不删图再重发 | P0 |
| TC101-032 | 07/R101-06 | MiniMax人物参考／整组参考 | 人物语义保留；整组reference-image拒绝 | P1 |
| TC101-033 | 07/R101-16 | 私网／DNS变化／新协议绑定text | 原保护生效，错误能力400，供应商调用0 | P0 |
| TC101-034 | 07/R101-17 | 旧凭据／旧BYOK编辑、新协议设置 | 旧值可选可保存，新帮助不暴露秘密 | P1 |
| TC101-035 | 08/R101-07 | observer.prepared持久化失败 | 上游调用0，原执行环正确收尾 | P0 |
| TC101-036 | 08/R101-07 | 两worker争同executionOperationId | 只有一个派发者、一个run绑定 | P0 |
| TC101-037 | 08/R101-07 | dispatching后进程失联 | unknown，不自动新建run或重发 | P0 |
| TC101-038 | 08/R101-07 | 原图保存后结算失败再恢复 | 复用同图同run，供应商调用不增加 | P0 |
| TC101-039 | 08/R101-16 | 任务图像配置／凭据轮换 | 执行前按冻结规则拒绝，不降独立 | P0 |
| TC101-040 | 08/R101-16 | 平台与个人／组织BYOK各执行一图 | 预算、积分、账务按现有不同口径 | P0 |
| TC101-041 | 08/R101-07 | 父操作相同但B尝试查询／恢复A运行 | 404；运行ID难猜不作为授权 | P0 |
| TC101-042 | 09/R101-08 | 五种targetAspect处理测试原图 | 尺寸准确，原图不改且文本区域保留 | P1 |
| TC101-043 | 09/R101-16 | 假扩展名、破图、超10MiB／25MP | 明确拒绝，内存和队列有界 | P0 |
| TC101-044 | 09/R101-08 | 同attempt并发登记artifact | 唯一artifact和媒体引用，hash准确 | P0 |
| TC101-045 | 09/R101-09 | 清理到期候选，存在采用／anchor依赖 | 依赖媒体不删，只清可删除副本 | P0 |
| TC101-046 | 10/R101-08 | 整组六图，首图慢／失败 | 后续不提前派发，失败不自动换锚 | P0 |
| TC101-047 | 10/R101-07 | 首图成功后后续同时运行 | 并发≤2且预算门禁仍有效 | P0 |
| TC101-048 | 10/R101-07 | Temporal启动失败、重启收养、history重放 | 同workflowId，父／子项不重复 | P0 |
| TC101-049 | 10/R101-08 | 在queued／dispatching分别取消 | 未发项停止；已发项核实，不假退全款 | P0 |
| TC101-050 | 10/R101-16 | quote超120s／参数／配置变动 | 409，模型调用0，要求重新估算 | P0 |
| TC101-051 | 10/R101-07 | unknown主动重做有／无确认IDs | 无确认拒绝；确认后新attempt且审计保留旧项 | P0 |
| TC101-052 | 11/R101-08 | 第3张失败，点击重做 | 仅第3项进入新请求，成功候选保留 | P0 |
| TC101-053 | 11/R101-17 | 离开／激活、60s长任务 | 轮询按2s/5s策略暂停恢复，无无限计时器 | P1 |
| TC101-054 | 11/R101-08 | 切原图／交付图、选择候选 | 尺寸标签真实，选择不等于保存成功 | P1 |
| TC101-055 | 11/R101-17 | 模型失败／unknown／无预算 | 错误可行动，没有假进度／隐形降级 | P1 |
| TC101-056 | 12/R101-09 | 采用一个新候选再刷新 | 只替换该item，其他文案和引用不变 | P0 |
| TC101-057 | 12/R101-16 | 采用时plan/draft版本变化 | 409且所有旧结果保持 | P0 |
| TC101-058 | 12/R101-16 | 采用其他草稿／账号／失败候选 | 拒绝且不写草稿版本 | P0 |
| TC101-059 | 12/R101-09 | 打开旧cards和未知studio schema | 旧稿可读；未知结构只读且不清空 | P0 |
| TC101-060 | 12/R101-09 | 同key采用重放、已有20引用边界 | 一次版本变更；超限明确拒绝不裁字段 | P0 |
| TC101-061 | 13/R101-01 | 独立小红书原稿完整M1 | 从来源到交付引用可追溯，明确区分UIfixture与API证据 | P0 |
| TC101-062 | 13/R101-16 | 抖音任务稿完整M1 | 平台、快照、预算和授权素材贯穿 | P0 |
| TC101-063 | 13/R101-09 | M1中断、重新登录、最近项目继续 | 正文、计划、候选、采用都恢复 | P0 |
| TC101-064 | 13/R101-18 | 新路由关、旧图卡与旧深链开 | 无新的空白页／永久loading | P1 |
| TC101-065 | 14/R101-10 | 公众号文章请求配图 | 1～8图、唯一封面、插图用途与段落真实对应 | P1 |
| TC101-066 | 14/R101-10 | 知乎回答模式配图 | 问题和开头保持，不误写文章标题 | P0 |
| TC101-067 | 14/R101-10 | 删除／修改被绑定段落 | 计划stale，旧候选可读但不自动采用 | P0 |
| TC101-068 | 14/R101-16 | format加图与cover-only | 原文字词不变；cover-only只有一个项 | P0 |
| TC101-069 | 15/R101-10 | 文章同时用实拍与生成图 | 原文顺序、来源和采用引用正确 | P1 |
| TC101-070 | 15/R101-17 | 封面设置与大图键盘操作 | 焦点完整、没有隐式生成 | P1 |
| TC101-071 | 15/R101-17 | 现有视频工坊读封面预设 | 旧生成／计费／视频任务行为不变 | P0 |
| TC101-072 | 16/R101-03 | 含金额日期的同稿两主题渲染 | 正文token相同，AI调用0 | P0 |
| TC101-073 | 16/R101-11 | 代码块、表格、中文标点与emoji | 文本准确，代码逐字一致，顺序不变 | P0 |
| TC101-074 | 16/R101-16 | script/事件/css/dataURL/javascript链接 | 双层净化生效，无执行／外部请求 | P0 |
| TC101-075 | 16/R101-11 | 外链转引用开／关 | 只改链接呈现，正文内容和原始URL不丢 | P1 |
| TC101-076 | 16/R101-11 | 未绑定远程图片／过期段落 | 明确缺项或位置提示，不自动抓取或猜位置 | P0 |
| TC101-077 | 16/R101-11 | 旧文章无studio，按历史媒体顺序预览 | 原稿可用、附图顺序可核对，不写空studio | P1 |
| TC101-078 | 17/R101-03 | format切主题／关闭／刷新 | 内容不改、无自动标题摘要调用 | P0 |
| TC101-079 | 17/R101-17 | 新旧版本预览响应乱序 | 只显示当前请求版本，旧html不覆盖 | P0 |
| TC101-080 | 17/R101-17 | 320px表格／长代码／键盘／亮暗 | 局部滚动、可读焦点，无整页溢出 | P1 |
| TC101-081 | 18/R101-12 | 历史v3对比当前v4导出 | 文本、媒体、元数据全来自v3 | P0 |
| TC101-082 | 18/R101-12 | ZIP实际解压 | 文件清单、字节、hash、图片顺序正确 | P0 |
| TC101-083 | 18/R101-12 | Markdown/HTML含媒体／无媒体 | 包装与真实contentType准确，相对路径可解析 | P1 |
| TC101-084 | 18/R101-16 | 媒体缺失、撤权、文件名穿越、超100MiB | 明确失败，不产生伪完整包或越界路径 | P0 |
| TC101-085 | 18/R101-12 | 同key并发导出、链接900s／文件7天过期 | 不重复AI；同快照可重取／重建 | P0 |
| TC101-086 | 18/R101-09 | 旧请求无version/requestId导出manifest | 旧默认和返回结构完全兼容 | P0 |
| TC101-087 | 19/R101-13 | 绑定合成凭据后检查DB与响应 | DB为密文，响应无secret/token，状态unverified | P0 |
| TC101-088 | 19/R101-13 | 同AppID跨账号并发绑定 | 一个有效绑定，不泄露另一owner信息 | P0 |
| TC101-089 | 19/R101-13 | 验证成功／invalid、secret轮换 | version+1和token失效准确，旧请求409 | P0 |
| TC101-090 | 19/R101-13 | 两实例同时刷新token | 单刷新锁、加密缓存、有界TTL，不互相覆盖新版本 | P0 |
| TC101-091 | 19/R101-16 | Redis／KEK缺失，恶意host参数 | 渠道503／400，不明文降级，本地编辑正常 | P0 |
| TC101-092 | 19/R101-13 | 断开、重新绑定、B尝试读A连接 | 旧secret不可用、本人恢复版本递增、B404 | P0 |
| TC101-093 | 20/R101-13 | 关闭／卸载／账号切换连接弹窗 | 内存secret清空，不写持久状态或URL | P0 |
| TC101-094 | 20/R101-17 | 校验失败／无连接时继续导出 | 输入说明可用，创作与导出未被阻断 | P1 |
| TC101-095 | 20/R101-17 | 键盘绑定、轮换、取消断开 | 焦点可达、取消无请求、成功文案真实 | P1 |
| TC101-096 | 21/R101-14 | 合法news单篇含封面／正文图片 | 两上传接口用途正确，articles长度1，draft/add一次 | P0 |
| TC101-097 | 21/R101-14 | 拿到media_id回读同／异内容 | 只有一致时succeeded，不按标题猜匹配 | P0 |
| TC101-098 | 21/R101-15 | draft/add响应丢失／5xx／解析失败 | unknown，无自动第二次draft/add | P0 |
| TC101-099 | 21/R101-15 | 同key和不同key同快照并发 | 复用已有进行中／unknown／成功记录 | P0 |
| TC101-100 | 21/R101-15 | 候选0／1／多项，最多100项 | 有界搜索、hasMore准确、需核实才成功 | P0 |
| TC101-101 | 21/R101-16 | 错连接／B的media_id／错误正文核实 | 不得标成功，不回显其他账号内容 | P0 |
| TC101-102 | 21/R101-16 | 上传后、提交前撤权或旋转凭据 | 不提交草稿；已提交只收尾核实 | P0 |
| TC101-103 | 21/R101-15 | Temporal重复activity／服务重启／cancel | 持久提交标记有效，不重复派发 | P0 |
| TC101-104 | 21/R101-14 | token明确失效／图片压缩不达标 | 有界刷新或明确拒绝，不删图强行成功 | P1 |
| TC101-105 | 22/R101-14 | 预览指定账号v3，用户编辑到v4 | 同步仍绑定v3，当前编辑不覆盖 | P0 |
| TC101-106 | 22/R101-15 | unknown刷新和重新进入 | 读同一sync，只提供核实，无自动重发 | P0 |
| TC101-107 | 22/R101-17 | 草稿成功／失败／核实中UI | 文案准确，无“已发布”和伪公开链接 | P1 |
| TC101-108 | 22/R101-17 | 改账号、双击、关闭、再打开 | request和owner隔离，正确恢复状态 | P0 |
| TC101-109 | 23/R101-16 | 每个NEW资源API跨账号调用 | 统一owner拒绝，无来源／prompt／secret泄露 | P0 |
| TC101-110 | 23/R101-16 | 伪造task/source/org与素材组合 | 服务端冻结校验，不能回退独立生成 | P0 |
| TC101-111 | 23/R101-16 | 模型／文件／上传输出注入恶意文本 | 作为数据处理，不执行指令／脚本 | P0 |
| TC101-112 | 23/R101-16 | 连续保存、生成、采用、多窗口冲突 | 版本单调、操作隔离、无丢稿或双计费 | P0 |
| TC101-113 | 23/R101-17 | 所有SC状态亮暗和移动回归 | 两套规范、对比度、焦点、320px边界通过 | P1 |
| TC101-114 | 23/R101-17 | 三入口及旧视频／点评／朋友圈回归 | 路由／来源／计费／保存无非预期变化 | P0 |
| TC101-115 | 23/R101-16 | 捕获浏览器、日志、导出、数据库审计 | 无明文凭据／签名地址泄露，标识可追踪 | P0 |
| TC101-116 | 23/R101-17 | 每模块15min探索性连点／异常顺序 | 问题按复现证据归类；无P0/P1未关闭 | P1 |
| TC101-117 | 24/R101-18 | 空库初始化到V84 | 表／CHECK／唯一键／索引完整 | P0 |
| TC101-118 | 24/R101-09 | V76存量升级并重放新DDL | 旧checksum和数据hash不变，旧接口可用 | P0 |
| TC101-119 | 24/R101-18 | 关写→排空→停worker→关Edge→恢复 | 在途操作可核实，无静默丢弃，旧入口保持 | P0 |
| TC101-120 | 24/R101-18 | 全部最终门禁与外部证据审计 | 命令真实、无漏测、现场与本地状态分开 | P0 |

### 12.3 每卡边界适用表

E01空输入；E02超长；E03重复；E04断网；E05服务错误；E06未登录；E07无权限；E08空数据；E09过期；E10刷新；E11快速切换；E12卸载；E13null/缺字段/枚举；E14长度数值边界；E15多端并发/乱序；E16已提交但回包丢失；E17跨账号组织门店；E18请求中撤权/删除；E19旧数据/客户端；E20部分成功/补偿/重放；E21时间/费用精度；E22超大列表/文件。

下表“适用”均要用本卡TC加全局TC109～120证明；“N/A”明确列出其他项及原因，不允许执行者自行删行。纯UI卡的服务端权限保证由对应后端卡及23卡测试，UI仍测试401/403/404的展示和缓存清理。

| 卡 | 适用E | N/A及原因 |
|---|---|---|
| 01 | 01、02、03、05、06、07、08、13、14、17、19 | 04、09～12、15、16、18、20～22：无写入／计时／文件；读取并发按重复GET验证 |
| 02 | 01～09、13～20、22 | 10～12：浏览器生命周期由03；21：无业务金额或本地时区判断 |
| 03 | 01～19、22 | 20：无服务端补偿；21：不计算金额／过期时间 |
| 04 | 01～22 | 无；UI与后端共同覆盖 |
| 05 | 01～09、13～22 | 10～12：由06的界面生命周期验证 |
| 06 | 01～19、22 | 20：补偿在后端；21：只展示服务端时间／估算，不自行计算 |
| 07 | 01～22 | 无；设置组件现有生命周期与本卡UI测试共同承担，TC034保留回归 |
| 08 | 01～09、13～22 | 10～12：无独立UI；恢复语义在后端重放验证 |
| 09 | 01～09、13～22 | 10～12：无浏览器状态，原图/衍生图读取由11验证 |
| 10 | 01～09、13～22 | 10～12：客户端暂停／恢复由11验证 |
| 11 | 01～22 | 无；费用只展示服务端值，测试不重复计费请求 |
| 12 | 01～22 | 无；新旧工作区及采用影响持久化 |
| 13 | 01～22 | 无；M1组合验收 |
| 14 | 01～09、13～22 | 10～12：文章UI由15验证 |
| 15 | 01～19、22 | 20：后端恢复由14/10；21：不改变费用与日期语义 |
| 16 | 01～09、13～19、22 | 10～12：由17；20：纯渲染不结算/派发；21：文字中的金额／日期按原文保留而非计算 |
| 17 | 01～19、22 | 20：不执行补偿；21：不计算业务金额／时间 |
| 18 | 01～22 | 无；文件、历史版本与URL过期均覆盖 |
| 19 | 01～09、13～22 | 10～12：设置UI由20验证 |
| 20 | 01～19、21、22 | 20：token刷新／后台取消由19/21验证 |
| 21 | 01～09、13～22 | 10～12：界面由22验证 |
| 22 | 01～22 | 无；用户侧核实与同步全状态 |
| 23 | 01～22 | 无；最终跨层覆盖 |
| 24 | 03、05、06、07、08、09、13～22 | 01、02、04、10～12：不新增业务输入／界面；通过已有SC与23结果核对 |

### 12.4 命令与环境

以下是验证命令定义，实际执行结果见 §14.1。完整部署栈默认端口继承 ci-e2e.sh：用户端18080、治理台18081、AI端18082；本轮独立 UI fixture 使用28080/28081/28082，Java IT 使用隔离 Testcontainers。不要使用生产地址替代缺失的本地环境。

| V编号 | 工作目录／命令 | 前置、预期与证据 |
|---|---|---|
| V-FE | 根目录，各卡列明的 `npm run test -- 文件`；`npm run typecheck` | 已安装lockfile依赖；退出0、指定用例实际发现 |
| V-JAVA | platform-java，各卡列明的 `./gradlew :services:intelligence-service:test --tests 完整类名` | Java25、隔离PostgreSQL/Testcontainers；退出0并检查JUnit实际执行数 |
| V-EDGE | 根 `npm run test -- tests/deployment/creation-studio.contract.test.ts`；platform-java的JavaRouteManifestGateTest | flag开/关、method、owner透传；不需要真实上游 |
| V-MIGRATION | platform-java，CreationStudioMigrationIT | 新库／V76升级／重放／回退；保留schema与hash比较 |
| V-M1 | 13卡命令＋M1相关Java IT | 界面、API、数据库证据分别记录；不是实际模型质量证明 |
| V-M2 | 18卡命令 | 真实下载文件解压与hash，禁止只验证下载按钮可点击 |
| V-M3 | 根 `npm run e2e -- tests/e2e/creation-studio.spec.ts --project=chromium --grep M3`；后端WechatDraftSyncIT/WorkflowTest | 默认仅模拟官方API形状；禁止自动访问真实连接 |
| V-E2E | 根 `npm run e2e -- tests/e2e/creation-studio.spec.ts` | 三引擎，明确UIfixture案例和真实Edge/API案例，不混报 |
| V-DESIGN | 根 `npx @google/design.md lint DESIGN.md`；`npx @google/design.md lint src/ops/DESIGN.md` | UI实施卡需截图检查，规范lint不能代替浏览器验收 |
| V-DOCS | 根 `npm run docs:links`；`npm run docs:status` | 新文档有索引，无死链；不把业务记成已完成 |
| V-SECRET | 根 `npm run security:secrets`；人工审查SC／日志／ZIP | 零新增真实秘密，自动工具不能替代人工样本检查 |

Java命令使用bash准备环境：

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
java -version
./gradlew --version
```

本地全隔离E2E沿用 `npm run e2e:ci`。新功能测试显式提供四个服务写／worker开关与两个Edge开关为true，测试结束恢复测试环境；默认配置仍为false。只在测试环境选择模拟provider／连接，脚本不得自动读个人 `.baoyu-skills/.env`。

卡级测试先实现对应测试文件再执行。无测试发现、只命中缓存而未满足当前证据要求、命令未启动或环境失败都不能记为PASS。出现可重试的已知容器资源故障可在缩小并发后重跑一次，记录原失败；不无限重试。

### 12.5 最终门禁（V-FINAL）

最后一次业务修改后执行一次完整门禁；后续只有新改动或失败才重跑相应范围。

根目录：

```bash
npm run lint
npm run typecheck
npm run test:coverage
COVERAGE_BASE_REF=7d5797756e3c0fb6df0e921637213eede384900e npm run coverage:changed
npm run build
npm run e2e -- tests/e2e/creation-studio.spec.ts
npm run docs:links
npm run docs:status
npm run security:secrets
git diff --check
```

platform-java（准备Java25后）：

```bash
./gradlew :services:intelligence-service:check :services:edge-bff:check
```

要求所有命令退出0；前端有效变更行覆盖率≥80%，Java按当前模块JaCoCo门槛；三HTML构建产物存在。覆盖率基线如因任务分支合法变更而调整，必须在交接记录给出精确commit和原因；不能把基线移动到最终HEAD使变更集合为空。所有新源文件都必须计入覆盖率，不能靠未跟踪文件遗漏绕过。

### 12.6 实际模型与公众号验收

**V-LIVE-IMAGE**：仅使用用户明确授权的模型与预算，至少完成1组6张同风格图卡及1套文章封面／配图；记录输入、采用策略、实际模型、模板版本、run/artifact、费用口径和关键文字核对。人工比较当前文字风格锚与实际参考图链，评价一致性、可读性、事实正确性及重做次数。预算上限由用户实际授权提供，不在任务书虚构金额；没有授权时保持NOT_RUN。

**V-LIVE-WECHAT**：仅对明确授权的测试公众号，保存1篇含封面＋至少2张正文图片的非敏感草稿，回读核对标题、摘要、文字和图片顺序；在平台草稿箱人工确认。不得公开发布，不删除用户原有草稿。记录账号内部ID、草稿ID和去敏截图，不记录AppSecret/token。缺实际接口权限则保留未验收原因，不能以模拟PASS替代。

**阶段结论**：LOCAL_PASS＝本地API/DB/UI/文件门禁通过；EXTERNAL_PASS＝适用真实渠道验证通过；RELEASE_READY＝两者通过且没有P0/P1缺陷、上线授权已具备。卡级本地验收可以继续推进，不等待未授权的现场动作；整个功能对外开放必须满足相应外部门槛。

### 12.7 发布判定与回退触发

上线前确保数据库迁移和必要依赖可用、开关默认关闭、worker正常、账号／模型配置经过对应验证。仅面向明确授权的试点开放；观察首次任务的引用、耗时、成本和unknown数，不预设“必然提高流量”的营销指标。

出现重复供应商派发／重复草稿、跨账号可读、原稿丢失、错误版本采用、凭据泄漏时，立即关闭相应新写开关并保留读／核实能力；按§7.6排空，不清理审计证据。恢复条件是根因修复、对应TC回归和试点确认。真实生产开关变更仍需当前用户授权。

## 13. 阻塞、偏差与恢复规则

### 13.1 真正需要停止受影响工作的条件

- 现有代码已改变公开契约、数据或授权前提，继续会覆盖他人实现或造成行为冲突。
- 必须增加本文之外的业务能力、依赖、写入文件、供应商、计费口径、生产权限或平台操作。
- 验证发现安全／资金不变量无法在现有接口内保持，需要更改已定义契约。
- 实际外部接口与§6协议有实质差异，导致不能安全写入或核实；先保留功能关闭，完成不依赖该接口的卡。
- 真实付费调用、公众号写操作或生产变更缺少当前会话明确授权。只暂停对应现场步骤，不把未授权当作本地开发／模拟测试也不能做的理由。

以下不自动阻塞：行号变化；工作区有不相关改动；本地依赖需要安装；本地容器需要启动；迁移编号被占用但可安全顺延；内部私有辅助函数拆分；已有可复用组件需要正常读取。先在授权范围内完成可做工作，再说明剩余条件。

### 13.2 报告内容

在对话中写清：受影响卡和条款、观察到的事实与证据、已尝试处理、哪些后续卡仍可继续、需要的唯一决定或资源、建议修订。不要只说“环境问题”或“等待确认”。批准后修订本文版本和相应卡／TC，复查白名单，再继续。

测试失败必须保留原输出。只有实际变更了目标行为，才按明确依据更新测试；不能删测试、跳过P0/P1、降低覆盖率或关闭权限／SSRF检查。

## 14. 执行结果与交接格式

每卡在对话记录以下内容，默认不另建完成报告文件：

1. **结果**：卡号、规格版本、状态、实际获得的用户能力。
2. **范围**：修改／新增的精确文件、已有改动保留情况、迁移与接口变化。
3. **验证**：命令、工作目录、退出码、用例实际数量、TC与SC结果、日志／截图／文件路径；缓存、跳过、未执行单列。
4. **不变量证据**：本卡涉及时列出原稿hash、版本、owner隔离、同操作调用次数、run/artifact与采用关系、同步状态；只使用脱敏ID。
5. **限制**：LOCAL_PASS／EXTERNAL_PASS／生产启用分别说明；未关闭问题与所需资源。
6. **交接**：后续卡号、依赖已交付的符号／API／schema；AUTO_CHAIN下一卡不需要逐卡再询问。

最终交接必须能回答：哪些流程已真正可用；哪些只做了模拟验证；哪种模型参考路径实测通过；公众号草稿是否已在授权账号验证；旧稿／旧接口／三入口是否兼容；是否执行过生产操作；如何关写并安全排空。

### 14.1 2026-09-14 本地复核与交接

**结果与范围**：C101-01～24 的实现和本轮必要修复已通过本地门禁。原稿导入、显式改编、计划编辑与确认、单图重做及采用、刷新恢复、保留原文排版和文件导出均有本地验证；公众号连接／同步／核实使用模拟官方 API 验证。代码保留在 `main@a08101e0` 的工作区，没有提交或推送本轮修复。

本轮主要修正了接口字段与版本校验、稳定计划身份、共享保存互斥、账号与页面生命周期、媒体撤权后的导出／同步限制、微信未知结果核实、V84 数据完整性，以及下列可直接复现的问题：

- 单图重做后，异步写 URL 期间旧任务 ID 会被恢复逻辑重新采用，造成新旧任务反复读取、采用状态丢失。修复后新 ID 在排队写地址栏时立即可读；同一浏览器复现从 823 次请求降至 35 次，采用及刷新恢复通过。前后日志：`closure-adopt-repro.log`、`closure-adopt-fixed.log`。
- 旧稿缺失交付标题／正文时继续使用稿件内容；用户明确清空标题、正文或话题时保留空值。两种情况先复现失败，再完成回归，日志：`closure-delivery-before.log`、`closure-delivery-after.log`。
- 共享弹窗逐层处理 Escape、限制与恢复焦点、锁定背景；原有 7 条弹窗测试保留。创作区和迁移弹窗在移动端使用 44px 触控目标、16px 输入字号，修正局部样式覆盖及占位文字对比度。
- 人工截图复核修正了公众号绑定／轮换字段的逐行排布和单选／复选标签的点击区域；显式载入远端计划后清除已解决的冲突提示。对应失败与回归保留在 `closure-plan-reload-before.log`、`closure-plan-reload-after.log`。

**证据基线**：原始覆盖率基线保持 `7d5797756e3c0fb6df0e921637213eede384900e`。`test-artifacts/task-101/verification-inputs.json` 记录复核 HEAD、实际源文件及 SHA-256；`current-coverage.json` 额外包含 4 个未跟踪新源文件，不以仅比较已提交代码的结果替代工作区覆盖率。以下日志均位于 `test-artifacts/task-101/`，没有另建完成报告文档。

| 门禁与实际命令 | 结果与证据 |
|---|---|
| `npm run test:coverage -- --maxWorkers=2 --minWorkers=1` | 228 个文件，1991 通过／0 失败／0 跳过，113.59s；行与语句 87.11%、分支 80.22%、函数 69.25%；`closure-coverage-complete.log` |
| `COVERAGE_BASE_REF=7d5797756e3c0fb6df0e921637213eede384900e npm run coverage:changed` | 退出0，已提交范围 92.48%；`closure-changed-coverage.log` |
| `node --import tsx scripts/local/task101-current-coverage.mjs` | 原基线到当前工作区并包含未跟踪新源文件：90.57%（5792/6395），缺失文件0；`closure-working-tree-coverage.log`、`current-coverage.json` |
| Java25 下 `./gradlew --no-daemon :services:intelligence-service:check :services:edge-bff:check --console=plain` | intelligence 1682 通过／0 失败／0 跳过，Edge 304 通过／0 失败／0 跳过，JaCoCo 与 Spotless 通过；实际全量日志 `verified-java-check.log`。最后一次同 Java 输入复核为 37 项 UP-TO-DATE，7s，见 `closure-java-check.log`，不重复计为新执行用例 |
| `AI_BASE_URL=http://127.0.0.1:28082 BASE_URL=http://127.0.0.1:28080 npm run e2e -- tests/e2e/creation-studio.spec.ts --workers=1` | Chromium／Firefox／WebKit 各12项，共36通过／0失败／0跳过，约1.7min；`closure-e2e-complete.log` |
| `npm run lint`、`npm run typecheck`、`npm run build` | 均退出0；lint 保留7条既有 warning、SFC体积门禁通过；`dist/index.html`、`dist/ai.html`、`dist/ops.html` 均存在；`closure-lint.log`、`closure-typecheck.log`、`closure-build.log` |
| 两份 `npx @google/design.md lint` 和禁用词检查 | 均通过；`closure-design.log`、`closure-ops-design.log`、`closure-design-legacy.log` |
| SC101-01～11 浏览器矩阵 | 两创作入口248张、治理及AI设置20张；1440/390与原稿320视口、亮暗主题；无整页横向溢出或页面异常，抽查可见控件尺寸与输入字号通过。`ui-matrix.json`、`ui-settings.json`、`ui/`，执行日志 `closure-ui-matrix-complete.log`、`closure-ui-settings-complete.log` |
| `npm run docs:links`、`npm run docs:status`、`npm run security:secrets`、`git diff --check` | 均退出0；文档178份／链接864条／错误0，秘密扫描3411文件通过。日志：`closure-docs-links.log`、`closure-docs-status.log`、`closure-secrets-final.log`、`closure-diff-check.log` |

**数据、文件与不变量**：`CreationStudioMigrationIT` 的2项实际数据库测试覆盖空库到V84、V76存量升级、8个新增迁移、DDL重放及旧内容／媒体／checksum保留。`CreationStudioSecurityIT`、`CreationStudioIntegrationIT` 与各域 IT 验证跨账号拒绝、原稿和版本绑定、同操作不重复调用、采用引用、实际导出字节和撤权后不触发微信写入。导出的物理表实现为 `creation_export`（V81），产物文件元数据保存在 `manifest_json`；V84追加构建令牌，不修改旧迁移。浏览器下载测试核对 HTTP 返回的 ZIP 内容及 SHA-256；后端 IT 独立核对实际渲染、ZIP图片字节与权限。

**失败记录与验证边界**：旧的失败输出保留。全量前端曾出现路由用例在高并发下超时，降低测试 worker 数后全量通过，未扩大超时或改路由断言；Java 媒体测试曾缺 FFmpeg 路径，准备本机运行路径后通过；本轮 `closure-e2e.log` 的3个失败均来自上面的新旧任务循环，修复后36项全过。UI浏览器使用真实页面加 HTTP fixture；Java IT 使用实际服务代码、隔离 PostgreSQL 等依赖和模拟供应商／微信。两者不等同于完整 Docker 部署栈联合验收，后者本轮 `NOT_RUN`，也没有宣称已取得 CI 运行结果。

**后续与开放条件**：本地开发没有剩余卡。`V-LIVE-IMAGE`、`V-LIVE-WECHAT`、完整部署栈及生产操作均 `NOT_RUN`；未确认任何真实模型参考图质量，也未向真实公众号写入草稿。现场验收仍需要获授权的模型与预算、指定测试公众号及其接口权限；之后才可判定 `EXTERNAL_PASS`，上线还需部署栈证据和授权。六个新开关默认仍为false。回退顺序保持：关业务写开关 → 排空／核实在途任务 → 关worker → 关Edge，保留表、历史、媒体和审计，不删除远端草稿。

## 附 A. 规格发布检查

本清单是任务书写作质量，不是业务实现完成状态。只有作者核对通过后才将文首置为READY_FOR_IMPLEMENTATION。

- [x] 范围、历史撤销边界、24卡和三个里程碑一致。
- [x] 31组API、共享类型、字段缺省、权限、状态和错误处理一致。
- [x] V77～V84分别有唯一DDL责任卡，无既有migration修改。
- [x] 卡依赖先于使用；没有以临时成功占位绕过后置实现。
- [x] 每卡写入文件精确，修改项已在本次基线存在或由前卡创建。
- [x] 来源／计划／run／artifact／导出／公众号快照的版本和权限链闭合。
- [x] 原文排版不触发改写；未知外部结果不自动付费／重复提交。
- [x] 24个AC、120个TC、31个API、SC/V引用无悬空或重复编号。
- [x] 生成、保存、采用和导出的副作用已分清，预算成本不冒充用户现金价格。
- [x] UI与后端验证分层明确，外部门槛未执行不会伪记PASS。
- [x] 文件路径、文档链接、标题结构、代码声明与命令已检查。
- [x] 初版只交付任务书；1.2修订已按实际证据区分本地验证、真实渠道和生产状态。

### 修订记录

| 版本 | 日期 | 作者与变更 | 状态 |
|---|---|---|---|
| 1.0 | 2026-09-12 | Codex根据用户“尽可能全并详细”的要求编写；沿用研究并补核#55撤销、工作区64KiB、保存队列、图片协议和真实草稿语义 | 作者交叉检查通过；业务未实施 |
| 1.1 | 2026-09-14 | ZCode 执行记录（实际边界，不改变规格）：C101-01～24 按 AUTO_CHAIN 全部实现（V77～V83）。本地已验：intelligence-service 全量 IT 1665 项、前端全量 1974 项、体积/lint/typecheck 门禁绿。实现口径备注：①同步 payload 哈希采用键排序规范 JSON（jsonb 不保键序）；②封面标记读取 delivery.coverRef（C101-12 采用写回形态），兼容手写 resultRefs role=cover；③uploadimg=正文 URL 语义、add_material=封面 media_id 语义，封面在内容里的占位以 add_material 返回 URL 替换（thumb 仍用 media_id）；④映射表加 ordinal 列固化文档序（created_at 同秒并列不定序）；⑤worker 关闭时 create 不启动 workflow（防僵尸工作流），行由收养清扫在重开后补起。未验边界（如实保留，不冒充）：三浏览器 e2e 与部署栈验收在 CI 执行（本地 macOS 不可跑完整栈）；V-LIVE-IMAGE / V-LIVE-WECHAT 真实渠道验收未获授权保持 NOT_RUN；测试堆 2g→3g（#101 新增上下文后全量 OOM 实录） | 24 卡实现完成；上线以 CI 与真实渠道授权验收为准 |
| 1.2 | 2026-09-14 | Codex按用户后续指派补完复核与修正；增加V84，修复版本／权限／生命周期、任务URL循环、旧稿交付恢复及弹窗／移动样式，恢复既有测试；最终1991项前端、1986项Java、36项三浏览器通过，工作区变更行覆盖率90.57%。详细证据见§14.1，替代1.1的当前状态说明 | LOCAL_PASS；真实渠道、完整部署栈与生产仍NOT_RUN |

## 附 B. 后续执行提示词

> 继续《草场任务书-101-AI创作工作流与图文交付升级.md》时，先读根AGENTS、适用DESIGN及§14.1，核对当前工作区与验证指纹；24卡已经完成本地复核，不要从C101-01重新实施。保留未提交修复、原稿、任务快照、旧接口、旧图卡和现有计费规则；仅对新变化补充验证。后续阶段为完整部署栈与获授权的真实模型／公众号验收，真实付费、公众号写入或生产变更必须具有明确的模型预算／账号／环境授权。本地fixture不能代替外部验收；继续将LOCAL_PASS、EXTERNAL_PASS及生产状态分别记录，不默认另建交付报告。

## 附 C. 依据与关系

- [本轮结合研究](../reviews/baoyu-skills-ai-creation-integration-2026-09-12.md)：上游能力和代码差距来源；#101将第一／二批收敛为固定实现规格。
- [任务书模板](任务书模板.md)：单文件合同、分卡与验收规则；本书纠正旧模板的设计流派名、旧test目录与旧版本快照。
- [#54 系列图卡](草场任务书-54-系列AI图卡.md)、[#55已撤销信息图](草场任务书-55-信息图生成.md)、[#56图像执行](草场任务书-56-图像BYOK执行消费.md)：保留现有产品与计费边界。
- [#92工作区](草场任务书-92-AI创作中心工作流闭环与最近项目.md)、[#100画布与AI](草场任务书-100-创作画布闭环与AI协作.md)：复用保存、版本、计划／应用与运行习惯，不重建视频。
- [#102画布复盘修复](草场任务书-102-创作画布复盘修复与真实闭环验收.md)：共享方法按实际合入结果兼容叠加，保持其草稿原子性、画布权限和beforeExport契约。
- [AI内容中心01](AI内容中心改造-01-生成正确性与工作区加固.md)、[02](AI内容中心改造-02-图文生产与平台交付.md)、[03](AI内容中心改造-03-视频素材创作与联合交付.md)：源码已有部分复用，本文只实现剩余差异。
- [根设计](../../DESIGN.md)、[治理设计](../../src/ops/DESIGN.md)、[目录地图](../架构/目录结构.md)、[测试说明](../../tests/README.md)：实现与验证约束。
- [固定版本baoyu-skills](https://github.com/JimLiu/baoyu-skills/tree/1567581c26ec29f4216c6e6835415bf30343b0e3/skills)：本地化工作流来源；不在运行时自动升级或执行原文件。
