# 开发规划:工作台个人设置弹窗——页签收敛与账号级内容归位(草场任务书 #73)

<!-- 弱模型执行时只需要读:第 3 节结构事实 + 第 4 节全局约束 + 当前卡。
     其余章节是给强模型自己和总控看的,不要求执行者通读。 -->

## 元信息
- 规划模型:GLM-5.3(ZCode),2026-09-04(用户当日四项拍板定稿;锚点均为当日 grep/读码核实,基于当前工作区含 #72 已提交的 main)
- 执行模型:能力较弱的编码模型(Qoder / 同级)
- 任务卡总数:3(卡 A–卡 C)
- 执行顺序:A → B → C(**严格串行**,任何一卡验收不绿不得进入下一卡)。A 产出新组件不动工作台;B 把工作台接到 A 的组件上并改写测试;C 浏览器实测与视觉自查。
- 性质:用户端纯前端壳搬家。**零后端、零 Flyway、零路由表、零 e2e 契约变更**;五张业务卡组件一行不改。

## 1. 目标与背景

**一句话目标**:工作台页签收敛为纯业务垄(商家 4 页/推荐官 3 页),「主页与分享」与「账号与合规」两页签的内容收进头部「个人设置」按钮打开的共享弹窗(两侧同构,推荐官两节/商家一节)。

**已定案的关键决策(执行期不得更改)**:

| # | 决策 | 理由 |
|---|---|---|
| D1 | 两侧同构:MERCHANT_TABS 删「账号与合规」(剩 4 页);RECOMMENDER_TABS 删「主页与分享」+「账号与合规」(剩 3 页)。原两页签内容移入共享「个人设置」弹窗——推荐官侧两节(主页与分享/账号与合规),商家侧一节(账号与合规) | 用户拍板;账号与合规(邮箱/会话/合规)本属账号级而非身份级,页签位一直错位;共享架构保留,只是「共享页签」变「共享弹窗」 |
| D2 | 弹窗形态:GlModal `wide`(960px)+ `scroll`(72vh 内滚)+ **persistent** | 用户拍板选弹窗(非抽屉);persistent 因为画像卡含编辑表单,防误触遮罩/ESC 丢草稿(关闭走右上角 ×) |
| D3 | 入口:工作台头部右侧加「个人设置」按钮,**一份共享**(header 在两侧身份面板之外,天然两侧可见) | 用户拍板;`.gl-header` 是 space-between,右侧天然空位 |
| D4 | 推荐官默认页签改为「任务大厅」(hall):**不写新逻辑**——既有「页签不在列表则回落 tabs[0]」watch 自动生效,RECOMMENDER_TABS[0] 变成 hall 即成立;只同步改掉 `GrasslandWorkbench.vue:15` 的过时注释 | 用户拍板;登录即看活,贴角色动线 |
| D5 | 五张卡组件零改动:MyRecommenderProfileCard / RecommenderShareCard / EmailBindingCard / MySessionsCard / PersonalDataComplianceCard 原样搬进新弹窗壳,import 方式(静态/异步)照原样跟随迁移 | 它们都是独立组件、独立测试;搬的只是壳。数据拉取时机从「进工作台」推迟到「开弹窗」,是收益不是破坏 |
| D6 | 旧深链 `?wtab=home` / `?wtab=account` 不做兼容映射:既有合法性校验(`GrasslandWorkbench.vue:511` 的 `activeTabs.some(...)`)不命中即忽略、回落默认页签,天然容错 | 全仓已 grep 核实(src/services/e2e)无任何构造处;通知锚点表 ANCHOR_TAB 亦无指向这两个页签的条目 |
| D7 | GlModal **内部自带 Teleport to body**(初版任务书误判为无,执行期核实修正),弹窗内容必然脱离 `.gl-field` 祖先——解法:插槽内容包一层 `<div class="gl-field">`(`.gl-field` 是纯作用域钩子类,零基础样式,包裹无副作用),恢复 `src/style.css:346-365` 的 `.gl-field .gl-tile` 系后代选择器链 | 任务抽屉的同款教训(`MerchantTaskForm.vue:2-5` 注释:Teleport 到 body 后根节点补挂 .gl-field,否则输入框/按钮裸奔) |
| D8 | 弹窗内布局:竖排两节滚动,节标题 + 全宽 `gl-tile gl-tile-wide` 卡;**不做弹窗内二级页签/竖栏** | 5 张卡在 960px 宽 + 72vh 滚动里竖排足够,不过度设计 |
| D9 | 顺带清理:删除 `.identity-open-tile` 一组死样式(模板已无引用,当日 grep 核实仅样式定义四行) | 搬家不留垃圾 |

## 2. 范围

**范围内(明确交付)**:
- 卡 A:新建 `PersonalSettingsModal.vue`(GlModal 壳 + 两节按 side 条件渲染)+ 组件测试
- 卡 B:工作台接线——页签收敛/SubTabId 类型收窄/头部入口/删除两个旧 section/import 迁移/注释更新/死样式清理 + `GrasslandWorkbench.test.ts` 改写
- 卡 C:浏览器冒烟 `verify-73.mjs` + 明暗双主题截图自查

**范围外(明确不做,遇到也不处理)**:
- 不动五张卡的任何内部代码/测试(它们各自 `.test.ts` 原样绿就是验收)
- 不动 ANCHOR_TAB、OWNED_QUERY_KEYS、wtab URL 同步逻辑(值域自然变小,机制不动)
- 不动 DefaultLayout/全局顶栏/路由表;不动治理台任何文件
- 不做旧 wtab 兼容映射/重定向(D6);不发任何后端请求变更
- 不动「争议与平台治理」共享区(`GrasslandWorkbench.vue:1267-1289`,它不是页签、始终显示,保持原样)
- 不动商家侧「AI 与治理」页签与其他任何页签的内部
- 不做弹窗打开态的 URL 持久化(弹窗开合不进 query)

## 3. 结构事实(执行者必读,全部已核实)

### 3.1 页签机制(卡 B 核心)
| 事实 | 锚点 |
|---|---|
| SubTabId 联合类型现含 9 值,本批删 `'account'`、`'home'` 两值 | `src/views/grassland/GrasslandWorkbench.vue:253` |
| MERCHANT_TABS(5 条,删 account)、RECOMMENDER_TABS(5 条,删 home+account);「共享页签标记只写一份」注释改写为「共享弹窗」 | `GrasslandWorkbench.vue:255-268`、架构注释 `:251-252` |
| 默认页签回落 watch(activeTabs 变化→不在列表则回落 tabs[0],immediate)——D4 靠它自动成立,**不改此 watch** | `GrasslandWorkbench.vue:274-276` |
| wtab 深链合法性校验(不命中即忽略)与 URL 写出(等于默认页签时不写 wtab)——机制不动 | `GrasslandWorkbench.vue:510-514`、`:462-463` |
| 「首屏页签 tasks(商家侧默认)与 home(推荐官侧默认)」注释→home 改 hall | `GrasslandWorkbench.vue:15` |
| ANCHOR_TAB 锚点映射无 account/home 条目,零改动 | `GrasslandWorkbench.vue:324-335` |

### 3.2 待搬迁的两个 section(卡 B 删除、卡 A 承接)
| 事实 | 锚点 |
|---|---|
| 「主页与分享」section:标题区 + MyRecommenderProfileCard + RecommenderShareCard 两张 `gl-tile-wide` 卡 | `GrasslandWorkbench.vue:1093-1114` |
| 「账号与合规」共享 section:EmailBindingCard + MySessionsCard + PersonalDataComplianceCard 三张 `gl-tile` 卡(渲染在两侧身份面板之后,`v-show` 常驻 DOM) | `GrasslandWorkbench.vue:1244-1264` |
| 五张卡 import 现状:MyRecommenderProfileCard 静态 import(`:7`),其余四张 defineAsyncComponent(`:23-25`、`:41`)——迁移时**照原样**(静态仍静态、异步仍异步) | `GrasslandWorkbench.vue:7、23-25、41` |
| 死样式 `.identity-open-tile*` 四段(模板无引用) | `GrasslandWorkbench.vue:1303-1311` |

### 3.3 弹窗基建与样式作用域(卡 A 核心)
| 事实 | 锚点 |
|---|---|
| GlModal:**内部 Teleport to body**(执行期核实,初版任务书误判),纯展示组件,父级 `v-if` 控制挂载;props title/wide/scroll/persistent;ESC 与遮罩点击仅在非 persistent 时 emit close(× 恒关);自带 `role="dialog" aria-modal` | `src/components/GlModal.vue:1-44`(Teleport 在 `:2`) |
| GlModal 消费先例(props open + `@close` 转发,`<GlModal v-if="open" ... persistent>`):照抄此结构 | `src/ops/admin/components/MerchantAccountInitDialog.vue:2、77` |
| 弹窗样式全局层:`.modal-overlay`/`.modal-card`(440px)/`.modal-card--wide`(960px)/`.modal-body--scroll`(max-height 72vh 内滚) | `src/style.css:404-436` |
| 田垄卡片全局层:`.gl-field .gl-zone-head/.gl-zone-title/.gl-zone-body/.gl-tile/.gl-tile-wide`——**依赖 `.gl-field` 祖先**(D7:弹窗留在 `.grassland` 根内即自动可用) | `src/style.css:346-365` |
| 弹窗内分节标题:若既有类不够用,新增全局类只允许挂在 `.modal-card` 后代作用域(先例 `.modal-card .field-label`),token-only;禁止 scoped 造轮子 | `src/style.css:523` 先例 |
| 头部按钮:朴素形态直接吃 `.gl-field button` 田垄层默认样式(`src/style.css:282`),不新增类;`.gl-btn-primary` 是主 CTA,**不用**(低频入口不抢主视觉) | `src/style.css:251、282` |
| 工作台头部:`.gl-header` space-between,左侧 `.gl-head-copy`,右侧空——按钮容器放这里 | `GrasslandWorkbench.vue:605-610` |
| 弹窗挂载点:工作台模板的共享弹窗区(任务表单结果弹窗附近),仍在 `.grassland` 根 section 内 | `GrasslandWorkbench.vue:1231-1241` |

### 3.4 测试现状(卡 A/B 改写锚)
| 事实 | 锚点 |
|---|---|
| 商家页签断言(五项数组 + `tabs[4]` 点击切换 account + 「账号与合规 zone 常驻 DOM」断言)——数组改四项,后两段删除 | `src/views/grassland/GrasslandWorkbench.test.ts:321-336`(断言在 `:328`、`:331-335`) |
| 推荐官页签断言(五项数组 `:365`;`:367` 深链 hall 是 `tabs[1]` → 默认页签改 hall 后变 `tabs[0]`;`:374` engagements `tabs[2]` 下标不变;测试名「四个子页签」本就与断言不符,顺手改「三个子页签」) | `GrasslandWorkbench.test.ts:338-375` |
| 异步卡 stub 先例(global.stumps 列卡组件名)——工作台测试点开弹窗时须把五张卡 stub 掉(照此先例) | `GrasslandWorkbench.test.ts:354-357` |
| 五张卡各自的 `.test.ts` 均存在且独立——**零改动**,它们保持绿即验收 | `src/components/MyRecommenderProfileCard.test.ts` 等 |
| happy-dom Teleport 坑:本批弹窗无 Teleport,**不需要** stubs.teleport | 仓内既有结论 |

### 3.5 冒烟先例(卡 C)
| 事实 | 锚点 |
|---|---|
| verify 脚本先例(Playwright 无头 + 造数手法 + 退出码 0):以项目根既有 verify-*.mjs 为模板,优先找用户端工作台先例 | 项目根 `verify-71.mjs` 等(`ls verify-*.mjs`) |
| 用户端测试账号造数:e2e-seed 先例;登录选身份进工作台的双身份路径 | `browser-screenshot-toolchain` 既有结论;`scripts/` 下 e2e-seed 相关 |
| 明暗双主题截图:主题须 addInitScript 写 theme-preference(store 竞态会翻回),**不可**改 DOM 属性 | 同上 |

## 4. 全局约束(适用于每一张卡)
- 只允许改动当前卡列出的文件,其他文件一律不碰。
- 不删除既有代码,除非当前卡明确要求。
- 不引入新依赖(零新包);不改后端任何文件。
- 不修改第 1 节决策表、不偏离第 3 节锚点描述的既有语义;发现锚点与实际代码不符时**停下来报告**,禁止自行发挥。
- 验收命令必须全绿才算完成;禁止为了绿灯修改断言含义或跳过用例。
- **卡住时**:同一问题最多尝试 2 次,然后停止,原样报告错误信息和已尝试的做法。禁止猜测、禁止绕过、禁止编造一个看起来合理的结果。
- 前端 UI:颜色/字号/圆角/间距只用根 `DESIGN.md` token;组件优先复用全局层(`.gl-tile` 系/GlModal),不在 scoped 样式重造;改前通读根 `DESIGN.md`(用户端规范)。
- 五张卡组件与其测试文件是**禁区**,一个字符都不改。

## 5. 任务总表
| 卡 | 标题 | 主要文件 | 依赖 | 验收方式 |
|---|---|---|---|---|
| A | PersonalSettingsModal 组件 + 组件测试 | 新建 `src/views/grassland/components/PersonalSettingsModal.vue` + `.test.ts` | 无 | vitest 指定文件绿 + build 绿 |
| B | 工作台页签收敛与弹窗接线 + 测试改写 | `GrasslandWorkbench.vue` + `GrasslandWorkbench.test.ts` | A | vitest 指定文件绿 + 全量 vitest 绿 + build 绿 |
| C | 浏览器冒烟 + 双主题视觉自查 | 新建 `verify-73.mjs` | A、B | 冒烟退出码 0 + 双主题截图 |

## 6. 任务卡

### 卡 A:PersonalSettingsModal 组件
**背景**:两个旧页签的内容需要新壳子承载;先独立成组件再接线,保证每一步可测。

**改动文件**:
- 新建 `src/views/grassland/components/PersonalSettingsModal.vue`
- 新建 `src/views/grassland/components/PersonalSettingsModal.test.ts`

**开始前检查**:
1. `git status` 干净;通读根 `DESIGN.md`。
2. 通读 `src/components/GlModal.vue` 与 `src/ops/admin/components/MerchantAccountInitDialog.vue:1-80`(消费先例)。
3. 核对 `src/style.css:346-365` 的 `.gl-field .gl-tile` 系类名拼写。

**参考代码**(骨架,照此结构):
```vue
<template>
  <GlModal v-if="open" title="个人设置" wide scroll persistent @close="$emit('close')">
    <!-- 节一:仅推荐官侧 -->
    <section v-if="side === 'recommender'" class="…" aria-label="主页与分享">
      <h4 class="…">主页与分享</h4>
      <article class="gl-tile gl-tile-wide"><MyRecommenderProfileCard /></article>
      <article class="gl-tile gl-tile-wide"><RecommenderShareCard /></article>
    </section>
    <!-- 节二:两侧 -->
    <section class="…" aria-label="账号与合规">
      <h4 class="…">账号与合规</h4>
      <article class="gl-tile"><EmailBindingCard /></article>
      <article class="gl-tile"><MySessionsCard /></article>
      <article class="gl-tile"><PersonalDataComplianceCard /></article>
    </section>
  </GlModal>
</template>
```
- props:`open: boolean`、`side: 'merchant' | 'recommender'`;emits:`close`。
- import 方式照工作台原样:MyRecommenderProfileCard 静态 import,其余四张 defineAsyncComponent(异步分包,打开弹窗才拉 chunk)。
- 分节标题样式:优先复用既有类;确需新类则加在 `src/style.css` 的 `.modal-card` 后代作用域(此时把 style.css 列入改动文件并在报告注明),token-only;**禁止 scoped 新样式**。

**做法**:
1. 按上述骨架实现组件;卡片容器类沿用 `gl-tile gl-tile-wide`(全宽竖排,D8)。
2. 写 `PersonalSettingsModal.test.ts`(五张卡全部 stub,照 `GrasslandWorkbench.test.ts:354-357` 先例):
   - ① `open=false` 不渲染;`open=true` 渲染。
   - ② `side='recommender'`:两节都在,节标题文本「主页与分享」「账号与合规」正确,五个卡占位齐全。
   - ③ `side='merchant'`:只有「账号与合规」节,三个卡占位,无「主页与分享」。
   - ④ persistent 语义:GlModal 传参断言(组件模板带 `persistent`——可通过点击遮罩不触发 close 验证)。
   - ⑤ 点 × / ESC 不触发 close(persistent);GlModal 的 × 按钮点击**会**触发 close(persistent 只挡遮罩与 ESC——以 GlModal.vue 实现为准,若实现是 persistent 连 × 也不挡/或 × 也挡,按实际行为断言并在报告注明)。

**边界**:
- 本卡不改 `GrasslandWorkbench.vue` 任何内容;不接路由;不发请求。
- 不给五张卡包任何新逻辑(不传 props、不监听事件)。

**验收**:
- `npx vitest run src/views/grassland/components/PersonalSettingsModal.test.ts` 退出码 0。
- `npm run build` 通过(类型零错)。

**完成后按此格式报告**:
```
改动文件：...
执行的命令与结果：...
偏离卡面之处：无 / <列出>
卡住项：无 / <错误原文>
```

### 卡 B:工作台页签收敛与弹窗接线
**背景**:卡 A 组件就绪;本卡把工作台切到新结构——页签收敛、头部入口、删除两个旧 section、测试改写。

**改动文件**:
- 修改 `src/views/grassland/GrasslandWorkbench.vue`
- 修改 `src/views/grassland/GrasslandWorkbench.test.ts`

**开始前检查**:
1. 卡 A 已完成且绿。
2. 逐条核对第 3.1/3.2 节锚点行号与当前文件一致(不一致则停下报告)。

**做法**(按序):
1. 页签收敛(`GrasslandWorkbench.vue`):
   - `:253` SubTabId 删 `'account'`、`'home'` 两值(剩 7 值)。
   - MERCHANT_TABS 删 account 条目(剩 4);RECOMMENDER_TABS 删 home、account 条目(剩 3,hall 成首项)。
   - `:251-252` 架构注释改写:「共享页签标记只写一份、渲染在两侧页签位之后」→「账号级内容收敛进共享『个人设置』弹窗(两侧头部入口同一份),页签只留业务垄」。
   - `:15` 注释「与 home(推荐官侧默认)」改为「与 hall(推荐官侧默认)」。
   - 回落 watch(`:274-276`)、wtab 校验/写出(`:510-514`、`:462-463`)、ANCHOR_TAB **一律不动**。
2. 头部入口:`:605-610` 的 `.gl-header` 内、`.gl-head-copy` 之后加右侧容器(`<div class="gl-head-actions">` 之类),内放一个 `<button type="button">个人设置</button>`:吃 `.gl-field button` 默认样式不新增类;`aria-label="打开个人设置"`;点击 `personalSettingsOpen = true`。新增 `const personalSettingsOpen = ref(false)`。
3. 删除两个旧 section:`:1093-1114`(主页与分享)与 `:1244-1264`(账号与合规)整段删除;五张卡的 import(`:7`、`:23-25`、`:41`)与模板使用一并移除(它们已在卡 A 组件里)。若 import 被别处共用则保留对应 import 并在报告注明。
4. 弹窗挂载:在任务表单结果弹窗附近(`:1231-1241` 共享弹窗区)加 `<PersonalSettingsModal :open="personalSettingsOpen" :side="side" @close="personalSettingsOpen = false" />`;**保持在 `.grassland` 根 section 内,不 Teleport**(D7)。scoped 样式若需 `.gl-head-actions` 一行布局对齐(gap/align-self),允许加最小 scoped 规则,token-only。
5. 死样式清理:删 `:1303-1311` 的 `.identity-open-tile` 四段。
6. 测试改写(`GrasslandWorkbench.test.ts`):
   - `:328` 商家断言数组改 `['任务与报名', '商家主体与门店', '资金与经营', 'AI 与治理']`;`:331-335`(account 页签点击与常驻 DOM 断言)删除;同用例内补:头部「个人设置」按钮存在,点击后弹窗出现且「账号与合规」节可见(商家侧无「主页与分享」节)。
   - `:338` 测试名改「推荐官工作台三个子页签;?wtab= 深链与锚点联动」;`:365` 数组改 `['任务大厅', '我的履约', '收益与结算']`;`:367` hall 断言 `tabs[1]` → `tabs[0]`;`:374` engagements `tabs[2]` 不变。
   - 新增用例「推荐官侧默认页签为任务大厅」(不带 wtab 挂载,断言 `tabs[0]` aria-selected 且 tabs[0] 文本=任务大厅)。
   - 新增用例「个人设置弹窗按身份分节」:推荐官侧点开弹窗断言两节标题与五个卡 stub 占位;`ESC/遮罩不关闭、× 关闭`(以 GlModal 实际行为为准,与卡 A ⑤口径一致)。
   - 新增用例「旧深链容错」:`?wtab=home` 挂载不抛错、回落默认页签(商家侧 `?wtab=account` 同理)。
   - 全文件其余用例若因页签收敛而引用失效(如别的用例点了 account/home 页签),只做**最小必要**调整并在报告逐条列出;不得改动用例的其余断言意图。

**边界**:
- 不动「争议与平台治理」区(`:1267-1289`)与任务表单结果弹窗。
- 不动 ANCHOR_TAB/OWNED_QUERY_KEYS/watch 逻辑;不新增 query 参数。
- 五张卡组件与其测试禁改。

**验收**:
- `npx vitest run src/views/grassland/GrasslandWorkbench.test.ts src/views/grassland/components/PersonalSettingsModal.test.ts` 退出码 0。
- `npx vitest run` 全量绿(证明无连坐)。
- `npm run build` 通过。

**完成后按此格式报告**:
```
改动文件：...
执行的命令与结果：...
偏离卡面之处：无 / <列出>
卡住项：无 / <错误原文>
```

### 卡 C:浏览器冒烟与双主题视觉自查
**背景**:壳搬家涉及登录动线与弹窗交互,vitest 覆盖不了真实浏览器;AGENTS.md 要求每页改完明暗双主题截图自查。

**改动文件**:
- 新建 `verify-73.mjs`(项目根)

**开始前检查**:
1. 卡 A/B 全部合入;`npm run build` 产物已部署或 dev 栈可用(照既有 verify-*.mjs 的启动前提)。
2. `ls verify-*.mjs` 找最近的用户端冒烟先例通读,照抄登录/造数/断言手法。

**做法**:
1. `verify-73.mjs`(Playwright 无头,退出码 0 为过)覆盖:
   - ① 推荐官账号登录进工作台:页签恰为「任务大厅/我的履约/收益与结算」三项,默认激活=任务大厅。
   - ② 头部「个人设置」按钮可见,点击弹窗打开:两节标题齐全,画像卡/二维码/绑邮箱/会话/合规五卡可见。
   - ③ 关闭弹窗(×),再开一次仍正常(开合不残留状态)。
   - ④ 商家账号登录:页签恰四项;弹窗只有「账号与合规」节。
   - ⑤ 旧深链容错:直接访问工作台 URL 带 `?side=recommender&wtab=home`,页面正常回落任务大厅不白屏。
2. 视觉自查:工作台页(弹窗关)+ 弹窗开(推荐官侧)各截**明/暗双主题**共 4 张到 `shots-73/`(主题用 addInitScript 写 theme-preference,**禁止**改 DOM 属性);对照根 `DESIGN.md` 检查 token/对比度/间距/层级,弹窗内 `.gl-tile` 卡是否吃到田垄层样式(不裸奔)。

**边界**:不做 e2e 正式 spec 入库;不做跨浏览器矩阵;不做移动端断点新适配(既有响应式行为不劣化即可)。

**验收**:
- `node verify-73.mjs` 退出码 0。
- 4 张截图产出且自查无 DESIGN.md 违例(在报告里逐张给结论)。

**完成后按此格式报告**:
```
改动文件：...
执行的命令与结果：...
偏离卡面之处：无 / <列出>
卡住项：无 / <错误原文>
截图自查结论：4 张逐条
```

## 7. 集成验收(全部卡完成后执行,可由强模型或人工触发)
- 回归:`npx vitest run` 全量绿;`npm run build`、`npm run lint` 通过。
- 冒烟:`node verify-73.mjs` 通过。
- 产物检查:`git status` 无任务书未列文件(截图目录 `shots-73/` 不入库,加入 .gitignore 若已有同类先例)。
- 视觉终检:卡 C 的 4 张截图对照根 `DESIGN.md`(对比度/层级/token),弹窗内卡片样式继承 `.gl-field` 祖先不裸奔。

## 附:返工卡格式(强模型 review 后按此格式产出,编号 R-1、R-2…)

### 返工卡 R-1(针对卡 X)
**问题位置**:`<文件:行>`
**期望行为 vs 实际行为**:<一句话对比>
**修复做法**:
1. <步骤化,同任务卡的写法>
**验收**:运行 `<命令>`,期望 `<…>`
