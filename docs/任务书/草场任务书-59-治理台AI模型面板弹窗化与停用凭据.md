# 草场任务书 #59：治理台 AI 模型面板——停用凭据可见可删 + 表单弹窗化

> 2026-08-31 立项。范围：`平台管理 → AI 模型` 页的三个面板（平台通用凭据 / 平台模型+受信端点 / 价目表）。
> 用户原始诉求（原文三条）：
> 1. 平台通用凭据停用后看不到已停用的凭据——加「显示已停用」选项；停用凭据可以删除，删除前弹窗确认；
> 2. 新增凭据、新增配置、添加端点改成弹出框形式；
> 3. 价目表点「单价」也改成弹出框。
>
> 改 UI 前必读 `src/ops/DESIGN.md`（这三个面板仅被治理台引用，走 grassland-admin 规范）。
> 颜色/字体/圆角/间距只用既有 token，禁止硬编码新值（AGENTS.md 硬规则 2）。

## 决策表（2026-08-31 拍板，实现时不得偏离）

| # | 决策 | 定死的内容 |
|---|---|---|
| D1 | 停用凭据可见 | 后端 `GET /api/admin/ai/credentials` 加 `?includeDisabled=true`（默认 false，**既有契约逐字节不变**）；前端凭据面板 header 加「显示已停用」checkbox。**红线：默认值不得翻转**——平台模型表单的凭据下拉、`openEdit` 反查凭据都依赖默认只回生效行，翻转会让停用凭据被选进新配置。 |
| D2 | 停用凭据可删 = 物理硬删 | 新端点 `DELETE /api/admin/ai/credentials/{id}/hard`。仅停用行（`enabled=false`）可删；生效行 → 409「请先停用」；被**任何** `platform_model_config` 行引用（含已停用历史行，普通 FK 物理拦截）→ 409 并报行数；勾选集 `platform_credential_model` 经既有 `ON DELETE CASCADE` 一并清除。**本任务零 DDL、零迁移**。 |
| D3 | 删除确认用弹窗 | 不用 `window.confirm`。确认弹窗含凭据名与不可恢复警示，确认按钮用全局 `.btn-confirm.danger`（红色危险样式，`src/style.css:492` 已有）。 |
| D4 | 弹窗化范围 = 三个面板全部内联表单 | 不止用户点名的三个按钮：同一面板一半弹窗一半内联展开会交互割裂。三个面板里所有 `.form-band` 内联块**全部**收口为弹窗（凭据：新增/编辑/轮换 + 获取模型勾选 + 删除确认；模型：新增/修订 + 端点新增/编辑；价目表：复制调价 + 单价）。共 8 处，共用一个新组件 `GlModal.vue`。 |
| D5 | 弹窗规格 | 复用 `src/style.css` 弹窗骨架共用层（`.modal-overlay/.modal-card/...`），全局层新增两档宽度与滚动类（见 S3）。普通档 `min(440px, 92vw)` 既有；宽档 `min(960px, 94vw)`。 |
| D6 | 单价弹窗 | 宽档 + 内容滚动。draft **可编辑态** `persistent`（点遮罩/Esc 不关闭，防误触丢掉已改的单价表）；只读态与其它弹窗默认可关。 |
| D7 | 停用行操作列 | 停用凭据行只显示「删除」（danger）；不给编辑/轮换/获取模型入口——后端这些端点全走 `findEnabledById`，对停用行本来就 404，UI 与后端口径一致。**不新增「恢复凭据」**（涉及目的地/标签唯一索引冲突处理，超出本需求，另立项）。 |
| D8 | 停用行样式 | 复刻平台模型面板既有范式：行置灰（`.row-disabled` 半透明）+ 版本列下挂「已停用」小 tag（`.disabled-tag`）。 |
| D9 | 兼容性红线 | 所有打开按钮的 `data-action` 属性与文案保持不变（`add-credential`/`add-model`/`add-origin`/`view-models`/`copy-active`/`edit-*`/`rotate-credential`/`disable-credential`/`fetch-models`/...）。已核实 e2e 不依赖这些面板的选择器，但保持稳定是纪律。既有前端单测的 API 调用序列断言也依赖它们。 |

## 现状锚点（动手前必读，行号为 2026-08-31 时点）

### 前端

| 文件 | 现状 |
|---|---|
| `src/components/AiPlatformCredentialsPanel.vue` | 凭据面板。列表只调 `api.listCredentials()`（无参=只回生效行）；内联 `.form-band` ×2：凭据表单（create/edit/rotate 三态，L40-76）、获取模型勾选（L78-108）。停用走 `window.confirm`（L285-294）。 |
| `src/components/AiPlatformModelsPanel.vue` | 模型+受信端点。「显示已停用」开关与 `includeDisabled` 范式在这里（L9-13、L221、L377-393）——**D1/D8 照抄此范式**。内联 `.form-band` ×2：端点表单（L90-112）、模型表单（L115-181）。停用行的「恢复/删除」双按钮范式在 L43-52。 |
| `src/components/AiPriceTablePanel.vue` | 价目表。内联 `.form-band` ×2：复制调价（L38-58）、逐模型单价（L60-128，draft 可编辑/active+retired 只读）。 |
| `src/composables/useAiControlPlane.ts` | L141-156 凭据 API 段；`listModels(includeDisabled)` 的 query 参数拼法在 L193-196。 |
| `src/types/ai-control-plane.ts` | L126-137 `PlatformProviderCredential` **已含 `enabled: boolean`**——前端类型零改动。 |
| `src/style.css` | L399-546 弹窗骨架共用层（`.modal-overlay/.modal-card/.modal-header/.modal-title/.modal-close/.modal-body/.modal-actions/.btn-cancel/.btn-confirm(+.danger)/.modal-card .field-*/.error-msg`）。弹窗经 Teleport 挂 body，样式收口在全局层——历史教训见 L399-403 注释。 |
| `src/ops/admin/components/CreationSkillsAdminPanel.test.ts` | L65-69 弹窗类组件测试范式：`mount(X, { global: { stubs: { Teleport: true } } })`。 |

### 后端（intelligence-service）

| 文件 | 现状 |
|---|---|
| `.../ai/controlplane/PlatformProviderCredentialController.java` | `list()` L77-82 只调 `repository.findAllEnabled()`；`@DeleteMapping("/{id}")` L245-258 = 软删（引用中 409 报数）。类 javadoc L30-38 有端点清单——**改完要同步**。 |
| `.../ai/controlplane/PlatformProviderCredentialRepository.java` | 只有 `findAllEnabled/findEnabledById/...`；无含停用查询、无硬删。 |
| `.../ai/controlplane/PlatformProviderCredentialResponse.java` | **已含 `enabled` 字段，零改动**。 |
| `.../ai/controlplane/PlatformModelConfigController.java` | L127-144 `includeDisabled` 的 `@RequestParam` 范式；L157-180+ 硬删已停用行的端点范式（409 语义/文案）——**S1 照此风格写**。 |
| `.../ai/controlplane/PlatformModelConfigRepository.java` | L231-246 `deleteDisabled()`——`DELETE ... WHERE enabled = false RETURNING id::text` + `.hasElement()` 的硬删仓储范式，S1 照抄此形。 |
| 迁移 V46 | `platform_model_config.credential_id` 普通 FK（无 CASCADE）——被引用行物理删不掉，D2 的 409 预检就是为此。同目的地/同标签唯一索引都是 `WHERE enabled = true` 部分索引。 |
| 迁移 V51 | `platform_credential_model.credential_id` **ON DELETE CASCADE**——硬删凭据时勾选集自动清，无需手删。 |
| 迁移 V47 | `ai_run.credential_version` 只是冻结的 bigint，无 FK——硬删不影响历史 Run。 |
| `.../ai/controlplane/PlatformProviderCredentialControllerIT.java` | IT 范式：`IntelligenceItSupport` + `signAdmin(ADMIN)` 头 + `@BeforeEach clean()` 清表顺序（history→slot→config→credential_model→credential）。 |

---

## S1 · 后端：凭据列表 includeDisabled + 硬删端点

### S1.1 Repository（`PlatformProviderCredentialRepository.java`）新增三个方法

```java
/** 全部凭据（含已停用，治理台「显示已停用」开关；生效行在前）。 */
public Flux<PlatformProviderCredential> findAllIncludingDisabled() {
    return db.sql("SELECT " + COLS + " FROM platform_provider_credential"
                    + " ORDER BY enabled DESC, provider, name")
            .map(PlatformProviderCredentialRepository::map)
            .all();
}

/** 按 id 读凭据（不限 enabled）——硬删前的存在性与状态校验用。 */
public Mono<PlatformProviderCredential> findById(UUID id) {
    return db.sql("SELECT " + COLS + " FROM platform_provider_credential"
                    + " WHERE id = CAST(:id AS uuid)")
            .bind("id", id.toString())
            .map(PlatformProviderCredentialRepository::map)
            .one();
}

/** 引用该凭据的全部模型配置行数（含已停用历史行）。FK 物理拦硬删，这里给可诊断的 409 而非 500。 */
public Mono<Long> countAllReferences(UUID id) {
    return db.sql("SELECT COUNT(*) AS n FROM platform_model_config"
                    + " WHERE credential_id = CAST(:id AS uuid)")
            .bind("id", id.toString())
            .map((row, meta) -> row.get("n", Long.class))
            .one();
}

/** 物理删除一行已停用凭据；勾选集（platform_credential_model）经 FK CASCADE 一并清除。 */
public Mono<Boolean> hardDelete(UUID id) {
    return db.sql("""
                    DELETE FROM platform_provider_credential
                    WHERE id = CAST(:id AS uuid) AND enabled = false
                    RETURNING id::text
                    """)
            .bind("id", id.toString())
            .map((row, meta) -> row.get("id", String.class))
            .one()
            .hasElement();
}
```

`WHERE enabled = false` 是安全闸（与 `PlatformModelConfigRepository.deleteDisabled` 同款）。

### S1.2 Controller

**改 `list()`**（照 `PlatformModelConfigController.list` 范式）：

```java
@GetMapping
public Flux<PlatformProviderCredentialResponse> list(
        @RequestParam(name = "includeDisabled", defaultValue = "false") boolean includeDisabled,
        ServerWebExchange exchange) {
    return callers.requireAdmin(exchange.getRequest())
            .flatMapMany(admin -> (includeDisabled
                    ? repository.findAllIncludingDisabled()
                    : repository.findAllEnabled())
                    .map(PlatformProviderCredentialResponse::from));
}
```

**加硬删端点**：

```java
/**
 * 硬删一行<b>已停用</b>凭据（任务书 #59 D2）。生效中 → 409（两步确认，防误删）；
 * 被任何 platform_model_config 行引用（含已停用历史行——历史行按其时点凭据复现审计，
 * 且普通 FK 物理拦截）→ 409 报行数。勾选集经 CASCADE 一并清除，无需手删。
 * ai_run.credential_version 是冻结 bigint 非 FK，历史 Run 不受影响。
 */
@DeleteMapping("/{id}/hard")
public Mono<ResponseEntity<Void>> hardDelete(@PathVariable UUID id, ServerWebExchange exchange) {
    return callers.requireAdmin(exchange.getRequest())
            .flatMap(admin -> repository.findById(id)
                    .switchIfEmpty(Mono.error(notFound(id)))
                    .flatMap(existing -> {
                        if (existing.enabled()) {
                            return Mono.error(new IntelligenceException(409,
                                    "凭据仍在生效中，请先停用后再删除"));
                        }
                        return repository.countAllReferences(id);
                    })
                    .flatMap(references -> references > 0
                            ? Mono.<ResponseEntity<Void>>error(new IntelligenceException(409,
                                    "该凭据仍被 " + references + " 个模型配置行引用（含已停用历史行），不可删除"))
                            : transactions.transactional(repository.hardDelete(id))
                                    .flatMap(done -> done
                                            ? Mono.just(ResponseEntity.noContent().<Void>build())
                                            : Mono.error(notFound(id)))))
            .onErrorMap(DataIntegrityViolationException.class, e -> new IntelligenceException(409,
                    "该凭据仍被模型配置行引用（含已停用历史行），不可删除"));
}
```

注意：`Mono.error` 写在 `flatMap` 的 lambda 里（惰性），**不要**放进 `switchIfEmpty(...)` 的参数位置（Reactor eager-assembly 陷阱，项目里吃过亏）。`onErrorMap(DataIntegrityViolationException...)` 是 count 与 delete 之间出现并发引用时的兜底（FK 拒删会抛这个），转成可读 409 而不是 500。

**同步类 javadoc** L30-38 的端点清单，补两行：
- `GET /api/admin/ai/credentials?includeDisabled=true — 含已停用行（治理台开关）`
- `DELETE /api/admin/ai/credentials/{id}/hard — 硬删已停用行（引用中 409；勾选集 CASCADE）`

---

## S2 · 前端 API 层（`src/composables/useAiControlPlane.ts`）

凭据段（L141-156）改两处：

```ts
/** `includeDisabled=true` 时含已停用行（治理台「显示已停用」开关）。默认只回生效行——
 *  平台模型表单的凭据下拉依赖此默认值，勿翻转。 */
const listCredentials = (includeDisabled = false) =>
  request<PlatformProviderCredential[]>(
    includeDisabled ? `${credentialsPath}?includeDisabled=true` : credentialsPath)

/** 硬删一行已停用凭据；生效中/被模型配置行引用（含历史行）后端回 409。 */
const hardDeleteCredential = (id: string) =>
  request<void>(`${credentialsPath}/${encodeURIComponent(id)}/hard`, { method: 'DELETE' })
```

- `listCredentials` 加默认参数，既有两处调用（凭据面板、模型面板）不传参行为不变——**模型面板的调用点一行都不要动**。
- return 对象里加 `hardDeleteCredential`；`disableCredential`（软删）**保留原名不改**。

---

## S3 · 共享弹窗组件 `GlModal.vue` + 全局 CSS

### S3.1 新建 `src/components/GlModal.vue`

```vue
<template>
  <Teleport to="body">
    <div class="modal-overlay" data-testid="gl-modal-overlay" @mousedown.self="onOverlay">
      <div class="modal-card" :class="{ 'modal-card--wide': wide }" role="dialog" aria-modal="true" :aria-label="title">
        <header class="modal-header">
          <h3 class="modal-title">{{ title }}</h3>
          <button type="button" class="modal-close" aria-label="关闭弹窗" data-action="close-modal"
                  @click="emit('close')">×</button>
        </header>
        <div class="modal-body" :class="{ 'modal-body--scroll': scroll }">
          <slot />
        </div>
        <footer v-if="$slots.actions" class="modal-actions modal-card__footer">
          <slot name="actions" />
        </footer>
      </div>
    </div>
  </Teleport>
</template>
<script setup lang="ts">
import { onBeforeUnmount, onMounted } from 'vue'

/** 哑组件：只管壳（Teleport/遮罩/Esc/宽窄档），确认逻辑与数据全在调用方。 */
const props = withDefaults(defineProps<{
  title: string
  wide?: boolean
  scroll?: boolean
  /** true 时点遮罩与 Esc 不关闭（防误触丢编辑中的表单，如 draft 单价表）。 */
  persistent?: boolean
}>(), { wide: false, scroll: false, persistent: false })

const emit = defineEmits<{ close: [] }>()

function onOverlay(): void {
  if (!props.persistent) emit('close')
}

function onKeydown(event: KeyboardEvent): void {
  if (event.key === 'Escape' && !props.persistent) emit('close')
}

onMounted(() => window.addEventListener('keydown', onKeydown))
onBeforeUnmount(() => window.removeEventListener('keydown', onKeydown))
</script>
```

约定：
- **组件自身零样式**——壳类名全部来自 `src/style.css` 全局层（Teleport 挂 body 后脱离任何容器作用域，样式收口全局层是本项目定死的规矩）。
- `@mousedown.self`（不是 click）：避免在弹窗内拖选文本、松手落在遮罩上时误关。
- 各面板表单的字段样式继续写在各面板 `<style scoped>` 里即可——Vue scoped 的 data-v 属性随 vnode 走，Teleport 不剥离；历史坑是「全局类依赖父级容器选择器」，不是 scoped 失效。

### S3.2 `src/style.css` 弹窗骨架共用层（L399 起）追加

```css
/* 宽档：单价表等宽表格弹窗（任务书 #59） */
.modal-card--wide {
  width: min(960px, 94vw);
}

/* 高内容滚动：模型勾选列表、单价表 */
.modal-body--scroll {
  max-height: min(72vh, 640px);
  overflow-y: auto;
}

/* GlModal 的 actions 独立 footer（.modal-body 已有自己的 20px padding） */
.modal-card__footer {
  padding: 0 20px 20px;
}
```

只加尺寸，不引入新颜色/字体/圆角 token。

---

## S4 · 凭据面板改造（`AiPlatformCredentialsPanel.vue`）

### S4.1 「显示已停用」开关

照 `AiPlatformModelsPanel.vue` L9-13 的结构，header 右侧改为：

```vue
<div class="heading-actions">
  <label class="toggle-disabled">
    <input type="checkbox" name="includeDisabledCredentials" :checked="includeDisabled"
           @change="onToggleDisabled(($event.target as HTMLInputElement).checked)" />
    显示已停用
  </label>
  <button type="button" class="primary-command" data-action="add-credential" @click="openCreate">新增凭据</button>
</div>
```

script 加（`onToggleDisabled` 触发重拉）：

```ts
const includeDisabled = ref(false)

async function loadCredentials(): Promise<void> {
  loading.value = true
  error.value = ''
  try {
    credentials.value = [...await api.listCredentials(includeDisabled.value)]
  } catch ...  // 其余不变
}

function onToggleDisabled(next: boolean): void {
  includeDisabled.value = next
  void loadCredentials()
}
```

`.heading-actions/.toggle-disabled` 样式从模型面板 scoped 里拷过来。

### S4.2 停用行的展示与操作

表格行：

```vue
<tr v-for="item in credentials" :key="item.id" :class="{ 'row-disabled': !item.enabled }">
  ...
  <td>v{{ item.version }} <small v-if="!item.enabled" class="disabled-tag">已停用</small></td>
  <td class="row-actions">
    <template v-if="item.enabled">
      <button ... data-action="edit-credential" ...>编辑</button>
      <button ... data-action="rotate-credential" ...>轮换</button>
      <button ... class="danger-command" data-action="disable-credential" ...>停用</button>
      <button ... data-action="fetch-models" ...>获取模型</button>
    </template>
    <template v-else>
      <button type="button" class="danger-command" data-action="delete-credential"
              @click="openDeleteConfirm(item)">删除</button>
    </template>
  </td>
</tr>
```

`.row-disabled`（行置灰半透明）与 `.disabled-tag`（警示色小字）样式照抄模型面板。停用流程本身（`disableCredential` + `window.confirm`）**不动**——停用后列表按当前开关状态重拉，勾着开关就能看到刚停用的行。

### S4.3 凭据表单 → 弹窗

- 删掉 L40-76 的 `<div v-if="mode" class="form-band">` 外壳，换成 `<GlModal v-if="mode" :title="formTitle" @close="closeForm">`；`<form @submit.prevent="submit">` 原样进默认插槽（字段、校验、提示一字不改）；「取消/保存」按钮进 `actions` 插槽。
- 原 `form-heading` 里的 × 按钮删除（GlModal 自带关闭按钮）；原 `form-heading`/`form-actions` 里承担这两个角色的 scoped 样式如不再被引用一并清理。
- **明文密钥只活一瞬的规矩保留**（`submit()` 里先取 `apiKey.value` 再清空绑定，L255-257）——弹窗化不改这行代码。

### S4.4 获取模型勾选 → 弹窗

- L78-108 的 `pickerTarget` form-band 换成 `<GlModal v-if="pickerTarget" :title="`${pickerTarget.name} · 勾选可用模型`" scroll @close="closePicker">`。
- `openPicker` 的合并逻辑（上游列表 + 存量勾选 stale 保留、上游失败不清空勾选集）**一行不改**，那是防「上游抖动擦掉运营选择」的关键行为。
- 「取消/保存勾选（N）」进 actions 插槽。

### S4.5 删除确认弹窗（新增）

```vue
<GlModal v-if="deleteTarget" title="删除平台凭据" @close="closeDeleteConfirm">
  <p class="delete-confirm-text">
    确认永久删除凭据「{{ deleteTarget.name }}」（{{ deleteTarget.provider }} · {{ deleteTarget.baseUrl }}）？
    此操作不可恢复，其已勾选的模型白名单将一并删除。
    若仍被模型配置行引用（含已停用历史行），删除会被拒绝。
  </p>
  <p v-if="deleteError" class="error-state compact" role="alert">{{ deleteError }}</p>
  <template #actions>
    <button type="button" class="btn-cancel" @click="closeDeleteConfirm">取消</button>
    <button type="button" class="btn-confirm danger" data-action="confirm-delete-credential"
            :disabled="deleteSubmitting" @click="confirmDelete">
      {{ deleteSubmitting ? '删除中...' : '确认删除' }}
    </button>
  </template>
</GlModal>
```

```ts
const deleteTarget = ref<PlatformProviderCredential | null>(null)
const deleteSubmitting = ref(false)
const deleteError = ref('')

function openDeleteConfirm(item: PlatformProviderCredential): void {
  deleteTarget.value = item
  deleteError.value = ''
}

function closeDeleteConfirm(): void {
  deleteTarget.value = null
  deleteError.value = ''
  deleteSubmitting.value = false
}

async function confirmDelete(): Promise<void> {
  const item = deleteTarget.value
  if (!item) return
  deleteSubmitting.value = true
  deleteError.value = ''
  try {
    await api.hardDeleteCredential(item.id)
    closeDeleteConfirm()
    await loadCredentials()   // 按当前 includeDisabled 状态重拉
  } catch (caught: unknown) {
    // 409 引用数原样透出在弹窗内，弹窗不关——运营要看到为什么删不掉
    deleteError.value = caught instanceof Error ? caught.message : '平台凭据删除失败'
  } finally {
    deleteSubmitting.value = false
  }
}
```

`.btn-cancel/.btn-confirm(.danger)` 用全局层现成类；`.delete-confirm-text` scoped 里给个常规正文样式（颜色 `var(--color-text-secondary)`）即可。

---

## S5 · 模型面板改造（`AiPlatformModelsPanel.vue`）

1. **模型表单 → 弹窗**：L115-181 form-band 换 `<GlModal v-if="mode" :title="mode === 'create' ? '新增平台模型' : `修订配置 · v${target?.version}`" wide @close="closeForm">`。所有字段与逻辑（能力/角色 edit 态禁改、凭据反查留空、上游模型列表降级手填、`__manual__` 出口、并发校验）**一字不改**，只搬位置。「取消/保存修订」进 actions。
2. **端点表单 → 弹窗**：L90-112 换 `<GlModal v-if="originFormMode" :title="originFormMode === 'create' ? '添加受信端点' : `编辑端点 · v${originTarget?.version}`" @close="closeOriginForm">`（普通档）。
3. 模型/端点的停用、删除、恢复按钮与 `window.confirm` 流程**全部不动**（见「明确不做」）。
4. scoped 样式清理：`.form-band` 及只为 form-band 存在的规则删除；`wide` 弹窗内的两列表单 grid 样式保留。

## S6 · 价目表面板改造（`AiPriceTablePanel.vue`）

1. **复制调价 → 弹窗**：L38-58 换 `<GlModal v-if="copyFormOpen" :title="`复制 ${activeVersion?.label} 为新 draft`" @close="closeCopyForm">`。hint 文案（改价必须走 draft 的原因说明）保留在弹窗 body。「取消/创建 draft」进 actions。`submitCopy` 成功后 `closeCopyForm()` + 自动打开新 draft 的单价弹窗（既有行为）。
2. **单价 → 弹窗**：L60-128 换：

```vue
<GlModal v-if="modelsTarget" wide scroll persistent={editable}
         :title="`${modelsTarget.label} · 逐模型单价${editable ? '（可改）' : '（已冻结）'}`"
         @close="closeModels">
```

（模板里写 `:persistent="editable"`。）表格、只读态、「新增模型/移除/保存单价」、number 转 Number、整份覆盖 PUT——逻辑一字不改，只搬位置。actions 插槽只在 `editable` 时给「新增模型/保存单价」；只读态弹窗无 actions（× 或遮罩关闭即可）。
3. 激活/删除 draft 的 `window.confirm` 不动。
4. scoped 样式清理同 S5。

---

## S7 · 测试与验收

### S7.1 前端单测（vitest + happy-dom）

**新文件 `src/components/GlModal.test.ts`**（mount 一律 `global: { stubs: { Teleport: true } }`，否则内容渲染到 body、findAll 全空——项目实测过的坑）：

1. 渲染：title 进 `.modal-title`；`wide` → `.modal-card--wide` 存在；`scroll` → `.modal-body--scroll` 存在。
2. 点 × (`[aria-label="关闭弹窗"]`) / 点遮罩 (`[data-testid="gl-modal-overlay"]` 上 `trigger('mousedown')`) / `window.dispatchEvent(new KeyboardEvent('keydown', { key: 'Escape' }))` → 各 emit 一次 `close`。
3. `persistent` 时遮罩与 Esc 不 emit；× 仍 emit。
4. 卸载后 Esc 不再触发（监听器已移除）。

**`AiPlatformCredentialsPanel.test.ts`**：既有 9 个用例全部改为 teleport stub 的 mount；既有断言目标（`data-action`、`form` submit、fetch 调用序列、明文不残留）**应全部保持通过**——这是 D9 兼容红线的直接验证。新增用例：

5. 初始 GET 不带 `?includeDisabled`（默认契约不变）；勾选「显示已停用」→ 重新 GET 带 `?includeDisabled=true`。
6. 停用行（fixture `enabled: false`）：行含「已停用」文案；`data-action="edit-credential"` 等四个按钮不存在，`data-action="delete-credential"` 存在。
7. 删除流：点删除 → 确认弹窗出现且含凭据名；点取消 → 无 DELETE 请求、弹窗关闭；再点删除 → 确认 → `DELETE /api/admin/ai/credentials/cred-x/hard` + 列表按开关状态重拉；成功后弹窗关闭。
8. 删除 409：mock 回 `{ error: '该凭据仍被 2 个模型配置行引用（含已停用历史行），不可删除' }` → 弹窗**不关**，`role="alert"` 里可见该文案。
9. 凭据表单在弹窗里：点 `add-credential` → `.modal-overlay` 存在；提交成功后弹窗消失（既有「creates a credential」用例改造后已覆盖，单列断言弹窗开合亦可）。

**`AiPlatformModelsPanel.test.ts` / `AiPriceTablePanel.test.ts`**：既有用例加 teleport stub 后保持通过；价目表补两个断言：点 `view-models` → `.modal-card--wide` 存在；draft 可编辑态点遮罩不关闭、只读态点遮罩关闭（persistent 生效）。

### S7.2 后端 IT（`PlatformProviderCredentialControllerIT` 扩展）

沿用本 IT 既有口径（`signAdmin(ADMIN)` 头、`clean()` 清表顺序不动）：

1. 建凭据→停用（DELETE `/{id}`）→ 默认 GET 不含它、`?includeDisabled=true` 含它且 `enabled=false`。
2. 硬删停用行 → 204；`?includeDisabled=true` 不再含；预插的勾选集行（`platform_credential_model`）随删消失（验证 CASCADE）。
3. 硬删**生效**行 → 409，文案含「先停用」。
4. 硬删被引用行：SQL 直插一条 `enabled=false` 的 `platform_model_config` 历史行 `credential_id` 指向它 → 409，文案含行数。
5. 硬删不存在的 id → 404。
6. 非管理员（`USER`）调 hard 与 includeDisabled → 沿用本 IT 既有鉴权断言口径（403）。

### S7.3 门禁（全绿才算完成）

```bash
# 前端
npx vitest run src/components/GlModal.test.ts src/components/AiPlatformCredentialsPanel.test.ts src/components/AiPlatformModelsPanel.test.ts src/components/AiPriceTablePanel.test.ts
npm run typecheck
npm run lint
npm test                       # 全量前端单测（防面板外连带破坏）

# 后端（JDK 25；本地部署栈在跑时先停或输出重定向，并存会挂死）
export JAVA_HOME=$(/usr/libexec/java_home -v 25)
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.ai.controlplane.PlatformProviderCredentialControllerIT'
```

### S7.4 浏览器验收（验收人执行；实现者本地有栈也可先自查）

1. 勾「显示已停用」→ 停用凭据出现（置灰 + 已停用 tag，操作列只有删除）；去勾消失。
2. 删除被引用的停用凭据 → 弹窗内出现 409 文案；删除未引用的 → 行消失。
3. 新增凭据/新增配置/添加端点/编辑/轮换/获取模型/复制调价/单价 全部以弹窗打开；Esc/遮罩可关（draft 单价编辑态除外）；保存与取消行为同改造前。
4. 单价弹窗为宽弹窗，表格横向滚动正常，明暗两主题各截一张图核对对比度与间距（AGENTS.md 硬规则 6）。

## 实施陷阱清单（每条都是本仓库实付过学费的）

1. **Teleport stub**：弹窗类组件的测试 mount 必带 `global: { stubs: { Teleport: true } }`，否则内容渲染到 body、`findAll` 为空。
2. **默认契约**：`listCredentials` 不传参必须仍只回生效行；模型面板两处调用点一行不改。
3. **Reactor**：`Mono.error` 写在 flatMap lambda 里（惰性）；放进 `switchIfEmpty` 参数位置会 eager 求值。
4. **number 输入**：价目表 v-model 出来是字符串，保存前 `Number()` 转换的既有代码不能丢。
5. **样式归属**：GlModal 壳样式只在 `src/style.css` 全局层加；面板表单样式留在各面板 scoped（data-v 随 vnode 走，Teleport 不剥离）。
6. **零迁移**：本任务无 DDL。自备容器 IT 不需要补建表语句。
7. **`data-action` 与按钮文案不改名**：兼容既有测试与选择器纪律。

## 明确不做（边界）

- 不新增「恢复凭据」端点/按钮（停用凭据想恢复=重新建一行；涉及唯一索引冲突处理，另立项）。
- 三个面板里其余 `window.confirm`（停用凭据、模型禁用/删除、端点删除、draft 激活/删除）**保持现状**——用户只要求删除凭据改弹窗确认；要统一另立任务。
- 不动掩码/加密/KEK/受信端点校验等后端逻辑；`PlatformProviderCredentialResponse` 与前端类型零改动。
- 不动 `AiProviderKeysPanel` / `AiOrgProviderKeysPanel`（用户/组织 BYOK 面板）。
- 不新增 e2e 用例（本地跑不了完整 e2e 栈；选择器稳定即可）。
- 不做焦点陷阱（focus trap）、body 滚动锁——只保证 `role="dialog"` + `aria-modal` + Esc 可关。
