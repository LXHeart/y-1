# C96-05 发布预览与完整条款

状态：VERIFIED。验证日期：2026-09-08。

## 实现与范围

- 发布表单支持交付期限、发布前审稿和分阶段取消补偿；草稿编辑回填，空比例沿用平台默认，显式 0 保留。
- GET /api/tasks/{id}/preview 返回完整条款、模板要点、服务端预计金额与可提现口径；商家草稿和推荐官任务详情均已接入。
- 可提现窗口复用 SettlementWindowPolicy；取消比例复用 EngagementMilestoneService；前端不重新计算结算金额。
- 预览请求以账号代次、任务和版本隔离，迟到响应不能覆盖新上下文。

实际修改：marketplace 的 TaskPreviewService、TaskController、EngagementMilestoneService、SettlementWindowPolicy、TaskPreviewIT；Edge 的 JavaRouteManifestGateTest；前端 useGrasslandMarketplace、types/grassland/task、MerchantTaskForm、MerchantTasksPanel、TaskDetailModal、TaskTermsPreview、TaskTermsPreviewCard、useWorkbenchTaskDrafts、useWorkbenchTaskPreview 及其相关测试。

映射：REQ-96-05 / AC-96-19~21 / TC96-019~021；TC96-025 覆盖本卡三个页面状态。

## 验证记录

工作目录除 Java 外均为仓库根目录。

| 命令 | 结果 | 证据 |
|---|---|---|
| npm test -- --run --maxWorkers=2 | 退出码 0；168 文件、1537 测试 | test-artifacts/task-96/v96-01-retry.log |
| npm run typecheck | 退出码 0 | test-artifacts/task-96/v96-02-typecheck.log |
| npm run lint | 退出码 0；既有警告 | test-artifacts/task-96/v96-02-lint.log |
| npm run build | 退出码 0 | test-artifacts/task-96/v96-02-build.log |
| ./gradlew :services:marketplace-service:test --max-workers=2（platform-java） | 前次续接执行退出码 0；本次核对 XML：673 测试，0 失败/错误/跳过 | test-artifacts/task-96/v96-03.log；服务 build/test-results/test |
| Edge JavaRouteManifestGateTest | 前次续接执行通过；本次核对 XML：119 测试，0 失败/错误/跳过 | edge-bff/build/test-results/test |
| 两份 DESIGN.md lint | 前次续接执行退出码 0；本次核对两份 JSON 均 errors=0，保留既有 token/对比度警告 | test-artifacts/task-96/v96-04-client.log；v96-04-ops.log |
| git diff --check；npm run security:secrets | 均退出码 0；扫描 2973 文件 | 本次命令输出 |
| node scripts/local/shot-96.mjs | 退出码 0；8 个场景组合无页面异常，12 张截图逐张查看 | test-artifacts/task-96/visual-results.json |

首次前端全量运行全部断言通过，但有 vitest-worker onTaskUpdate RPC 超时，退出码 1；降低 worker 并发后全量通过，未修改断言或忽略错误。

## 视觉与资金证据

截图位于 test-artifacts/task-96/shots/，命名 client-grassland-{merchant-preview,merchant-form,recommender-preview}-{light,dark}-{1440,390}.png；桌面 1440x900，移动端 390x844。逐张检查文字换行、弹窗边界、金额和主题对比，无重叠或横向越界。

浏览器使用本地合成响应，仅验证真实组件的呈现；不是后端全链路证据。PostgreSQL 集成测试 TaskPreviewIT 验证草稿权限、金额/取消比例及结算窗口口径。预览端点只读，不生成余额流水、里程碑或资金事件。

## 偏差与遗留

本卡无新增迁移。未接真实支付、生产数据或生产发布。C96-06 尚未完成，不能据本报告宣称任务书 #96 整体完成。生产默认比例、交付天数和审稿超时策略的待拍板项保持任务书原约束。
