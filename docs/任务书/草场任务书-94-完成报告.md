# 任务书 #94 完成报告：管理端积分调整安全修复与运营队列分页

> 任务书版本 1.0.0 ｜ 基线 fb7e3254 ｜ 报告日期 2026-09-08 ｜ 执行者：ZCode（AUTO_CHAIN C94-01→C94-05）
> 总状态：**五卡全部 VERIFIED**（代码 + 真实 V/TC + 四模块 gradle check + 真实栈串联验收 + 双主题截图）

## 1. 验证命令汇总（§12.3/§12.4）

| 编号 | 命令 | 工作目录 | 退出码 | 结果 |
|---|---|---|---|---|
| V-94-01 | `./gradlew :services:finance-service:test --tests CreditsServiceTest --tests CreditsControllerIT` | platform-java | 0 | 13 + 47 用例全绿（含新增 5+3；V22 随 Testcontainers 新库真实执行） |
| V-94-02 | `./gradlew :services:identity-service:test --tests AdminUserControllerTest --tests FinanceCreditsAdminClientTest` | platform-java | 0 | 11 + 9 用例全绿（新增 adminAdjust 透传/重试同键等） |
| V-94-03 | `npm run test -- src/ops/admin/AdminView.test.ts && npm run typecheck && npm run lint` | 仓库根 | 0 | 31/31；typecheck 绿；lint 仅既有 WARN；800 行门禁过 |
| V-94-04 | `./gradlew :services:marketplace-service:test --tests Ops*IT` + `npm run test`（OpsConsole 等） | 两处 | 0 | 三 IT 全绿（含新增分页/下推/400 用例）；前端全量 165 文件 / 1519 用例全绿 |
| V-94-05 | `./gradlew :services:identity-service:test --tests AdminUserListingEnrichmentIT --tests BackendRoleRepositoryIT` | platform-java | 0 | 6/6（含 spy 批量计数断言）+ 4/4（含批量新用例） |
| V-94-06 | `./gradlew :services:{finance,identity,marketplace,trust}-service:check` | platform-java | 0 | 四模块 check 全绿（trust 首轮 Spotless 违规 spotlessApply 后过） |

**§12.4 集成验收（真实栈串联，2026-09-08）**：bootJar 内容探针验真（`admin-adjust`/`countQueue`/`toDetailBody` 均在 jar 内）→ compose build/up 五容器 healthy → **V22 在真实库执行**（flyway history `22|credits admin adjust|t`；CHECK 值集含 `admin_adjust`；`operator_account_id` 列在位）。治理台以 platform_admin 对 `e2e-merchant` 执行：+5 → `493/525/82`→`498/530/82`（balance/earned 同增）；−5 → 回 `493/530/82`（earned/spent 不动）；**同键重放 −5 → dedup 零变化**；流水恰两行 `admin_adjust ±5` 且 `operator_account_id` 非空。60 条造数处置单翻页（共 69 条，第 1/2 页）与高危筛选 `severity=high` 服务端下推均浏览器实测（16/16 PASS）。回归：finance 模块全量 IT（award 注册赠送/refund/consume 402/余额流水一致）随 check 通过。

## 2. 分卡报告

### C94-01 finance 有符号管理调整命令 + V22 迁移 —— VERIFIED

- **实现**：`V22__credits_admin_adjust.sql`（CHECK 置换扩 `admin_adjust` + 可空 `operator_account_id`，V19/V20 同款两段式）；`CreditsService.adminAdjust`（D94-02 全部 400 校验；正向 `balance/earned+=a`、负向仅 `balance-=a`、不足 402「积分余额不足」；幂等键 `admin_adjust:` 前缀）；`CreditsRepository.adminDebit`（条件 UPDATE 余额下限 0）、`insertTransaction` operator 重载（既有 7 参委托 8 参，调用点零改动）、`ExistingOperation` 扩 `amount`；`CreditsController` `POST /internal/credits/admin-adjust`（requireService(IDENTITY)，响应四字段信封）+ `AdminAdjustRequest`；幂等闭环新增带绑定校验的 `idempotent` 重载（dedup 命中校验 accountId+amount，异参 409「积分操作幂等键冲突」；既有调用方走 null 跳过校验零行为变化）。
- **测试**：CreditsServiceTest 5 新用例（方向记账/边界 400 全枚举含 Integer.MIN_VALUE/dedup 绑定/402 零流水）；CreditsControllerIT 3 新用例（±5 精确断言+operator 落库/同键重放与异参 409/鉴权 401·403+参数 400+余额 3 扣 5→402 零流水）。
- **AC 对照**：100−5=95 且 earned/spent 不变 ✓；100+5=105 且 earned+5 ✓；幂等/绑定/越界全按 §6.1 表 ✓（IT 逐条断言）。

### C94-02 identity 调用切换、幂等键与错误透传 —— VERIFIED

- **实现**：`AdjustCreditsRequest` 增可选 `operationId`（缺省/空白生成 `admin_adjust:<uuid>`，透传前校验前缀+长度，违规 400）；`adjustCredits` 改 `financeCredits.adminAdjust(userId, amount, admin.id(), note, operationId)`（operator=后台账号）；删除 `FinanceCreditsAdminClient.refund(String,int,String)` 及调用点（D94-08）；新客户端方法 `adminAdjust` 用 `exchangeToMono`：finance 4xx 业务信封透传状态码+文案为 `IdentityException(同码, 文案)`，连接/超时/5xx 归 502；重试保持超时/IO 一次，幂等由键保证。
- **测试**：AdminUserControllerTest 负号直传（`adminAdjust(acct,-3,…)` 符号保留+operator=admin.id+生成键前缀）、客户端键透传、坏键 400；FinanceCreditsAdminClientTest 4 新用例（签名请求体/402·409 透传同文案/5xx→502/超时重试同键两次请求断言）；`adjustCreditsNegativeAmountCallsRefundWithAbsoluteValue`（固化错误行为的绿灯）删除。
- **AC 对照**：identity 层 abs/refund 清零 ✓；402/409 逐级可达浏览器（C94-03 前端文案实测）✓；重试仅超时/IO 且键不变 ✓。

### C94-03 治理台调整弹窗校验与一键重试 —— VERIFIED

- **实现**：`AdminUsersPanel` `openAdjust` 生成 `admin_adjust:${crypto.randomUUID()}`；`adjustAmount` 改受控字符串 + `parseAdjustAmount`（非零整数、1≤abs≤1_000_000，错误文案三条）+ blur 校验（仅清/写数量类错误）；提交带 operationId；402→「积分余额不足，无法扣减」/409→「该次提交已在处理，请刷新后查看余额」/502→「积分服务暂不可用，请稍后重试」；失败不关弹窗键保留、成功/关闭即丢弃。`AdjustCreditsDialog` 数量受控字符串 + `blur-amount` 事件 + 错误出现聚焦数量输入（`role="alert"`）。
- **测试**：AdminView.test.ts 更新既有用例（operationId 前缀断言）+ 新增：非法值四枚举逐值全新 mount 不发请求（含 1.5/±1_000_001）、三连提交（502→402→成功）同键断言三次请求体一致、402 文案。
- **偏差/发现**：happy-dom/DTU 既有怪癖——弹窗内首个被 setValue 的 input 触发重渲并替换输入节点，后续缓存的 wrapper 指向死节点（HEAD 原版同样复现，非本次回归）；测试据此按「number 先、note 后、逐值全新 mount」编排。
- **截图**：`test-artifacts/task-94/ops-admin-adjust-{default,validation,402}-{light,dark}.png`（6 张）。

### C94-04 运营三列表分页信封与筛选下推 —— VERIFIED

- **后端**：`OpsCaseController` 三端点改分页信封 `data:{items,total,limit,offset}`；cases 增 `source`（source_kind 等值）/`severity`（high/normal，非法 400）下推；三端点增 `offset`；`limit` 1–200/`offset`≥0 **越界 400（不再静默钳制，行为变更披露）**。`OpsCaseRepository.listQueue` 扩参+`countQueue`（同参数 COUNT）；DLT `list(+offset)/countList`；pending `list(+offset)/count`。
- **前端**：`useGrasslandGovernance` `listOpsCases(参数对象)`/`listOpsDlt`/`listOpsPendingVerifications` 走 `toPagedArray` 信封；`OpsConsole` 删本地 `filteredCases`，来源/高危改请求参数；三列表分页条（默认 50，档 50/100/200）+ 总量展示；空态改看 `total`（total=0 才显示）。
- **测试**：三 IT 各扩（60+3 条造数：默认页 50/total=63、severity=high 第 2 页见第 51+ 条、source 组合、limit/offset/severity 非法 400；DLT 55 条翻页+400；pending 动态 total 自洽断言+400）；OpsConsole.test.ts 信封化+新用例（高危/翻页/页大小/空态/来源下推）。
- **偏差（主动申报）**：OpsConsole 856 行破 800 硬顶——按 AGENTS.md 分层规约第 3 条拆出子组件（新建 `src/ops/ops-console/components/OpsPagerBar.vue`；评论复核/投诉工单两面板**原样搬迁**至 `OpsCommentReviewsPanel.vue`/`OpsComplaintsPanel.vue`，行为零变更；共享样式原样迁 `ops-console-shared.css`，父子组件 scoped 引入）。白名单字面未列新建文件，动因是仓库级硬性门禁，搬迁部分有全量前端测试（1519/1519）佐证零行为变化。
- **截图**：`test-artifacts/task-94/ops-queue-{paged,highrisk}-{light,dark}.png`（4 张）。

### C94-05 角色查询批量化 —— VERIFIED

- **实现**：`BackendRoleRepository.findByAccountIds`（`WHERE account_id = ANY(uuid[])`，入参去重+≤500 分批，无角色账号不在结果）；`collectBackendRoles` 改单次批量（`collectMultimap`+sorted，返回结构不变）。
- **测试**：BackendRoleRepositoryIT 新用例（三账号含双角色/空入参 empty）；AdminUserListingEnrichmentIT 以 `@MockitoSpyBean` 断言列表页 `findByAccountIds` 恰 1 次、`findByAccountId` 恰 1 次（仅鉴权链路，富化零逐账号查询）。
- **AC 对照**：列表行为零变化（该 IT 既有 5 用例仍绿）+ 计数断言通过 ✓。

## 3. 白名单核对

全部写入落在 §9.1 清单内，例外（新建文件，已在 C94-04 偏差申报）：`src/ops/ops-console/components/{OpsPagerBar,OpsCommentReviewsPanel,OpsComplaintsPanel}.vue`、`src/ops/ops-console/ops-console-shared.css`。未触碰：既有迁移、CI/锁文件、`.env`、`.pi/`、`docs/任务书/evidence/`、`adminTabs.ts` 角色相关（属 #95）、`src/ops/router.ts`（属 #95）、`OpsConsole.vue` 的 `open-dispute`（属 #95——本卡窗口内未动，#95 落地时处理）。黑名单端点（award/refund 语义、非 adjust 端点）零改动。

## 4. 未解决问题

- `ReputationControllerIT`（marketplace，本任务未触碰的模块）曾在一次全模块并发 run 中 2 例 202/200 断言失败，隔离重跑与最终 `check` 均绿——判定为共享容器时序 flaky，已如实记录，留观。
- 截图产物 `test-artifacts/` 属 .gitignore 范围（本地自查证据）；复现脚本 `seed-94-95.sh`/`shot-94-95.mjs` 随本报告入库。
- §12.4 用户复验项：以 platform_admin 在治理台对合成账号执行 ±5 并核对余额/流水（我方已用 API+DB 完成同等验证）。
