# 任务书 #95 完成报告：治理台专员角色矩阵与客服裁定入口

> 任务书版本 1.0.0 ｜ 基线 c5591a5b ｜ 报告日期 2026-09-08 ｜ 执行者：ZCode（AUTO_CHAIN C95-01→C95-03）
> 总状态：**三卡全部 VERIFIED**（代码 + 真实 V/TC + trust 模块 check + 真实栈双主题截图实测）

## 1. 验证命令汇总（§12.3/§12.4）

| 编号 | 命令 | 工作目录 | 退出码 | 结果 |
|---|---|---|---|---|
| V-95-01 | `./gradlew :services:trust-service:test --tests DisputeAdminControllerIT` | platform-java | 0 | 5/5（3 既有 + 2 新：脱敏详情 + 角色闸门/404） |
| V-95-02 | `npm run test -- src/ops/OpsApp.test.ts src/ops/admin/AdminView.test.ts && npm run typecheck && npm run lint` | 仓库根 | 0 | 40/40；typecheck/lint 绿；800 行门禁过 |
| V-95-03 | `npm run test -- src/ops/ops-console/OpsConsole.test.ts && npm run typecheck && npm run lint` | 仓库根 | 0 | 23/23；全量前端 165 文件 / 1519 用例绿 |
| V-95-04 | `./gradlew :services:trust-service:check` | platform-java | 0 | 模块 check 绿（Spotless 首轮违规已 spotlessApply） |

**§12.4 集成验收（真实栈，2026-09-08）**：镜像重建部署后以合成账号浏览器实测 16/16 PASS——customer_service 进争议页签：列表渲染（premium 徽标/脱敏理由）、详情抽屉（`participant-` 假名、原始手机号 13812345678 不出现在响应与界面）；finance 账号（`finance-shot@test.local`）登录管理后台：侧栏仅「交易与财务」组、仅「财务对账」页签（明暗各一截图）。回归：content_reviewer/customer_service/risk 既有可见集与「初始化商家账号」按钮门（`TAB_ROLES.users` 未动）由 AdminView.test.ts 既有+更新用例覆盖。

## 2. 分卡报告

### C95-01 trust 争议按 id 脱敏详情端点 —— VERIFIED

- **实现**：`DisputeAdminController` 新增 `GET /api/admin/trust/disputes/{id}`：`requireRole(CUSTOMER_SERVICE, PLATFORM_ADMIN)` → `findById` 空→`TrustException(404,"争议不存在")` → 按 §6.2 组装详情体（复用已注入 `redactor`；新注入 `DisputeEvidenceRepository`，`listByDispute` 既有签名一致）。脱敏红线：`openedByAlias/respondentAlias/finalDecidedByAlias` 全假名（respondent null→`participant-unknown` 与 pseudonym 空参语义一致；finalDecidedBy null→null）；`reason/decision/finalDecision` 过 `maskText`（null 透传 null，绕开 maskText 把 null 变空串的语义差）；证据 `redactor.redact(...)` 内联 + `summary` 条数摘要；不回 `openedByAccountId/respondentAccountId/finalDecidedBy/evidenceRef` 原文。
- **测试**：IT 新增两用例——①详情：造数含双账号原文/手机号/邮箱，断言响应体不含账号原文与 PII 明文、假名前缀、脱敏形（`138****5678`/`a***@example.com`）、`respondentAlias/evidenceSummary/submittedByAlias` 在位、channel=cs_direct；②闸门：cs/admin 200、匿名 401、risk 与服务断言 403、不存在 404 文案。
- **AC 对照**：TC-95-01~05 全命中（IT 逐条）。

### C95-02 前端角色矩阵镜像 —— VERIFIED

- **实现**：`OPS_ROUTE_ROLES.admin` 追加 `finance`、`merchant_reviewer`（其余项与顺序不动）；`TAB_REGISTRY` 三处行内补 roles——`finance` 页签 `['platform_admin','finance']`、`recommenders` `['platform_admin','merchant_reviewer','content_reviewer']`、`risk` `['platform_admin','risk']`；文件头注释同步角色口径（含 content_reviewer 补见认证页签）。服务端 `requireRole` 零改动；`users` 页签角色集与 `TAB_ROLES.users` 未动（按钮门零连坐）。
- **测试**：OpsApp.test.ts 补 finance/merchant_reviewer 两例（仅见管理后台）；AdminView.test.ts 更新 content_reviewer 可见集（D95-01 起补「推荐官认证」，回落第一可见页签改为认证）+ 新增三角色用例（merchant_reviewer 仅认证；finance 仅交易与财务组下财务对账；risk 既有用户管理+补见风险调查，点开风控组断言页签——侧栏只渲染展开组页签，断言按此编排）。
- **AC 对照**：TC-95-06~09 命中；`roles` 端点断言（OpsApp `.ops-user-roles`）既有用例仍绿。

### C95-03 OpsConsole 争议页签与裁定入口接线 —— VERIFIED

- **实现**：类型 `src/types/grassland/dispute.ts` 增 `AdminDisputeRow/AdminDisputeDetail/AdminDisputeEvidence/AdminDisputePage`（字段对齐 §6.2，命名与用户侧 `DisputeCase` 一致优先复用子字段类型）；`useGrasslandGovernance` 增 `listAdminDisputes({limit?,cursor?})`（游标语义：首屏 50，hasMore 带 nextCursor 追加，不做页码跳转）与 `getAdminDispute(id)`；`OpsConsole` Tab 增 `'disputes'`、页签「争议队列」（第六签，排最后）、删除 `defineEmits open-dispute`、按钮改 `goToDispute(sourceRef, caseId)`（关处置单抽屉→切页签→开争议抽屉→记住来源）与 `backToCase`（切回并重拉处置单）；详情 404 → `say(...,true)` 呈现。争议页签/列表/抽屉/审计懒加载在新子组件 `OpsDisputesPanel.vue`（§8：中文标签对齐用户端 DisputeDetailView 词表；抽屉 Escape 挂 document 且与处置单抽屉互斥；审计时间线懒加载复用既有 `GET /api/trust/disputes/{id}/audit`——D95-04）。
- **测试**：`OpsConsole.test.ts:446-453` 旧断言按卡文改造——点「前往客服裁定」断言发出 `GET /api/admin/trust/disputes/dispute-42`、争议抽屉可见、处置单抽屉关闭；「返回处置单」断言重新拉取 `/api/ops/cases/case-mr`；另补 404 呈现、页签懒加载、加载更多携带 cursor、审计懒加载用例。
- **偏差（主动申报）**：
  1. 新建 `src/ops/ops-console/components/OpsDisputesPanel.vue`（白名单字面未列）——OpsConsole 已 642 行（#94 拆分后），再加页签+抽屉必破 AGENTS.md 800 行硬顶，分层规约第 3 条（新页签拆子组件）的既定路径；与 #94 报告同一偏差族。
  2. §8 争议列表「通道（cs_direct 标 SLA 到期）」列未做——§6.1 明确列表端点「既有，零改动」而行体无 `channel/csDueAt`，契约优先；通道与 SLA 在详情抽屉完整呈现。（2026-09-08 用户拍板：维持现状，不作为缺陷补、不扩列表契约；任务书 §8 已随 v1.0.1 修订对齐交付形态。）
- **截图**：`test-artifacts/task-95/ops-disputes-{list,detail}-{light,dark}.png`、`role-finance-sidebar-{light,dark}.png`（4+2 张）。

## 3. 白名单核对

写入落点：`DisputeAdminController(.java/+IT)`、`src/ops/router.ts`、`src/ops/admin/adminTabs.ts`、`OpsApp.test.ts`、`AdminView.test.ts`、`OpsConsole.vue`、`OpsConsole.test.ts`、`useGrasslandGovernance.ts`、`src/types/grassland/dispute.ts` 均在 §9.1 清单内；例外仅 `OpsDisputesPanel.vue`（上文偏差 1）。未触碰：任何 `requireRole` 调用点、`DisputeCaseRepository`、`CaseEvidenceRedactor`、`DisputeAuditController`、`DisputeEvidenceController`、用户端 `DisputeDetailView`、`OpsApp.vue`、`useAdminUrlState.ts`、五个既有页签的取数与分页（#94 已交付部分未重做）、`.pi/`、`docs/任务书/evidence/`。

## 4. 未解决问题

- merchant_reviewer/content_reviewer 双角色同签的真实栈截图未逐一拍摄（AdminView.test.ts 已断言三角色可见集；finance 账号真实栈已拍）。§12.4 用户复验时如需补拍，账号 `reviewer-shot@test.local`（口令同 e2e `test-password-2026`）已随 `scripts/acceptance/seed-94-95.sh` 造好。
- 截图产物 `test-artifacts/` 属 .gitignore（本地自查证据）；`OpsDisputesPanel` 列表「通道」列取舍已于 2026-09-08 用户拍板：维持现状不扩列表契约（见 C95-03 偏差 2），无待确认项。
