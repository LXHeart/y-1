# 任务书 #92 卡 C-04 完成报告

## 实现结果

- 卡号：C-04（文章、图片、朋友圈恢复与自动保存）
- 完成状态：VERIFIED
- 行为变化：
  - 新增 `useWorkspaceAutosave` 域 composable（creation/）：字段变更 800ms 防抖保存（D-03）；空表单不创建（§5.3）；卸载前 flush 一次（§6.5）；保存状态 `保存中…/已保存/保存失败·重试`（§8.3，`WorkspaceSaveBadge`，aria-live、重试可聚焦）；409 冲突按 §6.4 GET 最新版本后以本地未提交字段为准自动合并重放一次，仍失败进入可重试冲突态；响应乱序 epoch 守卫丢弃。
  - 恢复：`pendingContinue`（C-03 交接）优先，其次 `?draft=` 深链（刷新恢复，AC-301）；旧 draft 缺 workspace 字段 → 步骤归一白名单首步、inputs 空对象安全回填（AC-303）。
  - 三类视图接线（各自字段映射与步骤白名单）：
    - 文章 `ArticleCreationView`：topic/platform/selectedTitle/outline/content/contentMode/question；步骤=双模式全部阶段；仅 AI 应用启用（`data-app="ai"`，草场共享挂载行为零变化）。
    - 图片 `ImageGenerationStudio`：prompt + 尺寸；素材/结果图为本地或临时资源不落库（D-04）。
    - 朋友圈 `MomentsCreationView`：topic/style/feelings；本地图片文件不落库（D-04）。
  - 自动保存不触发生成、扣费或素材删除（§5.4）——composable 只读写草稿端点。
- 未完成项：runId/runState 写入（生成三回调点）按 §6.7/§10.2 归 C-05/C-06 的运行关联线，本卡未做。

## 文件与范围

| 文件 | 变更 | 白名单对应 |
|---|---|---|
| `src/views/ai-center/creation/useWorkspaceAutosave.ts` | 新建：自动保存/恢复/冲突合并域 composable | §9.1 `creation/**` |
| `src/views/ai-center/creation/WorkspaceSaveBadge.vue` | 新建：保存状态徽标（§8.3） | §9.1 `creation/**` |
| `src/views/ai-center/creation/useWorkspaceAutosave.test.ts` | 新建：7 用例（TC-C04 核心 + AC-303 + 卸载 flush + engage 门） | §9.1 `creation/**` |
| `src/lib/creation-workspace.ts` | 增 `saveProject`（POST 创建 / PUT 乐观锁） | §9.1 lib |
| `src/views/ai-center/components/ImageGenerationStudio.vue`（+测试） | 图片接线 + 保存徽标 + 新用例（防抖创建/恢复回填） | V-006 指定测试文件对应视图 |
| `src/views/article/ArticleCreationView.vue` | 文章接线（AI 应用限定门） | **白名单外**（见偏差 1） |
| `src/views/moments/MomentsCreationView.vue` | 朋友圈接线（AI 应用限定门） | **白名单外**（见偏差 1） |
| `scripts/acceptance/shot-92-c04.mjs`、`docs/任务书/evidence/92/**` | 截图脚本与证据 | §8.8 |

既有工作区改动未触碰。

## 验证证据

| 项 | 工作目录 | 命令 | 退出码 | 结果 |
|---|---|---|---|---|
| V-006 | 仓库根 | `npm run test -- src/views/ai-center/AiCreationCenter.test.ts src/views/ai-center/components/ImageGenerationStudio.test.ts` | 0 | 54 用例全过 |
| composable 定向 | 仓库根 | `npm run test -- src/views/ai-center/creation/useWorkspaceAutosave.test.ts` | 0 | 7 用例全过 |
| 视图回归 | 仓库根 | `npm run test -- src/views/article/ArticleCreationView.test.ts src/views/article/ArticleCreationView.answerMode.test.ts src/views/moments/MomentsCreationView.test.ts src/views/ai-center/components/ImageGenerationStudio.test.ts` | 0 | 81 用例全过（共享视图草场侧行为零变化佐证） |
| typecheck / 体积门禁 | 仓库根 | `npm run typecheck` / `node scripts/check-file-size.mjs` | 0 / 0 | ArticleCreationView 640、MomentsCreationView 318、ImageGenerationStudio 739，全部 ≤800 |
| 双主题截图 | 仓库根 | `node scripts/acceptance/shot-92-c04.mjs`（dev + 路由拦截 fixture，SPA 内真实导航） | 0 | 6/6 断言过；真实自动保存链路创建 6 个 draft |

- TC-C04-001（+001b）：防抖创建（799ms 不发、801ms 发）、PUT expectedVersion、图片视图端到端。
- TC-C04-002：pendingContinue 交接优先 + `?draft` 深链次之，字段/步骤回填。
- TC-C04-003：409 → GET v5 → 本地字段合并重放 → saved、version 前进。
- TC-C04-004：保存失败 error 态不丢编辑、retry 转存。
- AC-301/302/303 分别由防抖+恢复链路、重试语义、旧字段缺省用例覆盖。
- 截图自查（analyze_image 双主题 ×2 页面抽查）：徽标与步骤条/导航协调，无截断/重叠/溢出；对比度良好。
- 证据绝对路径：`/Users/LXH/claude/y-1/docs/任务书/evidence/92/`（v006.log、c04-{article,image,moments}-{light,dark}.png、c04-screenshot.log）。

## 偏差说明

1. **`ArticleCreationView.vue`、`MomentsCreationView.vue`、`ImageGenerationStudio.vue` 不在 §9.1 字面白名单**：本书 §2.3/§2.4 假设三类创作视图位于 `src/views/ai-center/creation/`（事实漂移——该目录此前不存在，真实视图在 `src/views/article|moments/` 与 `ai-center/components/`）。REQ-004 的可观察行为（三类工作流自动保存与恢复）只能落在真实视图上；composable/徽标/测试主体均落在白名单内 `creation/**`，视图接线为最小插入（文章 +52 行、朋友圈 +33 行、图片 +25 行）。共享视图以 `data-app="ai"` 门控，草场侧 81 用例零改动全绿佐证零行为变化。ImageGenerationStudio 的测试文件为 V-006 指定目标，属验证锚点而非越权。
2. 其余无偏差。

## 未解决问题

无（runId 关联为 C-05/C-06 范围，非遗留）。
