# 开发规格：第三阶段——可维护性拆分与设计债务清偿

> 模板版本：2.0.0 ｜ 任务编号：81 ｜ 任务书版本：0.1.1
> 文档状态：READY_FOR_IMPLEMENTATION ｜ 目标执行者：获指派的编码模型
> 核验日期：2026-09-06（Asia/Shanghai；本地时钟已核对）
> 代码基线：25c7857b2dd929ffa77289804ba94d5a164b2710 ｜ 分支：main

> 规划与最终集成：主程；实现/卡级验证：获指派编码模型；产品/设计/发布审批：仓库负责人指定的人类负责人
> 仓库：`/Users/LXH/claude/y-1` ｜ 任务卡总数：6
> 顺序：#79/#80实际交付重基线 → C81-01 → C81-02 → C81-03 → C81-04 → C81-05 → C81-06 → 主程集成。状态基础先于拆分；endpoint在消费边界稳定后收敛；样式统一最后单一写入；状态文档最后如实更新。

**阅读协议**：本版已授权按顺序开工。收到明确卡号后按AGENTS→§0/§1/§9/§13/§14→当前卡/测试/锚点阅读，UI再读对应DESIGN。只做指定卡，不自作产品决策、不修改模板。

## 0. 执行协议
### 0.1 词义
FACT=本次源码事实；DECISION=本需求内技术选择和保留原行为；PROPOSAL_NOT_APPROVED=明确唯一推荐但尚未批准；BLOCKED停止；NOT_RUN没有执行；N/A保留理由。
### 0.2 强制规则
只写当前卡与§9.1交集。核HEAD/未提交改动/前卡hash；签名、权限、状态、路由、数据漂移先修订，不按旧片段覆盖新代码。不新增依赖、规则、未定义端点/字段；不全仓格式化、不降低覆盖率、不删失败测试、不清用户工作、不提交/建分支/上线。普通代码错误在卡内修，产品/安全/数据损失歧义立即阻塞。
### 0.3 完成定义
卡NOT_STARTED→IN_PROGRESS→IMPLEMENTED→VERIFIED，或BLOCKED；VERIFIED必须步骤、GWT、TC/V和适用截图全通过，无必需NOT_RUN/FAIL。六卡完成仍需主程集成；生产发布另批。文档状态≠卡状态≠集成状态≠生产状态。

## 1. 目标与范围
### 1.1 一句话目标
在前两阶段正确性和协议上统一账号生命周期，按责任拆工作台/管理台，集中本次触及的账户API端点与类型，清偿经冻结的UI token债务，并让权威状态文档反映真实验证程度。
### 1.2 背景与价值
大页面已存在领域composable和TAB_REGISTRY，不能重复造一套。维护风险在于多个生命周期入口、URL/网络编排和UI混放、样式/Canvas产物混为一谈；目标不是通过挪代码降低行数而破坏请求顺序/权限/KeepAlive。
### 1.3 范围内
| REQ | 可验收行为 | 规则/卡 | AC |
| --- | --- | --- | --- |
| REQ81-01 | 复用#79 epoch的统一清理注册机制；通知、模型来源、预算/治理局部状态、第三方会话客户端缓存换号/卸载不残留 | R81-01/C81-01 | AC81-01A/B |
| REQ81-02 | Workbench的URL适配/导航副作用与展示装配分离，既有query/DOM挂载/任务行为保持 | R81-02/C81-02 | AC81-02A/B |
| REQ81-03 | Admin registry、用户控制器和内联面板分离，23页签/5组/角色/副作用/深链保持 | R81-03/C81-03 | AC81-03A/B |
| REQ81-04 | 账户相关已触及端点与DTO集中定义，调用者不用重复字符串或猜信封，公开wire不变 | R81-04/C81-04 | AC81-04A/B |
| REQ81-05 | 全量扫描分类UI硬编码与导出内容；仅按批准逐文件token映射清偿，双主题/导出不误改 | R81-05/C81-05 | AC81-05A/B |
| REQ81-06 | status及进度准确区分已实现/已验收/未启动/生产阻塞，编号不混用 | R81-06/C81-06 | AC81-06A/B |
### 1.4 范围外
不改变#79身份默认/重置和#80信封/audience；不新增ops SSO、不改个人/组织AI治理权限与own计费语义、不重写交易/争议/内容生成；不以拆分引入全局神型store、万能API注册框架或新UI库。API集中只限§6明确账户端点，不宣称全仓所有领域API完成迁移。主题不自动改变用户导出图片像素/模板配色，相关决定见B81-02。
### 1.5 不许顺手修
不删除现有useWorkbench六域/已抽面板、GlModal、OpsPagination；不为“整齐”物理迁走所有治理组件；不改真实.env/lockfile/既有迁移；不清理verify-task78.mjs、smoke-77-shots或历史截图；不把状态日期刷新当完成证据。
### 1.6 用户、入口与限制
用户/AI/治理三入口及共享组件全部保留。状态清理是明确正确性修复；02/03/04纯拆分必须行为零变化；05获批视觉修正以token映射为限。前阶段未实现，后置实际基线、完整拆分props/事件表和全量token映射尚不闭合，故整本BLOCKED。

## 2. 仓库上下文
### 2.1 目标端
用户端Workbench、治理AdminView、AI治理及共享账户组件；Java仅接口/安全只读核验，没有业务服务修改。Vite实际三入口index/ops/ai，不能照“双前端”旧描述漏AI。
### 2.2 设计规范路由
已读根AGENTS、根DESIGN、src/ops/DESIGN；定向相关目录未发现更深AGENTS。Workbench/AI/共享组件按根规范；Admin/仅ops面板按ops规范；AI不建立第三体系。两款self-host字体、token配对、共享组件、每页双主题截图硬规则不可省。
### 2.3 入口/调用链
三HTML→各main/router→DefaultLayout/AiAppLayout/OpsApp；Workbench→六个useWorkbench域→useGrasslandIdentity/Marketplace/Governance/Audit→Edge→原五领域服务。Admin→TAB_REGISTRY选择/角色→内联6面板或组件→同原域服务。#79新增account-session/#80cross-app DTO仅是前置预期交付，当前HEAD不存在，实施前需实际核验。
### 2.4 复用模块
Workbench已有useWorkbenchSession、Engagements、Disputes、TaskHall、MyTasks、TaskDrafts，以及RecommenderTaskHall、TaskDetailModal、PersonalSettingsModal、MerchantTaskForm；Admin已有AdjustCreditsDialog、AdminUserDetailDrawer、Suspend/ResetPasswordDialog、MerchantAccountInitDialog、OpsPagination。复用它们，不能复制一个新基础面板体系。types/grassland已分域，不重新拼成巨大单文件。
### 2.5 当前行为（FACT）
1. Workbench中SubTabId为tasks/org/finance/hall/engagements/earnings；商家3签和推荐官3签。?wtab=ai已回tasks；settings旧'1'兼容；财务二级account/bill/commerce/analytics；v-show用于保留子卡摘要/DOM锚点，不能擅改v-if。
2. Workbench六composable已拆，SFC仍承担URL/query、通知锚点、creation handoff、页面装配和账号watch；不能说尚无拆分。
3. Admin已有23项TAB_REGISTRY、5组、roles/onActivate/badge/component/componentProps；users/kyb/recommenders/tasks/finance/ai-models仍内联。五列表limit默认10，OpsPagination支持10/20/50/100，切limit归零offset。
4. Admin onMounted按?section找可见页签并只首拉可见users/kyb；currentUser watch登录补拉；角色撤销回首可见签。registry闭包引用晚声明的refs，抽到模块不能在加载时提前求值。
5. notifications.store已reset/stopPolling，但异步run和回写缺账号epoch；poll为60000ms、page20。useModelSource已有requestEpoch/pendingLoad；useAiPersonalBudget只是无状态API，预算状态在PersonalAiBudgetCard。
6. AiGovernanceSection#refreshOrgScopes加载owner/admin列表，局部selectedOrgId；OrgBudget随organizationId watch拉取，PersonalBudget有version/六限；这些不是全局组织身份选择。useDouyinSession是module refs并有requestId防序，但不等于账号生命周期已集中。
7. 静态查找hex/rgb/hsl/font-family命中48个.vue/.css文件；这是候选集合，不是48个确定违规，也没覆盖所有.ts字符串/间距。VideoStudio的TITLE_FONT='bold 60px system-ui, sans-serif'与白字用于导出Canvas；ImageStudio bgConfig的色值是用户图片背景数据，不可直接改为'var(--token)'输入Canvas。
8. docs/status.yaml实际是JSON语法、schemaVersion1、backlog scope22-51且连续，只允许completed/in_progress/open；check-doc-status禁止open，允许in_progress；updatedAt=2026-09-01。任务书79–81不是已有backlog编号，不能直接追加造成52–78缺号。
### 2.6 当前问题
生命周期/拆分/格式债务已定位；全量语义违规尚未人工分类，不能以正则命中代替确定修复。设计规范与style.css部分映射不一，且Canvas字号/配色超过既有规范；未经批准新增token或改变导出效果必须阻塞。
### 2.7 基线核验
HEAD/main=`25c7857b2dd929ffa77289804ba94d5a164b2710`；本地2026-09-06T00:46:07+0800。模板M；verify-task78.mjs/smoke-77-shots未跟踪。模板SHA256=`ea248457e2e23273547a593e2f52cf1c11760d5f8897ac4770049f76b927f75c`；保留。实测Node22.22.3/npm10.9.8；依赖配置Vue^3.5.22/TS^5.9.3/Vite7.3.6/Vitest3.2.7/Playwright1.59.1；Java25/SpringBoot4.1.0/Gradle9.6.1。已读package/锁依赖位置、CI、vitest/playwright、质量/状态脚本、真实入口/类型/Controller。所有业务验证和截图本次NOT_RUN；旧任务书和附件测试数不作当前通过证据。
### 2.8 事实/决策/合成输入
当前源码事实与前阶段预期类型分开标注；后者不是已实现事实。测试输入固定且无生产资料；未知字段/props/日志/数据源由§13处理，不留“自行补全”授权。
### 2.9 影响/兼容
三入口、共享生命周期、DOM/URL、API调用点、全局样式和权威文档受影响；DB/Java wire/收费零变化。02/03/04必须旧新语义差分相同；05需批准视觉映射，不能把色值替换造成的导出变化当纯重构。

## 3. 技术决策
| 编号/类别 | 唯一结论 | 原因/禁止 |
| --- | --- | --- |
| D81-01 DECISION | 继续#79唯一account-session owner/epoch，只增加清理注册/注销，不建第二套epoch | 避免reset触发顺序分叉；局部请求revision仍保留 |
| D81-02 DECISION | 拆分按URL适配/导航副作用/域控制器/展示组件；不按行数随意切块 | 复用现有六域，保留v-show/KeepAlive和回调方向 |
| D81-03 DECISION | Admin registry抽工厂并注入onActivate/badge闭包；权限规则、标签、顺序照当前代码 | 不在模块导入时执行网络或读取未初始化ref |
| D81-04 DECISION | 仅将本轮账户端点/DTO集中成窄模块；动态ID/业务类型仍归原分域 | 不创造全仓万能HTTP客户端/第二套Java契约 |
| D81-05 PROPOSAL_NOT_APPROVED | UI色值映射token；Canvas导出样式与用户自选颜色单独登记，默认保持导出像素，不直接继承主题 | B81-02需批准“全面”边界及Canvas token规范；不能擅造第三种设计体系 |
| D81-06 DECISION | docs/status保留schema/backlog22-51，不把任务书编号塞进backlog；新增工程任务状态采用独立结构和校验 | 不补虚假的52–78完成记录；代码已写/验证缺失不能completed |

### 决策记录
技术拆分可在需求范围内规定；但新组件完整props/emits/组合接口必须由主程在真实前阶段基线冻结，B81-03未完成不得派卡。token新规范/导出视觉变化由设计/产品批准，不能由执行者拍脑袋映射到“差不多”的颜色。

## 4. 目标行为
### 4.1 用户流程
换账号→统一生命周期清理私有缓存/轮询/表单→当前账号重新加载；正常使用Workbench/Admin的标签、筛选、弹窗、角色、URL、请求顺序与前阶段已验收行为相同；查看权威状态文档能区分已实现待验收与真正完成。
### 4.2 行为变化表
| 场景 | 目标 |
| --- | --- |
| 正常/空/加载/错误 | 02/03/04逐值保持，01仅清旧私有态，05获批token显示一致 |
| 未登录/无权限 | 原服务401/403仍权威；不可见tab不发其首拉请求；不能只隐藏界面 |
| 重复/并发 | 统一清理每注册项每epoch一次；局部revision只接受新查询；请求不因拆分倍增 |
| 切页/刷新/回退 | 原query/其他参数/DOM锚点保留；v-show、KeepAlive策略不改；标签不新增多次导航 |
| 主题/导出 | 明暗界面遵循规范；导出内容是否变更须B81-02批准，不能跟主题随机变色 |
### 4.3 状态定义
生命周期继承#79的owner/epoch和原store状态，新增cleanup注册条目key/回调/dispose只在内存。组件loading/error/notice/分页状态按原字段；状态文档工程记录用not_started/implemented/verified/blocked，和现有backlog status不是同一枚举。
### 4.4 状态迁移
账号变化先使旧ticket失效再同步调用所有已注册reset；unregister后不再触发该局部组件回调；同key不能注册第二个未注销实例覆盖旧回调。共享单例注册归Pinia生命周期，局部组件注册归effect scope；一个弹窗关闭不能停全局通知轮询给其他组件用。具体先后及冲突签名需B81-01实交付后冻结。

## 5. 业务规则
### 5.1 输入规则
owner/epoch只来自#79；cleanup key为明确域常量，不含账号/邮箱。URL白名单保持owned keys：side/wtab/task/level/rate/q/platform/contentForm/minBounty/dist/settings；数组取原firstQueryParam语义，未知参数原样保留。Admin section按现有ADMIN_TAB_KEYS+角色过滤；未知/不可见回第一可见签。分页10/20/50/100仍不改。
六预算上限number|null、非负安全整数、单次≤每日≤每月；空白表示null、不限，0是真零；expectedVersion按原乐观锁，409不覆盖表单成成功。现金分仍money.ts，通知page20/poll60000ms不改。
### 5.2 校验规则
不要把无法确认的旧状态恢复成默认已加载；当前请求失败保留原文案，旧请求不显示错误；注销/卸载后停止局部资源。API descriptor只固定方法/路径/响应模式，不自动拼用户身份或任何provider地址。UI扫描先分类再替换，规范无映射先阻塞。
### 5.3 规则编号
R81-01至06对应§1.3；R81-07“纯拆分不增加请求次数/不改DOM存活”由TC81-02/03；R81-08“Canvas像素/用户颜色不是界面token字符串”由TC81-05；R81-09“状态不跳级”由TC81-06。
### 5.4 权限/不变量
前端角色显隐不替代Java；Admin仍platform_admin超集及现有各tab roles；owner/admin才能显示治理主体，下拉不成为全局身份。个人预算、组织预算、BYOK与运行记录仍原API服务端授权。积分/现金/流水/AI成本及内容安全闸无改动；所有拆分TC要断言没有新增写请求/扣费。

## 6. 接口契约
### 类型与调用签名
候选窄模块`src/api/account-endpoints.ts`只输出固定descriptor，类型声明：
```ts
export interface JsonEndpoint {
  readonly method: 'GET' | 'POST' | 'PUT'
  readonly path: string
  readonly response: 'envelope'
}
export type AccountEndpointName =
  | 'authMe' | 'authLogin' | 'authRegister' | 'authLogout'
  | 'analysisRead' | 'analysisWrite' | 'creditsBalance' | 'creditsHistory'
  | 'crossAppIssue' | 'crossAppExchange' | 'personalBudgetRead' | 'personalBudgetWrite'
export type AccountEndpoints = Readonly<Record<AccountEndpointName, JsonEndpoint>>
```
DTO仍复用types/auth、types/settings、types/cross-app（#80交付）以及types/grassland/organization的AiOrgBudget/UpdateAiOrgBudgetInput；Credits三类型从store移到新types/credits.ts，用旧useCredits再导出兼容。不复制新AuthUser，不把任意raw/SSE也标envelope。
### 6.1 请求信息
12个descriptor严格映射：GET /api/auth/me；POST /api/auth/login/register/logout；GET/PUT /api/settings/analysis；GET /api/credits/balance/history；POST /api/auth/cross-app-tokens及/exchange；GET/PUT /api/ai/me/budget。方法/路径分别对应原Controller/Edge flags，身份仍Cookie→Edge断言；无新公共路由。新模块不是运行时路由发现或代理服务。
### 6.2 请求参数
完整例：login=`{"email":"a@qa.invalid","password":"QaOnly!234567890"}`；analysis=`{"integrations":{"feishu":{"appId":"qa-app","folderToken":"qa-folder"}}}`；budget=`{"expectedVersion":2,"maxTokensPerRun":100,"maxTokensDaily":1000,"maxTokensMonthly":10000,"maxCentsPerRun":1,"maxCentsDaily":10,"maxCentsMonthly":100}`；issue=`{"audience":"ai"}`；exchange=`{"token":"AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA","audience":"ai"}`。这些是合成输入，不可当生产凭据；前两阶段若批准不同字段必须重基线，本书不覆盖。
### 6.3 成功响应
余额=`{"success":true,"data":{"balance":7,"totalEarned":10,"totalSpent":3}}`；历史空=`{"success":true,"data":{"history":[]}}`；预算=`{"success":true,"data":{"configured":true,"version":3,"maxTokensPerRun":100,"maxTokensDaily":1000,"maxTokensMonthly":10000,"maxCentsPerRun":1,"maxCentsDaily":10,"maxCentsMonthly":100,"usage":{"measured":false,"dailyTokens":null,"dailyCents":null,"monthlyTokens":null,"monthlyCents":null},"overCurrentUsage":false,"updatedAt":null}}`。其它原DTO完整定义保留原文件与#80批准契约，不修改nullable/缺字段语义。
### 6.4 错误契约
原success:false/error保持，没有新code。预算409仍“预算已被修改（可能在其他设备），请刷新后重试”；个人非法数字仍“预算上限必须是大于或等于 0 的整数”；useGrassland 402仍引导AI充值。网络失败无服务器code；旧ticket错误静默丢弃。权限401/403、过期token401、Redis503以#80真交付为准，新增descriptor不得改错误路径。
### 6.5 错误处理
不吞错误/空对象兜底；request只信封、requestRaw只裸JSON、requestText文本、fetchApi流、putToPresignedUrl存储上传。重复动作按原防连点与服务幂等，不添加自动写重试。中途权限撤销仍原失败，不能通过切tab继续操作。
### 6.6 不变量与观测
对02/03/04采用原输入→URL/DOM/请求method/path/body/次数/错误/副作用逐项差分；只允许文件位置变，不允许安全/计费行为变。01只允许旧私有态清理的预期变化；05只允许获批token视觉变化；06必须真实证据。对应TC81-01至06。
### 6.7 流/上传/异步
SSE/Multipart/Range/预签名不纳入account endpoint descriptor，N/A原因是运输契约不同；原能力入口/事件帧/轮询/支付补偿不改。清理本地订阅不代表取消服务器任务/退费，也不调用第三方logout注销服务器会话。

## 7. 数据模型与迁移
### 7.1 数据结构
应用内新增cleanup registry只放内存key/回调，不存Cookie/密钥。types/credits与endpoint为静态TS定义。docs/status保留schemaVersion1/backlog22-51原结构，新增顶层engineeringTasks数组（仅在06实际实现），项为{taskbookNumber:number,implementationStatus:'not_started'|'implemented'|'verified'|'blocked',verificationStatus:'not_run'|'failed'|'passed',productionStatus:'not_released',evidence:string[]}。
### 7.2 字段规则
engineeringTasks的taskbookNumber为79/80/81唯一；未实施=not_started/not_run，代码已实现缺必需验证=implemented/not_run或failed；verified只允许passed且evidence含实际报告路径。生产统一not_released，不能据开发完成改生产。文件日期为执行更新的本地绝对日，不预写未来日期。
### 7.3 兼容与迁移
无数据库/Flyway迁移；主题偏好仍原storage key，不因注销清理；私有缓存不加storage。原types re-export保留，避免消费者因移动类型被迫重写。状态脚本必须兼容既有backlog22–51且新增工程记录单独校验，不扩scope造成缺号。
### 7.4 生命周期/并发
同epoch单例cleanup只一次，卸载注销与新epoch同时发生也不调用已销毁引用；注册冲突不能覆盖既有cleanup。所有网络写前/后保留#79 ticket和局部revision；费用/流水在原服务事务，取消不补偿。
### 7.5 回滚证据
无SQL。应用回滚仍读原API/主题偏好；新增status字段可向后兼容读取，脚本同步回滚不能把已验证证据删除。C05导出像素变化未经批准不做，避免不可逆用户产物差异。

## 8. UI 实现规格
### 8.1 归属
Workbench根DESIGN；Admin/专用面板ops；AI/预算/共享组件根DESIGN，ops差异只data-app=ops。三入口皆验。
### 8.2 结构
保留Workbench两身份三签、组织/财务二级、原任务居中GlModal、共享个人设置；Admin仍五组两行导航和原23签，不能改侧栏；搬组件不改变role/aria/data-testid/DOM锚点、v-show/KeepAlive。
### 8.3 组件行为
users/kyb等加载/空/错误/翻页与旧界面一致；预算保存防连点/409保留表单；注销即清私有输入不清主题。抽取面板只展示并发events，业务副作用由明确控制器持有，不能父子都首拉。
### 8.4 交互
原GlModal初始焦点、焦点圈、Esc/遮罩/脏表单确认/关闭归还保留；图标有aria-label；activeSection replaceState保留其他query；未知tab回第一可见，不暗中调用不可见面板。
### 8.5 响应式
根：<768移动/768–1023平板/1024–1439桌面/≥1440宽；ops：<768/768–1024/1024–1440/>1440按其规范。视口390×844、820×1180、1440×900；长标题200汉字、用户表11列、50行、错误200字、弹窗长内容都验证，不用裁剪掩盖。
### 8.6 token与映射
| 用途 | 当前CSS映射/规范 | 暗/亮或共享值 |
| --- | --- | --- |
| 主色 | --color-accent；根colors.primary/primary-soft | #665efd/#533afd，ops active仍#4434d4对应已有作用域 |
| 文字/表面 | --color-text/--color-surface | #f0f2f8/#0d253d；#141825/#ffffff |
| 间距 | --space-md/lg/xl→根spacing.lg/xl/xxl及ops md/lg/xl | 16/24/32px |
| 圆角 | --radius-xs/sm/md/lg/xl/pill | 4/6/8/12/16/9999px |
| 字体 | --font-display/body | Space Grotesk/Inter self-host，无新字体/CDN |
不能把根spacing.md=12与CSS --space-md=16混为同名同值。style.css里的font-mono/12px文本/草绿等存量规范差异需要B81-02分类映射，不凭变量存在就宣称获准。新增token必须两DESIGN所属规范先批准、style暗亮成对；新共享基础样式只扩全局.gl-*，不重造组件库。
### 8.7 无障碍
tab/button可键盘，role/aria-selected关系保持；错误role=alert、成功role=status，不只依赖颜色。金额/数字保留tabular figures与money.ts，不用Intl.NumberFormat另造现金规则。
### 8.8 截图自查
产物`test-artifacts/task-81/shots/端-路由-主题-状态-视口.png`。01覆盖通知/AI治理/预算换号后；02Workbench六签与空/加载/错误；03Admin五组、users/kyb/finance/ai-models和只reviewer角色；04无视觉变化仍复核消费面；05扫描命中并实际修改的每页/共享挂载点必须覆盖。三视口、light/dark各一张且实际查看。Canvas导出另保存固定输入PNG像素证据，不用主题截图替代产物校验。06纯文档N/A。

## 9. 全局约束
### 9.1 文件白名单 / 黑名单
未来写入集合为§11逐文件表的精确并集；没有列出的48候选文件只有只读扫描权，不给“一键全仓替换”权限。B81-02补完逐文件映射并修订白名单后才能发布05。当前仅写本书。禁止模板/verify-task78.mjs/smoke-77-shots、真实.env/密钥、lockfile、原Flyway/.git/其他任务产物。生成物只test-artifacts/task-81/、coverage/dist及既有Playwright产物。最终主程可新建docs/任务书/草场任务书-81-交付报告.md；报告不是提前功能完成声明。
### 9.2 项目铁律
三入口/主题防闪脚本/Nginx CSP hash绑定保持；Edge原flag/method fail-closed404，固定上游edge-bff:8080，不扩可信代理；后台Java25/SpringBoot4，禁止Node服务。跨服务数据只经原服务，Flyway不改，资金/积分原事务/幂等/补偿不动。AiExecutionService/各能力与FrozenTextExecutionService独立文本、快照/预算/并发/ai_run/扣退款/内容安全保持；不因own模式重引旧免费能力或关提交L1闸；SSRF/DNS/SNI/trusted-host不放宽。WebFlux无block/手动subscribe，Mono.defer与record@JsonIgnore按原契约。HTTP/text/raw/SSE/upload分流；money.ts复用；UI两主题/token/两字体/共享组件硬规则不豁免。
### 9.3 环境事实
前端根`/Users/LXH/claude/y-1`、bash/zsh、Node22/npm10锁依赖；单测默认node，DOM文件级happy-dom。浏览器需获准Vite或隔离Compose与安装的Playwright；本次均未运行。Java只读，此阶段无Java构建必要；不得因文档制作起数据库/跑真AI。03/05浏览器测试使用合成fixture。所有安装/联网/浏览器/写目录副作用要当时授权。
### 9.4 安全/性能/兼容
纯拆分API调用次数不能增加；通知轮询60000ms、页20；Admin页10/20/50/100；无新无界缓存。清理不触业务写，日志/截图不包含真实凭据/资料。全局coverage76/76/58/76、变更可执行行80%；保留Java各模块既有JaCoCo，Java无改动N/A。不降断言/忽略新文件换绿灯。
### 9.5 适用矩阵
| 约束 | 卡/落实 |
| --- | --- |
| R-UI | 01/02/03/05及04消费面回归；06无视觉 |
| R-ENTRY | 01三端session、02/03路由DOM、05共享样式；不改HTML主题脚本 |
| R-JAVA | 全部只读保留，无后端写卡/新Edge路由 |
| R-DATA | 01内存registry、04类型兼容、06状态schema；SQL N/A |
| R-AI | 01治理预算/第三方缓存、05导出，执行与收费不改 |
| R-QUALITY | 全部TC/V；06不降status/覆盖门槛 |
| R-SAFE | 全部保留已有工作、合成数据、无生产副作用 |

## 10. 任务总表
| 卡 | 独立结果 | 前置交付物 | 需求/验收/状态 |
| --- | --- | --- | --- |
| C81-01 | 统一账号生命周期 | #79/80真实代码/报告，B81-01 | REQ81-01/AC81-01A/B/NOT_STARTED |
| C81-02 | Workbench责任拆分 | 01注册接口、原Workbench行为基线、B81-03 | REQ81-02/AC81-02A/B/NOT_STARTED |
| C81-03 | Admin责任拆分 | 01注册接口、02验证、B81-03 | REQ81-03/AC81-03A/B/NOT_STARTED |
| C81-04 | 账户endpoint/DTO集中 | 01–03稳定调用边界、#80协议 | REQ81-04/AC81-04A/B/NOT_STARTED |
| C81-05 | UI token盘点/获批清偿 | 02/03新布局、B81-02全文件映射 | REQ81-05/AC81-05A/B/NOT_STARTED |
| C81-06 | 权威状态回写 | 01–05真实卡证据及#79/#80报告 | REQ81-06/AC81-06A/B/NOT_STARTED |
### 10.1 拆分与顺序
所有同文件/全局style写入串行；不能把#79的正确性保护推给本阶段补完才生效。诊断清单不是后续自动写全部src的授权。卡05过大时主程必须在发布修订中拆子卡并冻结每组文件，不给弱模型一张无限清偿卡。
### 10.2 交接
每卡逐文件hash、导出类型、DOM/请求差分、TC/V与未完成验证；02/03输出props/emits/依赖图给04/05；05输出逐文件token映射/例外证据；06只根据真实报告更新状态。前置数据不足先阻塞，不凭git日志中feat推断验收完成。

## 11. 任务卡

### 卡 C81-01：账号生命周期注册表接入

- **负责人/状态**：获指派编码模型；`NOT_STARTED`。开工前核对 #79 实际账号生命周期导出；缺失时本卡停止并报告，不创建第二套epoch。
- **目标**：在不建立第二套 epoch 的前提下，把通知、模型来源、预算/治理局部状态及第三方会话客户端注册到 #79 的账号失效/卸载机制；账号变化时旧异步结果不得回写新账号。
- **文件白名单**：
  - 修改：`src/composables/useAccountLifecycle.ts`（仅使用 #79 已交付的实际导出；若不存在先阻塞，不得自行创造第二套协议）；`src/layouts/DefaultLayout.vue`（账号 watch/清理接入点）；`src/ai/AiAppLayout.vue`（AI 入口账号 watch/清理接入点）；`src/views/grassland/GrasslandWorkbench.vue`（工作台局部资源注册）；`src/stores/notifications.ts`、`src/stores/analysis-settings.ts`、`src/stores/credits.ts`（仅注册清理或 ticket，不改 wire）；相关既有测试文件。
  - 测试：`src/layouts/DefaultLayout.test.ts`、`src/ai/AiAppLayout.test.ts`、`src/views/grassland/GrasslandWorkbench.test.ts`；若当前没有对应测试文件，只能在同目录既有测试中追加。
  - 禁止：`index.html`、`ops.html`、`ai.html` 主题脚本，Java/Edge、数据库、lockfile、真实环境文件。
- **最小源码锚点**：`DefaultLayout.vue` 的 `watch(() => currentUser.value?.id ?? null, ...)`；`AiAppLayout.vue` 同名 watch；`GrasslandWorkbench.vue` 的账号初始化与 `useWorkbench*` 编排；`stores/notifications.ts` 的 `loadUnreadCount`/轮询；`stores/analysis-settings.ts` 的 `loadSettings`；`stores/credits.ts` 的 `loadBalance`。
- **输入/输出**：输入为旧账号ID、新账号ID、`epoch`、`AbortSignal` 或 #79 等价 ticket；输出为旧域清理完成、新账号只初始化一次、旧响应被丢弃。账号相同的重复 mounted/watch 不得重复 bootstrap。
- **必读引用/前置交付物**：`AGENTS.md`、根 `DESIGN.md`、`src/ops/DESIGN.md`、#79 交付报告与实际 diff、#80 交付报告；前置必须提供 #79 的导出符号、清理顺序和测试证据。
- **步骤与检查点**：1) 核对 #79 导出和三入口生命周期；2) 画出现有异步写点清单；3) 逐点加注册/失效 guard；4) 检查卸载停止轮询/Abort；5) 对比账号切换前后请求、DOM、store 和错误；6) 输出逐文件 hash 与未覆盖写点。任何未识别写点都停在 `BLOCKED`。
- **异常边界**：旧请求晚于新账号返回、旧请求失败、账号注销、组件卸载、同账号重复 watch、401/403、网络超时、请求被 Abort；旧请求错误不得覆盖新账号错误，清理不得发业务写请求。
- **验收 AC81-01A/B**：A. 三入口各完成 A→B→A 与卸载重挂，通知/积分/模型来源/预算/第三方客户端只显示当前账号；B. 旧响应和旧错误均不改变当前账号状态，网络日志无新增业务写请求，相关测试和截图证据完整。
- **交接**：提交 `C81-01` 卡报告，列出注册表符号、每个注册者、取消顺序、TC81-001～003 证据；不得把“代码已写”当 `VERIFIED`。

### 卡 C81-02：Workbench 编排、导航与展示拆分

- **负责人/状态**：获指派编码模型；`NOT_STARTED`；依赖 C81-01 `VERIFIED`。
- **目标**：在 `GrasslandWorkbench.vue` 保持页面行为零变化的情况下，把 URL query 解析/恢复、导航副作用、领域控制器编排和展示组件装配分离；保留六个工作台签、`v-show` 常驻语义、异步分包和任务/组织/资金行为。
- **文件白名单**：
  - 修改：`src/views/grassland/GrasslandWorkbench.vue`；`src/views/grassland/composables/useWorkbenchSession.ts`、`useWorkbenchTaskHall.ts`、`useWorkbenchMyTasks.ts`、`useWorkbenchEngagements.ts`、`useWorkbenchTaskDrafts.ts`、`useWorkbenchDisputes.ts`；必要时新建同目录 `useWorkbenchUrlState.ts`、`useWorkbenchNavigation.ts`，但必须先在报告中说明没有现成复用；对应 `.test.ts` 文件；只读核对 `src/views/grassland/components/*.vue`。
  - 禁止：改 Java/Edge、改路由语义、删除既有 composable、改变异步组件加载策略、全局 CSS。
- **最小源码锚点**：`GrasslandWorkbench.vue` 的 `useRoute/useRouter`、query 白名单、`switchSide/selectTask`、六域 composable 初始化与 template 页签；`useWorkbenchSession.ts` 的组织/视角切换；`TaskDetailModal.vue` 的弹窗边界。
- **输入/输出**：输入为 route query、当前组织/视角、六域状态及用户事件；输出为相同 URL 恢复、相同 DOM 挂载顺序、相同请求 method/path/body/次数和相同成功/失败文案。
- **必读引用/前置交付物**：`DESIGN.md`、C81-01 报告、`GrasslandWorkbench.test.ts`、各 composable 测试、当前路由测试；主程提供拆分前 DOM/请求快照。
- **步骤与检查点**：1) 记录 query key 与导航副作用；2) 记录每个 composable 的输入输出/写点；3) 只提取纯适配器和副作用控制器；4) 保持展示组件 props/emits；5) 用原输入重放六签、深链、刷新、返回、未知 query；6) 对比请求和截图。禁止在拆分时顺便改 UI 文案。
- **异常边界**：未知 query、数组 query、无权限签、组织切换期间旧请求、加载/空/错误、异步组件失败、浏览器后退前进、窄视口；均按基线行为处理。
- **验收 AC81-02A/B**：A. 六签、深链、组织/视角切换、任务弹窗、202 轮询和错误展示与基线一致；B. 代码中 URL/导航/编排/展示职责可分别定位，调用次数不增加，测试含 DOM 与网络差分。
- **交接**：输出 `props/emits/依赖图`、副作用清单、前后行为 diff，供 C81-04/05 使用。

### 卡 C81-03：Admin registry、控制器与面板拆分

- **负责人/状态**：获指派编码模型；`NOT_STARTED`；依赖 C81-01、C81-02 的测试基线，不能等待 C81-02 的业务代码改动才能保持独立。
- **目标**：保留 `TAB_REGISTRY`/`ADMIN_TAB_KEYS` 单一来源，把 `AdminView.vue` 的页签注册、URL/角色控制、用户控制器和内联面板装配分层；23 页签、5 组、角色显隐、reviewer 深链与分页行为不变。
- **文件白名单**：
  - 修改：`src/ops/admin/AdminView.vue`、`src/ops/admin/AdminView.test.ts`；必要时新建 `src/ops/admin/composables/useAdminNavigation.ts`、`useAdminAccess.ts`、`useAdminController.ts`；只按当前 registry 实际引用修改 `src/ops/admin/components/*AdminPanel.vue` 及测试，不物理搬迁全部面板。
  - 禁止：删除/复制 `TAB_REGISTRY`、手写第二份 23 项联合、改 `src/ops/router.ts` 的公开路径、改后端角色定义、改 ops 设计 token。
- **最小源码锚点**：`AdminView.vue` 的 `TAB_REGISTRY`、`ADMIN_TAB_KEYS`、`canSeeTab`、`activeSection`、`activeTabDef`、`visibleTabs`、`selectTab`、深链恢复和 `handle*` 控制器；现有 `AdminView.test.ts` 的角色、页签和 API mock。
- **输入/输出**：输入为 `/ops` query、角色、当前页签、分页和面板事件；输出为相同可见页签、回退规则、请求顺序/次数、分页选择和面板 props/emits。
- **必读引用/前置交付物**：`src/ops/DESIGN.md`、C81-01 报告、`AdminView.test.ts`、所有受触及面板测试；主程冻结当前 23/5/role 表。
- **步骤与检查点**：1) 抽出纯 registry/access/navigation；2) 抽出用户/批量动作控制器；3) 保持面板挂载与 KeepAlive/条件渲染；4) 逐角色验证可见性；5) 验证 users/kyb/finance/ai-models 与 reviewer 深链；6) 记录无新增请求/写操作。
- **异常边界**：未知 tab、不可见 tab、无权限 API、空列表、分页越界、重复提交、面板懒加载失败、刷新/后退；不得以客户端隐藏替代服务端权限。
- **验收 AC81-03A/B**：A. 23 页签/5 组/角色和深链与基线一致；B. registry 仍为唯一来源，控制器与面板边界可测试，失败和分页行为不变，网络日志无额外写请求。
- **交接**：输出 registry/access/navigation 依赖图和面板 props/emits 表，列明保留内联面板的原因。

### 卡 C81-04：账户 endpoint 与 DTO 窄范围集中

- **负责人/状态**：获指派编码模型；`NOT_STARTED`；依赖 C81-01～03 实际调用边界及 #80 `requestRaw` 交付。
- **目标**：只集中 §6 列出的 12 个账户端点与 DTO；调用者使用固定 descriptor/type，不改变公开 method/path、成功信封、错误文案、权限和 `request`/`requestRaw` 分流。
- **文件白名单**：
  - 新建：`src/api/account-endpoints.ts`、`src/types/credits.ts`（若 #80 已创建则只复用，不重复新建）。
  - 修改：实际存在的 API helper（以 #80 报告为准）、`src/stores/credits.ts`、`src/stores/analysis-settings.ts`、`src/types/auth.ts`、`src/types/settings.ts`、`src/types/cross-app.ts`（仅必要导出/兼容 re-export）、相关调用者测试。
  - 禁止：`ModelListingService` 整体删除；任意 raw/SSE/upload 迁入 envelope；新增路由、万能 API registry、Java/Edge 修改。
- **最小源码锚点**：`src/stores/credits.ts` 当前裸 `response.json()`；`src/stores/analysis-settings.ts` 的 `loadSettingsEnvelope`；`src/ai/AiAppLayout.test.ts` 的 cross-app method/path；#80 的 `requestRaw<T>` 和 `src/api/*` 实际导出。
- **输入/输出**：输入为固定 descriptor、已有 DTO 和原请求参数；输出为编译期窄类型、公开 wire 完全不变、错误/取消/信封处理可观察。
- **必读引用/前置交付物**：#80 实际报告、C81-01～03 调用差分、接口 controller/Edge route 只读核对；禁止从历史任务书猜路径。
- **步骤与检查点**：1) 扫描账户端点字符串和重复 DTO；2) 与当前 Controller/Edge flags 对照；3) 建最小 descriptor；4) 迁移已批准消费点；5) 编译与契约测试；6) 检查未迁移调用点并列出 N/A/后续任务。
- **异常边界**：裸 JSON、信封、文本、SSE、上传、401/403/409/429/5xx、空历史、nullable 字段；不能用 `as` 抹掉协议差异。
- **验收 AC81-04A/B**：A. §6 端点均有单一 descriptor/DTO 来源且消费点通过类型检查；B. 录制 method/path/body/响应与基线逐项一致，积分信封和 #80 兼容窗口按批准决策执行，无未授权兼容层。
- **交接**：输出端点矩阵、DTO 来源、未迁移清单、契约测试证据和变更影响面。

### 卡 C81-05：UI token 盘点与批准后的逐文件清偿

- **负责人/状态**：获指派编码模型；`NOT_STARTED`；先完成逐文件盘点，只有映射获批准的文件才能改样式；未批准命中保持只读并报告。
- **目标**：对命中的 48 个 Vue/CSS 文件逐文件分类；仅对批准映射执行 token 替换，保持暗/亮主题、Canvas 用户自选颜色、导出像素、图片模板配色和共享组件复用。
- **文件白名单**：当前为只读扫描 `src/**/*.vue`、`src/**/*.css`；写入白名单必须在 B81-02 解除后逐文件列入本卡修订版，至少包含 `src/style.css` 与实际命中的文件；测试截图写入 `test-artifacts/task-81/shots/`。禁止全仓替换、改 `index.html`/主题防闪、改 Canvas 导出算法、改外部字体。
- **最小源码锚点**：根 `DESIGN.md`、`src/ops/DESIGN.md`、`src/style.css` 的 `:root`/`[data-theme="light"]`、共享组件消费面、Canvas 绘制和导出函数；48 个命中文件以扫描清单为准，不得凭数量生成文件名。
- **输入/输出**：输入为硬编码颜色/字体/字号/圆角/间距命中及设计 token 映射；输出为逐文件映射表、例外理由、修改 diff、双主题截图和 Canvas 固定输入 PNG 像素证据。
- **必读引用/前置交付物**：两份 DESIGN、AGENTS、C81-02/03 新布局实际 diff、产品/设计批准的映射表；未批准映射必须保持只读。
- **步骤与检查点**：1) 只读扫描并分类为界面 token/Canvas 内容/导出数据/第三方或历史兼容；2) 为每个文件记录符号、操作、原因、禁止事项、完成标准；3) 主程批准后按文件串行改；4) 执行设计 lint；5) light/dark、三视口截图；6) 比较导出 PNG hash/像素。
- **异常边界**：token 无对应定义、暗亮值缺一、Canvas 自选色、SVG data URI、第三方样式、导出像素变化、共享组件两端差异；均阻塞或列 N/A，不自行猜测。
- **验收 AC81-05A/B**：A. 每个写入文件均有批准映射和截图；B. 设计 lint 无违规，双主题可读性保持，Canvas/导出固定输入证据无变化，未批准命中仍在阻塞清单。
- **交接**：输出扫描 hash、分类表、逐文件 diff、例外和截图/像素证据；不以“变量已存在”替代映射批准。

### 卡 C81-06：权威状态与工程进度回写

- **负责人/状态**：主程或获授权文档维护者；`NOT_STARTED`；依赖 C81-01～05 的真实报告。
- **目标**：只根据已验证证据更新 `docs/status.yaml` 或其当前权威结构，区分 `IMPLEMENTED`、`VERIFIED`、`NOT_STARTED`、`BLOCKED` 和生产发布状态；不把 #79～81 硬塞进现有业务 backlog 22～51。
- **文件白名单**：修改 `docs/status.yaml`（仅现有 schema 允许的字段）；必要时新建 `docs/任务书/草场任务书-81-交付报告.md`；测试/证据目录只写 `test-artifacts/task-81/`。禁止改模板、业务代码、历史任务书、无批准的 schema、日期造绿。
- **最小源码锚点**：当前 `docs/status.yaml` 的 schema、任务/阶段字段和现有 22～51 backlog；三份任务书 §0.3、§12、§14。
- **输入/输出**：输入为每张卡报告、命令退出码、截图/契约/coverage 证据、阻塞项；输出为可追溯状态记录和变更说明。
- **必读引用/前置交付物**：#79/#80/#81 报告、Git 基线、主程集成结论；没有证据的结果保持 `NOT_STARTED` 或 `BLOCKED`，不得伪造 `VERIFIED`。
- **步骤与检查点**：1) 核对 schema；2) 建卡号→状态→证据矩阵；3) 只写允许字段；4) 运行文档格式/解析检查；5) 记录未解决阻塞和生产发布分离；6) 主程复核后交接。
- **异常边界**：报告缺失、命令未运行、证据过期、卡状态跳级、schema 不兼容、工作区有未授权改动；停止写入并出返工卡。
- **验收 AC81-06A/B**：A. 每个状态都能指向卡报告和证据；B. 没有“代码实现=验收=发布”的混淆，没有覆盖 22～51 业务编号，文档解析成功且未改变业务状态。
- **交接**：输出状态 diff、证据索引、生产发布未批准说明；完成不等于上线。

## 12. 测试、验证命令与集成验收

### 12.1 需求追踪与验收覆盖

| 需求 | 规则 | 卡 | 验收 | 用例 | 命令/证据 |
|---|---|---|---|---|---|
| REQ81-01 | R81-01 | C81-01 | AC81-01A/B | TC81-001～003 | V81-01～03 |
| REQ81-02 | R81-02/R81-07 | C81-02 | AC81-02A/B | TC81-004～006 | V81-04～05 |
| REQ81-03 | R81-03/R81-07 | C81-03 | AC81-03A/B | TC81-007～009 | V81-06～07 |
| REQ81-04 | R81-04 | C81-04 | AC81-04A/B | TC81-010～012 | V81-08～09 |
| REQ81-05 | R81-05/R81-08 | C81-05 | AC81-05A/B | TC81-013～015 | V81-10～12 |
| REQ81-06 | R81-06/R81-09 | C81-06 | AC81-06A/B | TC81-016～017 | V81-13 |

### 12.2 合成数据与完整用例

所有用例使用 `qa.invalid` 邮箱、固定 UUID、`QaOnly!234567890`、虚构 token 和 mock fetch；不得访问生产、真实 AI、真实支付或真实用户资料。每例记录输入、操作、延迟/错误注入、展示/响应、持久化副作用、最终状态和证据路径。

| 用例 | 合成输入与操作 | 延迟/错误模拟 | 预期展示/响应与副作用 | 最终状态/证据 |
|---|---|---|---|---|
| TC81-001 | 账号 A 登录，触发通知/余额/设置/预算/模型来源；切到 B | A 的响应延迟 100ms，B 先返回 | 只展示 B；A 响应和错误丢弃；无业务写 | `VERIFIED`；请求日志+单测 |
| TC81-002 | A→B→A，卸载 AI 与 Workbench | Abort/轮询停止 | 旧轮询停止，新账号只初始化一次 | `VERIFIED`；计时器/Abort 断言 |
| TC81-003 | 注入 401、403、网络超时、旧请求失败 | 各请求分别失败 | 当前账号错误保留，旧账号错误不覆盖；无持久化 | `VERIFIED`；错误面板截图 |
| TC81-004 | 六签深链 query：`side=merchant&wtab=tasks&task=qa-task`，刷新/后退 | 组件加载延迟 | URL、选中签、DOM 挂载和请求次数与基线相同 | `VERIFIED`；DOM/网络 diff |
| TC81-005 | 切组织、切视角、打开任务详情、模拟 202→终态 | 中间轮询一次 202，一次超时 | 原文案/轮询/弹窗行为不变，无重复写 | `VERIFIED`；测试+截图 |
| TC81-006 | 空、加载、错误、窄视口六签 | fetch 500、异步组件 reject | 状态和可访问错误与基线一致 | `VERIFIED`；light/dark 三视口截图 |
| TC81-007 | `platform_admin` 访问五组和 23 签 | mock 允许全部 | registry 生成全部可见项，请求次数不增加 | `VERIFIED`；Admin 测试 |
| TC81-008 | `reviewer` 深链 users/kyb/finance/ai-models 与不可见 tab | 403/未知 tab | 回退第一可见签，不越权、不写数据 | `VERIFIED`；角色截图+网络日志 |
| TC81-009 | 分页 10/20/50/100、空列表、重复动作 | 409/5xx | 原分页与错误行为保持，无新增写请求 | `VERIFIED`；测试断言 |
| TC81-010 | 余额/历史、分析设置、budget、cross-app 按 §6 示例合成请求 | 裸/信封/401/409/503 | 每种响应走正确 helper，DTO 不丢 nullable | `VERIFIED`；契约 fixture |
| TC81-011 | 同一 endpoint 由两个调用者消费 | 编译期故意删除旧导出后恢复 | 只有一个 descriptor/DTO 来源；不创建万能 registry | `VERIFIED`；`tsc`/静态扫描 |
| TC81-012 | 跨应用 token audience/source/target 组合 | 错 audience、过期、重复兑换 | 按 #80 真交付错误，不扩大权限，不泄漏 token | `VERIFIED`；AI layout 测试 |
| TC81-013 | 扫描 48 命中文件，逐文件批准映射后修改 | 无映射/缺暗值/Canvas 命中 | 未批准保持阻塞；批准项双主题正确 | `VERIFIED` 或 `BLOCKED`；映射表+lint |
| TC81-014 | 固定 Canvas 输入颜色与导出参数，主题切换 | light/dark、重复导出 | PNG hash/像素不变；界面 token 变更不影响导出 | `VERIFIED`；PNG 对比 |
| TC81-015 | 共享组件在 index/ops/ai 挂载 | 亮暗主题、不同 data-app | 复用组件无复制，正确 token scope，无可读性回归 | `VERIFIED`；截图 |
| TC81-016 | 生成卡号→状态→证据矩阵 | 缺报告/NOT_RUN/状态跳级 | 保持 BLOCKED/NOT_STARTED，不写假绿 | `VERIFIED`；status diff |
| TC81-017 | 解析三份任务书与 status | schema 缺字段/未授权字段 | 文档检查失败即返工，不改业务 | `VERIFIED`；解析输出 |

### 12.3 验证清单与命令

- `V81-01` 前端单测：目录 `/Users/LXH/claude/y-1`；工具 Node 22/npm 10；执行 `npm run test -- --run src/ai/AiAppLayout.test.ts src/ops/admin/AdminView.test.ts src/views/grassland/GrasslandWorkbench.test.ts`；预期退出码 `0`。本任务书编制期间未执行。
- `V81-02` 类型/构建：同目录执行仓库现有 `npm run typecheck`、`npm run build`（若脚本名不存在，先读取 `package.json` 并记录命令不存在，不自行替代）；预期退出码 `0`，写入 `dist/` 的副作用须记录。本任务书编制期间未执行。
- `V81-03` 设计规范：同目录执行 `npx @google/design.md lint DESIGN.md` 与 `npx @google/design.md lint src/ops/DESIGN.md`；预期退出码 `0`；需联网/安装时先获授权。本任务书编制期间未执行。
- `V81-04` 静态边界：使用 `rg` 检查 endpoint 重复、第二份 registry、禁改文件和 `requestRaw` 分流；预期退出码按脚本约定记录，非零即返工。本任务书编制期间未执行。
- `V81-05` 浏览器回归：获授权后在隔离合成 fixture 启动实际 Vite/Playwright，覆盖三入口、light/dark、三视口；写入 `test-artifacts/task-81/shots/`，不使用真实服务。本任务书编制期间未执行。
- `V81-06` Admin 角色回归：同 V81-05，至少 `platform_admin`、`reviewer`、无权限合成用户；记录 URL、DOM、网络和最终状态。本任务书编制期间未执行。
- `V81-07` 账户协议：依据 #80 实际测试脚本执行前端测试及契约 fixture；Java/Edge 只读，不因文档任务启动服务。本任务书编制期间未执行。
- `V81-08` 类型/DTO静态扫描：检查 §6 的 12 个 descriptor 是否唯一、Credits DTO 是否重复、未迁移端点是否列明 N/A；本任务书编制期间未执行。
- `V81-09` #80 安全回归：仅在 #80 提供真实测试命令/fixture 后执行 audience、nonce、fail-closed、日志脱敏检查；没有证据保持阻塞。
- `V81-10` token 盘点：`rg` 输出命中文件和分类 hash；不得直接全仓替换；本任务书编制期间未执行。
- `V81-11` token 设计 lint 与截图：按 V81-03/V81-05，需实际查看 light/dark；本任务书编制期间未执行。
- `V81-12` Canvas 像素：使用仓库既有导出测试或批准的固定 fixture，比较 hash/像素；没有既有脚本不得临时添加生产逻辑。本任务书编制期间未执行。
- `V81-13` 文档验证：解析 Markdown 标题、编号、状态矩阵并核对 `git diff --check`；预期退出码 `0`。不得把文档检查当业务测试。本任务书编制期间未执行。

命令的授权、联网、服务启动、写目录和数据库/AI/支付副作用必须在执行前单独确认；任何未运行命令只能标 `NOT_RUN`。

### 12.4 最终集成验收与完成定义

主程按 C81-01→06 顺序检查：每卡 `VERIFIED`、文件在白名单、工作区没有未授权业务改动、REQ→R→C→AC→TC→V→证据链闭合；再做一次三入口 smoke、完整前端测试和构建。卡完成、代码实现、必需验证、最终集成、生产发布分别记录，不得合并成一个状态。任一必需用例 `FAIL/NOT_RUN`、前置事实缺失或生产配置未审批，当前卡保持 `BLOCKED`，不把整个任务书状态倒退成不可执行。

### 12.5 发布与回滚

本任务书不授权生产发布、数据库迁移、配置变更或真实 AI/计费验证；发布与回滚为 `N/A：需另行发布审批`。若拆分导致回归，回滚到主程确认的前一张卡 hash，不用删除用户工作或重置共享工作区。

## 13. 阻塞规则

### 13.1 当前最小阻塞问题

- **B81-01**：#79/#80 尚无实际 `VERIFIED` 交付，无法冻结账号生命周期和协议输入。解除条件：两份交付报告、真实 diff、测试命令与退出码由主程复核。
- **B81-02**：48 个硬编码命中文件尚未逐文件分类和批准映射，不能授权 C81-05 写入。解除条件：文件、符号、token、双主题值、Canvas/导出例外和设计负责人批准齐全。
- **B81-03**：Workbench/Admin 拆分后的真实 props/emits、副作用和 KeepAlive 边界尚未冻结。解除条件：主程提供拆分前快照，C81-02/03 各自产出依赖图和回放基线。
- **B81-04**：`docs/status.yaml` 是否新增独立工程任务结构未批准。解除条件：仓库负责人确认 schema、编号范围和是否允许新建工程任务节点。

### 13.2 阻塞报告格式

阻塞时必须报告 `BLOCKED`、卡号、事实证据、最小问题、影响文件/测试、需要谁在何处做唯一决策、解除条件、建议修订版本；不得用“按常识处理”“先实现再说”代替决策。阻塞恢复后从同一卡 `IN_PROGRESS` 重新核基线，旧证据不自动继承。

### 13.3 修订与恢复

任何新增端点、字段、角色、文案、token、迁移、依赖、生产配置或文件白名单都必须升任务书版本并重新跑附 B；只修错字可保留版本并记录。#79/#80 行为或文件变化时，C81-01/04 的前置交付物作废，重新基线；卡状态不得跳过 `IMPLEMENTED`/`VERIFIED`。

## 14. 完成报告格式

每张卡单独报告：

### 实现结果

写卡号、负责人、开始/结束时间、状态（`IMPLEMENTED`/`VERIFIED`/`BLOCKED`）、目标行为逐条结果；明确代码实现与验证是否分离。

### 文件与范围

逐文件写绝对路径、符号、操作、原因、禁止事项核对、前后 hash；列出未改但扫描过的文件和未授权改动。

### 验证证据

逐条写 TC/V 编号、完整命令、工作目录、工具链、服务/授权、副作用、退出码、输入 fixture、输出/截图/日志路径；`NOT_RUN` 必须如实保留。

### 偏差说明

记录与本书差异、谁批准、版本号、返工卡号；没有偏差写“无”。

### 未解决问题

列出阻塞编号、风险、恢复负责人和下一步；不得写“已完成”掩盖未决事项。

## 附 A：返工卡格式

### 返工卡 R81-01

- **触发**：任何 AC/TC/V 失败、文件越界、契约差异、截图未实际查看或证据缺失。
- **输入**：卡号、失败证据、基线 hash、失败命令/退出码、影响面。
- **动作**：只修失败根因；若涉及产品/安全/数据/发布决策，先转 B81 阻塞。
- **输出**：修订文件白名单、复现步骤、修复 diff、回归 TC/V、残余风险。
- **完成**：原失败用例与受影响邻接用例均通过，主程复核后才能回到 `VERIFIED`。

## 附 B：强模型写作规约与发布前检查

### B.1 反向审阅

逐项核对：需求是否可观察；规则是否唯一；卡是否有输入/输出/引用/前置/锚点/步骤/异常/验收/交接；文件是否逐个列明路径、符号、操作、原因、禁止事项、完成标准；接口是否有完整 method/path/body/response/error；数据/并发/计费/安全是否明确或写 N/A 原因；测试是否含合成数据、延迟/错误、展示/响应、持久化副作用、最终状态、证据；命令是否来自实际脚本且未将 `NOT_RUN` 写成通过；卡完成、代码、验证、集成、发布是否分开。

### B.2 逐项发布检查

- [x] 三阶段已拆成 #79/#80/#81 三份独立文件。
- [x] 当前仓库、入口、调用链、DTO、数据、复用模块、测试和脚本已核对；未执行命令明确标 `NOT_RUN`。
- [x] 模板、根/ops 设计规范和 AGENTS 约束已保留；本任务没有修改模板或业务代码。
- [x] 三入口 `index.html`/`ops.html`/`ai.html`、Java、Edge、AI、计费、数据、安全、质量约束均有映射或 N/A 原因。
- [x] 需求、规则、任务卡、AC、TC、V、证据编号稳定且不混用。
- [x] 关键产品/安全/接口/数据/计费决策未获批准处已阻塞，未让执行者临时拍板。
- [x] 文件白名单含测试、配置、迁移、文档和产物范围；未授权文件列入黑名单。
- [x] 附 B 反向审阅已执行；B81-01～04已转为执行前置核验或卡内停止条件。
- [x] `READY_FOR_IMPLEMENTATION`：已满足任务书级执行授权；卡级验证和生产发布仍分开。

### B.3 作者审阅与版本记录

- 2026-09-06 / 主程规划：基于 `25c7857b2dd929ffa77289804ba94d5a164b2710` 创建，状态 `READY_FOR_IMPLEMENTATION`；B81-01～04改为执行前置核验。
- 当前未执行业务测试、构建、lint、E2E、Java 或 Docker；未声称功能已实现。

## 附 C：交给编码模型的执行提示词

你只执行被明确指派的 `C81-xx`。先读适用 `AGENTS.md`、本书 §0/§1/§9/§13/§14、当前卡、必读引用和实际基线；确认 HEAD、工作区与前置报告。只改白名单文件，先做卡内检查点，再跑卡内 TC/V；所有命令、授权、退出码、证据和未运行项如实记录。遇到前置事实缺失、未批准的新行为或文件越界，立即按 §13 在当前卡阻塞，不自行猜测或扩散到后续卡；不得修改模板、顺手修业务、提交、建分支、上线或把代码实现写成已验收。

## 附 D：生成与修订边界

本任务书由主程依据当前源码和文档生成，现已授权按六张卡实施；修订只允许补充已核实事实、批准决策、文件白名单、测试与证据。任何会改变行为、契约、数据、权限、视觉或发布顺序的内容都必须升版本、记录批准并重新执行附 B。执行前置事实缺失时当前卡停止并报告，不把缺口当成执行者决策。
