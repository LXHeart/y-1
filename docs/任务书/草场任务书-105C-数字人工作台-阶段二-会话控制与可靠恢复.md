# 草场 #105C：数字人工作台 阶段二-会话控制与可靠恢复

| 项目 | 内容 |
| --- | --- |
| 版本/日期 | 2.0 / 2026-09-21 |
| 文档状态 | READY_FOR_IMPLEMENTATION：规格已细化；仅当前卡前置验收满足时可执行；真实条件单独门禁 |
| 实施/用例 | NOT_STARTED / NOT_RUN；本轮仅交付文档，未执行本书未来代码命令 |
| 卡/用例 | 5张任务卡，20组Given/When/Then用例；组内边界需参数化 |
| 基线 | 草场f2976aaeb329ee17e9e08a8997bffaabde78d3ac + 当前#103/#104既有修改；OpenTalking固定8c739a5a6f114daf71aeace832a668c3ad60f536 |
| 前置 | 105B全卡通过；105A共享契约与Fake runtime可运行 |
| 方式 | AUTO_CHAIN：卡级通过自动进入本阶段下一卡；不启子代理，不自动越到未指定的下一本或生产开放 |
| 入口 | [#105总纲](草场任务书-105-数字人工作台开发计划.md) · [PRD](../产品/数字人工作台需求文档.md) · [共享契约](草场任务书-105-数字人工作台共享契约.md) |

## 0. 开始与完成协议

本书供编码模型直接执行。先读本节、§9、§13、§14，再读当前卡列明的共享K节、源码和测试。CARD PASS才推进；已有真实服务授权在同范围内持续有效，不逐卡重复申请。普通本地依赖、Fake、合成数据库与浏览器验证可直接进行。
先保存`git status --short`和当前diff；不得清空工作区或覆盖已有#103/#104修改。行号漂移以本卡真实符号定位；需要改变业务契约/归属/资金规则才停止受影响卡，不因发现未提交文件而全部停工。所有新增接口都应拒绝未知字段，无密码/密钥进入证据。
完成必须同时有实现、非零用例、卡级命令退出0、适用边界/视觉检查、范围检查和对话交接。NOT_RUN不是PASS；真实条件不足只阻塞相应real/beta门禁，保留Fake可继续的明确路径。

## 1. 本阶段目标与范围

完成服务端权威状态机、预算预检查、控制租约、事件和内容同意；任何页面关闭都不能留下无限运行任务。

| 需求 | 负责卡 | 验收组 |
| --- | --- | --- |
| DH-R02 | C105C-01、C105C-05 | TC105C-01-01～TC105C-01-04、TC105C-05-01～TC105C-05-04 |
| DH-R06 | C105C-01、C105C-05 | TC105C-01-01～TC105C-01-04、TC105C-05-01～TC105C-05-04 |
| DH-R09 | C105C-03 | TC105C-03-01～TC105C-03-04 |
| DH-R10 | C105C-02、C105C-03、C105C-05 | TC105C-02-01～TC105C-02-04、TC105C-03-01～TC105C-03-04、TC105C-05-01～TC105C-05-04 |
| DH-R11 | C105C-01、C105C-02、C105C-03、C105C-05 | TC105C-01-01～TC105C-01-04、TC105C-02-01～TC105C-02-04、TC105C-03-01～TC105C-03-04、TC105C-05-01～TC105C-05-04 |
| DH-R12 | C105C-03、C105C-04 | TC105C-03-01～TC105C-03-04、TC105C-04-01～TC105C-04-04 |
| DH-R15 | C105C-01 | TC105C-01-01～TC105C-01-04 |
| DH-R17 | C105C-02、C105C-04、C105C-05 | TC105C-02-01～TC105C-02-04、TC105C-04-01～TC105C-04-04、TC105C-05-01～TC105C-05-04 |
| DH-R20 | C105C-02、C105C-05 | TC105C-02-01～TC105C-02-04、TC105C-05-01～TC105C-05-04 |

范围外：组织共享、知识库/记忆、对外客服/直播、克隆真人声音、摄像头驱动、任意模型URL、自动交易工具；不顺手改文章/视频工具或前序缺陷。未被本卡白名单列出的文件一律只读。

## 2. 仓库上下文与只读基线

前端Vue/TypeScript，AI独立入口`src/ai/router.ts`及`AiAppLayout.vue`；治理台`src/ops/admin/adminTabs.ts`；Java控制域在intelligence-service，Edge转发；Python实时域为本系列新增目录。现有鉴权、预算、媒体、生命周期只作定点复用。
必读根AGENTS.md与`docs/架构/目录结构.md`。涉及用户/AI UI读根DESIGN.md，治理UI读src/ops/DESIGN.md，共享组件默认根规范。每卡源码摘录是2026-09-21快照，仅作定位；真实实现优先核对签名和上下文。

## 3. 已定技术决策

Java拥有owner/会话/费用/清理事实；Python持有每会话Binding与媒体，无长期供应商key；Vue只有同源控制资格。首期STT为按键提交后内存整段转写；LLM采用受控计量流；TTS采用24000Hz PCM桥且沿既有平台资助模式；RTC只下行音视频。打断关闭旧peer并重建，租约独立回收。
固定QuickTalk/local为条件真实候选；Fake必须覆盖STT/LLM/TTS/render全链且外网请求为0。框架开源许可不代替权重许可。配置默认关闭，S1内测、S2个人试点分别验收。

## 4. 目标行为与状态

全局状态迁移见共享K04；本阶段只实现各卡明确触点，不增同义state。session、turn、invocation、settlement、recording、cleanup互相独立：会话ended不表示费用已结或文件已删。失联回收≤45秒、正常结束P95≤5秒，都是待实现/实测目标。

## 5. 业务不变量

首期owner固定个人，orgId不由浏览器提交。账号+epoch变化使旧异步结果无效。每动作requestId创建一次；同键异payload409，同键重放先owner再receipt。费用unknown不填0、不自动重放provider。原始mic不入磁盘、素材库、日志；未同意文本仅有界易失缓冲。

## 6. 接口、字段与内部签名

公开API01～40见共享K03，内部13端点与NDJSON见K07.1～K07.3，管理6端点见K10。金额单位cents、时长ms、UTC时间、JS安全整数、可null/必填及错误码见K01。类型不得用any/无约束map逃避合同；当前卡精确签名见§11。

## 7. 数据、事务与迁移

共享K05是DDL合同，B核心迁移一次创建B/C/D/G核心表；F另建媒体表。执行时按intelligence最大Flyway整数+1替换唯一NEW_DYNAMIC路径中的NEXT，先记录实际文件名，禁止修改V86/V87及其他历史SQL。锁顺序account gate→session→turn/recording→invocation/operation→media；不持事务等待网络。

## 8. UI与用户可观察状态

本阶段无新UI页面；不为协议/部署任务添加装饰页面。需要浏览器验收的卡使用已交付页面；结果/错误文案按K01，截图适用处见具体卡。

## 9. 写入白名单与禁止项

下面是阶段全局上限；每卡仍只可改其§11表内文件，取两者交集。NEW仅首创，MODIFY_AFTER表明前卡已创建，不得重建覆盖。只读锚点不是写权限。测试产物可写test-artifacts/task-105/C及卡内指定`test-artifacts/task-105/`子目录（Git忽略），不能放根目录或提交用户内容。

| 完整仓库相对路径 | 动作 | 用途 |
| --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanPreflightService.java` | NEW | 无扣费报价与60秒快照 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionService.java` | NEW | 状态/owner活动约束 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionController.java` | NEW | Controller只装配 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRuntimeClient.java` | NEW | 内部mTLS与确定超时 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseService.java` | NEW | epoch与截止时间 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionReaper.java` | NEW | 独立于浏览器回收 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanReaperTest.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-realtime/digital-human/src/grassland_dh/leases.py` | NEW | 运行时过期自行停 |
| `platform-realtime/digital-human/tests/test_leases.py` | NEW | Java失联/旧epoch不可续 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventService.java` | NEW | 序号、durable/volatile分层 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventController.java` | NEW | 鉴权与SSE心跳 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanContentBuffer.java` | NEW | 独立易失Redis/容量 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanContentBufferTest.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptService.java` | NEW | 主动同意与contentEpoch |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptController.java` | NEW | 版本/owner/file信封例外 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanLifecycleService.java` | MODIFY_AFTER_C105B-01 | 已有注销step扩展 |
| `tests/contracts/resource-lifecycle.registry.json` | MODIFY | 新owner/content清理反例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanControlFaultIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/edge-bff/src/test/java/com/grassland/edge/proxy/DigitalHumanSseProxyIT.java` | NEW | 控制HTTP/SSE完整隔离链验证 |
| `platform-realtime/digital-human/tests/test_control_contract.py` | NEW | 命令重复/状态回调时序 |
| `tests/contracts/digital-human.contract.test.ts` | MODIFY_AFTER_C105A-02 | 对照API07～26必要项 |

禁止：改根依赖/lock（除本书明确列入）、改历史迁移、给旧大视图增业务逻辑、修改组织BYOK/取消退款口径、持久化grant/key/原音频、更新既有生产开关、全仓格式化、删除失败测试、杜撰测试PASS。共用契约/PRD只读；发现实质冲突先按§13给具体修订建议。

## 10. 卡顺序、依赖与估算

| 卡 | 目标 | 依赖 | 估计人日 | 初始状态 |
| --- | --- | --- | --- | --- |
| C105C-01 | 预检查与原子创建会话 | C105B-05 | 3～4 | NOT_STARTED |
| C105C-02 | 控制租约、暂停恢复与回收器 | C105C-01 | 3～5 | NOT_STARTED |
| C105C-03 | 有序事件与只读重连 | C105C-02 | 2～3 | NOT_STARTED |
| C105C-04 | 保存同意、字幕导出与删除墓碑 | C105C-03 | 2～3 | NOT_STARTED |
| C105C-05 | 控制面故障注入与交付 | C105C-04 | 2～3 | NOT_STARTED |

人日是包含实现/测试/调试的计划区间，不是模型运行时长或日历承诺；外部硬件/许可等待不计。A04(local)只要求本地Fake与缺口清单；H03(conditional-beta)只约束真实开放。

## 11. 逐卡执行规格

### C105C-01 预检查与原子创建会话

需求：DH-R02、DH-R06、DH-R11、DH-R15。依赖：C105B-05。必读共享：K01、K02、K03、K04、K05、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanPreflightService.java` | NEW | `check/consume` | 无扣费报价与60秒快照 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionService.java` | NEW | `create/get` | 状态/owner活动约束 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionController.java` | NEW | `API07/API08/API10` | Controller只装配 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRuntimeClient.java` | NEW | `create/state/end` | 内部mTLS与确定超时 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionIT.java` | NEW | `DigitalHumanSessionIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/ai/byok/ByokRoutingService.java`，`resolveProvider(`，快照行78起。先核实真实上下文：

```text
	 */
	public Mono<ProviderResolution> resolveProvider(String organizationId, String accountId, String capability,
			boolean allowFallback) {

		// 任务书 #47 D9：按活动身份分叉。edge 保证「organizationId 非空 ⟺ merchant 活动身份」
		// （SessionIdentityResolver:75-80 的不变量，注释明说破坏它就破坏 HLD 7.4），故非空即商家视角：
		// 商家侧由组织统一配置模型，个人密钥不参与——**跳过个人查询**，不是降级排序。
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `check(PersonalActor actor, UUID profileId, int profileVersion, InputMode inputMode, UUID controllerId): Mono<Preflight>；只解析配置/报价，不prepareExecution。`
- `create(PersonalActor actor, UUID preflightId, UUID requestId, boolean saveTranscript): Mono<Session>；preflight归属、TTL、模型价表版本逐项核验。`

**严格执行步骤**：

1. 从profile revision/catalog/个人text总开关/平台voice/video_tts生成Preflight；缺不兼容voice或TTS拒绝，不能等调用时换模型；无真实配价时只允许隔离Fake。
2. 保存preflight于独立易失Redis60秒，含owner/配置/价表版本无密钥；create由operation与session.preflight_id唯一约束防二次消费，重复同键先查receipt；Redis丢失需重新预检、不自动派发。
3. 事务内共享lifecycle锁、reserve operation、建立session；DB活动owner唯一为最终并发判断。总slot无空位时按全局队列上限10排队，满则429。
4. 事务提交后用runtime client发create，超时查询相同runtime session，不另建；初始化失败进入failed+cleanup_pending直到release核对。
5. 不复用浏览器当前org；preflight报价变化、profile版本变化均409并返稳定code，输入配置仍可重新确认。
6. 本阶段RuntimeClient通过可替换transport和fake HTTP server验证控制协议；D02交付mTLS实际监听与连接，C不得先启无认证生产端口。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105C-01-01 正常创建

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R06、DH-R11、DH-R15 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionIT.java`；测试名称包含`tc105c_01_01`；跨语言同TC可各有子断言 |
| Given | A有效预检且有slot |
| When | 同键create两次 |
| Then | 同session、一个runtime create、一个活动槽，无双预留 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-01-01；`test-artifacts/task-105/C/C105C-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-01-02 预检无成本

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R11、DH-R15 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionIT.java`；测试名称包含`tc105c_01_02`；跨语言同TC可各有子断言 |
| Given | 有效/缺模型/缺价表组合 |
| When | 重复preflight |
| Then | provider调用数和credits变化均0 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-01-01；`test-artifacts/task-105/C/C105C-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-01-03 过期与配置变更

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R11、DH-R15 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionIT.java`；测试名称包含`tc105c_01_03`；跨语言同TC可各有子断言 |
| Given | 过60秒或profile/catalog变化 |
| When | create |
| Then | 409或410明确，无provider派发 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-01-01；`test-artifacts/task-105/C/C105C-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-01-04 全局/个人容量

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R11、DH-R15 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionIT.java`；测试名称包含`tc105c_01_04`；跨语言同TC可各有子断言 |
| Given | 1活动+10排队、同A两页 |
| When | 多账号并发create |
| Then | owner最多1、队列有界，超限429有Retry-After |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-01-01；`test-artifacts/task-105/C/C105C-01/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105C-02 控制租约、暂停恢复与回收器

需求：DH-R10、DH-R11、DH-R17、DH-R20。依赖：C105C-01。必读共享：K03、K04、K05、K07、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseService.java` | NEW | `heartbeat/pause/resume` | epoch与截止时间 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionReaper.java` | NEW | `scanExpired` | 独立于浏览器回收 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionService.java` | MODIFY_AFTER_C105C-01 | `end/takeover` | CAS终态 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanSessionController.java` | MODIFY_AFTER_C105C-01 | `API11～14` | 请求owner/epoch |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseIT.java` | NEW | `DigitalHumanLeaseIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanReaperTest.java` | NEW | `DigitalHumanReaperTest` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-realtime/digital-human/src/grassland_dh/leases.py` | NEW | `RuntimeLease` | 运行时过期自行停 |
| `platform-realtime/digital-human/tests/test_leases.py` | NEW | `lease tests` | Java失联/旧epoch不可续 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/IntelligenceAccountLifecycleRepository.java`，`class IntelligenceAccountLifecycleRepository`，快照行20起。先核实真实上下文：

```text
@Component
public class IntelligenceAccountLifecycleRepository {

	public record Lifecycle(String accountId, String closureRequestId, String state, long revision, Instant frozenAt,
			Instant erasedAt) {
	}

```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `heartbeat(PersonalActor actor, UUID sessionId,long leaseEpoch,UUID controllerId): Mono<HeartbeatResult>。`
- `resume(actor,sessionId,long expectedEpoch,UUID controllerId,boolean takeover,UUID requestId): Mono<Session>；锁内epoch+1。`
- `scanExpired(Instant now,int limit): Mono<ReaperResult>；每次最多100，以FOR UPDATE SKIP LOCKED领单，外部调用在事务后。`

**严格执行步骤**：

1. 严格按K04迁移表写switch/CAS；terminal不可复活；客户端reported state绝不写权威表。
2. pause立即取消新输出、写固定pausedUntil；重复pause/heartbeat不延长，30秒后ending；刷新resume与另一页takeover明确区分。
3. 控制器凭浏览器内存controllerId+server epoch；新页takeover=false返回已有会话提示，true才轮换。旧页心跳/WS/offer不能成功。
4. reaper每5秒扫描队列/connecting/失联/idle/总时长/cleanupPending；Worker租约30秒到期自行close，Java故障也不永远运行。
5. 用可注入Clock/fake scheduler测试，不在单测真实sleep45秒；真实H阶段kill进程再测墙钟。end重复无副作用，已释放slot不可负数。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105C-02-01 双页接管

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseIT.java`；测试名称包含`tc105c_02_01`；跨语言同TC可各有子断言 |
| Given | A1持有epoch1、A2已登录同账号 |
| When | A2接管→A1心跳/续生成 |
| Then | 只epoch2有效，旧页409与媒体关闭 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-02-01、V105C-02-02；`test-artifacts/task-105/C/C105C-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-02-02 隐藏不能续命

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseIT.java`；测试名称包含`tc105c_02_02`；跨语言同TC可各有子断言 |
| Given | pausedUntil=t+30秒 |
| When | 连续heartbeat和重复pause |
| Then | t+30进入ending，不另延长30秒 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-02-01、V105C-02-02；`test-artifacts/task-105/C/C105C-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-02-03 Java/浏览器消失

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanReaperTest.java`；测试名称包含`tc105c_02_03`；跨语言同TC可各有子断言 |
| Given | Fake时钟推进与网络隔断 |
| When | 最后心跳后45秒内 |
| Then | runtime和slot释放；费用unknown可保留但不继续推理 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-02-01、V105C-02-02；`test-artifacts/task-105/C/C105C-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-02-04 终态与迟到ready

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanLeaseIT.java`；测试名称包含`tc105c_02_04`；跨语言同TC可各有子断言 |
| Given | end已提交后旧runtime ready |
| When | 回调/重复end |
| Then | 保持终态；无槽复活，无重复结束扣费 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-02-01、V105C-02-02；`test-artifacts/task-105/C/C105C-02/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105C-03 有序事件与只读重连

需求：DH-R09、DH-R10、DH-R11、DH-R12。依赖：C105C-02。必读共享：K04、K05、K06、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventService.java` | NEW | `append/stream/snapshot` | 序号、durable/volatile分层 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventController.java` | NEW | `API18/internal events` | 鉴权与SSE心跳 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanContentBuffer.java` | NEW | `append/read/erase` | 独立易失Redis/容量 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventIT.java` | NEW | `DigitalHumanEventIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanContentBufferTest.java` | NEW | `DigitalHumanContentBufferTest` | 实现本卡TC，含真实DB/事务或fake网络负例 |

**只读源码锚点**：`platform-java/services/edge-bff/src/main/java/com/grassland/edge/proxy/RoutingProxyHandler.java`，`bodyToFlux`，快照行64起。先核实真实上下文：

```text
        restoreRateLimitHeaders(exchange, rateLimit, rateRemaining, rateReset);
        return exchange.getResponse().writeWith(response.bodyToFlux(DataBuffer.class));
    }

    static void restoreRateLimitHeaders(
            ServerWebExchange exchange, String limit, String remaining, String reset) {
        if (limit == null) {
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `append(RuntimeEvent event): Mono<EventEnvelope>；身份来自内部证书/Binding映射，不信body owner。`
- `stream(PersonalActor actor,UUID sessionId,Long afterSeq): Flux<ServerSentEvent<EventEnvelope>>；先snapshot，不查询时重做turn。`

**严格执行步骤**：

1. dh_event状态/usage与session last_seq同事务；文本事件只易失buffer，持久表不得包含prompt/delta/text。
2. 事件每条检查session/lease/turn/mediaEpoch；旧回调只计丢弃指标，不改新状态。eventId去重防重复settle。
3. SSE先snapshot(seq=S)，随后回放afterSeq到S的可用事件，再接S之后live；前端snapshot和replay去重水位分开，不能因先看到S就丢掉历史字幕。replayComplete=false不虚构缺失文本。
4. 20秒SSE注释心跳，断开取消订阅释放资源；每10秒复查owner/会话权限；最多两读流。
5. 代理集成测试用逐块到达时间证明无聚合，不只检查Content-Type；服务器返回HTML/异常体不透正文。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105C-03-01 顺序去重

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R09、DH-R10、DH-R11、DH-R12 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventIT.java`；测试名称包含`tc105c_03_01`；跨语言同TC可各有子断言 |
| Given | seq1/2/重复2/旧epoch事件 |
| When | 写入与订阅 |
| Then | 有序一次、旧epoch无状态作用 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-03-01；`test-artifacts/task-105/C/C105C-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-03-02 回放缺口

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R09、DH-R10、DH-R11、DH-R12 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventIT.java`；测试名称包含`tc105c_03_02`；跨语言同TC可各有子断言 |
| Given | afterSeq早于内存窗 |
| When | 重新订阅 |
| Then | 先snapshot+replayComplete=false，不发provider请求 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-03-01；`test-artifacts/task-105/C/C105C-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-03-03 内容不持久

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R09、DH-R10、DH-R11、DH-R12 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanContentBufferTest.java`；测试名称包含`tc105c_03_03`；跨语言同TC可各有子断言 |
| Given | 输入合成私有标记 |
| When | dump dh_event/日志/普通Redis |
| Then | 无正文，仅易失buffer和已同意transcript |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-03-01；`test-artifacts/task-105/C/C105C-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-03-04 权限撤销与流释放

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R09、DH-R10、DH-R11、DH-R12 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanEventIT.java`；测试名称包含`tc105c_03_04`；跨语言同TC可各有子断言 |
| Given | SSE打开后账号冻结 |
| When | 推进复查时间/断流 |
| Then | 订阅结束、连接数回落、B仍无法读取 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-03-01；`test-artifacts/task-105/C/C105C-03/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105C-04 保存同意、字幕导出与删除墓碑

需求：DH-R12、DH-R17。依赖：C105C-03。必读共享：K03、K05、K06、K09、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptService.java` | NEW | `setPreference/save/list/export/delete` | 主动同意与contentEpoch |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptController.java` | NEW | `API22～26` | 版本/owner/file信封例外 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptIT.java` | NEW | `DigitalHumanTranscriptIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanLifecycleService.java` | MODIFY_AFTER_C105B-01 | `eraseTranscript` | 已有注销step扩展 |
| `tests/contracts/resource-lifecycle.registry.json` | MODIFY | `transcript handler` | 新owner/content清理反例 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureService.java`，`class PersonalDataErasureService`，快照行25起。先核实真实上下文：

```text
@Component
public class PersonalDataErasureService {

	private static final Logger log = LoggerFactory.getLogger(PersonalDataErasureService.class);

	/** 清理回执（§6.5 erase 响应数据；erased=true 仅 state=completed）。 */
	public record ErasureReceipt(String manifestId, String state, boolean erased, Map<String, Long> counts,
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `setPreference(actor,id,boolean save,int expectedVersion,UUID requestId): Mono<TranscriptPreference>。`
- `saveBuffered(actor,id,int expectedVersion,UUID requestId): Mono<TranscriptSaveResult>；final only、同utteranceId一次。`
- `delete(actor,id,UUID requestId): Mono<Operation>；事务先contentEpoch+1并置content_deleted，再异步擦除。`

**严格执行步骤**：

1. 未勾选时dh_transcript零行；打开保存复制尚在buffer的最终文本，草稿delta不落；关闭只停后续，不删除已保存。
2. 结束10分钟内显式保存可用，过期410；删除墓碑后禁止重新保存旧buffer或迟到final，必须开新session才可新增内容。
3. 分页按utteranceSeq，txt导出标角色/UTC时间/中断状态；下载走本人授权Content-Disposition，filename不使用原prompt。
4. 删除立即不可读，后台按contentEpoch清Redis/runtime history，重试幂等；未知runtime删除状态不标物理完成。
5. 同session的独立已保存asset不连带删；页面文案说明该关系，注销时仍走个人所有权清理。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105C-04-01 默认不保存

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R12、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptIT.java`；测试名称包含`tc105c_04_01`；跨语言同TC可各有子断言 |
| Given | 两场分别save=false/true |
| When | 最终文本回调并结束 |
| Then | 默认无持久正文，另一场只final一次 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-04-01；`test-artifacts/task-105/C/C105C-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-04-02 同意切换

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R12、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptIT.java`；测试名称包含`tc105c_04_02`；跨语言同TC可各有子断言 |
| Given | false→true→false |
| When | 三个时点各输出最终句 |
| Then | 仅缓冲追溯+同意期间落库，关闭后不再写 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-04-01；`test-artifacts/task-105/C/C105C-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-04-03 删除迟到竞态

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R12、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptIT.java`；测试名称包含`tc105c_04_03`；跨语言同TC可各有子断言 |
| Given | delete与原contentEpoch final并发 |
| When | 重放旧回调和save |
| Then | 删后不复活，原asset不误删 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-04-01；`test-artifacts/task-105/C/C105C-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-04-04 过期/越权/导出

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R12、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanTranscriptIT.java`；测试名称包含`tc105c_04_04`；跨语言同TC可各有子断言 |
| Given | 超10分钟buffer、B请求A txt |
| When | 保存与下载 |
| Then | 410/404正确，无隐藏文本或reasoning |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-04-01；`test-artifacts/task-105/C/C105C-04/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105C-05 控制面故障注入与交付

需求：DH-R02、DH-R06、DH-R10、DH-R11、DH-R17、DH-R20。依赖：C105C-04。必读共享：K04、K06、K07、K12、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanControlFaultIT.java` | NEW | `DigitalHumanControlFaultIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/edge-bff/src/test/java/com/grassland/edge/proxy/DigitalHumanSseProxyIT.java` | NEW | `SSE passthrough` | 控制HTTP/SSE完整隔离链验证 |
| `platform-realtime/digital-human/tests/test_control_contract.py` | NEW | `Java wrapper contract` | 命令重复/状态回调时序 |
| `tests/contracts/digital-human.contract.test.ts` | MODIFY_AFTER_C105A-02 | `control endpoint matrix` | 对照API07～26必要项 |

**只读源码锚点**：`platform-java/services/edge-bff/src/main/java/com/grassland/edge/proxy/RoutingProxyHandler.java`，`class RoutingProxyHandler`，快照行22起。先核实真实上下文：

```text
@Component
public final class RoutingProxyHandler {
    private final UpstreamResolver resolver;
    private final WebClient webClient;

    public RoutingProxyHandler(UpstreamResolver resolver) {
        this.resolver = resolver;
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `故障fixture注入点固定beforeDBCommit/afterDBCommitBeforeReply/beforeRuntimeDispatch/afterRuntimeAcceptBeforeReply；每点一次可恢复失败。`

**严格执行步骤**：

1. 使用WireMock+真实Postgres复现响应丢失与runtime不可达；CAS/数据库约束用双连接而非串行调用冒充并发。
2. 验证未完成命令可查operation，不自动reset为成功；unknown不能开第二条provider管道。
3. 复核所有控制端点owner/非法状态/旧epoch、默认关、空结果、UTC与各超时临界；测试59,999/60,000/60,001毫秒。
4. 集成SSE代理与服务重启只恢复元数据/必要收尾；未保存文本丢失如实说明，不从日志复原。
5. 交付C后D可以只实现生成执行，不需再补会话安全；不把模型演示成功当C故障验收。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105C-05-01 事务提交响应丢失

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R06、DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanControlFaultIT.java`；测试名称包含`tc105c_05_01`；跨语言同TC可各有子断言 |
| Given | session已提交而HTTP断开 |
| When | 客户端原键重试 |
| Then | 返回原session，无重复create |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-05-01、V105C-05-02、V105C-05-03、V105C-05-04；`test-artifacts/task-105/C/C105C-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-05-02 runtime接受后未知

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanControlFaultIT.java`；测试名称包含`tc105c_05_02`；跨语言同TC可各有子断言 |
| Given | 控制指令已接受但响应丢失 |
| When | 故障恢复查询 |
| Then | 同commandId回读，unknown不自动复制运行 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-05-01、V105C-05-02、V105C-05-03、V105C-05-04；`test-artifacts/task-105/C/C105C-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-05-03 租约/冻结交错

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R06、DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanControlFaultIT.java`；测试名称包含`tc105c_05_03`；跨语言同TC可各有子断言 |
| Given | create、freeze、resume三个并发事务 |
| When | 按屏障排序释放 |
| Then | 不穿透freeze、不复活旧session |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-05-01、V105C-05-02、V105C-05-03、V105C-05-04；`test-artifacts/task-105/C/C105C-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105C-05-04 真实SSE逐块

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R10、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanControlFaultIT.java`；测试名称包含`tc105c_05_04`；跨语言同TC可各有子断言 |
| Given | Edge代理Fake定时三条event |
| When | 测客户端到达 |
| Then | 三次逐步到达并可abort，无全场缓冲 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105C-05-01、V105C-05-02、V105C-05-03、V105C-05-04；`test-artifacts/task-105/C/C105C-05/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

## 12. 验收命令、边界矩阵与阶段出口

所有命令均为**实施后执行合同**，本次写任务书没有运行业务测试。工作目录“仓库根”指`/Users/LXH/claude/y-1`。每条记录开始结束、退出码、实际测试数量、报告路径；0 tests/全部SKIPPED/缓存未执行不能当本次PASS。

### V105C-01-01 Java/intelligence-service（C105C-01）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-01/V105C-01-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanSessionIT' --rerun-tasks --no-build-cache
```

### V105C-02-01 Java/intelligence-service（C105C-02）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-02/V105C-02-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanLeaseIT' --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanReaperTest' --rerun-tasks --no-build-cache
```

### V105C-02-02 Python（C105C-02）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-02/V105C-02-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_leases.py -q
```

### V105C-03-01 Java/intelligence-service（C105C-03）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-03/V105C-03-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanEventIT' --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanContentBufferTest' --rerun-tasks --no-build-cache
```

### V105C-04-01 Java/intelligence-service（C105C-04）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-04/V105C-04-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanTranscriptIT' --rerun-tasks --no-build-cache
```

### V105C-05-01 Java/intelligence-service（C105C-05）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-05/V105C-05-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanControlFaultIT' --rerun-tasks --no-build-cache
```

### V105C-05-02 Java/edge-bff（C105C-05）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-05/V105C-05-02.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:edge-bff:test --tests 'com.grassland.edge.proxy.DigitalHumanSseProxyIT' --rerun-tasks --no-build-cache
```

### V105C-05-03 Python（C105C-05）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-05/V105C-05-03.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_control_contract.py -q
```

### V105C-05-04 Vitest（C105C-05）

目录：`仓库根`；shell：bash。前提/影响：当前npm lock与Node工具链；新增测试必须被vitest include覆盖。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/C/C105C-05/V105C-05-04.log`及工具原生报告。

```bash
npm run test -- tests/contracts/digital-human.contract.test.ts
```

### 阶段公共门禁

```bash
npm run quality:lifecycle
npm run docs:links
npm run docs:status
npm run security:secrets
git diff --check
```

`quality:lifecycle`从B建表开始必需；A阶段仅既有基线回归，不要求不存在的DH表。UI阶段追加适用DESIGN lint和明暗截图自查；设计工具失败/真实设备缺失均记录具体范围。Java修改阶段先执行本卡定向再受影响service的`:check`；H02汇总跨域门禁，不能将本书全完成代替下一阶段。
### E01～E22边界追踪

这是整条个人版链路的精确责任矩阵；专项卡未到达时标DEPENDENCY_PENDING，不宣称当前阶段已通过它。当前卡对应情形仍在其四组TC中参数化实现；不相关场景无需在纯锁文件/纯部署卡造UI。

| 编号 | 情形 | 固定输入与预期 | 专项责任/用例 |
| --- | --- | --- | --- |
| E01 | 空输入 | text="  "、persona=""、requestId缺失；422且零派发 | C105B-03 / TC105B-03-02 |
| E02 | 超长输入 | text2001/persona4001码点、SDP65537bytes；422/413 | C105B-03 / TC105B-03-02 |
| E03 | 重复提交 | 同request同体/异体各并发2次；原资源或409 | C105B-03 / TC105B-03-03 |
| E04 | 网络失败 | HTTP接受后断连；只查原键，不换经济键 | C105D-01 / TC105D-01-02 |
| E05 | 服务端错误 | provider500/半流、runtime不可达；受控code与pending | C105D-03 / TC105D-03-02 |
| E06 | 未登录 | 无cookie且无内部身份；401、零业务写 | C105B-02 / TC105B-02-02 |
| E07 | 无权限 | 非platform_admin调用管理端；403 | C105G-03 / TC105G-03-01 |
| E08 | 数据为空 | 无角色/历史/voice/catalog；空态、不能点开始 | C105E-02 / TC105E-02-03 |
| E09 | 数据过期 | grant29999/30000ms、preflight60秒、内容结束10分钟 | C105D-02 / TC105D-02-03 |
| E10 | 页面刷新 | 现有会话刷新、建立新controller后明确接管 | C105C-02 / TC105C-02-01 |
| E11 | 用户快速切换 | A→B→A，同账号epoch已变仍拒旧回包 | C105E-03 / TC105E-03-02 |
| E12 | 组件卸载在途 | 权限对话晚返回/流未完成；stop tracks并清timer | C105E-04 / TC105E-04-02 |
| E13 | 缺值与非法枚举 | null/缺字段/空白/tone=unknown/多余orgId | C105A-02 / TC105A-02-03 |
| E14 | 数值长度边界 | 0/1/max/max+1、2^53、负金额、小数sample | C105A-02 / TC105A-02-03 |
| E15 | 并发乱序 | 两个控制器接管、旧epoch/旧seq迟到 | C105C-02 / TC105C-02-01 |
| E16 | 已提交响应丢失 | asset save完成丢响应；原id重试一次资产 | C105F-03 / TC105F-03-02 |
| E17 | 跨主体 | B访问A会话/avatar/media、组织上下文不能越权 | C105G-01 / TC105G-01-02 |
| E18 | 中途撤权删除 | 六窗口撤权/删除墓碑；晚结果不得复活 | C105G-05 / TC105G-05-03 |
| E19 | 旧配置/旧缓存 | expectedVersion/catalogVersion失效、旧core升级 | C105H-02 / TC105H-02-02 |
| E20 | 部分成功补偿失败 | 原run结算503、录制partial、事件重复 | C105D-06 / TC105D-06-01 |
| E21 | 时区/金额 | UTC日切与[from,to)、cents整数、冻结价表 | C105G-01 / TC105G-01-01 |
| E22 | 大文件列表长文案 | 10MiB+1头像、200MiB录制、101条limit、长中文布局 | C105F-01 / TC105F-01-02 |

阶段出口：本书本地必需TC/V与范围门禁通过、前置合同无漂移、所有新增资源有handler/test、下阶段能使用已验证真实接口；条件真实缺口列明。不得将FAKE_PASS写成REAL_QUALIFIED。

## 13. 何时阻塞，何时继续

明确阻塞当前依赖路径：公开字段/权限/资金口径矛盾；现有改动与本卡无法共存；必需前卡失败；已批准模型仍无法满足核心协议且需要换方案。报告必须含卡号、准确文件/符号、最小复现、预期/实际、已排查项与最小所需决定。
可继续：行号/空白变化、正常实现细节、本地依赖安装、已有服务复用、Fake测试；真实硬件/许可/付费授权缺少时，只停止真实实验和开放，不阻塞明确无依赖的本地开发。禁止无限重试、假数据冒充真实或降低门槛。

## 14. 对话交接与执行提示词

逐卡在对话给出：卡号及状态；新增/修改文件；TC PASS/FAIL/NOT_RUN与命令/退出码/报告路径；前置既有失败；越界检查；下一卡。无需另建完成报告。用户明确要求运行手册与机器验收证据的文件已在白名单中，按卡更新即可。
可将下面提示词连同本书交给执行模型：

```text
执行 docs/任务书/草场任务书-105C-数字人工作台-阶段二-会话控制与可靠恢复.md，从 C105C-01 开始，采用 AUTO_CHAIN。
先核对当前卡前置，再读 AGENTS.md、适用 DESIGN.md、任务书第0/9/13/14节和该卡明确引用的共享契约K节。
只修改本卡与全局白名单的交集，保留现有工作区修改，不实现未定义的扩展。
每卡完成后真实运行列出的TC/V并汇报，再按依赖自动推进本阶段，不为普通本地步骤逐次等确认。
真实条件不具备如实标NOT_RUN，继续本书中无依赖的本地工作；不能自动部署、打开生产开关或把Fake当真实验收。
业务或资金决策冲突时给出精确阻塞点，不自己换方案。
```
