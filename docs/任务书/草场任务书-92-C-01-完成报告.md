# 任务书 #92 卡 C-01 完成报告

## 实现结果

- 卡号：C-01（修复创作入口、来源状态与能力矩阵）
- 完成状态：VERIFIED
- 行为变化：
  - `ai.html?capability=&storeId=&taskId=` 深链由统一函数解析为页面级 `CreationSourceContext` 单一状态；非法/缺失 `capability` 回退 `article`，未知 query 键一律忽略（不读不写敏感参数）。
  - AI 创作中心顶部（能力导航下方）新增来源胶囊条：能力 chip（文章/图片/视频/朋友圈）+ 来源胶囊（门店/任务），刷新与板块切换均保持；清除按钮只移除 `storeId/taskId`（`router.replace` 重写 query），能力与其它既有 query 原样保留。
  - 门店名走既有公开档案查询（`/api/stores/{id}/public-profile`，未登录也放行）；拉不到时回退 ID 截断态（>12 字符截断加省略号）。同时带门店与任务时以任务为准。
  - 快速连续切换 query 以最后一次为准；迟到的门店名解析经 epoch 守卫丢弃。
  - 既有 `?entry=store&org=&store=`（#76 门店锁定流）与 `?entry=hot&title=&platform=`（热点预填流）行为零改动（strip 清单不含新键，无键名重叠）。
- 未完成项：无。

## 文件与范围

| 文件 | 变更 | 白名单对应 |
|---|---|---|
| `src/ai/router.ts` | 新增 `CreationCapability`/`CreationSourceContext` 类型、`CREATION_CAPABILITIES`、`parseCreationSourceQuery`（仅读 capability/storeId/taskId 三键，trim、ID 64 截断、非法枚举回退 article） | §9.1 `src/ai/router.ts` |
| `src/views/ai-center/AiCreationCenter.vue` | 接入 `useRoute/useRouter`；`sourceContext` 页面级状态 + query watch；来源胶囊条模板与 token 样式；门店名 best-effort 解析（epoch 守卫）；`clearSourceContext` | §9.1 `AiCreationCenter.vue` |
| `src/views/ai-center/AiCreationCenter.test.ts` | 文件级 vue-router mock（reactive query + 语义对齐的 replace）；新增 `TC-C01-001/001b/002/003` 四用例 | V-001 指定测试文件（C-01 验证目标） |
| `scripts/acceptance/shot-92-c01.mjs`（项目根，工具脚本） | 双主题截图与浏览器级断言 | 截图自查工具（非产品代码） |
| `docs/任务书/evidence/92/**` | v001/v002 日志、双主题截图 ×4、截图断言日志 | §9.1 evidence/92/** |

- `AiCenterNavigation.vue` 在允许清单内但无需改动（板块导航已是能力切换载体，胶囊条独立于板块内容）。
- 既有工作区改动（#93 marketplace/finance 批次、`.pi/`、未跟踪 docs 等 88 项）全部保留未动。

## 验证证据

| 项 | 工作目录 | 命令 | 退出码 | 结果 |
|---|---|---|---|---|
| V-001 | 仓库根 | `npm run test -- src/views/ai-center/AiCreationCenter.test.ts src/views/ai-center/AiCenterExternalRedirect.test.ts src/views/ai-center/components/ModelSourceCard.test.ts` | 0 | 3 文件 54 用例全过（含新增 4 例） |
| V-002 | 仓库根 | `npm run typecheck` | 0 | 无类型错误 |
| 基线 V-001 | 仓库根 | 同 V-001（改动前） | 0 | 50 用例全过 |
| 基线 V-002 | 仓库根 | 同 V-002（改动前） | 0 | — |
| 回归（非必需） | 仓库根 | `npm run test -- src/ai/AiAppLayout.test.ts src/App.test.ts` | 0 | 19 用例全过（真实路由集成面） |
| 双主题截图 | 仓库根 | `node scripts/acceptance/shot-92-c01.mjs`（dev server localhost:5173） | 0 | 8/8 断言过 |

- TC-C01-001（+001b）：深链恢复——门店名解析「江畔门店」、任务 ID 截断态、板块切换保持、清除只移除来源（replace 载荷 `{query:{capability:'video'}}`）、重挂载（刷新语义）后能力与来源仍在。
- TC-C01-002：归一函数（空/非法/数组/null query、未知键忽略、trim）+ 组件层空 query 普通入口、非法 capability 默认 article、零导航副作用。
- TC-C01-003：同批连续 query 变更取最后一次；迟到门店档案（deferred resolve）被 epoch 丢弃。
- 证据绝对路径：`/Users/LXH/claude/y-1/docs/任务书/evidence/92/`（v001.log、v002.log、c01-ai-center-light.png、c01-ai-center-dark.png、c01-ai-center-cleared-{light,dark}.png、c01-screenshot.log）。
- 截图自查（analyze_image 双主题）：无截断/重叠/溢出/横向滚动；来源胶囊对比度良好；能力 chip 存在感偏弱为非阻断观感项（token 已按规范使用）。

## 偏差说明

- 测试文件 `src/views/ai-center/AiCreationCenter.test.ts` 不在 §9.1 字面清单，但为 V-001/V-005 指定的验证目标文件，本卡要求的组件测试落于此（与基线记录的测试基线一致）。其余无偏差。
- 修复过程中的实现缺陷（标签 watch 缺 `immediate`、`undefined` 不在 `LocationQueryValue` 值集）在卡内 DoD 验证前修正，非遗留。

## 未解决问题

无。
