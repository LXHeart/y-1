# 任务书 #90 完成报告：商家与推荐官工作台业务逻辑修复与优化

> 任务书版本 1.0.0 ｜ 基线 00c03079 ｜ 报告日期 2026-09-08 ｜ 执行者：ZCode（AUTO_CHAIN C90-01→C90-06）
> 总状态：**C90-01~05 VERIFIED ｜ C90-06 IMPLEMENTED**（代码+单测+截图已查；V90-04 e2e 未有效执行，见 §C90-06）

## 0. 执行环境与并行声明

- 工作区存在**并行会话**（任务书 #92 已完成、#93 刚立项）：其对前端工作台/推广卡/钱包、finance 钱包端点、intelligence 的未提交改动与本任务 C90-06 高度互补（消费了本任务新建的后端端点），全部保留并纳入验证。本报告验证结论基于**合并树**。
- 白名单外/偏差文件（全部主动申报，详见各卡「偏差」）：
  1. `SettlementExecution.java` 及其测试 `SettlementExecutionLadderTest`、`SettlementActivityImplTest`——C90-01 卡文明确要求「结算事件只在 capture 确认成功后写出」，该文件是唯一实现点，白名单未列（任务书内部不一致，按卡文最小足迹执行）。
  2. `docker-compose.yml`（+2 行）：`EDGE_ROUTE_APPLICATIONS_MARKETPLACE`（edge 契约测试强制每 flag 进容器）与 `MARKETPLACE_SYSTEM_ACTOR_ACCOUNT_ID` 本地默认值（并行会话加的空默认会让本地栈启动失败，补为固定 UUID）。
  3. `ApplicationStatus.java`、`SystemActorAccount.java`（新）、V53/V54 迁移（新）——按白名单「相关 DTO/状态/测试及新 migration」条款。

## 1. 验证命令汇总（§12）

| 命令 | 工作目录 | 退出码 | 结果 |
|---|---|---|---|
| V90-01 `npm run test -- --run` | 仓库根 | 0 | 165 文件 / 1511 测试全绿（含并行改动与其测试） |
| V90-02 `npm run typecheck && npm run lint && npm run build` | 仓库根 | 0 | lint 仅既有 WARN（>500 行 composables，非阻塞）；.vue 800 行门禁过 |
| V90-03 `./gradlew :services:marketplace-service:test :services:finance-service:test` | platform-java | 0 | 多轮全绿（最终复核 --max-workers=2；期间 1 次 Kafka IT flaky 复跑即绿，1 次 TaskControllerIT 同） |
| V90-04 `npm run e2e -- --project=chromium` | 仓库根 | — | **未有效执行**：需 ci-e2e 隔离栈（DNS pinning/假 AI 域名/重播种）；对共享栈直跑 12/15 失败均为环境不适配（跨 #92/#65/#8 各域），本地 macOS 起不了隔离栈为项目既有结论 |
| V90-05 `npx @google/design.md lint DESIGN.md` ×2 | 仓库根 | 0 | 两份规范 lint 均过 |
| V90-06 `git diff --check && npm run security:secrets` | 仓库根 | 0 | 2913 文件扫描过 |

数据库/部署证据：本地栈已重建部署（bootJar 探针验真 jar 含新类后 compose build/up）；marketplace 启动日志确认 **V53（application terms reconsent cancel）与 V54（promotion ends at）已在真实库执行**；重建后 `docker image prune -f` 已执行。

---

## 2. 分卡报告

### C90-01 资金闸门与结算对账 — VERIFIED

**实现**：
- finance `release`/`capture` 由 `resolveMerchantOrServices` 收窄为 `requireServices`（marketplace+trust 服务断言；商家用户 403）。新增服务端验证读端点 `GET /api/finance/reservations/{engagementRef}`（D90-02 核对通道，服务断言 + org 自查）。
- marketplace `FinanceEscrowClient.captureVerified`（200 核对组织/引用/冻结金额/收款人；404→`reservation_missing`；409 回读区分 captured(幂等)/released/refunded/scope mismatch）；`release` 升级验证语义（409 回读，captured/refunded 抛异常进对账）。
- `SettlementExecution`：capture 结论 `captured=false` → `SettlementHeld` + ops_case（reason 取自闭集 reservation_missing/released/refunded/scope_mismatch），**绝不发 EngagementSettled**；already-captured 幂等重发确定性 event_id。

**TC/AC 映射**：TC90-001 `EscrowControllerIT.releaseAndCaptureByMerchantForbidden`；TC90-002 `FinanceEscrowClientTest.captureVerifiedConflictWithReleasedState…` + `SettlementExecutionLadderTest.captureReconciliationOutcomeHoldsWithoutSettlingEvent` + `captureNonReservedConflict`（无 FundsCaptured/SplitCompleted 事件）；TC90-003 `duplicateCapturePaysPayeeExactlyOnce`（一条 task_payout 流水+一次 SplitCompleted）；TC90-004 `SettlementActivityImplTest.heldWhenDisputeOpen`（既有，保持绿）。
**修改文件**：FinanceCallerResolver、EscrowController（+GET）、FinanceEscrowClient、SettlementExecution、上述测试（EscrowControllerIT、ReservationReconciliationIT、Wallet*/LedgerProjection/Freebie/CommissionBonus 断言迁移、FinanceEscrowClientTest、Settlement*Test、ApplicationControllerIT/FreebieEscrowFlowIT/ReputationControllerIT mock 迁移）。
**偏差**：§6「capture 仅 marketplace」按 D90-01 保留 trust 服务（`AdjudicationActivityImpl` 争议判推荐官胜诉时直调 capture；trust 不在白名单不可改）。

### C90-02 取消、接受并发、条款重确认 — VERIFIED

**实现**：
- V53 迁移：`task_application` 增 `task_version_at_apply/terms_snapshot_json/reconsent_required/cancelled_at`（另预置 C90-05 的 `settlement_status/settlement_eligible_at`）+ trigger `trg_task_application_terms`（INSERT 冻结版本与条款快照、pending→reconsent 置标记、reconsent→pending 刷快照、→cancelled 置时间戳）+ (task_id,status) 索引；全幂等 DDL。
- 统一闸门（D90-04）：`TaskAcceptanceCounterRepository.claim` join 加 `t.status='published'`（SQL 原子兜底）+ `claimAcceptance` 前置任务状态闸门（closed/cancelled 分文案 409）+ reconsent 态 409「任务条款已更新…」。
- cancel 重构：任务取消同事务 `cancelPendingByTask`（pending/reconsent→cancelled）逐条 `ApplicationCancelled`；响应 `pendingCancelled/refundedCount/compensationPending`；幂等分支重算。
- Saga：`beginAcceptance` 非 published 且 pending → false（资金前中止）；`activateEngagement` 任务取消/离开 reserving → 抛异常转补偿（晚到不激活）；`compensateAcceptance` 取消态回退 `revertReservingToCancelled`（reason=task_cancelled，不留 pending 僵尸）。
- revise 关键条款（赏金/押金/平台/交付形态/交付要求）→ `markReconsentRequiredByTask` + `ApplicationReconsentRequired` 事件；新增 `POST /api/tasks/{id}/applications/{appId}/reconsent`（本人、reconsent 态、快照刷新）；withdraw 扩展 reconsent。
- 修复真实缺陷：cancel sweep 的 `flatMap(outbox.append)` 吞掉行导致 `pendingCancelled` 恒 0（测试驱动发现）。

**TC 映射**：TC90-005 `cancelFinalizesPendingAndBlocksSubsequentAccept`（含批量 accept 失败、无 reserve）；TC90-006 `cancelReportsCompensationPendingForInFlightReserving`（sweep 不越权）+ 单测 `activateEngagement_throwsWhenTaskCancelled…`/`compensateAcceptance_revertsReservingToCancelled…`；TC90-007 `keyRevisionRequiresReconsentBeforeAccept`/`displayOnlyRevisionDoesNotTriggerReconsent`；TC90-008 `cancelKeepsSubmittedEngagementOnContinuationPath` + 并行会话补的 SubmissionRepository 取消前已提交补交放行。
**行为变更（申报）**：closed 任务 accept 由「名额已满」改为「任务已关闭，不可接受报名」（§5 规则 2）；`TaskAutoCloseIT` 断言随之更新。报名状态新增 `cancelled`（§4.2 闭集未含，据 §7 `cancelled_at` 列与 §6 `pendingCancelled` 语义；无争议权、与 refunded 资金语义分离）。

### C90-03 套餐推广资格与结束 — VERIFIED

**实现**：
- V54：`task.promotion_ends_at` + 重建部分唯一索引（`promotion_ends_at IS NULL` 口径——结束的推广释放「每套餐一个进行中推广」占位；谓词 immutable 约束说明在迁移注释）。
- 归因资格：`findActivePromotionTaskId`/`hasAcceptedPromotionApplication` 接受 `published|closed` 且未结束（满员自动关闭不终止已接受推广）；`CommerceService.createOrder` 切换。
- `POST /api/tasks/{id}/end-promotion`（owner+乐观锁；非套餐 409；重复 200 幂等；unlink backfill；`TaskPromotionEnded` 事件）。
- 招募关闭正交化：`TaskController.close` 与 `TaskFullAutoCloser` 不再 unlink（保留归因）；off-sale 联动额外落 `promotion_ends_at`（下架终止的是推广本身）。
**TC 映射**：TC90-009 `slotsFullAutoCloseKeepsAttributionForAcceptedRecommender` + `promotionContinuesAfterRecruitmentClose…`（close 后仍归因/既有订单照常分账）；TC90-010 `promotionContinues…`（end-promotion 后新单自然流量、backfill 清空、快照订单分账不变）+ `endPromotionGuardsAndIdempotency`（非套餐 409、幂等单事件）；TC90-011 既有 `attributionGateSelfPurchaseAndV37Freeze`（自购 0/无资格自然流量）；TC90-012 既有 recommender/merchant promotions 两测试（权限口径）。
**行为变更（申报）**：手动/满员 close 不再终止归因（原 `taskEndFallsNewOrders…` 测试语义按 D90-03 反转重写）。

### C90-04 自动通过系统身份 — VERIFIED

**实现**：`SystemActorAccount`（`marketplace.system-actor-account-id` 或 env `MARKETPLACE_SYSTEM_ACTOR_ACCOUNT_ID`；缺失/非法 UUID **启动失败**，禁止 null——D90-05）；`acceptForDispatcher` 幂等查询/命令 actor/审计 reviewed_by 统一固定账号；UNIQUE(actor,key) 并发冲突回读重放；三个测试上下文基座补属性。
**TC 映射**：TC90-013 `autoAcceptUsesFixedSystemActorAndDeduplicatesAcrossRounds`（命令 actor=系统 UUID、reviewed_by=系统 UUID、第二轮无新命令）；TC90-014 `autoAcceptLeavesBelowLevelApplicationsPending`（保留 pending、零命令）；TC90-015 同 013（双轮=双实例等价幂等收敛）+ `SystemActorAccountTest` 四例；TC90-016 既有 `compensatedApplicationEntersCooldownAndIsSkippedNextRound`（资金不足冷却不空转）。
**部署注意**：生产 overlay 必须注入该 env（compose 本地默认 `00000000-0000-0000-0000-000000000901` 仅供本地/测试）。

### C90-05 修订审核、分页、结算契约 — VERIFIED

**实现**：
- 关键修订重审（D90-06）：`keyTermsChanged` 命中 → `resubmitForReview`（published→pending_review）+ `TaskSubmittedForReview`；`task_version` 旧行不可变（append-only，不覆盖公开版本）；展示字段修订保持 published。
- 互斥校验按提交值（D90-10）：`enforceReviseFundingContract` 以将要写入的值判定；可空资金字段显式 null=清空；`requirements` 列 NOT NULL → 缺省保持、显式 `{}` 清空（恢复 COALESCE 并文档化，杜绝「门槛合并/SQL 清空」的旧矛盾）。
- 报名列表分页（§5 规则 5）：`findByTaskIdPaged` keyset（(created_at,id) 倒序，本人过滤在 LIMIT 前）；端点改 `{items,nextCursor,hasMore}` 信封，limit 默认 20 上限 50；owner 附 stats；6+ 处测试断言迁移；前端 `listApplications` 兼容两种形状。
- 详情直查：`GET /api/tasks/{id}/applications/{appId}`（owner/本人可见，无关方 404）。
- 结算契约（D90-09）：`GET /api/applications/{id}/settlement`（新 edge 前缀 `/api/applications` 已登记 RouteManifest+compose flag）→ confirmedAt/settlementEligibleAt（T+N 预计到账）/settlementStatus/holdReason/allowedActions（按观察者角色闭集推导）。
**TC 映射**：TC90-017 `keyRevisionEntersPendingReviewWhileOldVersionSnapshotsRemain`；TC90-018 `applicationListPagesBeyondTwoHundredRows`（206 条含 205 直插翻页到底，limit 500 截 50）；TC90-019 `applicationDetailIsDirectlyAddressable`；TC90-020 `settlementContractExposesRecoverableState`（settling+confirmedAt+eligibleAt 不报失败、商家 contest、无关方 404）；TC90-022 `reviseFundingModeTransitionsAreValidatedOnSubmittedValues`；TC90-023 乐观锁既有 `reviseBumpsVersion…` + 各 409 断言；TC90-021 刷新恢复=状态可服务端恢复（详情/结算端点）+ 既有 `useWorkbenchRecovery` 测试。

### C90-06 工作台状态、收益视图、集成 — **IMPLEMENTED**

**覆盖（合并树）**：
- 票据闸（D90-08）：`useWorkbenchEngagements` 重写为 capture/isCurrent+context+sequence 三重闸（A→B→A 无旧写入）；既有 `GrasslandWorkbench` 组织隔离测试族保持绿。
- 详情直查/结算等待：`getApplication`/`getApplicationSettlement` 前端接入；`useWorkbenchSettlement.settlementLabel` 显示「已确认，预计 … 到账」。
- 套餐详情：`TaskDetailModal` commerce 任务展示 `RecommenderShareCard`（推广链接/订单/核销/佣金 + promotionEnded 态）且 `!commercePackageId` 才出现提交凭证与 actions-extra（无普通凭证按钮）。
- 钱包五分组：finance 钱包端点附 `positions/withdrawingCents`（并行会话）；`MyWalletCard` 可提现/托管押金/待结算/暂扣/提现处理中。
- 刷新恢复：URL 状态 + 详情/结算端点可恢复确认/评分/结算态（#84 既有 recovery）。

**截图（R-UI，已实际查看）**：`test-artifacts/task-90/shots/` 12 张（文件名=端-路由-主题-状态）：
商家 tasks/finance × 暗亮（桌面 1440×900）、推荐官 hall/my-tasks/earnings × 暗亮、移动端 390×844 2 张。抽查 4 张（analyze_image）：无渲染缺陷、对比度/层级合规、钱包五分组可见、亮色主题紫主色正常；推广卡空态隐藏属预期。

**未完成（如实）**：
1. **V90-04 e2e chromium 未有效执行**——需 ci-e2e 隔离栈（重播种/DNS pinning/假 AI 域名），本地 macOS 不可行（项目既有结论）；对共享栈直跑的 12 失败均为跨域环境不适配（含 #92 契约），不作数。待隔离环境补跑。
2. §12.3 浏览器级全链集成冒烟未做（同因）；其数据库级等价链路已由 `CommercePromotionTaskIT`（下单→核销→冷静期→分账）与 `ApplicationControllerIT`（确认→T+2→settled）覆盖。

---

## 3. 遗留与建议

1. V90-04/浏览器级冒烟待 CI 或隔离环境补跑（可直接复用 `scripts/local/snap-task90.mjs` 与本报告映射）。
2. 生产 overlay 需注入 `MARKETPLACE_SYSTEM_ACTOR_ACCOUNT_ID`（部署级固定 UUID）与 `EDGE_ROUTE_APPLICATIONS_MARKETPLACE`。
3. owner 报名列表的「声誉权重排序」现为页内展示序（DB 按 created_at 翻页）——全局权重序与 keyset 分页的融合建议单独立项（§5 分页规则优先的取舍，已在代码注释注明）。
4. `requirements` 的清空语义受 NOT NULL 列约束（缺省保持/显式 {} 清空）；如需彻底 D90-10 化需先改列约束，另行拍板。
