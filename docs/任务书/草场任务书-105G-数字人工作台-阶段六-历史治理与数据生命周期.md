# 草场 #105G：数字人工作台 阶段六-历史治理与数据生命周期

| 项目 | 内容 |
| --- | --- |
| 版本/日期 | 2.1 / 2026-09-22 |
| 文档状态 | IMPLEMENTED（2026-09-24）：v2.1规格；五卡按 AUTO_CHAIN 落地并实跑 TC/V；真实第三方渲染/实机仍单独门禁（REAL_NOT_RUN） |
| 实施/用例 | IMPLEMENTED / TC105G-01～05 全 PASS（本地真实 DB/容器/三引擎浏览器，证据 test-artifacts/task-105/G/）；真实 provider 项 NOT_RUN |
| 卡/用例 | 5张任务卡，20组Given/When/Then用例；组内边界需参数化 |
| 基线 | 草场f2976aaeb329ee17e9e08a8997bffaabde78d3ac + 当前#103/#104既有修改；OpenTalking固定8c739a5a6f114daf71aeace832a668c3ad60f536 |
| 前置 | 105F媒体闭环通过；#104个人与组织归属守卫、清理竞争修复有真实测试证据 |
| 方式 | AUTO_CHAIN：卡级通过自动进入本阶段下一卡；不启子代理，不自动越到未指定的下一本或生产开放 |
| 入口 | [#105总纲](草场任务书-105-数字人工作台开发计划.md) · [PRD](../产品/数字人工作台需求文档.md) · [共享契约](草场任务书-105-数字人工作台共享契约.md) |

## 0. 开始与完成协议

本书供编码模型直接执行。先读本节、§9、§13、§14，再读当前卡列明的共享K节、源码和测试。CARD PASS才推进；已有真实服务授权在同范围内持续有效，不逐卡重复申请。普通本地依赖、Fake、合成数据库与浏览器验证可直接进行。
先保存`git status --short`和当前diff；不得清空工作区或覆盖已有#103/#104修改。行号漂移以本卡真实符号定位；需要改变业务契约/归属/资金规则才停止受影响卡，不因发现未提交文件而全部停工。所有新增接口都应拒绝未知字段，无密码/密钥进入证据。
完成必须同时有实现、非零用例、卡级命令退出0、适用边界/视觉检查、范围检查和对话交接。NOT_RUN不是PASS；真实条件不足只阻塞相应real/beta门禁，保留Fake可继续的明确路径。

## 1. 本阶段目标与范围

补全个人记录、删除与注销、后台处置、费用核对及内容脱敏，使运行中的功能可以安全停用和清理。

| 需求 | 负责卡 | 验收组 |
| --- | --- | --- |
| DH-R02 | C105G-02、C105G-05 | TC105G-02-01～TC105G-02-04、TC105G-05-01～TC105G-05-04 |
| DH-R11 | C105G-01、C105G-02 | TC105G-01-01～TC105G-01-04、TC105G-02-01～TC105G-02-04 |
| DH-R12 | C105G-01 | TC105G-01-01～TC105G-01-04 |
| DH-R14 | C105G-01 | TC105G-01-01～TC105G-01-04 |
| DH-R15 | C105G-03、C105G-04、C105G-05 | TC105G-03-01～TC105G-03-04、TC105G-04-01～TC105G-04-04、TC105G-05-01～TC105G-05-04 |
| DH-R16 | C105G-03、C105G-05 | TC105G-03-01～TC105G-03-04、TC105G-05-01～TC105G-05-04 |
| DH-R17 | C105G-01、C105G-02、C105G-05 | TC105G-01-01～TC105G-01-04、TC105G-02-01～TC105G-02-04、TC105G-05-01～TC105G-05-04 |
| DH-R18 | C105G-03、C105G-04、C105G-05 | TC105G-03-01～TC105G-03-04、TC105G-04-01～TC105G-04-04、TC105G-05-01～TC105G-05-04 |
| DH-R19 | C105G-04、C105G-05 | TC105G-04-01～TC105G-04-04、TC105G-05-01～TC105G-05-04 |
| DH-R20 | C105G-02、C105G-03、C105G-05 | TC105G-02-01～TC105G-02-04、TC105G-03-01～TC105G-03-04、TC105G-05-01～TC105G-05-04 |

范围外：组织共享、知识库/记忆、对外客服/直播、克隆真人声音、摄像头驱动、任意模型URL、自动交易工具；不顺手改文章/视频工具或前序缺陷。未被本卡白名单列出的文件一律只读。

## 2. 仓库上下文与只读基线

前端Vue/TypeScript，AI独立入口`src/ai/router.ts`及`AiAppLayout.vue`；治理台`src/ops/admin/adminTabs.ts`；Java控制域在intelligence-service，Edge转发；Python实时域为本系列新增目录。现有鉴权、预算、媒体、生命周期只作定点复用。
必读根AGENTS.md与`docs/架构/目录结构.md`。涉及用户/AI UI读根DESIGN.md，治理UI读src/ops/DESIGN.md，共享组件默认根规范。每卡源码摘录是2026-09-21快照，仅作定位；真实实现优先核对签名和上下文。

## 3. 已定技术决策

> 2026-09-22 v2.1修订：K14是模型来源与扩展的统一约束。本地指应用开发、媒体处理和Fake测试，不包括任何模型推理服务；上游源码中的local后端仅是研究事实，不是实施选项。A旧Fake记录保留，v2.1修订复验已通过，可按本阶段前置推进。

Java拥有owner/会话/费用/清理事实；Python持有每会话Binding与媒体，无长期供应商key；Vue只有同源控制资格。首期STT为按键提交后内存整段转写；LLM采用受控计量流；TTS采用24000Hz PCM桥且沿既有平台资助模式；RTC只下行音视频。打断关闭旧peer并重建，租约独立回收。
全部模型均来自第三方服务，通过现有模型控制面使用与扩展；包括STT、LLM、TTS、数字人渲染及涉及模型的形象预处理，禁止本地或自托管模型部署。新增渲染能力沿现有模型/凭据/受信端点/价表链路扩展，不另建模型配置体系，不用env或renderer-profile保存供应商地址、模型名及key。Fake仅用于隔离测试，全链外网请求为0。详见共享K14；配置默认关闭，S1内测、S2个人试点分别验收。

## 4. 目标行为与状态

全局状态迁移见共享K04；本阶段只实现各卡明确触点，不增同义state。session、turn、invocation、settlement、recording、cleanup互相独立：会话ended不表示费用已结或文件已删。失联回收≤45秒、正常结束P95≤5秒，都是待实现/实测目标。

## 5. 业务不变量

首期owner固定个人，orgId不由浏览器提交。账号+epoch变化使旧异步结果无效。每动作requestId创建一次；同键异payload409，同键重放先owner再receipt。费用unknown不填0、不自动重放provider。原始mic不入磁盘、素材库、日志；未同意文本仅有界易失缓冲。

## 6. 接口、字段与内部签名

公开API01～40见共享K03，内部15端点与NDJSON见K07.1～K07.3，管理6端点见K10。金额单位cents、时长ms、UTC时间、JS安全整数、可null/必填及错误码见K01。类型不得用any/无约束map逃避合同；当前卡精确签名见§11。

## 7. 数据、事务与迁移

共享K05是DDL合同，B核心迁移一次创建B/C/D/G核心表；F另建媒体表。执行时按intelligence最大Flyway整数+1替换唯一NEW_DYNAMIC路径中的NEXT，先记录实际文件名，禁止修改V86/V87及其他历史SQL。锁顺序account gate→session→turn/recording→invocation/operation→media；不持事务等待网络。

## 8. UI与用户可观察状态

本阶段涉及UI。共享K11固定布局/组件与设备行为；只用现有token及gl-field/gl-btn-primary/EmptyState/GlModal。新增token必须明暗成对且先补定义（若本卡无style.css写权限，优先现有token；确需新增须修订本卡白名单）。每页loading/empty/error/submitting/disabled/result齐全；1440×900和390×844×明暗截图实际打开检查，另320px、200%缩放、键盘focus。Vue≤800行，composable承载业务，URL独立。

## 9. 写入白名单与禁止项

下面是阶段全局上限；每卡仍只可改其§11表内文件，取两者交集。v2.1新增的§9.1修订表同时补充阶段与指定卡白名单，按表中责任卡执行；其它卡不能借用。NEW仅首创，MODIFY_AFTER表明前卡已创建，不得重建覆盖。只读锚点不是写权限。测试产物可写test-artifacts/task-105/G及卡内指定`test-artifacts/task-105/`子目录（Git忽略），不能放根目录或提交用户内容。

| 完整仓库相对路径 | 动作 | 用途 |
| --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryService.java` | NEW | owner过滤/keyset/结束后清理 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryController.java` | NEW | 过滤器、时间窗、幂等 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `src/views/digital-human/components/DigitalHumanHistory.vue` | NEW | 筛选/分页/已保存/待核对 |
| `src/views/digital-human/composables/useDigitalHumanHistory.ts` | NEW | 请求代次/删除operation轮询 |
| `src/views/digital-human/composables/useDigitalHumanHistory.test.ts` | NEW | 筛选乱序/越界/换号 |
| `src/views/digital-human/DigitalHumanWorkbench.vue` | MODIFY_AFTER_C105E-01 | 挂载history组件 |
| `src/style.css` | MODIFY | 复用现有token/gl类；必要数字人布局与新增token明暗成对，不重写全局主题 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanLifecycleService.java` | MODIFY_AFTER_C105B-01 | 全资源清单+元数据保留 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanCleanupWorker.java` | NEW | 有界重试/逐对象证据 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/IntelligenceJobInventory.java` | MODIFY | 加入录制/保存/cleanup未完 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureRepository.java` | MODIFY | 个人归属、组织保留按既有Scope |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureService.java` | MODIFY | 顺序与verified约束 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataObjectCleanup.java` | MODIFY | 逐key删除与复核 |
| `platform-realtime/digital-human/src/grassland_dh/cleanup.py` | NEW | 租约失效/重启/文件清理 |
| `platform-realtime/digital-human/tests/test_cleanup.py` | NEW | 部分失败/路径保护 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanErasureIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanCleanupRaceIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/contracts/resource-lifecycle.registry.json` | MODIFY | producer/handler/verifier全覆盖 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminController.java` | NEW | requireAdmin/DTO/审计 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminService.java` | NEW | 乐观锁、证据、原经济键 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminAuditRepository.java` | NEW | 不可变脱敏处置审计 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminReconcileIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/edge-bff/src/main/resources/application.yml` | MODIFY | 仅现有admin身份路由 |
| `platform-java/services/edge-bff/src/test/java/com/grassland/edge/proxy/DigitalHumanRoutesTest.java` | MODIFY_AFTER_C105B-02 | 普通用户无管理权限 |
| `src/ops/admin/adminTabs.ts` | MODIFY | digital-human内容AI组，platform_admin |
| `src/ops/admin/tabs/AdminDigitalHumanPanel.vue` | NEW | 配置/会话/核对三个子区 |
| `src/ops/admin/components/DigitalHumanConfigForm.vue` | NEW | 版本冲突与能力证据 |
| `src/ops/admin/components/DigitalHumanSessionTable.vue` | NEW | 元数据/结束确认 |
| `src/ops/admin/components/DigitalHumanReconcilePanel.vue` | NEW | 证据/用量/不能改金额 |
| `src/ops/admin/composables/useDigitalHumanAdmin.ts` | NEW | 串行轮询/请求代次 |
| `src/ops/admin/composables/useDigitalHumanAdmin.test.ts` | NEW | 失活/错序/权限 |
| `src/ops/admin/tabs/AdminDigitalHumanPanel.test.ts` | NEW | 角色/状态/焦点 |
| `src/ops/admin/AdminView.test.ts` | MODIFY | 新页签与原权限不变 |
| `src/ops/admin/admin-shared.css` | MODIFY | 仅治理数字人面板样式，token来自ops规范 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanMetrics.java` | NEW | P50/P95/错误/cleanup/unknown |
| `platform-realtime/digital-human/src/grassland_dh/metrics.py` | NEW | phase/backend/error有限标签 |
| `platform-realtime/digital-human/tests/test_privacy.py` | NEW | 日志/Redis/文件/异常脱敏 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanPrivacyIT.java` | NEW | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/e2e/digital-human-governance.spec.ts` | NEW | 停用/终止/未知用量/注销 |
| `scripts/acceptance/task-105-privacy-check.ts` | NEW | 合成标记扫描/零外发 |
| `tests/acceptance/task-105-privacy-check.test.ts` | NEW | 正文/密钥/SDP反例 |
| `tests/fixtures/digital-human/privacy-manifest.json` | NEW | 仅合成隐私扫描输入 |

禁止：改根依赖/lock（除本书明确列入）、改历史迁移、给旧大视图增业务逻辑、修改组织BYOK/取消退款口径、持久化grant/key/原音频、更新既有生产开关、全仓格式化、删除失败测试、杜撰测试PASS。共用契约/PRD只读；发现实质冲突先按§13给具体修订建议。

### 9.1 v2.1修订补充白名单与执行要求

本表同时属于阶段与所列卡的写入上限；变更仅为K14落实，保留现有其它能力语义。

| 责任卡 | 完整仓库相对路径 | 动作/责任 |
|---|---|---|
| C105G-02 | `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRenderService.java` | MODIFY_AFTER_C105F-01：远端avatar/session回收与注销进度核验 |
| C105G-02 | `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanRenderProvider.java` | MODIFY_AFTER_C105F-01：供应商删除结果查询与幂等恢复 |

TC105G-02及原注销测试必须包含远端资源未删除/已删除/未知、账号墓碑阻断迟到供应商结果；远端未确认不得报complete。G03/G04模型配置跳转现有AI模型管理，数字人面板只编辑组合与业务开关。

## 10. 卡顺序、依赖与估算

| 卡 | 目标 | 依赖 | 估计人日 | 初始状态 |
| --- | --- | --- | --- | --- |
| C105G-01 | 个人历史检索与异步删除 | C105F-05 | 3～4 | VERIFIED（V105G-01-01 exit0 HistoryIT；V105G-01-02 exit0 5用例；V105G-01-03 视觉13/13） |
| C105G-02 | 完整注销与可验证物理清理 | C105G-01 | 4～6 | VERIFIED（V105G-02-01 exit0 ErasureIT 2/CleanupRaceIT 1；V105G-02-02 exit0 3用例；V90 修复 dh_guard 表达式） |
| C105G-03 | 管理员配置、终止与费用核对 | C105G-02 | 3～5 | VERIFIED（V105G-03-01/02 exit0；AdminIT 3/ReconcileIT 1/RoutesTest 4） |
| C105G-04 | 治理台独立面板与权限可见性 | C105G-03 | 2～4 | VERIFIED（V105G-04-01 exit0 47用例；V105G-04-02 typecheck exit0；lint/双主题截图/滚动与焦点探针过） |
| C105G-05 | 治理闭环与日志隐私门禁 | C105G-04 | 3～4 | VERIFIED（V105G-05-01..05 五条 exit0；e2e 三引擎各4过1按设计skip；扫描239文件0违规；撤销矩阵六窗+指标基数双绿） |

人日是包含实现/测试/调试的计划区间，不是模型运行时长或日历承诺；第三方服务开通/服务条款核验及实机等待不计。A04(Fake)只要求隔离Fake与缺口清单；H03(conditional-beta)只约束真实开放。

## 11. 逐卡执行规格

### C105G-01 个人历史检索与异步删除

需求：DH-R11、DH-R12、DH-R14、DH-R17。依赖：C105F-05。必读共享：K03、K04、K05、K09、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryService.java` | NEW | `list/delete` | owner过滤/keyset/结束后清理 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryController.java` | NEW | `API09/API38` | 过滤器、时间窗、幂等 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryIT.java` | NEW | `DigitalHumanHistoryIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `src/views/digital-human/components/DigitalHumanHistory.vue` | NEW | `history list` | 筛选/分页/已保存/待核对 |
| `src/views/digital-human/composables/useDigitalHumanHistory.ts` | NEW | `list/remove` | 请求代次/删除operation轮询 |
| `src/views/digital-human/composables/useDigitalHumanHistory.test.ts` | NEW | `history cases` | 筛选乱序/越界/换号 |
| `src/views/digital-human/DigitalHumanWorkbench.vue` | MODIFY_AFTER_C105E-01 | `assembly` | 挂载history组件 |
| `src/style.css` | MODIFY | `DH reusable styles only` | 复用现有token/gl类；必要数字人布局与新增token明暗成对，不重写全局主题 |

**只读源码锚点**：`src/stores/account-session.ts`，`export interface AccountSessionPort`，快照行20起。先核实真实上下文：

```text
/** 消费方（设置/积分/身份/工作台等私有域）与账号会话的唯一对接面。 */
export interface AccountSessionPort {
  capture(): AccountTicket
  isCurrent(ticket: AccountTicket): boolean
}

/**
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `list(PersonalActor actor,HistoryFilter filter,String cursor,int limit): Mono<Page<SessionSummary>>；HistoryFilter只含profileId/state/from/to。`
- `delete(PersonalActor actor,UUID sessionId,UUID requestId): Mono<Operation>；先标deleted_at和content_deleted，再结束runtime/清正文与临时产物。`

**严格执行步骤**：

1. 列表一律owner=当前个人账号，from/to为UTC半开区间且≤90天，按createdAt/id倒序keyset；删掉行不导致重复分页，不额外COUNT全表。
2. 删除请求事务立即设置墓碑/轮换contentEpoch，新订阅下载返回404，清理异步；终止失败显示operation running/failed，不能先提示全部物理删除。
3. 资产独立归属：删除session不删除已保存视频、附属SRT或账务；UI确认明确列出保留内容，用户需在素材库另行删除。
4. 历史不持久显示未经保存的正文；查看详情只调用已保存transcript/recording，已删除session不复活，旧链接不暴露存在性。
5. UI过滤变更取消旧读并清分页cursor；快切筛选的旧回复不得覆盖新列表，空/错/加载/删除中分别展示。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105G-01-01 分页与UTC

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R11、DH-R12、DH-R14、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryIT.java`；测试名称包含`tc105g_01_01`；跨语言同TC可各有子断言 |
| Given | 21场会话+同时间UUID并列+跨北京时间午夜 |
| When | 20条分页及from/to过滤 |
| Then | 不重不漏、UTC边界唯一，费用pending保留 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-01-01、V105G-01-02；`test-artifacts/task-105/G/C105G-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-01-02 隔离与伪造cursor

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R11、DH-R12、DH-R14、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryIT.java`；测试名称包含`tc105g_01_02`；跨语言同TC可各有子断言 |
| Given | B拿A id/cursor及org上下文 |
| When | 查询/删除 |
| Then | 统一404或非法cursor422，不返A数据 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-01-01、V105G-01-02；`test-artifacts/task-105/G/C105G-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-01-03 删除活动会话

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R11、DH-R12、DH-R14、DH-R17 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanHistoryIT.java`；测试名称包含`tc105g_01_03`；跨语言同TC可各有子断言 |
| Given | A有活动runtime、临时段和独立asset |
| When | 并发delete/迟到final/save |
| Then | 即时禁读，旧事件不复活，asset仍可用 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-01-01、V105G-01-02；`test-artifacts/task-105/G/C105G-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-01-04 前端乱序

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R11、DH-R12、DH-R14、DH-R17 |
| 实现位置 | `src/views/digital-human/composables/useDigitalHumanHistory.test.ts`；测试名称包含`tc105g_01_04`；跨语言同TC可各有子断言 |
| Given | 筛选1慢、筛选2快，随后换号 |
| When | 依次返回 |
| Then | 只当前账号和筛选状态提交，无旧正文残留 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-01-01、V105G-01-02；`test-artifacts/task-105/G/C105G-01/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105G-02 完整注销与可验证物理清理

需求：DH-R02、DH-R11、DH-R17、DH-R20。依赖：C105G-01。必读共享：K04、K05、K09、K10、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanLifecycleService.java` | MODIFY_AFTER_C105B-01 | `eraseAll/verifyResidue` | 全资源清单+元数据保留 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanCleanupWorker.java` | NEW | `claim/retry/verify` | 有界重试/逐对象证据 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/IntelligenceJobInventory.java` | MODIFY | `DH all active kinds` | 加入录制/保存/cleanup未完 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureRepository.java` | MODIFY | `DH manifest and residue` | 个人归属、组织保留按既有Scope |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureService.java` | MODIFY | `DH cleanup steps` | 顺序与verified约束 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataObjectCleanup.java` | MODIFY | `DH derived objects` | 逐key删除与复核 |
| `platform-realtime/digital-human/src/grassland_dh/cleanup.py` | NEW | `cleanup_session/scan_orphans` | 租约失效/重启/文件清理 |
| `platform-realtime/digital-human/tests/test_cleanup.py` | NEW | `cleanup cases` | 部分失败/路径保护 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanErasureIT.java` | NEW | `DigitalHumanErasureIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanCleanupRaceIT.java` | NEW | `DigitalHumanCleanupRaceIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/contracts/resource-lifecycle.registry.json` | MODIFY | `DH complete graph` | producer/handler/verifier全覆盖 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureScope.java`，`class PersonalDataErasureScope`，快照行13起。先核实真实上下文：

```text
 */
final class PersonalDataErasureScope {

	private PersonalDataErasureScope() {
	}

	/**
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `eraseAll(String accountId,UUID manifestId): Mono<ErasureProgress>；包含profile revision、transcript、avatar、preview、buffer、runtime目录、未保存recording。`
- `verifyResidue(String accountId,UUID manifestId): Mono<ResidueReport>；各类remaining计数，unknown/failed不计0。`
- `scan_orphans(root:Path,now:datetime) -> CleanupReport；只操作manifest已登记且过期的受控目录，符号链接不跟随。`

**严格执行步骤**：

1. 读取最新#104实现后加DH步骤，不复制旧SQL覆盖Scope；先拒新写/撤资格/终止session，再清文本/对象，经济未核对留脱敏事实。
2. 临时音频本来不落盘，仍扫描禁止目录与日志；preview内存清除、Redis buffer删除、runtime history清除都纳入ack/截止而非只删Postgres。
3. 清理claim/retry沿既有media引用与注销锁；manifest先持久，再发删除，失败重试1/5/30分钟上限10次后告警保failed，不把404之外异常当成功。
4. 已保存资产按当前owner/组织保留规则；session删除与账号注销策略不同，账户全删除仍须按既有素材归属处理，不能永远保留全部asset。
5. 注销verified仅所有可删资源0残留、受保留资源有明确retained原因、经济活动处理符合现有屏障后成立；DB无行不是文件清完证据。
6. 扩展逐对象清理含dh_asset_attachment，session删除保独立asset附件，asset删除后再回收附件；验证主引用与SRT引用均受同锁防GC。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105G-02-01 逐类清理

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanErasureIT.java`；测试名称包含`tc105g_02_01`；跨语言同TC可各有子断言 |
| Given | A各类DH对象、B对象、已转组织对象 |
| When | A注销两次并重启worker |
| Then | A可删清净，B/组织按Scope保留，结果幂等 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-02-01、V105G-02-02；`test-artifacts/task-105/G/C105G-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-02-02 删除中写入

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanCleanupRaceIT.java`；测试名称包含`tc105g_02_02`；跨语言同TC可各有子断言 |
| Given | freeze与turn/save/晚回调并发屏障 |
| When | 交错提交 |
| Then | 无新内容/引用逃过清单，终态仅允许单向收尾 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-02-01、V105G-02-02；`test-artifacts/task-105/G/C105G-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-02-03 文件失败与孤儿

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_cleanup.py`；测试名称包含`tc105g_02_03`；跨语言同TC可各有子断言 |
| Given | 对象删除503、runtime被kill、残留manifest |
| When | 重试/重启扫描 |
| Then | 未完成保持failed/running，成功后逐key核验 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-02-01、V105G-02-02；`test-artifacts/task-105/G/C105G-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-02-04 路径与共享引用

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R11、DH-R17、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_cleanup.py`；测试名称包含`tc105g_02_04`；跨语言同TC可各有子断言 |
| Given | manifest含../、软链或共享asset引用 |
| When | cleanup |
| Then | 拒绝危险路径并告警，无出根或误删 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-02-01、V105G-02-02；`test-artifacts/task-105/G/C105G-02/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105G-03 管理员配置、终止与费用核对

需求：DH-R15、DH-R16、DH-R18、DH-R20。依赖：C105G-02。必读共享：K02、K08、K10、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminController.java` | NEW | `ADMIN01～06` | requireAdmin/DTO/审计 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminService.java` | NEW | `config/terminate/reconcile` | 乐观锁、证据、原经济键 |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminAuditRepository.java` | NEW | `append` | 不可变脱敏处置审计 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminIT.java` | NEW | `DigitalHumanAdminIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminReconcileIT.java` | NEW | `DigitalHumanAdminReconcileIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `platform-java/services/edge-bff/src/main/resources/application.yml` | MODIFY | `admin digital-human route` | 仅现有admin身份路由 |
| `platform-java/services/edge-bff/src/test/java/com/grassland/edge/proxy/DigitalHumanRoutesTest.java` | MODIFY_AFTER_C105B-02 | `admin route tests` | 普通用户无管理权限 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/contentsafety/ContentSafetyLexiconAdminController.java`，`requireAdmin(`，快照行35起。先核实真实上下文：

```text
    public Mono<Map<String, Object>> list(ServerWebExchange exchange) {
        return callers.requireAdmin(exchange.getRequest())
                .then(lexicons.repository().list().map(this::summary).collectList())
                .map(items -> success(Map.of("items", items)));
    }

    @GetMapping("/{id}")
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `updateConfig(AdminActor actor,UpdateConfig input): Mono<AdminConfig>；expectedVersion/requestId/reason为必填。`
- `reconcile(AdminActor actor,UUID invocationId,ReconcileInput input): Mono<Operation>；不能接受金额字段，只接受providerEvidenceRef与有证据的UsageUnits。`

**严格执行步骤**：

1. 全部接口调用requireAdmin，当前只有platform_admin；运营其他role不因前端出现入口而自动获权。
2. 配置字段白名单按K10，不接任意URL/secret/原始JSON；capacity大于实测、test_only用于生产、无许可approved、speech协议未批准均422。
3. newSessionsAllowed关闭只拒新建，已存在的end/delete/settlement可继续；enabled关闭停新业务但不失去清理入口。
4. 管理员terminate先落审计与幂等意图，沿正常end/reaper，失败真实可查；理由1～200只保存处置理由，不抄用户聊天文本。
5. unknown核对先CAS expectedVersion及证据格式，使用原run的冻结价表执行既有settle/compensate；同证据重放不二次结算，缺证据保持unknown，禁止直接改credits表。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105G-03-01 角色隔离

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R15、DH-R16、DH-R18、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminIT.java`；测试名称包含`tc105g_03_01`；跨语言同TC可各有子断言 |
| Given | 普通用户/customer_service/finance/platform_admin |
| When | 调用六管理端点 |
| Then | 前三403，admin可元数据管理，无正文权限 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-03-01、V105G-03-02；`test-artifacts/task-105/G/C105G-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-03-02 开关与容量

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R15、DH-R16、DH-R18、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminIT.java`；测试名称包含`tc105g_03_02`；跨语言同TC可各有子断言 |
| Given | 活动会话+默认capacity1、实测1 |
| When | 关闭新建/设置capacity2 |
| Then | 新建拒绝而end可用；capacity2拒绝，审计准确 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-03-01、V105G-03-02；`test-artifacts/task-105/G/C105G-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-03-03 核对幂等

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R15、DH-R16、DH-R18、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminReconcileIT.java`；测试名称包含`tc105g_03_03`；跨语言同TC可各有子断言 |
| Given | unknown调用+同证据两次+版本冲突 |
| When | reconcile |
| Then | 仅原经济键一次结算，旧版409，金额不得自填 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-03-01、V105G-03-02；`test-artifacts/task-105/G/C105G-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-03-04 外部依赖失败

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R15、DH-R16、DH-R18、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanAdminIT.java`；测试名称包含`tc105g_03_04`；跨语言同TC可各有子断言 |
| Given | 财务503或runtime不可达 |
| When | 管理处置 |
| Then | operation保pending/failed并可恢复，不假称已完成 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-03-01、V105G-03-02；`test-artifacts/task-105/G/C105G-03/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105G-04 治理台独立面板与权限可见性

需求：DH-R15、DH-R18、DH-R19。依赖：C105G-03。必读共享：K10、K11、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `src/ops/admin/adminTabs.ts` | MODIFY | `ADMIN_TAB_KEYS/TAB_REGISTRY` | digital-human内容AI组，platform_admin |
| `src/ops/admin/tabs/AdminDigitalHumanPanel.vue` | NEW | `panel assembly` | 配置/会话/核对三个子区 |
| `src/ops/admin/components/DigitalHumanConfigForm.vue` | NEW | `config editor` | 版本冲突与能力证据 |
| `src/ops/admin/components/DigitalHumanSessionTable.vue` | NEW | `session actions` | 元数据/结束确认 |
| `src/ops/admin/components/DigitalHumanReconcilePanel.vue` | NEW | `reconcile form` | 证据/用量/不能改金额 |
| `src/ops/admin/composables/useDigitalHumanAdmin.ts` | NEW | `query/update/terminate/reconcile` | 串行轮询/请求代次 |
| `src/ops/admin/composables/useDigitalHumanAdmin.test.ts` | NEW | `admin async tests` | 失活/错序/权限 |
| `src/ops/admin/tabs/AdminDigitalHumanPanel.test.ts` | NEW | `admin UI cases` | 角色/状态/焦点 |
| `src/ops/admin/AdminView.test.ts` | MODIFY | `registry regression` | 新页签与原权限不变 |
| `src/ops/admin/admin-shared.css` | MODIFY | `DH governance local selectors` | 仅治理数字人面板样式，token来自ops规范 |

**只读源码锚点**：`src/ops/admin/adminTabs.ts`，`export const TAB_REGISTRY`，快照行78起。先核实真实上下文：

```text
 */
export const TAB_REGISTRY: readonly AdminTabDef[] = [
  // ---- 审核队列 review ----
  // 任务书 #91 A2：users/kyb 内联复合页面板化（tabs/AdminUsersPanel/AdminKybPanel），走统一渲染器。
  { key: 'kyb', label: 'KYB 审核', group: 'review', component: AdminKybPanel },
  { key: 'org-renames', label: '主体更名', group: 'review', component: OrganizationRenameAdminPanel },
  {
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `useDigitalHumanAdmin(account:AccountSessionPort): DigitalHumanAdminState；复用HTTP封装，GET无自动副作用。`
- `页签key=digital-human，label=数字人，group=content-ai，roles=[platform_admin]，component=AdminDigitalHumanPanel。`

**严格执行步骤**：

1. 先读src/ops/DESIGN.md；沿现有registry挂载独立tab，不改AdminView大视图加入长分支。
2. 配置只显示批准backend/容量/功能开关，模型/凭据配置跳转既有AI模型管理；第三方服务证据、未实测与测试项有清楚文案，不能把unknown显示为正常。
3. 会话表仅脱敏账号标识/状态/运行时长/阶段延迟/待核对数量；不显示prompt、字幕、头像原图、SDP或grant。
4. 强制终止和费用核对须显式确认，先展示影响及原因；提交锁同动作，成功重新读真实状态，不能optimistic地把unknown变已结算。
5. 仅面板active且visible时串行5秒轮询，前次完成后再计时；失活/换号abort并清状态；明暗主题、390宽和键盘focus完整。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105G-04-01 入口权限

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R15、DH-R18、DH-R19 |
| 实现位置 | `src/ops/admin/tabs/AdminDigitalHumanPanel.test.ts`；测试名称包含`tc105g_04_01`；跨语言同TC可各有子断言 |
| Given | 四治理角色 |
| When | 读取导航并访问API |
| Then | 只有platform_admin显示及可操作，无其他角色扩权 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-04-01、V105G-04-02；`test-artifacts/task-105/G/C105G-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-04-02 版本冲突

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R15、DH-R18、DH-R19 |
| 实现位置 | `src/ops/admin/composables/useDigitalHumanAdmin.test.ts`；测试名称包含`tc105g_04_02`；跨语言同TC可各有子断言 |
| Given | 两管理员编辑同版本 |
| When | 顺序提交 |
| Then | 后者保表单并提示重新加载，无静默覆盖 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-04-01、V105G-04-02；`test-artifacts/task-105/G/C105G-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-04-03 核对确认

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R15、DH-R18、DH-R19 |
| 实现位置 | `src/ops/admin/tabs/AdminDigitalHumanPanel.test.ts`；测试名称包含`tc105g_04_03`；跨语言同TC可各有子断言 |
| Given | unknown与缺证据表单 |
| When | 点击核对/重复点击 |
| Then | 无证据禁提交，有证据一次请求、待完成不显示成功 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-04-01、V105G-04-02；`test-artifacts/task-105/G/C105G-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-04-04 失活和主题

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R15、DH-R18、DH-R19 |
| 实现位置 | `src/ops/admin/composables/useDigitalHumanAdmin.test.ts`；测试名称包含`tc105g_04_04`；跨语言同TC可各有子断言 |
| Given | 在途poll后切tab/账号；两主题两视口 |
| When | 迟到回复/截图自查 |
| Then | 不恢复轮询，不露旧数据，focus与提示可见 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-04-01、V105G-04-02；`test-artifacts/task-105/G/C105G-04/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105G-05 治理闭环与日志隐私门禁

需求：DH-R02、DH-R15、DH-R16、DH-R17、DH-R18、DH-R19、DH-R20。依赖：C105G-04。必读共享：K08、K09、K10、K12、K13、K14；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/digitalhuman/DigitalHumanMetrics.java` | NEW | `bounded tags/timers` | P50/P95/错误/cleanup/unknown |
| `platform-realtime/digital-human/src/grassland_dh/metrics.py` | NEW | `stage metrics` | phase/backend/error有限标签 |
| `platform-realtime/digital-human/tests/test_privacy.py` | NEW | `canary scan` | 日志/Redis/文件/异常脱敏 |
| `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanPrivacyIT.java` | NEW | `DigitalHumanPrivacyIT` | 实现本卡TC，含真实DB/事务或fake网络负例 |
| `tests/e2e/digital-human-governance.spec.ts` | NEW | `governance flow` | 停用/终止/未知用量/注销 |
| `scripts/acceptance/task-105-privacy-check.ts` | NEW | `scan allowed evidence roots` | 合成标记扫描/零外发 |
| `tests/acceptance/task-105-privacy-check.test.ts` | NEW | `scanner tests` | 正文/密钥/SDP反例 |
| `tests/fixtures/digital-human/privacy-manifest.json` | NEW | `canary patterns/allowlist` | 仅合成隐私扫描输入 |

**只读源码锚点**：`platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/compliance/PersonalDataErasureService.java`，`class PersonalDataErasureService`，快照行25起。先核实真实上下文：

```text
@Component
public class PersonalDataErasureService {

	private static final Logger log = LoggerFactory.getLogger(PersonalDataErasureService.class);

	/** 清理回执（§6.5 erase 响应数据；erased=true 仅 state=completed）。 */
	public record ErasureReceipt(String manifestId, String state, boolean erased, Map<String, Long> counts,
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `privacy-check CLI：--artifact-root test-artifacts/task-105 --manifest tests/fixtures/digital-human/privacy-manifest.json；只读，命中受禁canary退出1，合成fixture源文件排除但日志不排除。`

**严格执行步骤**：

1. 追加合成canary夹具到H共用隐私manifest前，G卡登记本文件白名单；不同marker代表prompt/mic/key/grant/SDP，不能用真实数据。
2. 扫描Java/Python/Nginx日志、trace、持久Redis快照和产物目录；允许用户显式保存的合成正文单列allowlist，原mic/key/grant永远禁止。
3. 指标仅phase/backend/provider/stableCode，禁止account/session/token/text做标签；text_preview/异常body回归测试。
4. 用治理台关闭新会话、结束活动、原键核对、再账户注销；检查后端真实状态、余额与对象，不仅toast。
5. 权限撤销发生于WS/STT/LLM/TTS/录制/save各窗口，确保旧epoch事件和晚到结果被挡；生成可机验风险覆盖清单。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105G-05-01 隐私canary

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R15、DH-R16、DH-R17、DH-R18、DH-R19、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_privacy.py`；测试名称包含`tc105g_05_01`；跨语言同TC可各有子断言 |
| Given | Fake全链各层注入五种标记 |
| When | 正常/异常/timeout后扫描 |
| Then | 受禁正文/凭据无日志持久泄漏，主动保存范围可解释 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-05-01、V105G-05-02、V105G-05-03、V105G-05-04、V105G-05-05；`test-artifacts/task-105/G/C105G-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-05-02 管理员闭环

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R15、DH-R16、DH-R17、DH-R18、DH-R19、DH-R20 |
| 实现位置 | `tests/e2e/digital-human-governance.spec.ts`；测试名称包含`tc105g_05_02`；跨语言同TC可各有子断言 |
| Given | 活动会话+unknown费+临时产物 |
| When | 停新建→终止→核对→注销 |
| Then | 资源最终释放、原经济键结清、删干净 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-05-01、V105G-05-02、V105G-05-03、V105G-05-04、V105G-05-05；`test-artifacts/task-105/G/C105G-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-05-03 撤销矩阵

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R15、DH-R16、DH-R17、DH-R18、DH-R19、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanPrivacyIT.java`；测试名称包含`tc105g_05_03`；跨语言同TC可各有子断言 |
| Given | 六个派发/写回窗口 |
| When | 每窗口撤权并迟到回复 |
| Then | 拒继续写/播且不重复费用，错误受控 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-05-01、V105G-05-02、V105G-05-03、V105G-05-04、V105G-05-05；`test-artifacts/task-105/G/C105G-05/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105G-05-04 指标基数

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R15、DH-R16、DH-R17、DH-R18、DH-R19、DH-R20 |
| 实现位置 | `platform-java/services/intelligence-service/src/test/java/com/grassland/intelligence/digitalhuman/DigitalHumanPrivacyIT.java`；测试名称包含`tc105g_05_04`；跨语言同TC可各有子断言 |
| Given | 100合成sessionId |
| When | 抓取metrics |
| Then | series不按session增长，无正文/secret标签 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105G-05-01、V105G-05-02、V105G-05-03、V105G-05-04、V105G-05-05；`test-artifacts/task-105/G/C105G-05/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

## 12. 验收命令、边界矩阵与阶段出口

所有命令均为**实施后执行合同**，本次写任务书没有运行业务测试。工作目录“仓库根”指`/Users/LXH/claude/y-1`。每条记录开始结束、退出码、实际测试数量、报告路径；0 tests/全部SKIPPED/缓存未执行不能当本次PASS。

### V105G-01-01 Java/intelligence-service（C105G-01）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-01/V105G-01-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanHistoryIT' --rerun-tasks --no-build-cache
```

### V105G-01-02 Vitest（C105G-01）

目录：`仓库根`；shell：bash。前提/影响：当前npm lock与Node工具链；新增测试必须被vitest include覆盖。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-01/V105G-01-02.log`及工具原生报告。

```bash
npm run test -- src/views/digital-human/composables/useDigitalHumanHistory.test.ts
```

### V105G-02-01 Java/intelligence-service（C105G-02）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-02/V105G-02-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanErasureIT' --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanCleanupRaceIT' --rerun-tasks --no-build-cache
```

### V105G-02-02 Python（C105G-02）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-02/V105G-02-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_cleanup.py -q
```

### V105G-03-01 Java/intelligence-service（C105G-03）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-03/V105G-03-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanAdminIT' --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanAdminReconcileIT' --rerun-tasks --no-build-cache
```

### V105G-03-02 Java/edge-bff（C105G-03）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-03/V105G-03-02.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:edge-bff:test --tests 'com.grassland.edge.proxy.DigitalHumanRoutesTest' --rerun-tasks --no-build-cache
```

### V105G-04-01 Vitest（C105G-04）

目录：`仓库根`；shell：bash。前提/影响：当前npm lock与Node工具链；新增测试必须被vitest include覆盖。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-04/V105G-04-01.log`及工具原生报告。

```bash
npm run test -- src/ops/admin/composables/useDigitalHumanAdmin.test.ts src/ops/admin/tabs/AdminDigitalHumanPanel.test.ts src/ops/admin/AdminView.test.ts
```

### V105G-04-02 类型（C105G-04）

目录：`仓库根`；shell：bash。前提/影响：不运行远程服务。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-04/V105G-04-02.log`及工具原生报告。

```bash
npm run typecheck
```

### V105G-05-01 Java/intelligence-service（C105G-05）

目录：`platform-java`；shell：bash。前提/影响：JDK25；IT依赖隔离Postgres/Testcontainers，不连生产。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-05/V105G-05-01.log`及工具原生报告。

```bash
source ../scripts/lib/java-runtime.sh
ensure_java_runtime 25 || exit 1
./gradlew :services:intelligence-service:test --tests 'com.grassland.intelligence.digitalhuman.DigitalHumanPrivacyIT' --rerun-tasks --no-build-cache
```

### V105G-05-02 Python（C105G-05）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-05/V105G-05-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_privacy.py -q
```

### V105G-05-03 Vitest（C105G-05）

目录：`仓库根`；shell：bash。前提/影响：当前npm lock与Node工具链；新增测试必须被vitest include覆盖。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-05/V105G-05-03.log`及工具原生报告。

```bash
npm run test -- tests/acceptance/task-105-privacy-check.test.ts
```

### V105G-05-04 治理浏览器（C105G-05）

目录：`仓库根`；shell：bash。前提/影响：隔离账号与合成财务；无真实付款。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-05/V105G-05-04.log`及工具原生报告。

```bash
DH_E2E=1 E2E_SPECS=tests/e2e/digital-human-governance.spec.ts E2E_ENGINES="chromium firefox webkit" E2E_WORKERS=1 bash scripts/ci-e2e.sh
```

### V105G-05-05 隐私扫描（C105G-05）

目录：`仓库根`；shell：bash。前提/影响：先运行本卡Fake链生成日志，空目录不能通过。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/G/C105G-05/V105G-05-05.log`及工具原生报告。

```bash
npx --no-install tsx scripts/acceptance/task-105-privacy-check.ts --artifact-root test-artifacts/task-105 --manifest tests/fixtures/digital-human/privacy-manifest.json
```

### 阶段公共门禁

```bash
npm run typecheck
npm run lint
npm run build
npm run quality:lifecycle
npm run docs:links
npm run docs:status
npm run security:secrets
git diff --check
```

`quality:lifecycle`从B建表开始必需；A阶段仅既有基线回归，不要求不存在的DH表。UI阶段追加适用DESIGN lint和明暗截图自查；设计工具失败/真实设备缺失均记录具体范围。Java修改阶段先执行本卡定向再受影响service的`:check`；H02汇总跨域门禁，不能将本书全完成代替下一阶段。
### E01～E22边界追踪

这是整条个人版链路的精确责任矩阵；专项卡未到达时标DEPENDENCY_PENDING，不宣称当前阶段已通过它。当前卡对应情形仍在其四组TC中参数化实现；不相关场景无需在纯锁文件/纯部署卡造UI。

| 编号 | 情形 | 固定输入与预期 | 专项责任/用例 |
| --- | --- | --- | --- |
| E01 | 空输入 | text="  "、persona=""、requestId缺失；422且零派发 | C105B-03 / TC105B-03-02 |
| E02 | 超长输入 | text2001/persona4001码点、SDP65537bytes；422/413 | C105B-03 / TC105B-03-02 |
| E03 | 重复提交 | 同request同体/异体各并发2次；原资源或409 | C105B-03 / TC105B-03-03 |
| E04 | 网络失败 | HTTP接受后断连；只查原键，不换经济键 | C105D-01 / TC105D-01-02 |
| E05 | 服务端错误 | provider500/半流、runtime不可达；受控code与pending | C105D-03 / TC105D-03-02 |
| E06 | 未登录 | 无cookie且无内部身份；401、零业务写 | C105B-02 / TC105B-02-02 |
| E07 | 无权限 | 非platform_admin调用管理端；403 | C105G-03 / TC105G-03-01 |
| E08 | 数据为空 | 无角色/历史/voice/catalog；空态、不能点开始 | C105E-02 / TC105E-02-03 |
| E09 | 数据过期 | grant29999/30000ms、preflight60秒、内容结束10分钟 | C105D-02 / TC105D-02-03 |
| E10 | 页面刷新 | 现有会话刷新、建立新controller后明确接管 | C105C-02 / TC105C-02-01 |
| E11 | 用户快速切换 | A→B→A，同账号epoch已变仍拒旧回包 | C105E-03 / TC105E-03-02 |
| E12 | 组件卸载在途 | 权限对话晚返回/流未完成；stop tracks并清timer | C105E-04 / TC105E-04-02 |
| E13 | 缺值与非法枚举 | null/缺字段/空白/tone=unknown/多余orgId | C105A-02 / TC105A-02-03 |
| E14 | 数值长度边界 | 0/1/max/max+1、2^53、负金额、小数sample | C105A-02 / TC105A-02-03 |
| E15 | 并发乱序 | 两个控制器接管、旧epoch/旧seq迟到 | C105C-02 / TC105C-02-01 |
| E16 | 已提交响应丢失 | asset save完成丢响应；原id重试一次资产 | C105F-03 / TC105F-03-02 |
| E17 | 跨主体 | B访问A会话/avatar/media、组织上下文不能越权 | C105G-01 / TC105G-01-02 |
| E18 | 中途撤权删除 | 六窗口撤权/删除墓碑；晚结果不得复活 | C105G-05 / TC105G-05-03 |
| E19 | 旧配置/旧缓存 | expectedVersion/catalogVersion失效、旧core升级 | C105H-02 / TC105H-02-02 |
| E20 | 部分成功补偿失败 | 原run结算503、录制partial、事件重复 | C105D-06 / TC105D-06-01 |
| E21 | 时区/金额 | UTC日切与[from,to)、cents整数、冻结价表 | C105G-01 / TC105G-01-01 |
| E22 | 大文件列表长文案 | 10MiB+1头像、200MiB录制、101条limit、长中文布局 | C105F-01 / TC105F-01-02 |

阶段出口：本书本地必需TC/V与范围门禁通过、前置合同无漂移、所有新增资源有handler/test、下阶段能使用已验证真实接口；条件真实缺口列明。不得将FAKE_PASS写成REAL_QUALIFIED。

## 13. 何时阻塞，何时继续

明确阻塞当前依赖路径：公开字段/权限/资金口径矛盾；现有改动与本卡无法共存；必需前卡失败；已批准模型仍无法满足核心协议且需要换方案。报告必须含卡号、准确文件/符号、最小复现、预期/实际、已排查项与最小所需决定。
可继续：行号/空白变化、正常实现细节、本地依赖安装、已有服务复用、Fake测试；第三方服务配置/服务条款证据/测试额度缺少时，只停止真实实验和开放，不阻塞明确无依赖的本地开发。禁止无限重试、假数据冒充真实或降低门槛。

## 14. 对话交接与执行提示词

逐卡在对话给出：卡号及状态；新增/修改文件；TC PASS/FAIL/NOT_RUN与命令/退出码/报告路径；前置既有失败；越界检查；下一卡。无需另建完成报告。用户明确要求运行手册与机器验收证据的文件已在白名单中，按卡更新即可。
可将下面提示词连同本书交给执行模型：

```text
执行 docs/任务书/草场任务书-105G-数字人工作台-阶段六-历史治理与数据生命周期.md，从 C105G-01 开始，采用 AUTO_CHAIN。
先核对当前卡前置，再读 AGENTS.md、适用 DESIGN.md、任务书第0/9/13/14节和共享契约K14及该卡明确引用的其余K节。
只修改本卡与全局白名单的交集，保留现有工作区修改，不实现未定义的扩展。
每卡完成后真实运行列出的TC/V并汇报，再按依赖自动推进本阶段，不为普通本地步骤逐次等确认。
真实条件不具备如实标NOT_RUN，继续本书中无依赖的本地工作；不能自动部署、打开生产开关或把Fake当真实验收。
业务或资金决策冲突时给出精确阻塞点，不自己换方案。
```
