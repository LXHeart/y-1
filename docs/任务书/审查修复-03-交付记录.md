# 审查修复 03 交付记录：终态与派生数据

- 状态：IMPLEMENTED（代码与全量自动化门禁已通过；生产历史诊断待执行）。
- 交付日期：2026-09-10。
- 执行规格：[审查修复-03-终态与派生数据](./审查修复-03-终态与派生数据.md)。
- 实际开工 HEAD：`5a95d001ff0b3fdcf84395d3f0506bfa421e90c4`；工作区保留前序及用户已有前端改动。
- 证据目录：[test-artifacts/audit-fix-03/](../../test-artifacts/audit-fix-03/)。

## 修改

| 问题 | 修改 | 永久回归 |
|---|---|---|
| R08 | `ApplicationLifecycleService.exitNoFault` 保持 Finance RPC 在本地事务外；报名 `withdrawn/no_fault`、名额回收、pending 协商申请取消和 `ApplicationExitedNoFault` outbox 在同一 `TransactionalOperator` 内。Finance 使用稳定 engagement ref，重试不会重复释放。 | `EngagementDeliveryLifecycleIT.noFaultExitRollsBackWhenCapacityReleaseFails`；既有无责退出、并发终态和超时测试 |
| R09 | 售后裁定先持久化金额、原因、操作者和 `refund_operation_id` 到售后行；意图写入不命中 open 售后单时事务失败并阻止外部退款；`completeRefund` 在一个本地事务内完成订单退款事实、退款明细、售后结案、补库存和完成 outbox。结案按 operation id 匹配，普通/过期/补偿退款不会误关售后。 | `CommerceControllerIT.afterSalesDisputeDetailIsVisibleToOwnerAndStoreStaffAndPartialResolutionRefunds`；`ExpiryRefundReconciliationIT` |
| R10 | 声誉 SQL 按 `exit_kind` 分类：`timeout` 进入责任分母，`merchant_cancel` 豁免，`no_fault/negotiated` 保持 withdrawn 口径；空值沿用存量兼容规则。 | `EngagementDeliveryLifecycleIT` 超时完成率断言；`ReputationControllerIT`、`MerchantCreditIT` |

新增 `V63__commerce_refund_facts.sql` 建立 append-only `consumer_order_refund`，以 operation id 幂等记录每次已确认退款的金额、来源和发生时间。经营看板与历史诊断可按发生时间查询，旧记录无明细时只用 `refunded_at` 回退，不改写资金源事实。

新增 `V64__after_sales_resolution_actor.sql` 为售后裁定增加可空 `resolution_actor_account_id`；新退款/驳回裁定写入认证操作者，存量记录保持兼容。

只读诊断脚本为 [scripts/acceptance/audit-fix-03-diagnostics.sql](../../scripts/acceptance/audit-fix-03-diagnostics.sql)，覆盖名额/待处理退出申请、退款已成功但售后仍 open、timeout 被旧商家取消口径豁免三类异常。

## 验证

- `:services:marketplace-service:compileJava`：通过。
- `:services:marketplace-service:test`：全量通过（含 `EngagementDeliveryLifecycleIT`、售后/退款、声誉、MerchantCreditIT、OpsOrderHoldIT）。
- `:services:identity-service:test`、`:services:trust-service:test`：全量通过；`./platform-java/gradlew -p platform-java test` 多模块全量通过。
- `:services:marketplace-service:spotlessCheck`：通过（新增/修改 Java 与测试按仓库格式化）。
- `./platform-java/gradlew -p platform-java spotlessCheck`、`scripts/validate-released-migrations.sh`、`git diff --check`：通过。
- 前端全量 `npm test`：178 文件/1591 用例通过；`npm run typecheck`、`npm run lint`、`npm run build`：通过。

尚未执行：生产库诊断（当前未配置 `DATABASE_URL`）、真实浏览器截图。截图验收因本机 macOS 处于锁屏状态无法使用 CUA，记录在 [test-artifacts/audit-fix-03/visual-check.md](../../test-artifacts/audit-fix-03/visual-check.md)。
