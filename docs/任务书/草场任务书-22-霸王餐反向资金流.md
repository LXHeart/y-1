# 任务书 #22：霸王餐反向资金流（ADR + 实现）

> 生成日期：2026-08-17。交办对象：Qoder（独立开发代理，无会话上下文，本文自包含）。
> 仓库：当前 main。背景与缺口登记：`docs/草场开发进度与续接指南.md` 横向缺口 #22、推荐执行顺序第三批第 9 项。
> 完成后须回写进度指南（#22 标完成）并同步 CLAUDE.md。
>
> **本任务分两个 Phase**：Phase A 写 ADR（内容已在本文「ADR 决策包」给全，Qoder 誊写落地、实现期发现细节可微调，**重大偏离须停下回报**）；Phase B 按 ADR 实现。这是第三批「先定规则再编码」的立项，顺序不可倒置。

## 目标（PRD §2.2 模式一 / §8.2）

- 推荐官报名被接受时**预付押金进平台托管**（资金方向与现有商家出资 escrow 相反）
- 达标（核实 + 商家确认）→ **全额返还推荐官**（无平台费）
- 未达标/超时终局 → **资金释放给商家**（作为补偿）
- 本质：推荐官以「完成任务」换免费体验；商家让利 = 餐费本身

**一个关键推导（已论证，支撑数据模型）**：霸王餐与佣金模式的资金方向矩阵**完全同构**——成功 → 钱到推荐官侧（佣金 capture / 押金 refund），失败 → 钱到商家侧（佣金 release / 押金 compensate）。差异只在语义标签与费用处理（佣金 capture 抽平台费、押金 refund 全额）。因此任务用**正交字段**建模，未来组合模式天然可加；但 v1 强制单模式 XOR（见 D1）。

## ADR 决策包（Phase A 誊写为 `docs/adr/DXX-freebie-escrow.md`，编号先 `ls docs/adr/` 确认）

### D1 范围：v1 单模式 XOR，组合后置

`freebie_deposit_cents` 与 `bounty_cents` 同时 >0 → 400。组合模式（同任务佣金+押金）为后续 backlog：同构方向矩阵已论证其可行性，但 Saga 需双预留步、结算需双腿拆分，不值得首期背。ADR 明记此边界。

### D2 账务：独立 journal 类型，不复用既有语义

新增 journal_type：`FREEBIE_RESERVE`（推荐官钱包 → 托管）、`FREEBIE_REFUND`（托管 → 推荐官钱包，全额无费）、`FREEBIE_COMPENSATE`（托管 → 商家 org 账户）。不复用 RESERVE/RELEASE/CAPTURE——它们承载「商家出资」方向语义（CAPTURE=打款推荐官并抽费），混用会毁掉对账可读性。posting 账户：`WALLET:{accountId}` / `RESERVE` 池（account_owner=推荐官 accountId，account_ref=engagementRef）/ `ESCROW:{orgId}`；钱包流水新 entry_type：`freebie_reserve`（负）/ `freebie_refund`（正）。

### D3 预付资金源 = 推荐官钱包余额；v1 不做钱包充值

平台内闭环拍板（2026-08-15：不接第三方支付）下，推荐官钱包余额的唯一来源是任务收益沉淀。冷启动用户接不了霸王餐任务——**风险登记进 ADR**，充值通道与真实 PSP 同批做（届时本来就绕不开）。余额不足 = accept 失败（D6），DB 层 `balance_cents >= 0` CHECK 兜底防透支。

### D4 无平台费

§8.2 明文「全额返还」；商家让利即餐费。fee_cents 恒 0。

### D5 发布门槛 = 资金交易权限 tier

霸王餐涉及托管与商家收款（补偿），按 PRD §2.1「涉及预付、托管…的任务」走与 bounty 任务相同的 funding 权限闸门与发布校验链（挂 `enforceLadderBudget` 同一位置附近）。

### D6 失败归因矩阵（本 ADR 的核心，逐格实现+测试）

| 终局事件 | 资金去向 | 依据 |
|---|---|---|
| 核实/确认成功（Settlement 成功路径） | **退推荐官**（全额） | §8.2 |
| 核实未达标且争议终局判商家（DisputeFinalized for_merchant） | **补偿商家** | 未达标 |
| 争议终局判推荐官（for_recommender） | **退推荐官** | 达标方胜诉 |
| 履约超时未提交凭证且任务终局（closed/completed） | **补偿商家** | §8.2 超时 |
| **商家取消任务**（D-03 cancel 流程） | **退推荐官** | ⚠️ 与既有 `EngagementRefundedOnCancel`（退**商家**）方向相反——商家取消不是推荐官的失败。cancel 处理器必须按资金来源分支 |
| 推荐官主动放弃/撤回（accepted 后如有该路径） | 退推荐官 | 未消费体验 |

### D7 押金快照 pinning（镜像 bounty）

任务修订改 deposit 只影响新报名；已接受履约按 accept 时冻结值结算。镜像 V14（bounty 快照）+ V27（task_context 触发器）既有模式，把 `freebie_deposit_cents` 纳入快照列。

### D8 事件与通知

outbox 新事件：`FreebieReserved` / `FreebieRefunded` / `FreebieCompensated`。identity 通知模板（模仿 2026-08-16 的 #28 写法）：Reserved/Refunded → 收件人 `recommenderAccountId`（WALLET 类）；Compensated → 双方（`taskOwnerId` + `recommenderAccountId`，ENGAGEMENT 类）。payload 不泄露账号。

### D9 结算唯一钱侧入口分支

`SettlementExecution`（及其本地门闩）内按资金来源分支调 freebie 生命周期，**不新增第二个钱侧入口**；settlement_reconciliation 对 freebie 行同样落账。

## 现状锚点（动手前先读；行号为 2026-08-17 快照，漂移按符号搜）

- `finance-service/.../escrow/EscrowController.java:88-177` — 既有 reserve（扣 org 余额，不足 409）/ release（退商家）/ capture（打推荐官+抽费）内部端点，**全部反向语义的镜像对象**
- `finance-service/.../escrow/EscrowLifecycleService.java:129-170` — splitToPayee 分账
- `finance-service/.../db/migration/V3__recommender_wallet.sql` — 钱包/流水 schema（`balance_cents >= 0` CHECK、带符号金额、fee_cents）；`V5__ledger.sql` — journal/posting 双录与账户枚举
- `marketplace-service/.../workflow/saga/ApplicationReservationActivityImpl.java:140` — accept Saga 发 `ApplicationAccepted`/`ApplicationAcceptanceStarted` 的分支点（freebie 分支挂这里）
- `marketplace-service/.../workflow/saga/SettlementExecution.java:117-121` — 「固定佣金保持两参 capture」注释处 = 钱侧分支挂点
- marketplace cancel 流程（D-03 §5，`EngagementRefundedOnCancel` 发射点）— D6 商家取消行的改造点
- `marketplace-service/src/main/resources/db/migration/` V14（bounty 快照）/ V27（task_context 触发器）— D7 镜像对象；下一版本号 `ls` 确认
- `identity-service/.../notification/NotificationTemplates.java` — #28 刚加过两个 case，最新模仿样板
- 前端：`src/views/grassland/components/MerchantTaskForm.vue`（赏金输入，XOR 校验挂点）、`RecommenderTaskHall.vue`（任务卡片展示）、`src/components/MyWalletCard.vue`（流水类型）、`src/types/grassland/task.ts`

## Phase B 实施阶段

### Stage B1 — finance：freebie 生命周期

- `FreebieEscrowLifecycleService` + 内部端点（`/internal/**`，服务断言，镜像 EscrowController 形态）：
  - `reserve`：校验金额>0 → 钱包原子扣减（`balance >= amount` 守卫，不足 409）+ journal `FREEBIE_RESERVE`（DEBIT WALLET / CREDIT RESERVE 池）+ wallet_ledger `freebie_reserve`，同事务；operationId 幂等（engagementRef 域）
  - `refund`：journal `FREEBIE_REFUND` + 钱包入账全额（fee=0）+ wallet_ledger `freebie_refund`
  - `compensate`：journal `FREEBIE_COMPENSATE`（CREDIT ESCROW:{orgId}）+ org 账户余额投影更新
  - 纠错走既有 Reversal journal 模式（不可变账本铁律），不提供 UPDATE
- reconcile 扩展：钱包/托管派生余额 SUM 必须覆盖 freebie 腿（`reconcile/wallet/{accountId}`、`reconcile/escrow/{orgId}` 各自不变式仍然成立）
- `WalletEntryType` 扩展 + 前端流水 label（B4）
- **测试**：reserve 成功/余额不足 409/幂等重放、refund 全额无费、compensate 入 org、借贷零和不变式、reconcile 派生==投影、钱包非负守卫

### Stage B2 — marketplace：任务模型 + Saga 分支 + 归因矩阵

- Migration：`task` 加 `freebie_deposit_cents bigint NULL CHECK (freebie_deposit_cents >= 0)`；XOR 校验（create/update/revise 三入口 + `enforceLadderBudget` 同位置加 funding 权限闸门复用）；快照列扩展（D7：task_version / task_application 快照带上 deposit）
- accept Saga 分支：freebie 任务（bounty=0 且 deposit>0）→ 调 finance `freebie/reserve`；失败（余额不足）→ 补偿回 pending（镜像商家 `insufficient_funds` 语义）+ 通知商家；成功 → `ApplicationAccepted`（复用，#28 通知自动达）+ `FreebieReserved`
- `SettlementExecution` 钱侧分支（D9）：成功路径 → `freebie/refund`；D6 矩阵各失败格 → `freebie/compensate`；**cancel 流程改造**：freebie 履约取消时调 refund 而非既有「退商家」逻辑（既有 bounty 行为零改动）
- 事件（D8）+ settlement_reconciliation 落账
- **测试**：表驱动归因矩阵 IT（逐格断言资金去向）、余额不足补偿回 pending、快照 pinning（改 deposit 后旧履约按旧值）、XOR 400、funding 权限闸门、既有佣金链路零回归（Slice 4F/结算/争议全套 IT）

### Stage B3 — identity：通知模板

- `NotificationTemplates` + `NotificationRecipientResolver` 加 FreebieReserved/Refunded/Compensated 三 case（D8 收件人规则），邮件经 MailTemplates 委托自动覆盖（WALLET/ENGAGEMENT 均在高价值子集）
- **测试**：镜像 `ExternalNotificationRoutingTest` 的 #28 样板（收件人+分类+payload 不泄露账号）

### Stage B4 — 前端

1. 类型：`Task`/`CreateTaskInput`/`UpdateTaskInput`/`ReviseTaskInput` 加 `freebieDepositCents`；`TaskRequirements` 不动
2. 发布表单：押金输入（元↔cents），与赏金 XOR（其一 >0 时另一禁用+提示）；hint「推荐官报名被接受时从钱包预付，达标全额返还」
3. 任务大厅/详情：freebie 任务显示「需预付 ¥X · 达标全额返还」徽标；apply 时余额不足给前置提示（软检查，硬闸在后端）
4. 钱包卡片：新流水类型 label（预付托管/押金返还）
5. **门禁**：`npm run test && npm run typecheck && npm run build` + 新 vitest（XOR 交互、徽标渲染、类型映射）

### Stage B5 — 收尾

1. 全量门禁：finance/marketplace/identity `./gradlew :services:<svc>:test` + 前端三件套
2. 回写进度指南：#22 标完成（ADR 编号 + 测试证据）、推荐执行顺序第三批第 9 项划掉、横向缺口第一档 #22 行更新
3. CLAUDE.md：资金域描述补 freebie 反向流一句

## 验收标准（端到端）

1. 商家（资金权限）发布押金 ¥100 的霸王餐任务 → 推荐官 apply → 商家 accept → 推荐官钱包被扣 100 进托管；wallet_ledger/journal 双录一致且借贷零和
2. 钱包余额不足的推荐官被 accept → 补偿回 pending，商家收通知，无部分扣款
3. 提交凭证 → 核实通过 → 商家确认 → **全额 100 返还推荐官**（fee=0），通知送达
4. 核实未达标且争议判商家 → 100 入商家 org 账户，双方通知
5. **商家取消任务** → 未提交凭证的 freebie 履约 100 退**推荐官**（归因矩阵关键差异行，与 bounty 行为相反，各自 IT 锁死）
6. 修订押金为 200 → 已接受履约仍按 100 结算（pinning）
7. 同时设 bounty>0 且 deposit>0 → 400
8. 既有佣金 escrow 全链路 IT 零回归

## 代码库陷阱清单（必读）

- Java 构建：JDK 25（`JAVA_HOME=/opt/homebrew/opt/openjdk@25`），入口 `platform-java/` 下 `./gradlew`
- **双录账本不可变**：一切纠错走 Reversal journal；wallet_ledger append-only；余额行是投影+并发守卫，不是真相源
- **带符号金额**：wallet_ledger 负数=出账；SUM 直接累加，别按类型再手工加符号
- Reactor：Saga/服务间调用在 DB 事务外派发（镜像既有 `transactions.transactional(write).flatMap(dispatch...)`）；`switchIfEmpty` 副作用包 `Mono.defer`
- 内部端点：`/internal/**` 树、服务断言（复用 marketplace↔finance 既有密钥对）、拒绝 `X-Forwarded-*`；公网零新前缀
- Jackson 3：请求 record 可选数值字段必须包装类型；`is` 前缀派生态加 `@JsonIgnore`
- migration 版本号动手前 `ls` 确认；金额一律 cents
- 通知模板 payload 不泄露账号（镜像 #28 测试断言）
- 提交按语义拆分（建议：ADR 文档 / finance 生命周期 / marketplace 模型+Saga+归因 / 通知 / 前端 / docs 各一个 commit），中文 commit message、`feat(scope):` / `test(scope):` / `docs:` 风格
