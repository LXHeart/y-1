# 草场 #105A：数字人工作台 阶段零-上游固定与可行性验证

| 项目 | 内容 |
| --- | --- |
| 版本/日期 | 2.0 / 2026-09-21 |
| 文档状态 | READY_FOR_IMPLEMENTATION：规格已细化；仅当前卡前置验收满足时可执行；真实条件单独门禁 |
| 实施/用例 | NOT_STARTED / NOT_RUN；本轮仅交付文档，未执行本书未来代码命令 |
| 卡/用例 | 4张任务卡，16组Given/When/Then用例；组内边界需参数化 |
| 基线 | 草场f2976aaeb329ee17e9e08a8997bffaabde78d3ac + 当前#103/#104既有修改；OpenTalking固定8c739a5a6f114daf71aeace832a668c3ad60f536 |
| 前置 | 无；可在隔离本地环境开始 |
| 方式 | AUTO_CHAIN：卡级通过自动进入本阶段下一卡；不启子代理，不自动越到未指定的下一本或生产开放 |
| 入口 | [#105总纲](草场任务书-105-数字人工作台开发计划.md) · [PRD](../产品/数字人工作台需求文档.md) · [共享契约](草场任务书-105-数字人工作台共享契约.md) |

## 0. 开始与完成协议

本书供编码模型直接执行。先读本节、§9、§13、§14，再读当前卡列明的共享K节、源码和测试。CARD PASS才推进；已有真实服务授权在同范围内持续有效，不逐卡重复申请。普通本地依赖、Fake、合成数据库与浏览器验证可直接进行。
先保存`git status --short`和当前diff；不得清空工作区或覆盖已有#103/#104修改。行号漂移以本卡真实符号定位；需要改变业务契约/归属/资金规则才停止受影响卡，不因发现未提交文件而全部停工。所有新增接口都应拒绝未知字段，无密码/密钥进入证据。
完成必须同时有实现、非零用例、卡级命令退出0、适用边界/视觉检查、范围检查和对话交接。NOT_RUN不是PASS；真实条件不足只阻塞相应real/beta门禁，保留Fake可继续的明确路径。

## 1. 本阶段目标与范围

建立不会偷偷调用真实服务的可复现适配基线，并把未知真实条件变成机器可检查的门禁。

| 需求 | 负责卡 | 验收组 |
| --- | --- | --- |
| DH-R02 | C105A-02 | TC105A-02-01～TC105A-02-04 |
| DH-R03 | C105A-01、C105A-04 | TC105A-01-01～TC105A-01-04、TC105A-04-01～TC105A-04-04 |
| DH-R06 | C105A-02 | TC105A-02-01～TC105A-02-04 |
| DH-R07 | C105A-03 | TC105A-03-01～TC105A-03-04 |
| DH-R08 | C105A-03 | TC105A-03-01～TC105A-03-04 |
| DH-R09 | C105A-03、C105A-04 | TC105A-03-01～TC105A-03-04、TC105A-04-01～TC105A-04-04 |
| DH-R10 | C105A-03 | TC105A-03-01～TC105A-03-04 |
| DH-R11 | C105A-02 | TC105A-02-01～TC105A-02-04 |
| DH-R13 | C105A-04 | TC105A-04-01～TC105A-04-04 |
| DH-R15 | C105A-02、C105A-04 | TC105A-02-01～TC105A-02-04、TC105A-04-01～TC105A-04-04 |
| DH-R16 | C105A-01、C105A-03、C105A-04 | TC105A-01-01～TC105A-01-04、TC105A-03-01～TC105A-03-04、TC105A-04-01～TC105A-04-04 |
| DH-R17 | C105A-02 | TC105A-02-01～TC105A-02-04 |
| DH-R20 | C105A-01 | TC105A-01-01～TC105A-01-04 |

范围外：组织共享、知识库/记忆、对外客服/直播、克隆真人声音、摄像头驱动、任意模型URL、自动交易工具；不顺手改文章/视频工具或前序缺陷。未被本卡白名单列出的文件一律只读。

## 2. 仓库上下文与只读基线

前端Vue/TypeScript，AI独立入口`src/ai/router.ts`及`AiAppLayout.vue`；治理台`src/ops/admin/adminTabs.ts`；Java控制域在intelligence-service，Edge转发；Python实时域为本系列新增目录。现有鉴权、预算、媒体、生命周期只作定点复用。
必读根AGENTS.md与`docs/架构/目录结构.md`。涉及用户/AI UI读根DESIGN.md，治理UI读src/ops/DESIGN.md，共享组件默认根规范。每卡源码摘录是2026-09-21快照，仅作定位；真实实现优先核对签名和上下文。

## 3. 已定技术决策

Java拥有owner/会话/费用/清理事实；Python持有每会话Binding与媒体，无长期供应商key；Vue只有同源控制资格。首期STT为按键提交后内存整段转写；LLM采用受控计量流；TTS采用24000Hz PCM桥且沿既有平台资助模式；RTC只下行音视频。打断关闭旧peer并重建，租约独立回收。
固定QuickTalk/local为条件真实候选；Fake必须覆盖STT/LLM/TTS/render全链且外网请求为0。框架开源许可不代替权重许可。配置默认关闭，S1内测、S2个人试点分别验收。

## 4. 目标行为与状态

全局状态迁移见共享K04；本阶段只实现各卡明确触点，不增同义state。session、turn、invocation、settlement、recording、cleanup互相独立：会话ended不表示费用已结或文件已删。失联回收≤45秒、正常结束P95≤5秒，都是待实现/实测目标。

## 5. 业务不变量

首期owner固定个人，orgId不由浏览器提交。账号+epoch变化使旧异步结果无效。每动作requestId创建一次；同键异payload409，同键重放先owner再receipt。费用unknown不填0、不自动重放provider。原始mic不入磁盘、素材库、日志；未同意文本仅有界易失缓冲。

## 6. 接口、字段与内部签名

公开API01～40见共享K03，内部13端点与NDJSON见K07.1～K07.3，管理6端点见K10。金额单位cents、时长ms、UTC时间、JS安全整数、可null/必填及错误码见K01。类型不得用any/无约束map逃避合同；当前卡精确签名见§11。

## 7. 数据、事务与迁移

共享K05是DDL合同，B核心迁移一次创建B/C/D/G核心表；F另建媒体表。执行时按intelligence最大Flyway整数+1替换唯一NEW_DYNAMIC路径中的NEXT，先记录实际文件名，禁止修改V86/V87及其他历史SQL。锁顺序account gate→session→turn/recording→invocation/operation→media；不持事务等待网络。

## 8. UI与用户可观察状态

本阶段无新UI页面；不为协议/部署任务添加装饰页面。需要浏览器验收的卡使用已交付页面；结果/错误文案按K01，截图适用处见具体卡。

## 9. 写入白名单与禁止项

下面是阶段全局上限；每卡仍只可改其§11表内文件，取两者交集。NEW仅首创，MODIFY_AFTER表明前卡已创建，不得重建覆盖。只读锚点不是写权限。测试产物可写test-artifacts/task-105/A及卡内指定`test-artifacts/task-105/`子目录（Git忽略），不能放根目录或提交用户内容。

| 完整仓库相对路径 | 动作 | 用途 |
| --- | --- | --- |
| `platform-realtime/digital-human/upstream.lock.json` | NEW | 记录固定提交、上游源文件摘要、补丁顺序 |
| `platform-realtime/digital-human/pyproject.toml` | NEW | 仅建立独立Python3.11项目，不改根Node/Gradle |
| `platform-realtime/digital-human/uv.lock` | NEW | 实际解析冻结并记录平台差异 |
| `platform-realtime/digital-human/.python-version` | NEW | 独立Python minor |
| `platform-realtime/digital-human/.gitignore` | NEW | 第三方源与运行产物不入库 |
| `platform-realtime/digital-human/scripts/bootstrap.py` | NEW | 下载固定源码并校验路径/摘要，不启动任何provider |
| `platform-realtime/digital-human/tests/test_bootstrap.py` | NEW | 源码固定/补丁漂移/路径穿越反例 |
| `contracts/digital-human.v1.json` | NEW | 完整K合同机器可读形状 |
| `contracts/digital-human.v1.examples.json` | NEW | A/B两合成账号、事件/WS/金额边界 |
| `scripts/quality/check-digital-human-contracts.ts` | NEW | 检查端点/type/错误/引用/默认值闭合，零修改 |
| `tests/contracts/digital-human.contract.test.ts` | NEW | 删端点/错enum/错epoch/重复requestHash反例 |
| `platform-realtime/digital-human/src/grassland_dh/__init__.py` | NEW | 无import副作用 |
| `platform-realtime/digital-human/src/grassland_dh/app.py` | NEW | 只挂health/受控internal，不挂原生管理API |
| `platform-realtime/digital-human/src/grassland_dh/bindings.py` | NEW | 每会话provider工厂/租约快照 |
| `platform-realtime/digital-human/src/grassland_dh/adapters.py` | NEW | 固定上游适配入口 |
| `platform-realtime/digital-human/src/grassland_dh/fakes.py` | NEW | 确定流/错误/用量，禁止外部出站 |
| `platform-realtime/digital-human/patches/0001-injected-runtime-bindings.patch` | NEW | 有限适配、agent关闭、内容日志去除 |
| `platform-realtime/digital-human/tests/test_fake_pipeline.py` | NEW | 两会话隔离与真实音视频对象 |
| `platform-realtime/digital-human/tests/test_import_boundary.py` | NEW | import不自动请求/下载模型 |
| `platform-realtime/digital-human/Dockerfile` | NEW | A首创可供E本地compose，H扩GPU与生产参数 |
| `platform-realtime/digital-human/renderer-profile.json` | NEW | 明确profile状态与证据引用 |
| `platform-realtime/digital-human/license-manifest.json` | NEW | 逐权重摘要/来源/适用证据，unknown不可approved |
| `platform-realtime/digital-human/scripts/probe_runtime.py` | NEW | 有界连接/打断/录制插桩/资源测量 |
| `platform-realtime/digital-human/tests/test_probe_runtime.py` | NEW | 无GPU/无许可/缺数据不得PASS |
| `docs/运维/数字人工作台运行手册.md` | NEW | 真实执行者记录可复现版本与后续运行合同 |

禁止：改根依赖/lock（除本书明确列入）、改历史迁移、给旧大视图增业务逻辑、修改组织BYOK/取消退款口径、持久化grant/key/原音频、更新既有生产开关、全仓格式化、删除失败测试、杜撰测试PASS。共用契约/PRD只读；发现实质冲突先按§13给具体修订建议。

## 10. 卡顺序、依赖与估算

| 卡 | 目标 | 依赖 | 估计人日 | 初始状态 |
| --- | --- | --- | --- | --- |
| C105A-01 | 锁定上游与建立独立依赖环境 | 无 | 2～3 | NOT_STARTED |
| C105A-02 | 固化共享契约、合成夹具与变更检查器 | C105A-01 | 2～3 | NOT_STARTED |
| C105A-03 | 建立受控wrapper与全链Fake探针 | C105A-02 | 3～5 | NOT_STARTED |
| C105A-04 | 形成真实候选验证门禁与阶段交接 | C105A-03 | 2～3 | NOT_STARTED |

人日是包含实现/测试/调试的计划区间，不是模型运行时长或日历承诺；外部硬件/许可等待不计。A04(local)只要求本地Fake与缺口清单；H03(conditional-beta)只约束真实开放。

## 11. 逐卡执行规格

### C105A-01 锁定上游与建立独立依赖环境

需求：DH-R03、DH-R16、DH-R20。依赖：无。必读共享：K00、K10、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-realtime/digital-human/upstream.lock.json` | NEW | `upstreamCommit/patches/dependencyLockSha256` | 记录固定提交、上游源文件摘要、补丁顺序 |
| `platform-realtime/digital-human/pyproject.toml` | NEW | `project/dependency-groups` | 仅建立独立Python3.11项目，不改根Node/Gradle |
| `platform-realtime/digital-human/uv.lock` | NEW | `locked dependency graph` | 实际解析冻结并记录平台差异 |
| `platform-realtime/digital-human/.python-version` | NEW | `3.11` | 独立Python minor |
| `platform-realtime/digital-human/.gitignore` | NEW | `vendor/.venv/data` | 第三方源与运行产物不入库 |
| `platform-realtime/digital-human/scripts/bootstrap.py` | NEW | `main/verify_source` | 下载固定源码并校验路径/摘要，不启动任何provider |
| `platform-realtime/digital-human/tests/test_bootstrap.py` | NEW | `BootstrapTest` | 源码固定/补丁漂移/路径穿越反例 |

**只读源码锚点**：`AGENTS.md`，`## 目录归位规约`，快照行41起。先核实真实上下文：

```text

## 目录归位规约

完整地图与旧路径迁移对照见 [docs/架构/目录结构.md](docs/架构/目录结构.md)。

- 根目录保留应用入口、构建/部署配置和仓库说明；新增截图、一次性脚本与专题文档放入对应目录。
- 自动化测试统一放 `tests/`，前端已有的就近 `src/**/*.test.ts` 继续与源码同目录；不要重新创建 `test/`。
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `bootstrap.py: main(argv: list[str] | None = None) -> int；参数--verify-only只读，--fetch仅下载固定源码到vendor/opentalking，--resolve-lock仅解析本项目依赖。`
- `verify_source(root: Path, lock: dict[str, object]) -> list[str]；返回具体漂移文件，不修复/覆盖未知文件。`

**严格执行步骤**：

1. 固定commit=8c739a5a6f114daf71aeace832a668c3ad60f536，下载归档只允许github/codeload；安全提取拒绝绝对路径、..、软链接出根；源归档不含生产凭据。
2. FACT本机默认Python3.14不满足本任务目标；用uv项目独立3.11，禁止替换系统python。先按上游pyproject的基础约束+dev组解析，uv.lock是本阶段的版本冻结产物；后续--frozen不自动升级。
3. 依赖允许集合=固定上游pyproject核心依赖、dev测试工具；GPU extras只在A04条件实验使用。记录每个锁定包版本/来源/许可证；不因禁用记忆就假设没有import依赖。
4. bootstrap默认禁止权重下载、禁止执行上游quickstart/doctor安装脚本；只为明确批准的源文件应用本任务patch目录。源码不通过pip editable隐式读取用户全局环境。
5. 生成只含文件摘要/版本的upstream.lock.json；不得填“latest”。第二次verify零写入；变更上游字节必须失败，不用重新生成hash绕过。
6. 本卡bootstrap必须仅用Python标准库；先python3 platform-realtime/digital-human/scripts/bootstrap.py --fetch，再uv lock --project platform-realtime/digital-human --python 3.11，再uv sync --project platform-realtime/digital-human --python 3.11 --frozen。uv.lock首次实际解析后记录摘要，后续卡只能--frozen。pyproject用setuptools src布局和grassland_dh包发现；将固定upstream作为vendor源码可导入路径，不运行原生Web服务。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105A-01-01 固定提交复现

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R16、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_bootstrap.py`；测试名称包含`tc105a_01_01`；跨语言同TC可各有子断言 |
| Given | 空vendor与合成网络归档 |
| When | bootstrap fetch两次并verify |
| Then | 同commit/源摘要，二次不覆盖本地未知修改 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-01-01、V105A-01-02；`test-artifacts/task-105/A/C105A-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-01-02 错误源与穿越

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R16、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_bootstrap.py`；测试名称包含`tc105a_01_02`；跨语言同TC可各有子断言 |
| Given | 归档含../escape或摘要不符 |
| When | 执行bootstrap |
| Then | 非零退出，无workspace外写入/执行脚本 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-01-01、V105A-01-02；`test-artifacts/task-105/A/C105A-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-01-03 环境隔离

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R16、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_bootstrap.py`；测试名称包含`tc105a_01_03`；跨语言同TC可各有子断言 |
| Given | 系统Python3.14、无GPU |
| When | 创建uv Python3.11隔离项目 |
| Then | 项目python=3.11，系统不变，无GPU模型/真实provider启动 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-01-01、V105A-01-02；`test-artifacts/task-105/A/C105A-01/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-01-04 冻结依赖漂移

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R16、DH-R20 |
| 实现位置 | `platform-realtime/digital-human/tests/test_bootstrap.py`；测试名称包含`tc105a_01_04`；跨语言同TC可各有子断言 |
| Given | 改一个锁定包或补丁源摘要 |
| When | verify-only |
| Then | 报告具体项并非零；不能悄悄更新锁 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-01-01、V105A-01-02；`test-artifacts/task-105/A/C105A-01/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105A-02 固化共享契约、合成夹具与变更检查器

需求：DH-R02、DH-R06、DH-R11、DH-R15、DH-R17。依赖：C105A-01。必读共享：K00、K01、K02、K03、K04、K05、K06、K07、K08、K09、K10、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `contracts/digital-human.v1.json` | NEW | `definitions/endpoints/events/defaults` | 完整K合同机器可读形状 |
| `contracts/digital-human.v1.examples.json` | NEW | `valid/invalid/idempotency` | A/B两合成账号、事件/WS/金额边界 |
| `scripts/quality/check-digital-human-contracts.ts` | NEW | `checkDigitalHumanContracts` | 检查端点/type/错误/引用/默认值闭合，零修改 |
| `tests/contracts/digital-human.contract.test.ts` | NEW | `contract invariant tests` | 删端点/错enum/错epoch/重复requestHash反例 |

**只读源码锚点**：`scripts/quality/check-lifecycle-contracts.ts`，`import `，快照行19起。先核实真实上下文：

```text

import { existsSync, readFileSync, readdirSync, statSync } from 'node:fs'
import path from 'node:path'

export interface Violation {
  registry: string
  entry: string
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `checkDigitalHumanContracts(repoRoot: string): {errors: string[]; endpointCount: number; exampleCount: number}；纯读取，CLI有error退出1。`
- `JSON契约的version固定1；每endpoint含id/method/path/requestType/responseType/errors/ownerStage；definitions只引用存在类型。`

**严格执行步骤**：

1. 逐项落实K02/K03全部公开DTO/API01～40，以及K07内部13端点与K10管理6端点；ID分公共API/INTERNAL/ADMIN三空间。public additionalProperties=false，状态/金额/epoch都明确类型。
2. 统一requestId，旧v1的commandId仅上游内部映射，前端不能同时传两套。所有API归属及列表游标顺序确定。
3. 写合成fixtures：account A/B、UUID固定、UTC时钟、PCM小样本由代码生成、原经济键/重复及异payload、mediaEpoch乱序；不复制真实人像或音频。
4. 使用仓库已有Node/TS，不新增通用Schema依赖。实现本合同所用$ref/type/required/properties/additionalProperties/enum/const/oneOf/items/minimum/maximum/minLength/maxLength/pattern/format，并用反例覆盖；遇未支持关键字必须报错，不声称完整实现Draft2020-12。
5. 生成端点覆盖报告进test-artifacts/task-105/A02；每endpoint均有成功、缺字段、未知字段、未授权四种数据样例；未来controller tests消费同fixture。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105A-02-01 契约完整

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R11、DH-R15、DH-R17 |
| 实现位置 | `tests/contracts/digital-human.contract.test.ts`；测试名称包含`tc105a_02_01`；跨语言同TC可各有子断言 |
| Given | K03全部端点 |
| When | 检查器遍历 |
| Then | 每id唯一且所属阶段/DTO/errors存在，40项公共端点齐全 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-02-01、V105A-02-02；`test-artifacts/task-105/A/C105A-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-02-02 删契约负例

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R02、DH-R06、DH-R11、DH-R15、DH-R17 |
| 实现位置 | `tests/contracts/digital-human.contract.test.ts`；测试名称包含`tc105a_02_02`；跨语言同TC可各有子断言 |
| Given | 复制契约到临时fixture并删API20或leaseEpoch |
| When | 运行检查器 |
| Then | 失败且点名删除字段，不以非空数组判完整 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-02-01、V105A-02-02；`test-artifacts/task-105/A/C105A-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-02-03 边界数值

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R11、DH-R15、DH-R17 |
| 实现位置 | `tests/contracts/digital-human.contract.test.ts`；测试名称包含`tc105a_02_03`；跨语言同TC可各有子断言 |
| Given | seq=2^53、negative cents、voice任意URL |
| When | 校验非法样例 |
| Then | 全部拒绝；未知usage只允许null+pending |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-02-01、V105A-02-02；`test-artifacts/task-105/A/C105A-02/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-02-04 幂等跨语言样例

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R02、DH-R06、DH-R11、DH-R15、DH-R17 |
| 实现位置 | `tests/contracts/digital-human.contract.test.ts`；测试名称包含`tc105a_02_04`；跨语言同TC可各有子断言 |
| Given | 字典键顺序不同/中文/换行/null |
| When | 计算canonical JSON摘要 |
| Then | 同语义同hash、内容变化不同hash；各语言后续读相同fixture |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-02-01、V105A-02-02；`test-artifacts/task-105/A/C105A-02/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105A-03 建立受控wrapper与全链Fake探针

需求：DH-R07、DH-R08、DH-R09、DH-R10、DH-R16。依赖：C105A-02。必读共享：K06、K07、K08、K10、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-realtime/digital-human/src/grassland_dh/__init__.py` | NEW | `package` | 无import副作用 |
| `platform-realtime/digital-human/src/grassland_dh/app.py` | NEW | `create_app` | 只挂health/受控internal，不挂原生管理API |
| `platform-realtime/digital-human/src/grassland_dh/bindings.py` | NEW | `SessionBinding/TurnBinding` | 每会话provider工厂/租约快照 |
| `platform-realtime/digital-human/src/grassland_dh/adapters.py` | NEW | `RunnerAdapter` | 固定上游适配入口 |
| `platform-realtime/digital-human/src/grassland_dh/fakes.py` | NEW | `FakeSpeech/FakeLlm/FakeTts/FakeRenderer` | 确定流/错误/用量，禁止外部出站 |
| `platform-realtime/digital-human/patches/0001-injected-runtime-bindings.patch` | NEW | `factory/runner injection` | 有限适配、agent关闭、内容日志去除 |
| `platform-realtime/digital-human/tests/test_fake_pipeline.py` | NEW | `fake pipeline cases` | 两会话隔离与真实音视频对象 |
| `platform-realtime/digital-human/tests/test_import_boundary.py` | NEW | `forbidden routes/providers` | import不自动请求/下载模型 |
| `platform-realtime/digital-human/Dockerfile` | NEW | `CPU Fake image` | A首创可供E本地compose，H扩GPU与生产参数 |
| `platform-realtime/digital-human/upstream.lock.json` | MODIFY_AFTER_C105A-01 | `patch manifest` | 记录本卡适配补丁hash与适用源码hash |

**只读源码锚点**：`src/ai/AiAppLayout.vue`，`creationContextEpoch`，快照行80起。先核实真实上下文：

```text
      <router-view v-slot="{ Component }">
        <KeepAlive :key="creationContextEpoch">
          <component
            :is="Component"
            v-bind="currentViewProps"
            @open-view="handleOpenView"
            @start-workflow="handleStartWorkflow"
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `create_app(test_mode: bool = False, surface: Literal["audio","internal"] = "internal") -> FastAPI；生产默认test_mode=False且DH_ENABLED=false；两个listener分别挂自己的路由。`
- `RunnerAdapter.create(binding: SessionBinding) -> Awaitable[RuntimeSession]；start_turn(turn: TurnBinding, text: str) -> Awaitable[None]；interrupt(turn_epoch: int) -> Awaitable[None]；close() -> Awaitable[None]。`
- `SessionBinding frozen dataclass：session_id,lease_epoch,media_epoch,backend_id,profile_revision,expires_at,bridge_base_url,grant_provider；无长期key。`

**严格执行步骤**：

1. 按OT07真实factory调用点注入实例Binding；补丁只触及task_consumer/runner/synthesis_runner/timing及必要import，逐文件manifest记原因，不改原生所有API。
2. 包装器直接持有runner，不启动未经保护的apps.api.main；显式关闭agent/memory/knowledge及原生voice clone/outputs/runtime-config路由。
3. Fake STT按fixture返回最终文本，Fake LLM分三段并含非零usage，Fake TTS生成确定PCM正弦波，MockRenderer生成自有测试图。端到端包含媒体对象，不只返回success JSON。
4. 所有Fake端点仅127.0.0.1或隔离Docker网络，连接其他地址测试立即失败；不得用Edge TTS做“免费fake”。
5. 验证A/B双Binding同时运行不会共享prompt/grant/text历史；原生peer disconnected自动close需补丁交由adapter的lease处理，主动换peer不得把整session终结。
6. 删除SpeechTiming.text_preview、异常body与debug音频写盘；用临时目录文件差分证明无原音频写盘。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

**Given / When / Then 验收组**：

#### TC105A-03-01 全链无外网

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R07、DH-R08、DH-R09、DH-R10、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_fake_pipeline.py`；测试名称包含`tc105a_03_01`；跨语言同TC可各有子断言 |
| Given | 所有Fake、封锁公共网络 |
| When | 运行文字/音频各一轮 |
| Then | 得到PCM/video/subtitle/usage，外部请求计数0 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-03-01；`test-artifacts/task-105/A/C105A-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-03-02 两会话隔离

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R07、DH-R08、DH-R09、DH-R10、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_fake_pipeline.py`；测试名称包含`tc105a_03_02`；跨语言同TC可各有子断言 |
| Given | A/B人设与grant不同 |
| When | 交错生成、A取消 |
| Then | B配置/音画不变；A旧turn输出归零 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-03-01；`test-artifacts/task-105/A/C105A-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-03-03 默认关闭危险面

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R07、DH-R08、DH-R09、DH-R10、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_import_boundary.py`；测试名称包含`tc105a_03_03`；跨语言同TC可各有子断言 |
| Given | wrapper已启动 |
| When | 访问/sessions原生、runtime-config、memory、outputs |
| Then | 全部404，无自动agent/tool调用 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-03-01；`test-artifacts/task-105/A/C105A-03/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-03-04 peer重建与内容日志

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R07、DH-R08、DH-R09、DH-R10、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_fake_pipeline.py`；测试名称包含`tc105a_03_04`；跨语言同TC可各有子断言 |
| Given | 主动media reset并输入合成敏感标记 |
| When | 捕获日志/文件变化 |
| Then | session可保留重新协商，日志/落盘无标记与音频 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-03-01；`test-artifacts/task-105/A/C105A-03/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

### C105A-04 形成真实候选验证门禁与阶段交接

需求：DH-R03、DH-R09、DH-R13、DH-R15、DH-R16。依赖：C105A-03。必读共享：K09、K10、K12、K13；本卡接口/字段不另定义替代名。

**输入与交付**：输入为前置卡的已验收契约/代码/合成fixture；输出为下表文件中的具体能力与本卡四组测试。每组测试必须落实参数与副作用断言，代码编译不代替行为验收。列出的测试文件负责自动可证部分；涉及截图、真实设备、真实provider的Then必须另执行本卡手工/条件门禁，不以单测替代。

| 完整路径 | 动作 | 定位/新增符号 | 本卡责任 |
| --- | --- | --- | --- |
| `platform-realtime/digital-human/renderer-profile.json` | NEW | `mock/quicktalk local evidence` | 明确profile状态与证据引用 |
| `platform-realtime/digital-human/license-manifest.json` | NEW | `component/weight/asset licenses` | 逐权重摘要/来源/适用证据，unknown不可approved |
| `platform-realtime/digital-human/scripts/probe_runtime.py` | NEW | `main/run_probe` | 有界连接/打断/录制插桩/资源测量 |
| `platform-realtime/digital-human/tests/test_probe_runtime.py` | NEW | `probe result integrity` | 无GPU/无许可/缺数据不得PASS |
| `docs/运维/数字人工作台运行手册.md` | NEW | `S0 local section` | 真实执行者记录可复现版本与后续运行合同 |

**只读源码锚点**：`docs/产品/数字人工作台需求文档.md`，`## 10. 非功能要求`，快照行277起。先核实真实上下文：

```text

## 10. 非功能要求与目标指标

以下是**拟验收目标**，不是上游或本机实测。基准需固定后端版本、模型/量化、GPU/显存、网络、分辨率、并发与音色，并报告冷启动和热启动两组。

| 指标 | 首期目标 | 测量方法 |
|---|---|---|
```

**目标签名/形状**（NEW表示计划，不是已有事实）：

- `probe_runtime.py main(argv: list[str] | None = None) -> int；--mode fake|real、--output path、--profile path；fake退出0证明协议；real缺批准资源退出2。`
- `输出JSON：mode,status,commit,pythonVersion,os,hardware,backend,dimensions,fpsSamples,latencySamples,costUnits,licenseStatus,missingEvidence；不得填推测数字。`

**严格执行步骤**：

1. 固定真实候选quicktalk/local，逐项列quicktalk.pth、repair.npy、HuBERT、InsightFace检测权重与所有辅助文件；未取得许可文本标unknown，不能从框架Apache推导。
2. 先执行Fake probe，记录暖/冷、interrupt、program audio/video分支能否绑定。真实条件未满足只写REAL_NOT_RUN，不把整个Fake合同阶段判失败。
3. 只有已有合法测试资产+已授权设备/provider额度才执行真实probe；在本机Mac可做流程验证，但不得拿MPS低帧率冒充720p/20fps目标通过。
4. 采样≥100轮，真实候选输出原生尺寸，不上采样过验；另测5分钟AV与30分钟连续多场资源趋势；成本只用实量×实际价表，不预设单价。
5. 锁定依赖/补丁/协议测试通过后A本地交付完成；将实际lock摘要/补丁清单/不支持能力写入profile供D读取，H必须再完成真实开放门禁。

**边界与禁止项**：先验证本卡输入/owner/状态，再副作用；遇重复/过期/旧epoch按K01/K04返回确定结果；不得靠客户端禁按钮替代后端约束。mock仅替换外部依赖，不能mock本卡要证明的事务、鉴权、文件真实性或账务幂等。§12边界矩阵指定专项责任卡，本卡涉及的同类负例不能省略。

本卡真实实验为条件分支；其未运行阻塞H正式开放，不阻塞B～G在Fake上的开发。不得把它写成产品已经验收。

**Given / When / Then 验收组**：

#### TC105A-04-01 无硬件/许可

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R09、DH-R13、DH-R15、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_probe_runtime.py`；测试名称包含`tc105a_04_01`；跨语言同TC可各有子断言 |
| Given | fake可运行、真实profile unknown |
| When | 运行real probe |
| Then | exit2且missingEvidence具体，不自动下载或换模型 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-04-01、V105A-04-02；`test-artifacts/task-105/A/C105A-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-04-02 Fake结果分级

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R09、DH-R13、DH-R15、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_probe_runtime.py`；测试名称包含`tc105a_04_02`；跨语言同TC可各有子断言 |
| Given | 完整Fake media样例 |
| When | 生成报告 |
| Then | status=FAKE_PASS，不得出现REAL_PASS或真实fps承诺 |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-04-01、V105A-04-02；`test-artifacts/task-105/A/C105A-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-04-03 测量缺口

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P0；DH-R03、DH-R09、DH-R13、DH-R15、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_probe_runtime.py`；测试名称包含`tc105a_04_03`；跨语言同TC可各有子断言 |
| Given | 缺音轨/只有平均fps无采样 |
| When | 校验报告 |
| Then | 不能标AV或性能通过，保留partial |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-04-01、V105A-04-02；`test-artifacts/task-105/A/C105A-04/`记录日志及原测试report引用；初始NOT_RUN |

#### TC105A-04-04 资源条件满足

| 项目 | 执行合同 |
| --- | --- |
| 优先级/追踪 | P1；DH-R03、DH-R09、DH-R13、DH-R15、DH-R16 |
| 实现位置 | `platform-realtime/digital-human/tests/test_probe_runtime.py`；测试名称包含`tc105a_04_04`；跨语言同TC可各有子断言 |
| Given | 批准的合成资产/设备与上限 |
| When | 执行固定时长probe |
| Then | 记录冷暖/每阶段/版本，失败不无限试真实provider |
| 数据与依赖 | 合成账号A/B、固定UTC时钟和已批准fixture；账号id按现有测试seed真实格式，不用生产账号。若涉及网络，只mock最外层provider；DB锁/owner/经济键使用真实实现。 |
| 边界展开 | 正文中出现多组值必须逐项参数化；并发用2个独立事务/浏览器页与屏障控制先后，时间推进Clock，不能任意sleep换绿灯。 |
| 副作用/最终状态 | 除Then外检查provider请求次数、活动槽/媒体句柄、DB行或文件引用；失败不能新增隐藏扣费/资源。状态按本卡目标进入可观察终态或明确pending。 |
| 清理 | 关闭测试tracks/peer/server、删除本用例合成数据与临时目录；不得清空共享开发库或其他任务产物。 |
| 运行/证据 | V105A-04-01、V105A-04-02；`test-artifacts/task-105/A/C105A-04/`记录日志及原测试report引用；初始NOT_RUN |

**本卡交接**：汇报实现文件、四组TC实际结果、命令退出码、未运行条件、契约偏差和下一卡前置是否满足。存在P0失败不得进入依赖卡；无依赖的本书后续卡可按已写依赖继续，不自行扩大范围。

## 12. 验收命令、边界矩阵与阶段出口

所有命令均为**实施后执行合同**，本次写任务书没有运行业务测试。工作目录“仓库根”指`/Users/LXH/claude/y-1`。每条记录开始结束、退出码、实际测试数量、报告路径；0 tests/全部SKIPPED/缓存未执行不能当本次PASS。

### V105A-01-01 Python（C105A-01）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-01/V105A-01-01.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_bootstrap.py -q
```

### V105A-01-02 上游固定（C105A-01）

目录：`仓库根`；shell：bash。前提/影响：fetch/resolve后验证；无权重下载。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-01/V105A-01-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python platform-realtime/digital-human/scripts/bootstrap.py --verify-only
```

### V105A-02-01 Vitest（C105A-02）

目录：`仓库根`；shell：bash。前提/影响：当前npm lock与Node工具链；新增测试必须被vitest include覆盖。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-02/V105A-02-01.log`及工具原生报告。

```bash
npm run test -- tests/contracts/digital-human.contract.test.ts
```

### V105A-02-02 机器契约（C105A-02）

目录：`仓库根`；shell：bash。前提/影响：本卡新建脚本；实际退出码和端点计数。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-02/V105A-02-02.log`及工具原生报告。

```bash
npx --no-install tsx scripts/quality/check-digital-human-contracts.ts
```

### V105A-03-01 Python（C105A-03）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-03/V105A-03-01.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_fake_pipeline.py platform-realtime/digital-human/tests/test_import_boundary.py -q
```

### V105A-04-01 Python（C105A-04）

目录：`仓库根`；shell：bash。前提/影响：先完成A01 Python3.11与uv.lock；网络fake。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-04/V105A-04-01.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python -m pytest platform-realtime/digital-human/tests/test_probe_runtime.py -q
```

### V105A-04-02 Fake探针（C105A-04）

目录：`仓库根`；shell：bash。前提/影响：不得调用真实provider；real条件另列。预期：退出0且相关TC实际通过；条件真实项无条件时NOT_RUN。证据：`test-artifacts/task-105/A/C105A-04/V105A-04-02.log`及工具原生报告。

```bash
uv run --project platform-realtime/digital-human --frozen python platform-realtime/digital-human/scripts/probe_runtime.py --mode fake --profile platform-realtime/digital-human/renderer-profile.json --output test-artifacts/task-105/A04/probe.json
```

### 阶段公共门禁

```bash
npm run quality:lifecycle
npm run docs:links
npm run docs:status
npm run security:secrets
git diff --check
```

`quality:lifecycle`从B建表开始必需；A阶段仅既有基线回归，不要求不存在的DH表。UI阶段追加适用DESIGN lint和明暗截图自查；设计工具失败/真实设备缺失均记录具体范围。Java修改阶段先执行本卡定向再受影响service的`:check`；H02汇总跨域门禁，不能将本书全完成代替下一阶段。
### E01～E22边界追踪

这是整条个人版链路的精确责任矩阵；专项卡未到达时标DEPENDENCY_PENDING，不宣称当前阶段已通过它。当前卡对应情形仍在其四组TC中参数化实现；不相关场景无需在纯锁文件/纯部署卡造UI。

| 编号 | 情形 | 固定输入与预期 | 专项责任/用例 |
| --- | --- | --- | --- |
| E01 | 空输入 | text="  "、persona=""、requestId缺失；422且零派发 | C105B-03 / TC105B-03-02 |
| E02 | 超长输入 | text2001/persona4001码点、SDP65537bytes；422/413 | C105B-03 / TC105B-03-02 |
| E03 | 重复提交 | 同request同体/异体各并发2次；原资源或409 | C105B-03 / TC105B-03-03 |
| E04 | 网络失败 | HTTP接受后断连；只查原键，不换经济键 | C105D-01 / TC105D-01-02 |
| E05 | 服务端错误 | provider500/半流、runtime不可达；受控code与pending | C105D-03 / TC105D-03-02 |
| E06 | 未登录 | 无cookie且无内部身份；401、零业务写 | C105B-02 / TC105B-02-02 |
| E07 | 无权限 | 非platform_admin调用管理端；403 | C105G-03 / TC105G-03-01 |
| E08 | 数据为空 | 无角色/历史/voice/catalog；空态、不能点开始 | C105E-02 / TC105E-02-03 |
| E09 | 数据过期 | grant29999/30000ms、preflight60秒、内容结束10分钟 | C105D-02 / TC105D-02-03 |
| E10 | 页面刷新 | 现有会话刷新、建立新controller后明确接管 | C105C-02 / TC105C-02-01 |
| E11 | 用户快速切换 | A→B→A，同账号epoch已变仍拒旧回包 | C105E-03 / TC105E-03-02 |
| E12 | 组件卸载在途 | 权限对话晚返回/流未完成；stop tracks并清timer | C105E-04 / TC105E-04-02 |
| E13 | 缺值与非法枚举 | null/缺字段/空白/tone=unknown/多余orgId | C105A-02 / TC105A-02-03 |
| E14 | 数值长度边界 | 0/1/max/max+1、2^53、负金额、小数sample | C105A-02 / TC105A-02-03 |
| E15 | 并发乱序 | 两个控制器接管、旧epoch/旧seq迟到 | C105C-02 / TC105C-02-01 |
| E16 | 已提交响应丢失 | asset save完成丢响应；原id重试一次资产 | C105F-03 / TC105F-03-02 |
| E17 | 跨主体 | B访问A会话/avatar/media、组织上下文不能越权 | C105G-01 / TC105G-01-02 |
| E18 | 中途撤权删除 | 六窗口撤权/删除墓碑；晚结果不得复活 | C105G-05 / TC105G-05-03 |
| E19 | 旧配置/旧缓存 | expectedVersion/catalogVersion失效、旧core升级 | C105H-02 / TC105H-02-02 |
| E20 | 部分成功补偿失败 | 原run结算503、录制partial、事件重复 | C105D-06 / TC105D-06-01 |
| E21 | 时区/金额 | UTC日切与[from,to)、cents整数、冻结价表 | C105G-01 / TC105G-01-01 |
| E22 | 大文件列表长文案 | 10MiB+1头像、200MiB录制、101条limit、长中文布局 | C105F-01 / TC105F-01-02 |

阶段出口：本书本地必需TC/V与范围门禁通过、前置合同无漂移、所有新增资源有handler/test、下阶段能使用已验证真实接口；条件真实缺口列明。不得将FAKE_PASS写成REAL_QUALIFIED。

## 13. 何时阻塞，何时继续

明确阻塞当前依赖路径：公开字段/权限/资金口径矛盾；现有改动与本卡无法共存；必需前卡失败；已批准模型仍无法满足核心协议且需要换方案。报告必须含卡号、准确文件/符号、最小复现、预期/实际、已排查项与最小所需决定。
可继续：行号/空白变化、正常实现细节、本地依赖安装、已有服务复用、Fake测试；真实硬件/许可/付费授权缺少时，只停止真实实验和开放，不阻塞明确无依赖的本地开发。禁止无限重试、假数据冒充真实或降低门槛。

## 14. 对话交接与执行提示词

逐卡在对话给出：卡号及状态；新增/修改文件；TC PASS/FAIL/NOT_RUN与命令/退出码/报告路径；前置既有失败；越界检查；下一卡。无需另建完成报告。用户明确要求运行手册与机器验收证据的文件已在白名单中，按卡更新即可。
可将下面提示词连同本书交给执行模型：

```text
执行 docs/任务书/草场任务书-105A-数字人工作台-阶段零-上游固定与可行性验证.md，从 C105A-01 开始，采用 AUTO_CHAIN。
先核对当前卡前置，再读 AGENTS.md、适用 DESIGN.md、任务书第0/9/13/14节和该卡明确引用的共享契约K节。
只修改本卡与全局白名单的交集，保留现有工作区修改，不实现未定义的扩展。
每卡完成后真实运行列出的TC/V并汇报，再按依赖自动推进本阶段，不为普通本地步骤逐次等确认。
真实条件不具备如实标NOT_RUN，继续本书中无依赖的本地工作；不能自动部署、打开生产开关或把Fake当真实验收。
业务或资金决策冲突时给出精确阻塞点，不自己换方案。
```
