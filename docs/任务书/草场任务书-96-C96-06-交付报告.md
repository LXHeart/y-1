# C96-06 动作契约与双方待办

日期：2026-09-08。状态：IMPLEMENTED；本卡代码、自动化测试和双主题视觉检查通过，任务书整体验收仍有下述未覆盖项。

## 实现结果

- 既有 GET /api/applications/{id}/settlement 保留 allowedActions 闭集，追加 blockedReason、nextActionDueAt、nextActionGroup、nextActionLabel、benefitStatus。
- EngagementActionContract 从报名、提交、体验权益和延期申请事实生成下一步：筛选、新条款确认、资金预留、审稿、审稿超时、修改、交付补救、验收、体验兑现/失约回应、延期审批、观察期、异常及终态。
- 商家当前任务的报名列表、推荐官当前分页的全部报名均按服务端下一步标签分组。前端倒计时只格式化服务端截止，缺失契约显示待刷新，不以 application.status 重新推导分组。
- 治理台来源筛选增加 draft_review_timeout 和 auto_confirm_held，复用既有分页查询，不给这两类来源开放资金释放操作。

## 修改文件

- Java：新增 taskcatalog/EngagementActionContract.java、EngagementActionContractTest.java；修改 EngagementDecisionService.java、ApplicationControllerIT.java。
- 前端：types/grassland/task.ts、ops.ts；useWorkbenchMyTasks.ts、useWorkbenchEngagements.ts；RecommenderEngagementsPanel.vue、TaskApplicantsPanel.vue；新增 EngagementNextAction.vue 及其测试、useWorkbenchMyTasks.test.ts。
- 治理台：ops-console/OpsConsole.vue、OpsConsole.test.ts。
- 本地验收：scripts/local/shot-96-actions.mjs、test-artifacts/task-96/visual-fixture.mjs 与截图、日志。本报告为入库文档；临时脚本/截图按仓库约定忽略。

## 验收映射

| 映射 | 验证 |
|---|---|
| REQ-96-06 / AC-96-22 / TC96-022 | 7 个动作派生单测；ApplicationControllerIT 的真实 PostgreSQL 接口测试校验观察者权限、下一步字段和观察期截止 |
| AC-96-23 / TC96-023 | 同为 accepted 的不同服务端标签分组测试；倒计时刷新与卸载清理测试；双方真实组件截图 |
| AC-96-24 / TC96-024 | 两类来源筛选参数下推回归测试；批准态仍不得释放资金；治理台双主题截图 |
| AC-96-25 / TC96-025 | 1440x900，商家/推荐官待办各明暗两张，治理台两种筛选各明暗两张，8 张实际查看 |

## 执行证据

| 命令（默认仓库根目录） | 结果 | 日志 |
|---|---|---|
| npm test -- --run --maxWorkers=2 | 退出码 0；170 文件，1542 通过，0 失败 | test-artifacts/task-96/c06-all-frontend.log |
| npm run typecheck | 退出码 0 | c06-typecheck.log |
| npm run lint | 退出码 0；7 个既有 ESLint 警告；Vue 体积门禁通过 | c06-lint.log |
| npm run build | 退出码 0 | c06-build.log |
| ./gradlew :services:marketplace-service:test --max-workers=2（platform-java） | 退出码 0；680 通过，0 失败/错误/跳过；3m58s | c06-java-all.log；服务 build/test-results/test |
| git diff --check | 退出码 0 | 本次命令输出 |
| npm run security:secrets | 退出码 0；2979 文件 | 本次命令输出 |
| node scripts/local/shot-96-actions.mjs | 退出码 0；6 场景组合，0 页面异常 | visual-actions-results.json |

未写目录前缀的日志均位于 test-artifacts/task-96/。设计规范文件未修改，复用 C96-05 续接的两份 design lint 零错误结果，仍有既有警告。

截图：shots/client-grassland-{merchant,recommender}-actions-{light,dark}-1440.png；shots/ops-ops-{draft_review_timeout,auto_confirm_held}-{light,dark}-1440.png。治理台夹具补齐真实 ops.html 的 data-app="ops" 后重拍，最终文件覆盖错误样式作用域的初稿。

## 资金、事件与边界

本卡只读，不写余额、流水、里程碑、迁移或新资金事件。观察期截止沿用结算窗口同源计算；交付/补救与失约回应使用已落库时间。审稿与补交沿用当前 C96-04 配置和提交时间，未补造合同快照。

## 未解决项与偏差

- 未运行任务书 §12.3 的整栈浏览器端到端发布至结算冒烟；浏览器使用合成 API 响应，不能替代这项证据。Java 真实 PostgreSQL 全量集成测试已通过。
- 当前 C96-01 没有持久化的协商退出申请状态，因此无法据事实列出“待退出确认”队列；本卡只展示已有无责退出条件与异常提示，没有自行新增退出状态机。完成该项需补齐退出申请契约和审批规则。
- 延期审批没有独立的既定 SLA，nextActionDueAt 返回原交付截止，并明确延期获批前原期限仍有效；未虚构审批截止。
- 商家待办分组作用于当前选中任务的报名页，尚无跨全部任务的统一待办端点；顶部汇总仍沿用已有进度口径。
- 原任务书列明的生产默认比例、审稿超时缺省与交付天数仍待拍板；未变更生产配置、未发布、未提交 Git commit。

因此不能把本次测试通过解释为任务书 #96 所有目标均已 VERIFIED。
