# 草场使用说明

> 面向开发者与运营人员的安装、运行、配置与功能使用手册  
> 配套文档：[产品需求文档](./草场产品需求文档.md)、[Java 微服务架构](./草场Java微服务技术架构与渐进迁移方案.md)、[系统技术总体设计](./草场系统技术总体设计（HLD-v0.1）.md)、[旧 API 兼容契约矩阵](./草场旧API兼容契约矩阵.md)

---

## 一、项目概览

草场是「全功能营销工具库」产品，正在向「商家 × 推荐官任务撮合 + AI 内容创作」平台演进。当前后端运行面已统一到 Java：

- **Java 平台**（默认 Compose 承载）：`platform-java/`，含统一入口 `edge-bff` 和 identity、marketplace、finance、trust、intelligence 五个已承载业务的领域服务
- **Node 边界**：Express/TypeScript 后端已移除；Node 主工具链用于前端开发/构建、Vitest、E2E seed 和前端 Playwright。Intelligence 的 Java Playwright 仍按上游机制启动 Node driver，但后端 HTTP 与领域逻辑全部是 Java 25

默认 Compose 栈由前端经 `edge-bff` 进入 Java 路由所有者；对应 `EDGE_ROUTE_*` 显式设为 `false` 时，该路由 fail-closed 404。

### 技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue 3.5 + Vite + TypeScript |
| 后端 | Java 25 + Spring Boot 4 + Spring Cloud Gateway |
| 数据库 | PostgreSQL |
| 页面抓取 | Java Playwright（Chromium） |
| HTML 解析 | Java HTML parser |
| 音频处理 | ffmpeg |
| 测试 | Vitest + Supertest（TS）/ JUnit 5（Java） |

---

## 二、环境准备

### 必需

- **Node.js 20+**（推荐 20.20.x）与 npm
- **JDK 25**（Java 后端构建）
- **PostgreSQL**（本地或远程）

### 可选（按功能）

- **ffmpeg**：抖音音频提取
- **Playwright 浏览器**：前端 E2E，`npx playwright install chromium`
- **Docker**：容器化部署与 Java BFF 验证

### 验证环境

```bash
node -v        # >= 20
ffmpeg -version
java -version  # >= 25
docker version # 仅 Docker 部署需要
```

---

## 三、快速开始：本地开发

### 1. 安装依赖

```bash
npm install
npx playwright install chromium   # 可选：抖音扫码登录增强
```

### 2. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env，至少配置：
#   DATABASE_URL=postgresql://用户:密码@主机:5432/库名
#   SESSION_SECRET=<至少 32 字符>
#   DOUYIN_PROXY_TOKEN_SECRET=<至少 32 字符>
#   BILIBILI_PROXY_TOKEN_SECRET=<至少 32 字符>
```

### 3. 启动 Java 后端

```bash
cp .env.docker.example .env.docker
# 编辑 .env.docker，至少填写 QWEN_BASE_URL/QWEN_API_KEY 和四项 MinIO 凭据；
# database-bootstrap 会初始化基础表，缺少必填配置时 Compose 在创建容器前 fail-fast。
docker compose --env-file .env.docker up -d --build
```

`database-bootstrap` 初始化共享账号/session/settings 表，各 Java 领域服务随后执行自己的 Flyway migration。

### 4. 启动开发服务

```bash
npm run dev:client
```

- 前端 Vite：http://localhost:5173
- Java Edge：http://localhost:8081
- Vite 开发代理把 `/api` 转发到 `localhost:8081`

> Node 是前端与测试工具链的必要依赖，但不再启动 Express 后端。

### 其他开发命令

| 命令 | 说明 |
|---|---|
| `npm run dev:client` | 仅前端 |
| `npm run test` | 前端与退役期 TypeScript 契约测试 |
| `npm run e2e:ci` | Java 后端 + Playwright 公共入口 E2E |

---

## 四、生产构建与启动

```bash
npm run build:client
docker compose --env-file .env.docker up -d --build
```

生产模式由 Nginx frontend 托管前端静态文件并把 `/api` 统一转发至 Java Edge；不存在监听 3000 的 Node 后端。

---

## 五、管理员与账号

### 平台管理员与后台角色

仓库当前没有 `npm run admin:create`，也没有生产管理员 bootstrap CLI。后台权限以 Identity 服务的 `backend_role` 表为权威；Flyway `V26__backend_role.sql` 只把已有 `app_users.role=admin` 账号回填为 `platform_admin`，不会创建新账号。

- 已有平台管理员可调用 `PUT /api/admin/users/{id}/roles`，请求体为 `{"action":"grant","role":"platform_admin"}`，为已注册账号授予平台管理员角色；撤销时把 `action` 改为 `revoke`
- 首个平台管理员必须由部署运维在受控数据库环境完成引导并留存审计，不能依赖普通注册流程自行提权
- `npm run e2e:seed` 和 `npm run e2e:seed:auth` 只用于隔离测试数据，禁止用于生产管理员初始化

### 注册流程（普通用户）

1. `GET /api/auth/captcha` 获取图形验证码
2. `POST /api/auth/send-code`（需正确图形验证码）发送邮箱验证码
3. `POST /api/auth/register`（需邮箱验证码）完成注册

新注册用户默认获得积分（`FREE_CREDITS_ON_REGISTER`，默认 3）。

### 登录与登出

- `POST /api/auth/login` 邮箱 + 密码登录
- `GET /api/auth/me` 获取当前用户
- `POST /api/auth/logout` 登出
- 登录失败有 per-IP 与 per-account-IP 限流

---

## 六、配置说明

### 环境变量

完整列表见 `.env.example`、`.env.docker.example` 及各 Java 服务 `application.yml`。常用变量：

| 变量 | 默认 | 说明 |
|---|---|---|
| `SERVER_PORT` | 各 Java 服务默认端口 | Java 服务 HTTP 端口 |
| `CORS_ORIGIN` | `http://localhost:5173,...` | 允许的前端来源 |
| `DATABASE_URL` | 空 | PostgreSQL 连接串 |
| `SESSION_SECRET` | 空 | Session 签名密钥（≥32 字符）|
| `SESSION_COOKIE_NAME` | `y1.sid` | Cookie 名 |
| `DOUYIN_PROXY_TOKEN_SECRET` | 必填 | 抖音代理 token 签名（≥32 字符）|
| `BILIBILI_PROXY_TOKEN_SECRET` | 必填 | B站代理 token 签名（≥32 字符）|
| `FFMPEG_PATH` | `ffmpeg` | ffmpeg 路径 |
| `PUBLIC_BACKEND_ORIGIN` | 空 | 后端公网地址（第三方回源用）|
| `FREE_CREDITS_ON_REGISTER` | `3` | 新用户赠送积分 |
| `LOG_LEVEL` | `info` | 日志级别 |

### AI 功能配置

AI 分析提供者与凭据由**服务端持久化设置**决定，浏览器不能直接调用第三方服务。配置优先级：服务端用户设置 > 环境变量默认值。

支持两类提供者：

- **Coze 工作流**：`COZE_ANALYSIS_BASE_URL` / `COZE_ANALYSIS_API_TOKEN`
- **Qwen（OpenAI 兼容）**：`QWEN_ANALYSIS_BASE_URL` / `QWEN_ANALYSIS_API_KEY` / `QWEN_ANALYSIS_MODEL`
- **图片生成**：`IMAGE_GENERATION_BASE_URL` / `IMAGE_GENERATION_API_KEY` / `IMAGE_GENERATION_MODEL`；任务创作还会冻结 `IMAGE_GENERATION_PROVIDER` / `IMAGE_GENERATION_PRICING_VERSION` / `IMAGE_GENERATION_UNIT_PRICE_CENTS` / `IMAGE_GENERATION_PLATFORM_MODEL_VERSION`，运行配置漂移后旧任务生图会 fail-closed。
- **视频制作脚本**：`VIDEO_PRODUCTION_SCRIPT_BASE_URL` / `VIDEO_PRODUCTION_SCRIPT_API_KEY` / `VIDEO_PRODUCTION_SCRIPT_MODEL`

> 文章生成仅支持 Qwen。

在「设置弹窗」中：

- 密钥留空保存 = 保留已保存的密钥
- 输入空格后保存 = 清空密钥
- 可用「模型验证」测试配置是否可用

### 首页热点配置

- **60s API**（默认）：抖音/微博/知乎，缓存 2 小时
- **ALAPI**：需 Token，抖音/微博/微信/小红书，缓存 5 分钟

---

## 七、功能模块使用

### 1. 视频提取分析（抖音 / Bilibili）

- 粘贴分享文本或链接 → 后端解析视频地址
- 预览 / 下载走后端签名代理，不暴露上游地址
- 抖音支持音频提取（ffmpeg）
- AI 视频分析（需登录、消耗积分）；视频时长 >10 分钟不支持分析

抖音登录增强（Playwright 扫码）仅作为 fallback，非默认路径。

### 2. 图片评价文案

上传图片（≤6 张）→ 选择平台（淘宝/大众点评）→ AI 生成评价（SSE 流式）→ 一键复制 / 导出飞书。

多步骤生成：草稿 → 文案优化 → 风格精修；支持评价风格记忆。

### 3. 爆款文章创作

主题 → 选平台（微信公众号/知乎/小红书）→ AI 生成标题 → 流式大纲 → 流式正文 → 按段落配图（推荐/搜索/AI 生成）。

### 4. 脱口秀创作

输入主题 → 仿风格流式生成脚本（需登录、消耗积分）。

### 5. 图片生成

文字描述 + 可选参考图 → AI 生成图片（支持 @mention）。

### 6. 视频内容改编

视频分析结果 → 分镜脚本（角色/场景/道具）→ 生成资产图/场景图（单张/批量）。

### 7. 视频制作

上传素材图片（≤9 张，base64，JSON body ≤10MB）+ 店铺信息 → AI 脚本生成（SSE 流式）→ 异步视频生成。视频任务由后端真实状态驱动，支持任务查询、失败补偿与按实际秒数记录成本。

> 默认 `VIDEO_GENERATION_MODE=sandbox`，无需第三方凭据即可测试完整异步和计费闭环。后续设置 `seedance` 或 `minimax`，并配置 `VIDEO_GENERATION_BASE_URL`、`VIDEO_GENERATION_API_KEY`、`VIDEO_GENERATION_MODEL`、`VIDEO_GENERATION_UNIT_PRICE_CENTS` 即可切换真实渠道；供应商字段需用真实账号联调确认。

### 8. 积分系统

- 新用户注册赠送积分
- 视频分析、图片评价、文章生成、脱口秀、图片生成、视频改编、视频制作脚本均消耗积分
- 积分扣减使用原子操作，防并发超扣
- 管理员可调整任意用户积分

### 9. 管理后台

后台入口按 `backend_role` 显示，服务端仍会独立校验角色；前端显隐不构成授权。`platform_admin` 是超集角色，管理模块包括用户与积分、KYB、推荐官与任务审核、等级权益、审判官准入、财务、风险、积分套餐、经营分析、订单核销、AI 模型和统一审计。

### 10. 设置与主题

- 分析设置 + 首页设置弹窗（需登录）
- 主题：亮色 / 暗色 / 跟随系统（Header 切换按钮循环切换）

---

## 八、Docker Compose 部署

### 1. 准备配置

```bash
cp .env.docker.example .env.docker
# 编辑 .env.docker
```

### 2. 启动

```bash
docker compose --env-file .env.docker build
docker compose --env-file .env.docker up -d
```

- 前端容器（Nginx）：宿主机 `${FRONTEND_PORT:-8080}` → 80
- Nginx 把 `/api` 和 `/health` 统一代理到 `edge-bff:8080`
- Edge 按 RouteManifest 分流到 identity/marketplace/finance/trust/intelligence；未命中、method 不匹配或关闭的 route flag 返回 404
- Edge `${EDGE_BFF_PORT:-8081}` 只绑定 `127.0.0.1`，不能从公网绕过 Nginx

> Docker 部署包含 Intelligence 的 Java Playwright 浏览器登录增强；容器内 Node 仅作为 Playwright driver，不是后端应用服务。

### 3. 验证

```bash
docker compose --env-file .env.docker config --quiet
curl http://<host>:<FRONTEND_PORT>/health
curl -i http://<host>:<FRONTEND_PORT>/api/auth/captcha
```

### 4. Edge BFF 路由门禁

`edge-bff`（Spring Cloud Gateway WebFlux）是默认 API 入口。对象存储 presigned PUT `:9002` 是有意保留的直传例外。

```bash
# 本地构建 Java（需 JDK 25）
cd platform-java && ./gradlew clean build && cd ..

# 默认栈已包含 Edge 与五个 Java 领域服务
docker compose --env-file .env.docker up -d --build

# 验证 BFF 透明代理
curl -i http://localhost:8081/health
curl -i http://localhost:8081/api/auth/captcha
```

- BFF 本机诊断映射 `127.0.0.1:${EDGE_BFF_PORT:-8081}` → 容器 8080
- 逐路由停用：设置对应 `EDGE_ROUTE_*` 为 `false` 后 recreate `edge-bff`，该路由返回 404
- 当前已迁移 Java 路由默认开启，包括 Bilibili/Douyin 媒体、抖音热点和视频内容改编；视频改编使用平台级 Qwen，不读取用户级 BYOK 配置
- `API_UPSTREAM` 必须保持 `edge-bff:8080`；不支持整入口回退到 Express
- TLS 在上游 LB/ingress 终止时设置 `PUBLIC_FORWARDED_PROTO=https` 和实际的 `TRUSTED_PROXY_CIDR`；禁止直接信任客户端转发头
- 不做自动 upstream failover，避免非幂等请求重复执行
- 可观测性组件按需以 `--profile observability` 启动

详见 [旧 API 兼容契约矩阵](./草场旧API兼容契约矩阵.md) 与 [Java 微服务架构](./草场Java微服务技术架构与渐进迁移方案.md)。

---

## 九、Java 平台（草场目标架构）

### 目录结构

```
platform-java/
├── settings.gradle.kts          # Gradle 多模块根
├── gradle/libs.versions.toml    # 版本目录
├── services/
│   ├── edge-bff/                # 透明代理（已实现）
│   ├── identity-service/        # 账号、身份、组织、KYB、通知
│   ├── marketplace-service/     # 任务、履约、运营、套餐与消费订单
│   ├── finance-service/         # 托管、钱包、积分、消费者支付与双录账本
│   ├── trust-service/           # 争议、审判与客服终审
│   └── intelligence-service/    # AI 控制面、创作、媒体与热点
└── contracts/legacy-wire-fixtures/
```

### 构建与测试

```bash
cd platform-java
export JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home
./gradlew clean build          # 全部 6 模块
./gradlew :services:edge-bff:test   # 仅 BFF 契约测试
```

### 运行时约定

- 五个领域服务已承载身份、撮合、资金、争议和智能域的已迁移路由
- `edge-bff` 是默认且唯一的公网 API 服务入口
- 新草场领域数据直接进入 Java 服务，不写回旧库
- 详细迁移路线与状态见 [Java 微服务架构](./草场Java微服务技术架构与渐进迁移方案.md)

---

## 十、测试与质量检查

### 现有应用

```bash
npm run test        # Vitest 全量测试
npm run typecheck   # vue-tsc + tsc 类型检查
npm run build       # Vue/Vite 前端生产构建
```

### Java 平台

```bash
cd platform-java
./gradlew clean test
```

### 优先测试文件（按模块）

| 模块 | 测试 |
|---|---|
| 提取 | `douyin-resolve`、`douyin.controller`、`douyin-proxy` |
| 音频 | `douyin-audio.service` |
| 分析 | `video-analysis.service`、`bilibili-video-analysis.service` |
| 文章 | `article-generation-dispatch.service` |
| 图片评价 | `image-analysis.controller` |
| 认证 | `auth.controller`、`auth` |
| 热点 | `hot-topics-60s.service`、`homepage-hot.service` |
| 设置 | `settings.controller`、`analysis-settings.service` |

---

## 十一、常见问题与排错

### 启动相关

| 问题 | 解决 |
|---|---|
| 后端 503「认证不可用」 | 检查 `DATABASE_URL` 和 `SESSION_SECRET` 是否配置 |
| 注册收不到验证码 | 检查 `SMTP_*` 配置；本地可用邮件捕获服务 |
| 视频无法播放/下载 | 确认 `DOUYIN_PROXY_TOKEN_SECRET` / `BILIBILI_PROXY_TOKEN_SECRET` 与签发时一致 |
| 抖音音频提取失败 | 确认 `FFMPEG_PATH` 指向可用 ffmpeg |
| 视频分析超时 | 视频 >10 分钟不支持；可调 `VIDEO_ANALYSIS_API_TIMEOUT_MS`（默认 180s） |

### 配置相关

| 问题 | 解决 |
|---|---|
| AI 功能报「未配置」 | 在设置弹窗配置对应提供者；或设置环境变量默认值 |
| 文章生成报 provider 不支持 | 文章仅支持 Qwen，Coze 不支持自由文本 |
| 热点不更新 | 60s 缓存 2 小时；ALAPI 需配置 Token |
| 密钥保存后丢失 | 密钥留空 = 保留旧值；输入空格 = 清空 |

### Docker 相关

| 问题 | 解决 |
|---|---|
| `docker pull` 报 EOF | Docker Hub 网络波动，稍后重试或配置镜像加速 |
| Java 后端镜像构建失败 | 先在 `platform-java/` 运行对应模块的 Gradle 测试或 `bootJar` |
| BFF 容器健康但 502 | 检查目标 Java 服务健康状态与对应 `*_SERVICE_BASE_URL` |

### Java 平台相关

| 问题 | 解决 |
|---|---|
| `./gradlew` 找不到 Java | `export JAVA_HOME=/opt/homebrew/opt/openjdk@25/...` |
| edge-bff 启动报 base-url 非法 | 确认各 Java `*_SERVICE_BASE_URL` 是合法 `http(s)://host` 且无 query/fragment |
| BFF 代理 SSE 卡住 | 确认未启用响应压缩；`X-Accel-Buffering: no` 已透传 |

---

## 十二、安全注意事项

- 浏览器**不能**直接调用第三方 AI/分析服务，凭据由服务端管理
- 视频/媒体预览下载走后端签名代理，不暴露上游真实地址
- Intelligence 的 `ProviderUrlGuard` 校验 provider URL、协议和私网地址，媒体链路另有受信 host 白名单
- Edge 负责公网身份校验和内部断言，Java 领域服务按 `backend_role` 再做服务端 RBAC
- 新密码使用 Argon2id；历史 bcrypt 哈希在登录成功后自动升级，Session Cookie 由服务端签名
- 所有端点带基础限流，登录失败有独立限流

> ⚠️ `.env`、`.env.docker` 含密钥，勿提交到 Git。仓库中仅保留 `.env.example` / `.env.docker.example`。

---

## 十三、文档导航

| 文档 | 用途 |
|---|---|
| [草场使用说明](./草场使用说明.md)（本文档） | 安装、运行、配置、功能使用 |
| [草场产品需求文档](./草场产品需求文档.md) | 产品定位、角色、业务规则、AI 内容创作中心 |
| [草场 Java 微服务技术架构](./草场Java微服务技术架构与渐进迁移方案.md) | Java 迁移目标架构与路线 |
| [草场系统技术总体设计（HLD）](./草场系统技术总体设计（HLD-v0.1）.md) | 系统级技术设计基线 |
| [草场旧 API 兼容契约矩阵](./草场旧API兼容契约矩阵.md) | BFF 透明代理契约 |
| [归档](./归档/) | 合并前的历史来源文档 |
