# 开发规划：<需求/项目名>

<!-- 弱模型执行时只需要读：第 4 节全局约束 + 当前卡。
     其余章节是给强模型自己和总控看的，不要求执行者通读。 -->

## 元信息
- 规划模型：<模型名 + 日期>
- 任务卡总数：N
- 执行顺序：<卡1 → 卡2 → 卡3；标明哪些卡可并行、哪些必须串行>

## 1. 目标与范围
**一句话目标**：<做什么，做完后系统具备什么能力>

**范围内（明确交付）**：
- <条目>

**范围外（明确不做，遇到也不处理）**：
- <条目，如：不改现有登录逻辑 / 不做国际化 / 不写 UI 样式>

## 2. 技术决策（已定案，执行期不得更改）
| 决策项 | 结论 |
|---|---|
| 语言/框架/版本 | <如 TypeScript 5.x + Vue 3.4> |
| 新增依赖 | <精确到包名@版本；写"无"则禁止新增任何包> |
| 文件布局 | <完整文件树，标明新建/修改> |
| 错误处理策略 | <统一模式，如：错误统一走 Result 包装，禁止裸 throw> |
| 命名约定 | <文件名、导出名、CSS 类的规则> |
| 风格参照 | <现有文件路径，如：导出与注释风格照 utils/date.ts> |

## 3. 接口契约（本节内容为最终代码，直接照抄进项目，不得修改）
```ts
<!-- 类型、函数签名、API 端点契约、数据 schema。
     全部写成可粘贴的完整代码，不用自然语言描述。
     弱模型只负责实现这些签名背后的逻辑，无权改签名。 -->
export interface TaskRecord {
  id: string
  title: string
  status: 'todo' | 'doing' | 'done'
}

export function createTask(input: NewTaskInput): Promise<TaskRecord>
```
<API 契约：方法 + 路径 + 请求/响应 schema + 状态码>
<存储 schema / 迁移脚本>

## 4. 全局约束（适用于每一张卡）
- 只允许改动当前卡列出的文件，其他文件一律不碰。
- 不删除既有代码，除非当前卡明确要求。
- 不引入第 2 节依赖清单之外的包。
- 不修改第 3 节任何签名、类型和契约。
- 验收命令必须全绿才算完成；禁止为了绿灯修改断言或跳过用例。
- **卡住时**：同一问题最多尝试 2 次，然后停止，原样报告错误信息和已尝试的做法。禁止猜测、禁止绕过、禁止编造一个看起来合理的结果。

## 5. 任务总表
<!-- 给人看进度用的，一行一卡 -->
| 卡 | 标题 | 主要文件 | 依赖 | 验收方式 |
|---|---|---|---|---|
| 1 | <标题> | src/a.ts | 无 | npm test a 通过 |
| 2 | <标题> | src/b.ts | 卡1 | npm run build 通过 |

## 6. 任务卡

### 卡 1：<一句话标题>
**背景**：<为什么做，1–2 句。只给执行者判断"在做什么"所需的最低上下文>

**改动文件**：
- 新建 `src/xxx.ts`
- 修改 `src/yyy.ts`（仅限其中的 X 部分）

**开始前检查**：<开始时系统应处于的状态及确认方式，如：卡 1 已完成、工作区干净、`npm test` 全绿>

**参考代码**（与本卡直接相关的现有代码，直接贴全，不要求执行者自己去找）：
```ts
<!-- 贴片段 -->
```

**做法**：
1. <动作一：精确到"在哪个文件、加什么、放哪里"，不用形容词>
2. <动作二>
3. <…>

**边界**（本卡特有的禁止事项）：
- <如：不做并发控制，那是卡 3 的事>

**验收**：
- 运行 `<精确命令>`，期望 `<精确输出 / 退出码>`
- 运行 `<命令>`，期望 `<…>`

**完成后按此格式报告**：
```
改动文件：...
执行的命令与结果：...
偏离卡面之处：无 / <列出>
卡住项：无 / <错误原文>
```

### 卡 2：<…同上结构>

## 7. 集成验收（全部卡完成后执行，可由强模型或人工触发）
- 端到端：运行 `<命令>`，期望 `<…>`
- 回归：运行 `<全量测试命令>`，期望全绿
- 产物检查：`<lint / 类型检查 / build>`

## 附：返工卡格式（强模型 review 后按此格式产出，编号 R-1、R-2…）

### 返工卡 R-1（针对卡 2）
**问题位置**：`src/b.ts:34`
**期望行为 vs 实际行为**：<一句话对比>
**修复做法**：
1. <步骤化，同任务卡的写法>
**验收**：运行 `<命令>`，期望 `<…>`