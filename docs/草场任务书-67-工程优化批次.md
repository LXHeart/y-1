# 开发规划:工程优化批次(草场任务书 #67)

<!-- 弱模型执行时只需要读:第 3 节架构事实 + 第 4 节全局约束 + 当前卡。
     其余章节是给强模型自己和总控看的,不要求执行者通读。 -->

## 元信息
- 规划模型:GLM-5.3(ZCode),2026-09-03(基于同日全项目盘点的十大优化点)
- 执行模型:能力较弱的编码模型(Qoder / 同级)
- 任务卡总数:9(卡 A–卡 I)
- 执行顺序:A → B → C → D → E → F → G → H → I(**严格串行**,任何一卡验收不绿不得进入下一卡)。
  刻意排序:**卡 D(Jacoco 门禁)必须在卡 F/G(Java 拆分)之前**——拆分重构要在覆盖率门禁保护下做;**卡 C(manualChunks)必须在卡 I 之前**——卡 I 的 chunk 体积验收以最终分包配置为口径。
- 前置依赖:main 干净基线(`git status --short` 为空)。

## 1. 目标与背景

**一句话目标**:清偿 2026-09-03 全项目盘点定位的工程债——生产冒烟路由收口、设计 token 违规清零、构建分包与覆盖率门禁补齐、账本 DB 级兜底、四个超大文件拆分——全部**行为零变更**,门禁全绿。

**盘点处置表(2026-09-03 拍板,执行期不得更改)**:

| 盘点项 | 处置 |
|---|---|
| 1. git 历史里的 64MB 截图 | **非 Qoder**(历史重写需用户拍板,见附录 B) |
| 2. 生产冒烟路由公网可达 | 卡 A |
| 3. 根目录一次性产物清理 | **非 Qoder**(.gitignore 注释已声明「留磁盘留档」意图,是否清盘待用户拍板,见附录 B) |
| 4. 拆超大文件 | 卡 F/G(Java)+ 卡 H(ImageAnalysisView);GrasslandWorkbench 只做异步分包(卡 I),行数拆分另批;ArticleCreationView / VideoProductionView / MerchantKybCard **范围外**(见 §2) |
| 5. Jacoco 覆盖率门禁缺位 | 卡 D |
| 6. 42 处硬编码 hex 违反 DESIGN.md | 卡 B |
| 7. 账本无 DB 级不可变/平衡兜底 | 卡 E |
| 8. vite manualChunks 偏薄 | 卡 C |
| 9. TaskController 1066 行 / MediaController 959 行 | 卡 F / 卡 G |
| 10. .gitignore tmp/ 缺口 + 陈旧 worktree | tmp/ 进卡 A;worktree 清理**非 Qoder**(见附录 B) |

**关键决策(已定案)**:
- 冒烟路由:**生产 overlay 显式置 false,本地 compose 默认 true 不动**(本地链路验证依赖它;`scripts/production-smoke.sh` 已核实不打该端点,关闭无副作用)。
- 账本兜底:UPDATE/DELETE 一律 DB 触发器拦死,唯一维护通道是 `ledger_maintenance_delete_journals()` 函数;借贷平衡用 **DEFERRABLE INITIALLY DEFERRED 约束触发器在 COMMIT 时校验**(全部 posting 与 journal 头同一事务写入,已核实 `LedgerRepository.postJournal` 不自启事务、由调用方单事务包裹,Deferred 安全)。
- 覆盖率门禁:**先度量后设门**——阈值 = 各模块基线指令覆盖率下取整 − 5 个百分点,下限 30%;升阈值另批。
- 颜色替换:能语义对上的用现成 token;DESIGN.md 无对应弱底色 token 的,一律 `color-mix(in srgb, var(--语义token) 10%, transparent)` 派生(双主题自动成立),**禁止新造 token、禁止保留任何裸 hex**。

## 2. 范围

**范围内(明确交付)**:
- 卡 A:生产 compose 关闭冒烟路由 + 契约测试防回归 + .gitignore 补 `tmp/`
- 卡 B:全仓 .vue 裸 hex 清零(7 个文件 42 处)→ DESIGN.md token / color-mix 派生
- 卡 C:vite manualChunks 扩充(marked / dompurify / qrcode vendor 块)
- 卡 D:Jacoco 覆盖率阈值门禁(基线实测 → 填表 → violationRules → check 接线)
- 卡 E:finance-service V21 迁移(journal/posting 不可变触发器 + 借贷平衡约束触发器 + 维护函数)+ 既有 IT 清理改造 + 新增护栏 IT
- 卡 F:TaskController 提取 admin review 块 → `TaskReviewAdminController`
- 卡 G:MediaController 提取 KYB 族端点 → `KybMediaController`
- 卡 H:ImageAnalysisView(1818 行)script 区块提取 4 个 composable
- 卡 I:GrasslandWorkbench 重资产卡片 defineAsyncComponent 懒加载(chunk 345KB → <250KB)

**范围外(明确不做,遇到也不处理)**:
- **不做** GrasslandWorkbench 的模板/行数拆分(状态全在 useWorkbench* composables 里,盲目提取子组件会引入大量 props 传递,需总控先出提取设计,另批立项)
- **不做** ArticleCreationView(1710)/ VideoProductionView(1698)/ MerchantKybCard(1539)的拆分(同上,另批)
- 不改任何业务逻辑、任何接口契约、任何测试断言(行为零变更红线)
- 不引入任何新 npm 依赖、任何新 Gradle 插件
- 不动 git 历史(截图剥离见附录 B)
- 不清理根目录留档产物(附录 B,待用户拍板)
- 不做浏览器截图自查(本地跑不了完整栈的惯例;卡 B 的视觉回归由总控验收时补拍)

## 3. 架构事实(执行者必读,全部已核实)

### 3.1 冒烟路由(compose 与 edge)

| 事实 | 锚点 |
|---|---|
| edge 路由 `/api/intelligence` → intelligence-service,由 `EDGE_ROUTE_INTELLIGENCE_SMOKED` 门控,**默认 true** | `platform-java/services/edge-bff/src/main/resources/application.yml:341-344` |
| 该前缀承载的是 SmokeController(真实消耗平台 Qwen 上游积分的链路验证端点);**前端 src/ 零业务调用** | `intelligence-service/.../smoke/SmokeController.java`;`grep -rn "api/intelligence" src/` 无业务命中 |
| 本地栈默认开(本地冒烟流程依赖) | `docker-compose.yml:353`:`EDGE_ROUTE_INTELLIGENCE_SMOKED: ${EDGE_ROUTE_INTELLIGENCE_SMOKED:-true}` |
| 生产 overlay 的 edge-bff environment **没有**任何 EDGE_ROUTE_* 覆盖(即生产也是 true) | `docker-compose.production.yml:67-72` |
| compose 合并语义:overlay 与 base 的 environment map 按 key 合并,同 key overlay 覆盖 base;生产文件顶层有 `:?` 强制变量,`docker compose config` 离线跑不通,验收用文件内容断言 | Docker Compose 规范 |
| 发布冒烟脚本不打该端点(只打 404 探针/视频配置/带鉴权 marketplace 读) | `scripts/production-smoke.sh:58-73` |
| 部署契约测试已存在,新增断言照它的文件常量风格写 | `test/deployment/production-operations.contract.test.ts`(`REPOSITORY_ROOT` 常量、`readFileSync` 已在用) |

### 3.2 设计 token(DESIGN.md / src/style.css)

| 事实 | 锚点 |
|---|---|
| 语义色 token 双主题成对定义(`:root` 暗 + `[data-theme="light"]` 亮):`--color-text / --color-text-secondary / --color-text-muted / --color-danger / --color-success / --color-warning / --color-info / --color-border / --color-border-hover / --color-surface / --color-surface-hover / --color-surface-strong / --color-primary` | `src/style.css:16-33`(暗)、`:104-118`(亮) |
| 全仓 .vue 裸 hex 共 42 处,分布:OrganizationBrandCard 19、DefaultLayout 5、LoginModal 3、StoreMediaGallery 1、ImageStudioView 1、DouyinParsePanel 1、BilibiliParsePanel 1(其余在上述文件内) | `grep -rc '#[0-9a-fA-F]\{6\}' src --include='*.vue'` |
| SVG 渐变 `stop-color` 属性写法不吃 CSS 变量,须写成 `style="stop-color: var(--color-primary)"` | DefaultLayout.vue:10-12、LoginModal.vue:13-15(现值 #665efd 与 --color-primary 同值) |

### 3.3 构建与测试门禁

| 事实 | 锚点 |
|---|---|
| vite manualChunks 目前只有 `vue` 一块;`marked@18 / dompurify@3.4 / qrcode@1.5` 均在 dependencies | `vite.config.ts:15-19`、`package.json:32-36` |
| 路由 100% 懒加载,双入口(index.html / ops.html),nginx immutable 缓存承接哈希文件名 | `src/router/index.ts`、`vite.config.ts`、`nginx.conf` |
| 最大业务 chunk 是 GrasslandWorkbench(≈345KB);AdminView 157KB、AiCreationCenter 143KB | `npm run build` 后 `dist/assets/` 实测 |
| Jacoco 已配 toolVersion 0.8.15 + XML/HTML 报告 + test finalizedBy jacocoTestReport,**无阈值**(注释自认「阈值门禁待基线数字稳定后另批引入」——本批就是那一批) | `platform-java/build.gradle.kts:55-68` |
| 前端门禁:vitest 阈值 76/76/58/76 + CI changed-coverage 80% | `vitest.config.ts` |
| vitest 收录 `src/**/*.test.ts` 与 `test/**/*.test.ts`(部署契约测试跑 vitest) | `vitest.config.ts:16` |
| **本地 Gradle 必须 JDK 25**:`JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home`,JDK 8 直接挂 | 本机既成事实 |
| Gradle 命令一律在 `platform-java/` 目录下执行 `./gradlew`;spotless ratchetFrom origin/main(新文件会被格式化,存量零 churn) | `platform-java/build.gradle.kts` |

### 3.4 账本(finance-service)

| 事实 | 锚点 |
|---|---|
| journal/posting 纯 INSERT 落表;`postJournal` **不自启事务**,调用方在其 TransactionalOperator 作用域内一次写入 journal 头 + 全部 posting(不存在跨事务追加 posting 的通路)→ COMMIT 时校验平衡是安全的 | `finance-service/.../ledger/LedgerRepository.java:14,31`;`LedgerService.java:225-273` |
| 服务层已有 `assertBalanced(postings)`(每笔 journal 借贷平衡的代码级保证);本卡补的是 **DB 级兜底** | `LedgerService.java` 各 post* 方法 |
| V5 schema:`journal` 表(journal_type/operation_id 幂等唯一索引/…)与 `posting` 表(direction CHECK IN ('DEBIT','CREDIT')、amount_cents CHECK > 0) | `finance-service/src/main/resources/db/migration/V5__ledger.sql` |
| 不可变触发器**仓内先例**(照抄结构):credits 配额表 V10、identity PII 审计 V37 | `V10__ai_daily_quota.sql:57,168`、`identity-service/.../V37__personal_data_compliance.sql:92` |
| **既有测试用裸 DELETE 清理账本行**(不可变触发器落地后会被拦,必须同卡改造):`DELETE FROM posting …; DELETE FROM journal …` | `finance-service/src/test/.../aicredits/CreditsPurchaseControllerIT.java:34-37` |
| finance 迁移最新版本号 **V20**(新迁移命名 V21) | `finance-service/src/main/resources/db/migration/` |
| Flyway 重放测试要求迁移幂等:DDL 一律 `CREATE OR REPLACE FUNCTION` + `DROP TRIGGER IF EXISTS` + `CREATE TRIGGER` | 仓内全部触发器迁移惯例 |
| `released-migrations.sha256` 只登记**已发布环境应用过**的迁移(V20 都不在内)——新迁移 V21 **不需要**登记,但契约测试必须保持绿 | `platform-java/deploy/released-migrations.sha256` 头注释 |

### 3.5 超大文件与测试基线

| 事实 | 锚点 |
|---|---|
| TaskController 1066 行;L457-593 是独立的「任务内容审核(GL-P2-ADMIN-003)」块(`/api/admin/tasks/review` 五端点 + history/stats),与其余端点无共享私有方法 | `marketplace-service/.../taskcatalog/TaskController.java:457-593` |
| MediaController 959 行;KYB 族端点(kyb-upload-tickets / kyb-retentions×3 / kyb-metadata / kyb-download-url)自成一族可整体搬移 | `intelligence-service/.../media/MediaController.java:150,226-300` |
| ImageAnalysisView 1818 行(模板约到 L577,script 从 L578 起);script 内四块自洽状态可提取:飞书凭据(L598-720)、多版本对比(L747-800)、图片预览灯箱(previewIndex/previewImage/closePreview)、上传拖拽(isDragging/uploadError/fileInput 族) | `src/views/image/ImageAnalysisView.vue` |
| composable 提取的仓内先例(照抄形态):GrasslandWorkbench 曾 1817→733 五 composable 化,行为零变更 | `src/views/grassland/composables/` |
| 两个视图各有既有测试,是「行为零变更」的验收锚点 | `src/views/image/ImageAnalysisView.test.ts`、`src/views/grassland/GrasslandWorkbench.test.ts` |

## 4. 全局约束(适用于每一张卡)
- 只允许改动当前卡列出的文件,其他文件一律不碰。
- 不删除既有代码,除非当前卡明确要求(卡 F/G/E 的搬移/改造除外,且必须整块搬、不改内容)。
- 不引入新依赖(npm 包 / Gradle 插件均禁止)。
- **行为零变更红线**:接口路径、请求/响应结构、权限注解、UI 渲染结果、测试断言一律不动;「测试零改动」仅允许卡 I 明示的 `flushPromises()` 例外。
- 验收命令必须全绿才算完成;禁止为了绿灯修改断言、缩小测试范围或跳过用例。
- Gradle 一律在 `platform-java/` 目录、`JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home` 下执行;npm/vitest 一律在仓库根执行。
- **卡住时**:同一问题最多尝试 2 次,然后停止,原样报告错误信息和已尝试的做法。禁止猜测、禁止绕过、禁止编造一个看起来合理的结果。

## 5. 任务总表

| 卡 | 标题 | 主要文件 | 依赖 | 验收方式 |
|---|---|---|---|---|
| A | 生产冒烟路由收口 + tmp/ 忽略 | docker-compose.production.yml、production-operations.contract.test.ts、.gitignore | 无 | 契约测试绿 + 双向 grep |
| B | 全仓裸 hex → token | 7 个 .vue | 无 | grep 0 命中 + vitest + build |
| C | vite manualChunks 扩充 | vite.config.ts | 无 | build 产物检查 |
| D | Jacoco 阈值门禁 | platform-java/build.gradle.kts | 无(C 后执行只为排序习惯,无硬依赖) | gradle test 全绿 + 阈值自检红/绿 |
| E | 账本 V21 DB 兜底 | finance V21 迁移、CreditsPurchaseControllerIT、新 IT | 无 | finance-service test 全绿 + 契约测试绿 |
| F | TaskController 拆分 | TaskController.java、新 TaskReviewAdminController.java | **D 必须先完成** | marketplace-service test + spotlessCheck |
| G | MediaController 拆分 | MediaController.java、新 KybMediaController.java | **D 必须先完成** | intelligence-service test + spotlessCheck |
| H | ImageAnalysisView 拆分 | ImageAnalysisView.vue、新 composables/×4 | 无 | 测试零改动全绿 + 行数 <1300 |
| I | GrasslandWorkbench 异步分包 | GrasslandWorkbench.vue、GrasslandWorkbench.test.ts(仅限 flushPromises) | **C 必须先完成** | vitest 全绿 + 主 chunk <250KB |

## 6. 任务卡

### 卡 A:生产冒烟路由收口 + tmp/ 忽略
**背景**:`/api/intelligence` 前缀(冒烟链路端点,真实消耗平台上游积分)在生产的 edge 路由默认开、公网可达。生产不需要它(发布冒烟脚本不打它、前端零调用),应显式关闭并加契约测试防回归。

**改动文件**:
- 修改 `docker-compose.production.yml`(仅 edge-bff 的 environment)
- 修改 `test/deployment/production-operations.contract.test.ts`(新增一个 it 块)
- 修改 `.gitignore`(仅 tmp 相关一行)

**开始前检查**:`git status --short` 为空;`grep -n "EDGE_ROUTE_INTELLIGENCE_SMOKED" docker-compose.yml` 输出 L353 默认 true(不要动它)。

**参考代码**:
```yaml
# docker-compose.production.yml 现状(L67-72):
  edge-bff:
    <<: *production-resources
    environment:
      <<: [*otel-production, *structured-logging]
      LOGGING_STRUCTURED_ECS_SERVICE_NAME: edge-bff
```

**做法**:
1. 在 `docker-compose.production.yml` 的 edge-bff `environment` 内(`LOGGING_STRUCTURED_ECS_SERVICE_NAME` 之后)加:
```yaml
      # 任务书 #67:生产关闭 intelligence 冒烟路由(SmokeController 真实消耗平台上游积分,
      # 仅限本地链路验证;production-smoke.sh 不打该前缀,前端零业务调用)。
      EDGE_ROUTE_INTELLIGENCE_SMOKED: "false"
```
2. 在 `test/deployment/production-operations.contract.test.ts` 中新增(放在任一相邻 describe 内,沿用文件既有常量):
```ts
it('任务书 #67:生产 overlay 必须显式关闭 intelligence 冒烟路由', () => {
  const productionCompose = readFileSync(
    resolve(REPOSITORY_ROOT, 'docker-compose.production.yml'),
    'utf8',
  )
  expect(productionCompose).toContain('EDGE_ROUTE_INTELLIGENCE_SMOKED: "false"')
})
```
3. `.gitignore` 中把 `tmp/e2e-validation-artifacts/` 这一行替换为 `tmp/`(整目录;原注释行 `# Playwright MCP 会话产物…` 保留不动)。

**边界**:
- 不改 `docker-compose.yml` 的默认 true(本地冒烟依赖)。
- 不改 edge-bff 的 application.yml 路由表本身。
- 不动 `validate-production-compose.sh`。

**验收**:
- 仓库根运行 `npx vitest run test/deployment/production-operations.contract.test.ts`,期望全绿(含新增 it)。
- `grep -c 'EDGE_ROUTE_INTELLIGENCE_SMOKED: "false"' docker-compose.production.yml` 期望 `1`。
- `grep -n 'EDGE_ROUTE_INTELLIGENCE_SMOKED' docker-compose.yml` 期望仍为 `${EDGE_ROUTE_INTELLIGENCE_SMOKED:-true}`。
- `git check-ignore -v tmp/anything` 期望命中 `.gitignore` 的 `tmp/` 规则。

**完成后按此格式报告**:
```
改动文件:...
执行的命令与结果:...
偏离卡面之处:无 / <列出>
卡住项:无 / <错误原文>
```

### 卡 B:全仓裸 hex → DESIGN.md token
**背景**:DESIGN.md 规定颜色只允许用 token。全仓 .vue 尚有 42 处裸 hex,近半集中在 OrganizationBrandCard。本卡一次性清零。

**改动文件**(仅这 7 个):
- `src/components/OrganizationBrandCard.vue`(19 处,L330-496 区域)
- `src/layouts/DefaultLayout.vue`(5 处,L10-12 SVG)
- `src/components/LoginModal.vue`(3 处,L13-15 SVG)
- `src/views/grassland/components/StoreMediaGallery.vue`(1 处)
- `src/views/ai-center/components/ImageStudioView.vue`(1 处)
- `src/components/DouyinParsePanel.vue`(1 处,L370 `#06080d`)
- `src/components/BilibiliParsePanel.vue`(1 处,L379 `#06080d`)

**开始前检查**:通读 `src/style.css` 的 `:root`(暗)与 `[data-theme="light"]`(亮)token 清单;`grep -rn '#[0-9a-fA-F]\{6\}' src --include='*.vue'` 确认与上表一致(行号允许有少量漂移)。

**映射表(定死,逐处对号入座)**:

| 现值 | 用途 | 替换为 |
|---|---|---|
| `#6b7280` / `#9ca3af` | `color:` 次要/弱文本 | `var(--color-text-muted)` |
| `#374151` | `color:` 较强次要文本 | `var(--color-text-secondary)` |
| `#111827` / `#06080d` / `#111` | `color:` 主文本 | `var(--color-text)` |
| `#b91c1c` | 红色文本/图标 | `var(--color-danger)` |
| `#047857` | 绿色文本/图标 | `var(--color-success)` |
| `#3b82f6` | 蓝色描边/文本 | `var(--color-info)` |
| `#fef2f2` | 红色弱底 | `color-mix(in srgb, var(--color-danger) 10%, transparent)` |
| `#ecfdf5` | 绿色弱底 | `color-mix(in srgb, var(--color-success) 10%, transparent)` |
| `#d1d5db` / `#e5e7eb` | `border:` / `border-color:` | `var(--color-border-hover)` / `var(--color-border)`(#e5e7eb 较浅用后者) |
| `#9ca3af`(border-color 语境,L460) | 边框 | `var(--color-border-hover)` |
| `#f9fafb` | 弱底/hover 底 | `var(--color-surface-hover)` |
| `#665efd`(SVG stop-color) | 渐变主色 | `style="stop-color: var(--color-primary)"`(**必须**用 style 写法,属性写法不吃 CSS 变量) |

**做法**:
1. 逐文件按映射表替换;每个文件先 `grep -n '#[0-9a-fA-F]' <file>` 列全,逐一消灭,不确定语义的按「color 属性→text 族、border→border 族、background 弱底→color-mix 派生」就近归类。
2. 同值多处的(如 #d1d5db)用编辑器全局替换该文件内全部出现。

**边界**:
- **禁止新造 token**(不去 style.css / DESIGN.md 加变量);禁止把映射表外的值换成猜测的 token——拿不准就停,按报告格式上报。
- 不改任何结构/类名/布局,只换颜色值与 SVG stop 写法。
- 不动 `DESIGN.md`、`src/ops/DESIGN.md`。

**验收**:
- `grep -rn '#[0-9a-fA-F]\{3,8\}' src --include='*.vue' | grep -v 'var('` 期望 **0 行**。
- `npx vitest run src/components src/layouts src/views` 期望全绿。
- `npm run build` 期望成功。

**完成后按此格式报告**:(同卡 A 格式,另附每个文件的替换计数)

### 卡 C:vite manualChunks 扩充
**背景**:框架运行时已单独成块,但 marked/dompurify/qrcode 三个库会跟着某个视图 chunk 走,导致无关页面互相拖缓存。拆成独立 vendor 块后,业务发版时这些库命中浏览器缓存。

**改动文件**:
- 修改 `vite.config.ts`(仅 manualChunks 块)

**开始前检查**:`grep -n "marked\|dompurify\|qrcode" package.json` 确认三者均在 dependencies(已核实:marked ^18.0.9 / dompurify ^3.4.13 / qrcode ^1.5.4)。

**参考代码**(现状):
```ts
        manualChunks: {
          vue: ['vue', 'vue-router', 'pinia'],
        },
```

**做法**:改为(注释保留原意并补一句):
```ts
        // 框架运行时 + 重型三方库单独成块:业务代码发版时浏览器仍命中缓存的 vendor chunk。
        // 内容哈希文件名由 nginx 的 immutable 一年缓存策略承接(见 nginx.conf)。
        manualChunks: {
          vue: ['vue', 'vue-router', 'pinia'],
          markdown: ['marked'],
          sanitizer: ['dompurify'],
          qrcode: ['qrcode'],
        },
```

**边界**:不加其他包;不改 input/server 配置。

**验收**:
- `npm run build` 成功。
- `ls dist/assets | grep -c -E '^(markdown|sanitizer|qrcode)-'` 期望 ≥3(哈希文件名带块名前缀)。
- `ls dist/assets | grep -c '^vue-'` 期望 ≥1(vue 块不回归)。

### 卡 D:Jacoco 覆盖率阈值门禁
**背景**:Java 侧覆盖率只出报告无门禁(前端已有 80% changed-coverage)。本卡按「先度量后设门」补齐:各模块阈值 = 基线指令覆盖率下取整 − 5pp,下限 30%。

**改动文件**:
- 修改 `platform-java/build.gradle.kts`(仅第 3.3 节所述 jacoco 配置块)

**开始前检查**:在 `platform-java/` 目录,`JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home ./gradlew help` 成功。

**做法**(四步,顺序不得颠倒):
1. **跑基线**(全量测试约 45 分钟,勿中断):
```bash
cd platform-java && JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home ./gradlew test jacocoTestReport
```
2. **解析基线**(仓库根执行):
```bash
for f in $(find platform-java -path '*/build/reports/jacoco/test/jacocoTestReport.xml'); do
  python3 - "$f" <<'PY'
import sys, xml.etree.ElementTree as ET
root = ET.parse(sys.argv[1]).getroot()
c = [e for e in root if e.tag == 'counter' and e.get('type') == 'INSTRUCTION']
if c:
    m, cv = int(c[0].get('missed')), int(c[0].get('covered'))
    if m + cv > 0:
        print(f"{sys.argv[1].split('/build/')[0].split('/')[-1]}  {cv * 100 // (m + cv)}%")
PY
done
```
只记录「有报告且 INSTRUCTION 总数 > 0」的模块;无测试的纯库模块跳过(不登记)。
3. **写入门禁**:在 `build.gradle.kts` 现有 jacoco 配置块(`tasks.matching { it.name == "test" }…finalizedBy` 那段)之后追加:
```kotlin
    // 任务书 #67:覆盖率阈值门禁(先度量后设门)。阈值=基线指令覆盖率下取整-5个百分点,
    // 下限 30%;升阈值需另批。未登记的模块不设门(新模块入库时留待下一轮基线)。
    val jacocoInstructionFloor = mapOf(
        // "finance-service" to 0.55,  // ← 第 2 步基线实测后按公式填,示例值仅为格式
    )
    tasks.withType<JacocoCoverageVerification>().configureEach {
        val floor = jacocoInstructionFloor[project.name] ?: return@configureEach
        violationRules {
            rule {
                limit {
                    counter = "INSTRUCTION"
                    value = "COVEREDRATIO"
                    minimum = floor.toBigDecimal()
                }
            }
        }
    }
    plugins.withId("jacoco") {
        tasks.matching { it.name == "check" }.configureEach {
            dependsOn(tasks.matching { it.name == "jacocoTestCoverageVerification" })
        }
    }
```
注意:`return@configureEach` 若编译不过(Kotlin DSL 版本差异),改为 `if (floor != null) { … }` 包裹等价写法。map 值按公式:`max(0.30, (基线整数百分数 - 5) / 100.0)`,例如基线 58% → 0.53。
4. **自检阈值真的会拦**:任选一个已登记模块,把阈值临时改成 `1.0`,运行 `JAVA_HOME=… ./gradlew :services:<模块>:jacocoTestCoverageVerification`,期望**失败**(exit 非 0);恢复正确值后重跑,期望成功。

**边界**:
- 不做增量/changed-file 覆盖率(那是前端机制,Java 侧另案)。
- 不配置 exclusion(本批不排除任何类)。
- 不动前端 vitest 阈值。

**验收**:
- `cd platform-java && JAVA_HOME=… ./gradlew test` 全绿(此时 check 已接线 verification)。
- 报告格式回填:map 中每个条目带模块名与最终阈值,并附第 2 步基线输出原文。
- 契约测试 `npx vitest run test/deployment` 仍绿。

### 卡 E:账本 V21——不可变触发器 + 借贷平衡兜底
**背景**:journal/posting 的不可变与平衡目前只靠代码纪律(LedgerService 构造 + assertBalanced),无 DB 兜底;credits 表 2026-08 就有 immutability 触发器先例。本卡把两条护栏下推到数据库,并给测试清理留唯一维护通道。

**改动文件**:
- 新建 `platform-java/services/finance-service/src/main/resources/db/migration/V21__ledger_immutability_and_balance.sql`
- 修改 `platform-java/services/finance-service/src/test/java/com/grassland/finance/aicredits/CreditsPurchaseControllerIT.java`(仅清理段)
- 新建 `platform-java/services/finance-service/src/test/java/com/grassland/finance/ledger/LedgerImmutabilityIT.java`

**开始前检查**:
- 读 `V5__ledger.sql`(表结构)、`V10__ai_daily_quota.sql:150-175`(触发器先例)、`CreditsPurchaseControllerIT.java:25-45`(将被改造的清理段)、`ledger/LedgerAtomicityIT.java` 与 `FinanceItSupport.java`(IT 写法与事务样板)。
- 全仓 `grep -rn "DELETE FROM posting\|DELETE FROM journal" platform-java --include='*.java'`,确认除 CreditsPurchaseControllerIT 外无其他生产/测试 DELETE(若有遗漏,一并改造并在报告注明)。

**参考代码**(CreditsPurchaseControllerIT 现状,L34-37 区域):
```java
db.sql("DELETE FROM posting WHERE journal_id IN (SELECT id FROM journal WHERE operation_id LIKE 'ai-credit-purchase:%')").then().block();
db.sql("DELETE FROM journal WHERE operation_id LIKE 'ai-credit-purchase:%'").then().block();
```

**做法**:
1. 新建迁移,**SQL 全文照抄**(不得改动):
```sql
-- V21__ledger_immutability_and_balance.sql
-- 任务书 #67:账本 DB 级兜底(对齐 credits 表 V10 trg_credits_quota_transaction_immutable 先例)。
-- 1) journal/posting 仅允许追加:UPDATE/DELETE 一律拒绝,维护通道 = ledger_maintenance_delete_journals()。
-- 2) 每笔 journal 借贷合计为零在 COMMIT 时校验(DEFERRABLE 约束触发器;
--    LedgerRepository.postJournal 不自启事务、journal 头与全部 posting 由调用方单事务写入,
--    因此 COMMIT 时点校验安全——这是服务层 assertBalanced 的 DB 兜底)。
-- 幂等:全部 CREATE OR REPLACE / DROP TRIGGER IF EXISTS,Flyway 重放安全。

CREATE OR REPLACE FUNCTION reject_ledger_mutation() RETURNS trigger AS $$
BEGIN
    IF coalesce(current_setting('grassland.ledger_mutation_allowed', true), 'off') <> 'on' THEN
        RAISE EXCEPTION 'ledger journal/posting is append-only (HLD §6.4); use a reversal journal or ledger_maintenance_delete_journals()';
    END IF;
    IF TG_OP = 'DELETE' THEN
        RETURN OLD;
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS trg_journal_immutable ON journal;
CREATE TRIGGER trg_journal_immutable
    BEFORE UPDATE OR DELETE ON journal
    FOR EACH ROW EXECUTE FUNCTION reject_ledger_mutation();

DROP TRIGGER IF EXISTS trg_posting_immutable ON posting;
CREATE TRIGGER trg_posting_immutable
    BEFORE UPDATE OR DELETE ON posting
    FOR EACH ROW EXECUTE FUNCTION reject_ledger_mutation();

CREATE OR REPLACE FUNCTION assert_journal_balanced() RETURNS trigger AS $$
DECLARE
    debits bigint;
    credits bigint;
BEGIN
    SELECT coalesce(sum(amount_cents) FILTER (WHERE direction = 'DEBIT'), 0),
           coalesce(sum(amount_cents) FILTER (WHERE direction = 'CREDIT'), 0)
      INTO debits, credits
      FROM posting
     WHERE journal_id = NEW.journal_id;
    IF debits <> credits THEN
        RAISE EXCEPTION 'journal % unbalanced at commit: debit % <> credit %', NEW.journal_id, debits, credits;
    END IF;
    RETURN NEW;
END;
$$ LANGUAGE plpgsql;

DROP TRIGGER IF EXISTS trg_posting_journal_balanced ON posting;
CREATE CONSTRAINT TRIGGER trg_posting_journal_balanced
    AFTER INSERT ON posting
    DEFERRABLE INITIALLY DEFERRED
    FOR EACH ROW EXECUTE FUNCTION assert_journal_balanced();

-- 维护/测试通道:单语句内开事务局部开关(set_config 第 3 参 true=事务局部,
-- 同语句内行级触发器可见)后按 operation_id 前缀删除。仅供 IT 清理与运维排障;
-- 业务代码禁止调用——业务路径的 UPDATE/DELETE 被触发器默认拦死。
CREATE OR REPLACE FUNCTION ledger_maintenance_delete_journals(p_operation_prefix text)
RETURNS integer AS $$
DECLARE
    deleted_postings integer;
BEGIN
    PERFORM set_config('grassland.ledger_mutation_allowed', 'on', true);
    WITH doomed AS (
        DELETE FROM posting
         WHERE journal_id IN (SELECT id FROM journal WHERE operation_id LIKE p_operation_prefix)
        RETURNING 1
    )
    SELECT count(*) INTO deleted_postings FROM doomed;
    DELETE FROM journal WHERE operation_id LIKE p_operation_prefix;
    RETURN deleted_postings;
END;
$$ LANGUAGE plpgsql;
```
2. 改造 `CreditsPurchaseControllerIT` 清理段:两句裸 DELETE 换成一句
```java
db.sql("SELECT ledger_maintenance_delete_journals('ai-credit-purchase:%')").then().block();
```
(变量名以文件实际为准;保留该清理段原有用途注释并补一句「走 V21 维护通道,裸 DELETE 已被触发器拦截」。)
3. 新建 `LedgerImmutabilityIT`(参照 `LedgerAtomicityIT` 的事务与 db client 用法,继承 `FinanceItSupport`),四个用例,operation_id 统一用 `it-immutable:<uuid>:` 前缀:
   - `UPDATE posting SET memo = 'x' …` 期望异常,异常链(沿 getCause() 逐层)中某层消息含 `append-only`;
   - `DELETE FROM journal WHERE …` 期望异常含 `append-only`;
   - `SELECT ledger_maintenance_delete_journals('it-immutable:%')` 执行后对应行消失、无异常;
   - 事务内插入不平衡 journal(journal 头 + 仅 1 条 DEBIT posting)→ **commit 时**期望异常含 `unbalanced`(Deferred 触发器在 COMMIT 触发;断言要沿异常链找消息,若 r2dbc 包装后消息不可达,退而在用例末断言「该 journal 查询不到(事务已回滚)」并同时保留异常类型断言)。
   - 每个用例收尾用维护通道清理自己的前缀数据(@AfterEach)。

**边界**:
- 不改任何生产 Java 代码(LedgerService/LedgerRepository 一行不动)。
- 不登记 `released-migrations.sha256`(只登记已发布环境的迁移;V21 由未来发布流程登记)。
- 不给 journal 表加列。
- 若某既有 finance IT 因触发器意外红:先判断是否它也在裸 DELETE/UPDATE 账本行——是则按本卡方式改造并在报告注明;不是则停,按格式上报。

**验收**:
- `cd platform-java && JAVA_HOME=… ./gradlew :services:finance-service:test` 全绿(含新 IT 与改造后的 CreditsPurchaseControllerIT)。
- `npx vitest run test/deployment` 全绿(迁移校验不回归)。
- `ls platform-java/services/finance-service/src/main/resources/db/migration/ | grep V21` 期望命中且仅一个文件。

### 卡 F:TaskController 拆分——提取 admin review 块
**背景**:TaskController 1066 行超拆分红线。L457-593 的「任务内容审核(GL-P2-ADMIN-003 全审政策)」块是自洽子域(治理台专用端点),整体搬出即可降到 ~915 行,行为零变更(照 ApplicationController 五服务化先例)。

**改动文件**:
- 修改 `platform-java/services/marketplace-service/src/main/java/com/grassland/marketplace/taskcatalog/TaskController.java`
- 新建 同包 `TaskReviewAdminController.java`

**开始前检查**:卡 D 已完成;通读 TaskController.java L457-593,列出该块引用的全部字段/依赖/records(开始前检查的一部分是把这个清单写进报告)。

**做法**:
1. 新建 `TaskReviewAdminController`,把 L457-593 的五个端点原样搬入:`GET /api/admin/tasks/review`、`GET /api/admin/tasks/{id}/review/history`、`GET /api/admin/tasks/review/stats`、`POST /api/admin/tasks/{id}/review/approve`、`POST /api/admin/tasks/{id}/review/reject`。
2. 搬移规则:**整块搬、逐字符不动**(方法体、注解、javadoc、异常处理、调用的 record/DTO 一并搬;仅类声明与构造函数新写)。该块专用的构造依赖字段随迁;TaskController 与新块共用的依赖两边都保留。
3. TaskController 删除已搬内容后,删除仅被搬移代码使用的 import 与字段;不得删除仍被剩余代码引用的任何东西。
4. 格式化:在 platform-java 目录 `JAVA_HOME=… ./gradlew :services:marketplace-service:spotlessApply`(ratchetFrom 只碰新文件与改动文件)。

**边界**:
- 路由路径、方法签名、权限/角色注解、响应结构、状态码一律不变——这是行为零变更卡,现有 IT 是唯一裁决。
- 不拆其他块;不改 service/repository 层。
- 两文件最终行数均应 <950,若不满足,报告实际行数即可,不得为凑行数再拆别的块。

**验收**:
- `cd platform-java && JAVA_HOME=… ./gradlew :services:marketplace-service:test spotlessCheck` 全绿。
- `grep -rn "api/admin/tasks/review" platform-java/services/marketplace-service/src/main/` 期望五条路由全部仍在(分布于新文件)。
- `wc -l TaskController.java TaskReviewAdminController.java` 记入报告。

### 卡 G:MediaController 拆分——提取 KYB 族端点
**背景**:MediaController 959 行。KYB 上传/留存端点族(kyb-upload-tickets、kyb-retentions×3、kyb-metadata、kyb-download-url,约 L150-300)是商家认证专属子域,整体搬出后主文件 ~810 行。

**改动文件**:
- 修改 `platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/media/MediaController.java`
- 新建 同包 `KybMediaController.java`

**开始前检查**:卡 D 已完成;通读 MediaController,列出 KYB 族七个端点及其专用依赖/records 清单。

**做法**:与卡 F 相同的整块搬移规则——七个 KYB 端点(`POST /kyb-upload-tickets`、`POST /{id}/kyb-retentions`、`PUT /{id}/kyb-retentions/{referenceId}`、`DELETE /{id}/kyb-retentions/{referenceId}`、`GET /{id}/kyb-metadata`、`GET /{id}/kyb-download-url`)逐字符搬入 `KybMediaController`;共用依赖两边保留;`spotlessApply`。

**条件步骤**:若搬移后 MediaController 仍 ≥950 行(以实际为准),再把 `avatar-metadata / avatar-download-url / brand-logo-upload-tickets / brand-logo-url` 四端点搬入新建 `ProfileMediaController.java`(同规则);否则不做此步。

**边界**:同卡 F(行为零变更;类级 @RequestMapping 前缀保持与原路由拼接结果一致——先确认原类的 RequestMapping 前缀并在两个新类上原样保留)。

**验收**:
- `cd platform-java && JAVA_HOME=… ./gradlew :services:intelligence-service:test spotlessCheck` 全绿。
- `grep -rn "kyb-" platform-java/services/intelligence-service/src/main/java/com/grassland/intelligence/media/` 端点全部仍在。
- `wc -l` 两(或三)文件记入报告。

### 卡 H:ImageAnalysisView 拆分——script 区块提取 4 个 composable
**背景**:ImageAnalysisView 1818 行是全仓最大 Vue 文件。模板约 577 行不动,script 里四块自洽状态按 GrasslandWorkbench composable 先例提取,目标 <1300 行,行为零变更。

**改动文件**:
- 修改 `src/views/image/ImageAnalysisView.vue`(仅 script setup 区)
- 新建 `src/views/image/composables/useFeishuCredentials.ts`
- 新建 `src/views/image/composables/useSessionVersions.ts`
- 新建 `src/views/image/composables/useImagePreview.ts`
- 新建 `src/views/image/composables/useImageUpload.ts`

**开始前检查**:`npx vitest run src/views/image` 现状全绿(记录用例数);通读 ImageAnalysisView.vue L578-1000,对照下方清单确认函数/状态归属。

**提取清单(定死;行号为锚点,允许小漂移,以名字为准)**:
1. `useFeishuCredentials`:`analysisSettings / showFeishuConfig / feishuAppId / feishuAppSecret / feishuFolderToken / feishuSaveError / savingFeishu / feishuSecretSaved / feishuConfigured / toggleFeishuConfig / submitFeishuCredentials / handleExportToFeishu`(L598-720 区域 + L811)。入参:无(内部 `useAnalysisSettings()`);返回同名 ref/computed/函数。
2. `useSessionVersions`:`sessionVersions / selectedVersionId / versionLabelForStage / saveVersionSnapshot / selectVersion / removeVersion` 与 `watch(generationStage, …)`(L747-800)。入参:`generationStage` Ref 与保存快照所需的现有上下文参数(以实际函数签名缺什么传什么,保持逻辑逐字符不变)。
3. `useImagePreview`:`previewIndex / previewImage / closePreview`(L744, L803-810)。
4. `useImageUpload`:`isDragging / uploadError / fileInput` 与同区块的拖拽/文件选择处理函数(L736-739 起,搬该函数族中**只被上传交互使用**的部分;与生成流程耦合的留在主文件)。

**做法**:
1. 每个 composable 一文件,函数体逐字符搬移,不改逻辑、不改名;组件内改为 `const { … } = useXxx(…)` 解构,**模板绑定名不变**。
2. TypeScript 类型(如 `SessionVersion`、`GenerationStage`)随迁或从原 import 处保留导出,确保 `npm run typecheck` 过。
3. 拆完后主文件按原相对顺序摆放解构调用,避免初始化顺序问题(watch 依赖的 ref 先建)。

**边界**:
- 模板(L1-577)与样式区一律不动。
- 不抽「生成流程」状态(generationStage/result/steps 等留在主文件)。
- `ImageAnalysisView.test.ts` **零改动**必须全绿——这是行为零变更的裁决。

**验收**:
- `npx vitest run src/views/image` 全绿且用例数与开始前一致。
- `npm run typecheck`、`npm run build` 成功。
- `wc -l src/views/image/ImageAnalysisView.vue` < 1300,记入报告。

### 卡 I:GrasslandWorkbench 重资产卡片异步分包
**背景**:GrasslandWorkbench 是全站最大业务 chunk(≈345KB),因为它静态 import 了全部 30+ 卡片组件,而多数卡片只在非默认页签渲染。改为 defineAsyncComponent 懒加载即可拆小,不动模板结构。

**改动文件**:
- 修改 `src/views/grassland/GrasslandWorkbench.vue`(仅 import 区)
- 修改 `src/views/grassland/GrasslandWorkbench.test.ts`(**仅允许**追加 `await flushPromises()` 与相应 import,断言一律不动)

**开始前检查**:卡 C 已完成;`npm run build` 后记录 `ls -S dist/assets/*.js | head -5` 现状(报告基线);通读 GrasslandWorkbench.test.ts,标记 mount 后立即断言 DOM 的用例(它们可能需要 flushPromises)。

**做法**:
1. 把下列**6 个**重资产、非默认页签卡片改为 `defineAsyncComponent(() => import('…'))`(其余 import 保持静态,不做扩大化):
   - `MerchantTaskForm`(../…/components/MerchantTaskForm.vue,任务表单抽屉)
   - `BusinessAnalyticsPanel`(经营分析)
   - `OrgCreationAuditPanel`(组织创作审计)
   - `RecommenderHistoryCard`(历史任务)
   - `RecommenderIncomeStatsCard`(收入统计)
   - `RecommenderShareCard`(推广分享)
2. 从 `vue` 的 import 中补 `defineAsyncComponent`。
3. 跑 `npx vitest run src/views/grassland`:若异步导致断言时组件未挂载,**只允许**在对应用例 mount 之后补 `await flushPromises()`(从 `@vue/test-utils` 导入;文件未有则加 import)。断言、用例数、描述串一律不动;若有用例补了 flushPromises 仍红,停下来按格式上报,不得改断言。

**边界**:
- 只改这 6 个;不提取模板区块、不外移样式(行数拆分另批,见 §2 范围外)。
- 不改 ops 侧任何文件。

**验收**:
- `npx vitest run src/views/grassland` 全绿,用例数不变;报告注明补 flushPromises 的用例清单(若有)。
- `npm run build` 成功,且 `ls -S dist/assets/*.js | head -3` 中 GrasslandWorkbench 主 chunk < 250KB(vendor 的 vue-*.js 除外);新旧 chunk 体积对比记入报告。

## 7. 集成验收(全部卡完成后执行)
- 仓库根:`npm run lint`、`npm run typecheck`、`npx vitest run`、`npm run build` —— 全绿。
- `npx vitest run test/deployment` —— 全绿。
- `cd platform-java && JAVA_HOME=/opt/homebrew/opt/openjdk@25/libexec/openjdk.jdk/Contents/Home ./gradlew test spotlessCheck` —— 全绿。
- 复查表(记入交付报告):TaskController / TaskReviewAdminController / MediaController / KybMediaController / ImageAnalysisView / GrasslandWorkbench 各自行数;dist 最大 5 个 chunk 名与体积;jacoco 阈值 map 终值;`grep -rn '#[0-9a-fA-F]\{3,8\}' src --include='*.vue' | grep -v 'var('` 为 0。
- 全程不改任何路由、契约、断言;git diff 里不应出现本任务书 9 卡之外的文件。

## 附 A:返工卡格式(强模型 review 后按此格式产出,编号 R-1、R-2…)

### 返工卡 R-1(针对卡 X)
**问题位置**:`<文件:行>`
**期望行为 vs 实际行为**:<一句话对比>
**修复做法**:
1. <步骤化,同任务卡写法>
**验收**:运行 `<命令>`,期望 `<…>`

## 附 B:非 Qoder 事项(总控/用户处理,Qoder 禁止执行)

1. **git 历史剥离 64MB 截图**:`gui-redesign/`(32 个 PNG)+ `gui-test-screenshots/`(72 个文件)已被 git 跟踪。步骤:确认无引用 → .gitignore 加 `/gui-redesign/`、`/gui-test-screenshots/` → `git rm -r --cached` 提交 → (可选,需用户拍板时点)`git filter-repo --path gui-redesign --path gui-test-screenshots --invert-paths` 重写历史 + force push + 各克隆重拉。历史重写影响所有克隆,须协调。
2. **根目录留档产物**:`.gitignore` 注释已声明「任务书浏览器实测产物……留磁盘留档不入库」,**这是有意设计**。19 个 smoke/verify/snap-*.mjs + 2 个 dbg-*.png + 8 个 bt-shots-* 目录是否清盘待用户拍板;若拍板清,直接 `rm` 即可(已核实全仓零引用)。
3. **陈旧 worktree**:`.claude/worktrees/` 内有旧 agent worktree(含已删除的 VIDEO_ANALYSIS env 与 docker-compose 备份)。`git worktree list` 核对后 `git worktree prune` + 删除对应目录。
