# 草场（Grassland）技术架构详解

> 本文档由 2026-08-19 的一次代码级架构梳理整理而成，所有关键论断均核对过真实代码/配置/DDL，
> 数字以本次实测为准（`项目速览.md` 中部分规模数字已滞后：前端现为 13 视图模块 / 54 composable / 267 文件，
> Java 侧 942 主源文件 / 409 测试文件，各服务迁移版本见下表）。
> 权威设计真相源仍是 `docs/`（HLD、PRD、ADR）；本文是"代码实际怎么实现的"的详细注解。

---

## 1. 项目定位

**草场（Grassland）**：商家 × 推荐官的**种草推广任务撮合平台**，内置 AI 内容创作中心 + 到店核销消费闭环。

两条 Sandbox 闭环已跑通：

1. **任务撮合**：发任务 → 报名 → 预留 → 履约 → 核验 → 确认 → 争议 → 结算 → 钱包
2. **到店消费**：推广 → 套餐 → 支付 → 核销 → 分账/退款 → 评价

真实资金链路被 **D-01 门禁**阻塞（真实 PSP/存管/合规未接入），禁止上线。

---

## 2. 总体拓扑

```
浏览器 (Vue 3 SPA)
   │  dev: :5173 代理 /api；prod: Nginx → edge-bff:8080
   ▼
edge-bff :8080 ── 唯一公网入口，RouteManifest 路由（95 条 EDGE_ROUTE_* flag）
   │  fail-closed 404：未登记 / method 不匹配 / flag=false 一律拒绝
   ├──▶ identity-service      :8082（管理口 9082）
   ├──▶ marketplace-service   :8083
   ├──▶ finance-service       :8084
   ├──▶ trust-service         :8085
   └──▶ intelligence-service  :8086

基础设施：PostgreSQL（逻辑隔离）· Kafka 4.2.1(KRaft) · Redis · MinIO/S3
         · Temporal 1.37 · Prometheus / Alertmanager / Grafana
```

服务间另有 **15 条内部 WebClient 直连**（不经 edge，签名服务断言），以及 **Kafka 事件流**（见 §7）。

## 3. 技术栈

| 层 | 技术 |
|---|---|
| 前端 | Vue 3.5 + Vite + TypeScript + vue-router + Pinia；13 视图模块 / 54 composable / 267 文件；Vitest + Playwright |
| 后端 | Java 25 + Spring Boot 4.1 + WebFlux/R2DBC（全响应式），Gradle Kotlin DSL 多模块；942 主源文件 / 409 测试文件 |
| 异步 | Kafka(KRaft) + Transactional Outbox/Inbox + Temporal 1.37 Saga（共 6 个） |
| 存储 | PostgreSQL 逻辑隔离（每服务独立 Flyway 目录与历史表）+ Redis + MinIO/S3 |
| 加密/媒体 | platform-crypto（KEK 信封加密）+ Java Playwright（抖音/B站解析）+ FFmpeg |
| 部署 | docker-compose 三套（默认 / observability / production） |

**Express/Node 后端已完全移除**（JBE-01~08）。Node 仅用于前端构建/测试、E2E seed、覆盖率/密钥扫描，
以及 intelligence 容器内 Java Playwright 上游要求的 driver。不存在 Node HTTP 服务或领域 Worker。

## 4. 六服务 + 四共享库

**`platform-java/` Gradle 多模块**（`services/` + 共享库）：

| 服务(:端口) | 最新迁移(主迁移数) | 核心职责 |
|---|---|---|
| `edge-bff`(:8080) | — | 唯一公网入口、RouteManifest（514 行）、Cookie/Session 解析、按上游分钥签发身份断言、移动端 access token 验签、SSE/Multipart/Range 零聚合透传 |
| `identity-service`(:8082) | V38 (38) | 账号/会话/活动身份/组织/门店/成员/权限审核/KYB(OCR+信封加密)/邀请/通知中心/邮件outbox/RBAC(7角色)/移动端token；32 controller |
| `marketplace-service`(:8083) | V44 (45) | 任务版本/报名/履约凭证/Verification v2/商家确认/核销/消费订单/声誉Lv1-5/确定性推荐匹配与邀请/运营处置/分析归因；5 个 Temporal saga |
| `finance-service`(:8084) | V19 (19) | 双录账本(journal/posting)/escrow/积分/provider operation/Webhook Inbox/对账/推荐官钱包/提现 |
| `trust-service`(:8085) | V12 (13) | 争议/7人审判/平票重开/上诉/客服MFA终审/等级投影/风控信号/证据脱敏；1 个 Temporal saga |
| `intelligence-service`(:8086) | V35 (35) | AI 控制面(Run/BYOK/平台模型)/抖音B站媒体/创作上下文快照/素材库/创作助手/草稿/视频生成/内容安全词库；33 controller |

**共享库**：

- `platform-storage` — S3/MinIO 封装，对外只发短时签名 URL
- `platform-identity-assertion` — 断言 + access token（5 服务依赖；Redis 防重放）
- `platform-crypto` — KEK 信封加密（KYB/BYOK 共用 `CRYPTO_KEK_BASE64`）
- `platform-http` — 服务间 WebClient 连接池、超时、响应体治理

## 5. 路由归属速查（`/api` 前缀 → 服务）

- **identity**：`/api/auth/*`、`/api/me`、`/api/organizations`、`/api/recommenders`、`/api/admin/{users,adjust-credits,permission-requests,kyb-requests,recommender-requests}`
- **marketplace**：`/api/tasks`（含 recommendations）、`/api/analytics`、`/api/v2`（消费）、`/api/ops`、`/api/reputation`
- **finance**：`/api/finance`、`/api/credits`
- **trust**：`/api/trust`
- **intelligence**：`/api/{ai,settings,homepage,media,content-assets,creation-*,comedy-generation,douyin,bilibili,article-generation,video-production,video-recreation,image-analysis}` 等

真相源：`platform-java/services/edge-bff/src/main/resources/application.yml`（每条路由带 `EDGE_ROUTE_*` flag）。

---

## 6. 数据架构：PostgreSQL

### 6.1 database-per-service 的"逻辑隔离"实际落地

**模式含义**：每个微服务排他拥有自己的数据，跨服务取数只能走内部 API 或事件投影；
数据库是服务私有实现的一部分。**逻辑隔离** vs 物理隔离：物理隔离是每服务独立实例/独立库+独立账号；
逻辑隔离是共用物理存储，边界靠机制和纪律维持。

**本项目现状**（HLD 6.1 目标与代码实测）：

- HLD 6.1：初期可共用 PostgreSQL Cluster，但要求独立逻辑库、独立账号、独立 Flyway 历史
  （目标库划分 `identity_db` / `marketplace_db` / `finance_db` / `trust_db` / `intelligence_db`）
- **实际**：五个服务共用一个 `DATABASE_URL` → 同一物理库、同一 `public` schema
  （`DatabaseSchemaVerifier` 全部按 `public.<table>` 校验）。隔离靠三件事：
  1. **各服务独立 Flyway 历史表**（见 6.2）
  2. **HLD 6.2 跨服务规则**：引用只用 UUID、禁止跨服务 FK/JOIN、禁止共享 ORM Entity；
     跨服务读走 BFF 组合 / 内部 WebClient / Kafka 本地投影（投影记 `source_event_id` 等溯源列）
  3. **登记的例外**：edge-bff 直读 identity 的 `session` 表签发内部断言（只读，同 `DATABASE_URL`）

**收益**：单库省运维；Flyway 历史独立保住 schema 独立演进；将来拆物理库 = dump 各自表 + 换 `DATABASE_URL`。
**代价**：边界是约定级（同库同账号理论上能跨查），HLD 要求的独立账号是未来收紧项。

### 6.2 Flyway：机制详解

**基本方式**：每个服务在 `src/main/resources/db/migration` 放手写的版本化 SQL（`V<n>__描述.sql`），
启动时 Flyway 与历史表对账，只执行未记录的版本。

**为什么共库必须各持历史表**：五服务共用物理库，若都用默认 `flyway_schema_history`，
版本号互相冲突（identity 的 V10 会挡住 marketplace 的 V10，或 checksum 不匹配直接启动失败）。
实际配置：

| 服务 | 历史表 |
|---|---|
| identity | `flyway_schema_history`（默认名，最早迁入 legacy 库） |
| marketplace | `marketplace_flyway_schema`（`MarketplaceDataSourceConfig:54` `.table(...)`） |
| finance | `finance_flyway_schema` |
| trust | `trust_flyway_schema` |
| intelligence | `intelligence_flyway_schema` |

**命名规则**：`V<版本号>__<描述>.sql`——大写 V（区分大小写）、版本号整数（本项目不用小数）、
**双下划线**分隔（单下划线解析错）、小写 snake_case 描述、`.sql`。
项目附加约定：出处/决策写在文件头注释（发布后注释也不可改）；全仓 **0 个 `R__` 可重复迁移**，纯版本式。

**checksum 算法**（实测自本项目 Flyway 12.4.0，`ChecksumCalculator`）：
CRC-32 逐行喂入（`readLine` 剥行尾符、跳过空行、去 BOM、UTF-8 字节），强转有符号 int 存库。

| 变体 | 影响 |
|---|---|
| LF ↔ CRLF、空行增删、末尾换行 | **不变** |
| 注释内容、行内空格、大小写 | **变**（所以手册要求"字节级不可变，包括注释"） |

**触发时机三场景**：
1. 本地/compose：Spring 容器启动期间（`@Bean(initMethod="migrate")`，条件 `*.datasource.from-database-url=true`），
   先于服务流量；失败即拒启（fail-fast）。纯 R2DBC 应用需手写 Flyway bean（FlywayAutoConfiguration 不激活）
2. 集成测试：Testcontainers 单例容器 + 测试上下文启动（"生产同款 DataSourceConfig"）；
   另有专门迁移重放测试（`DatabaseBootstrapMigrationTest` 等）从空库重放全链——因此新迁移一律幂等（`IF NOT EXISTS`）
3. 生产：独立 Release Job 执行，应用启动不竞争；`scripts/production-release.sh` 门禁校验
   `platform-java/deploy/released-migrations.sha256`（已发布迁移字节级不可变）+ 要求
   `MIGRATION_COMPATIBILITY_APPROVED=yes`（expand/contract 评审确认）

### 6.3 database-bootstrap：共享基线表 + Schema 校验

**背景**：`app_users` / `session` / `user_settings` / `email_verification_codes` 四张 legacy 共享表
不专属任何新服务但多服务共用（identity 读写 app_users、edge 直读 session、intelligence 读写 user_settings），
需要一个独立责任方。

**实现**：one-shot 容器（compose 注释："the only empty-database prerequisite for Java services"）：

- 自有 Flyway：目录 `classpath:db/bootstrap`、历史表 `database_bootstrap_flyway_schema`、全部 `IF NOT EXISTS`
- `ApplicationRunner`（`DatabaseBootstrapApplication:50`）：等 Postgres（重试 30 × 2s）→ `flyway.migrate()` → `verifier.verify()`
- **Verifier 三层校验**（`DatabaseSchemaVerifier:16-62`）：逐列（名/类型/可空）、逐约束
  （pkey / unique(email) / FK on delete cascade / check）、漂移探测（连 `app_users.role` 默认值都查）
- 完成后 JVM 退出；compose 中领域服务等 `service_completed_successfully`（成功退出码 0）才启动

| 场景 | Flyway | Verifier |
|---|---|---|
| 空库首启 | V1/V2 真执行建表 | 全量校验 |
| 之后启动 | 历史表有记录，脚本不跑 | **照常全量校验**（每次体检） |
| legacy 库 | IF NOT EXISTS 跳过已存在表 | 逐列校验，漂移即失败退出 |

### 6.4 强一致性的落地（四层机制）

**分工原则**：系统里只有 PostgreSQL 提供事务（ACID），所有"错了不能挽回"的状态都住在 Postgres，
不变量下推到数据库层强制执行，不依赖应用代码自觉。

1. **事务边界**：journal 头 + posting 明细 + 余额投影 + Outbox 事件行**同一事务**落库
2. **约束下推**（`finance V5__ledger.sql` 实例）：
   - `amount_cents CHECK (amount_cents > 0)`、`direction CHECK (IN ('DEBIT','CREDIT'))`
   - `posting.journal_id REFERENCES journal(id)`
   - **幂等网**：`CREATE UNIQUE INDEX ON journal(operation_id) WHERE operation_id IS NOT NULL`
     （Saga 重试同 operation_id 再来 → 撞唯一索引 → 识别重复 → 跳过）
   - 身份域：`app_users_email_key` 唯一（一邮箱一账号）
3. **Trigger 级不可变**（`intelligence V23`）：
   ```sql
   CREATE FUNCTION reject_creation_context_mutation() ... RAISE EXCEPTION 'creation context snapshots are immutable';
   CREATE TRIGGER trg_creation_context_immutable BEFORE UPDATE ON creation_context_snapshot ...;
   ```
   同表 `UNIQUE (account_id, application_id, task_version, platform_id, content_form_id)` 防重复快照
4. **Append-only + 余额可重建**：`LedgerRepository` 通篇只有 INSERT，无 UPDATE/DELETE；
   记错账追加 `REVERSE` journal 对冲；余额行只是投影，真相源是 posting 流水，
   任意时刻 `SUM(credit) - SUM(debit)` 重算（V5 的 OPENING 回填保证迁移起投影可重建）

跨行不变量（借贷合计为零）在应用层事务内断言（`LedgerService:94 assertBalanced`）。

| 领域状态 | 机制 |
|---|---|
| 金融账本 | 单事务多行 + CHECK/唯一索引 + append-only + 投影可重建 |
| 任务版本快照 | 不可变快照表 + 冻结 trigger 拒 UPDATE |
| 身份/KYB | UNIQUE + KEK 信封加密（机密性） |
| 争议审判 | append-only 决议/投票 + 唯一约束 |

---

## 7. 异步事件：Kafka（KRaft）

### 7.1 KRaft 是什么

Kafka 4.2.1 单容器（compose `659-675`）：`KAFKA_PROCESS_ROLES: "broker,controller"`、
controller 走 Raft 自选主——**不再需要 ZooKeeper 伴生系统**，元数据自管理。

### 7.2 Topic 与消费拓扑（`@KafkaListener` 全量核实）

发布：五服务各一条事件流（经 Outbox）+ `identity.mail-outbox` + 各 `.DLT` 死信。

| 消费者 | 订阅 | 用途 |
|---|---|---|
| identity `NotificationEventConsumer` | identity.events | 自有事件 → 通知中心 + 邮件 |
| identity `ExternalNotificationEventConsumer` | marketplace/trust/finance 三条 | 跨域通知（任务状态/争议结果/资金变动） |
| marketplace `TrustEventConsumer` | trust.events | 争议结果回写任务状态 |
| marketplace `OpsDltConsumer` | trust.events.**DLT** | 死信收殓进运营处置台 |
| trust `MarketplaceReputationEventConsumer` | marketplace.events | 声誉/风控信号投影 |
| finance `JudgeRewardEventConsumer` | trust.events | 审判官投票完成 → 积分入账（#31/ADR-D15） |

典型场景：争议审判结束 → trust 发一次事件 → finance 发积分 / marketplace 更新任务 / identity 发通知，
发布方不关心谁在听。

### 7.3 与同步调用的分工

| | 同步 WebClient（15 条） | Kafka 事件 |
|---|---|---|
| 语义 | "现在就要答案"（查授权/验余额/发起争议） | "出了件事你知道一下"（状态通知） |
| 耦合 | 阻塞等待，对方挂自己也挂 | 发布方不知道谁消费 |
| 失败影响 | 直接失败 | 重启后从 offset 继续，不丢 |

### 7.4 Outbox 投递实现（`platform-messaging` 共享库，四服务同构；marketplace 独立实现）

双写问题：DB 提交了但 send 失败 → 事件丢；先 send 后提交 → 幻觉事件。
解法：事件行与业务数据**同事务**写 `*_outbox` 表，独立 relay 异步搬运：

- `@Scheduled` 每 2s，`claimBatch`（随机 claimToken + 5 分钟租约）——多实例不抢单，挂了租约到期重投
- **单聚合有序**：claim SQL `NOT EXISTS (... earlier unpublished ...)`——同 aggregate 只认领最老事件，前一条没发后一条不发（FIFO）
- 指数退避重试 + ack 超时（10s）
- Micrometer 指标：积压量、最老未发年龄（`grassland.outbox.oldest.pending.age`）、成功/失败计数

**代码组织（2026-08-20 下沉）**：identity/finance/trust/intelligence 四份逐字相同的
`EventEnvelope/OutboxRepository/OutboxPublisher/OutboxProperties` 收敛到 `platform-messaging`
共享库；各服务只留薄绑定层（`OutboxProperties` 记录前缀与默认 topic + `OutboxMessagingConfig`
声明表名/调度属性名）。trust 的 `bigserial + jsonb` 历史表结构以 `OutboxSchema` 方言参数吸收，
不改线上表。**marketplace 未并入**：其链路是真分叉（String claimToken、fail-fast 校验、
`latestReservationFailureReason` 等业务查询方法），保持独立实现，迁移需单独立项。

### 7.5 消费侧可靠性

- **Inbox 幂等**：处理过的 `event_id` 记 `identity_inbox` / `marketplace_inbox`，重投跳过
- **手动 ack**（`AckMode.MANUAL`）：业务落库成功才确认
- **DLT**：重试耗尽进 `*.DLT` topic → `OpsDltConsumer` 收殓进 ops_case + 30 天清理 worker → 人工工单

一句话：**PostgreSQL 保证"状态对"，Kafka 保证"状态变了这件事传到"**——不丢（Outbox）、
业务上不重（Inbox + 幂等键）、不堵死（DLT）。

---

## 8. Temporal 1.37：长流程 Saga 编排

Kafka 管"通知"，Temporal 管**带状态、带定时器的长流程**（跨小时/天等待窗口、失败要补偿）。
共 6 个 workflow：

- marketplace 5 个：`ApplicationReservationWorkflow`（报名预留）、`ConfirmationWindowWorkflow`（商家确认窗口）、
  `SettlementWindowWorkflow`（结算窗口）、`SettlementReconciliationWorkflow`（结算对账）、
  `MerchantRejectionReviewWorkflow`（拒绝复核）
- trust 1 个：`DisputeAdjudicationWorkflow`（争议审判：7 人审判/平票重开/上诉完整周期）

对应前端 202 中间态轮询的后端实现（accept/confirm 轮询到 accepted/compensated/settled/held 终态）。

## 9. Redis：安全关键短时状态（不是缓存层）

仅两处用途，均为防护性质：

1. **身份断言防重放**（主用途）：edge 签发的断言/token 一次性消费，`SET NX + TTL` 记指纹；
   `IDENTITY_ASSERTION_REPLAY_STORAGE: redis`，`platform-identity-assertion` 统一实现
2. **游客试用限流**：intelligence `GuestTrialRateLimiter`

## 10. MinIO/S3：对象存储

大文件媒体资产唯一归宿：抖音/B站解析媒体（`douyin-analysis-media` / `bilibili-analysis-media`）、
KYB 证照、创作素材库、视频生产产物。`platform-storage` 统一封装，只发短时签名下载 URL；
`deploy/storage/` 有 service-account 最小权限策略。领域库只存元数据/权限/引用（HLD 6.2）。

---

## 11. 可观测性：Prometheus / Alertmanager / Grafana

配置全部在 `platform-java/deploy/observability/`。

```
六服务(/actuator/prometheus, Micrometer) ──每15s抓──▶ Prometheus ──规则命中──▶ Alertmanager ──▶ webhook
                                                          │
Grafana（7 块预置面板）◀────查询──────────────────────────┘
```

**Prometheus**：6 个抓取 job（edge:9080 / 五服务:9082 管理口）；4 个规则文件，覆盖三层：

| 层 | 规则 |
|---|---|
| 存活 | `GrasslandServiceDown`（up==0 持续 2m，critical） |
| 服务健康 | 5xx 占比 >5% 持续 10m；JVM 堆 >85% 15m；CPU >90% 15m |
| 业务可靠性 | 邮件死信 >0、DLT 积压、Outbox 最老未发年龄、Kafka 消费滞后 |

第三层与 §7 的可靠性机制形成闭环：机制兜业务，指标兜"机制是否正常工作"。

**Alertmanager**（GL-P3-PLATFORM-001）：`group_by [service, alertname]`、group_wait 30s、
group_interval 5m、repeat 4h；唯一 receiver `operations-webhook`，URL 从 secret 文件读（不入库），
`send_resolved: true`。**边界**：webhook 仍是 example 占位，真实值班渠道未接（D 类缺口）。

**Grafana**：`platform-overview.json` 预置面板（服务可用性/请求速率/5xx/JVM 堆/CPU/Outbox 年龄/消费滞后），
datasource 自动供给，密码走 secret 文件；挂在 compose `observability` profile 下，
默认 `docker compose up` 不启动。

---

## 12. 前端架构（`src/`）

- 13 个视图模块：`admin / ai-center / article / comedy / commerce / grassland / home / image / image-gen / moments / ops / video / video-production`
- `App.vue` 顶层 shell：AI 创作中心为默认一级入口，兼容入口收纳
- `composables/`（54 个）：请求与状态逻辑（`useAuth`、`useGrasslandIdentity` 等）
- 关键坑（CLAUDE.md 约定）：请求/响应字段不对称（`type` vs `identityType`）typecheck 抓不到，
  写前端前逐字读后端契约；轮询中间态只读 DOM 别发探测请求

## 13. 部署形态

- `docker-compose.yml`：默认栈（frontend + 六 Java 服务 + database-bootstrap + postgres-local + kafka + redis + minio + temporal + prometheus/alertmanager/grafana[profile]）
- `docker-compose.observability.yml`：可观测性独立编排
- `docker-compose.production.yml`：生产形态（外部 DATABASE_URL/Secret Manager 方向）
- `deploy/security/`：SOPS 安全物化、identity-assertion key pairs；`deploy/storage/`：MinIO service-account

## 14. 关键架构决策速查

1. **Edge fail-closed**：未登记路由一律 404，无回退（`edge.default-upstream: fail-closed`）
2. **身份断言 keyring**：按上游分钥（5 audience）；移动端 token kid + previous-keys 三阶段轮换；Redis 防重放
3. **内部端点 `/internal/*`**：不在 `/api` 树下，拒绝任一 `X-Forwarded-*`（返回 404）；无通用 INTERNAL_API_KEY
4. **金融不可变**：双录 journal/posting，Finalized 只能追加 Reversal，余额可由 Posting 重建
5. **积分闭环**：consume → CreditCharge 句柄 → 上游失败 refund()（键 `refund:<operationId>` 幂等）；用户 abort 不退
6. **创作上下文快照冻结**（PRD §4.12）：进创作冻结版本/规则/素材/AI 配置，DB trigger 拒 UPDATE；
   BYOK 只存元数据不存明文
7. **配置不可篡改**：task_version / package_version / platform_model_config_history / content_asset_version 不可变快照表
8. **Outbox/Inbox**：事务性投递 + 幂等消费 + DLT 人工兜底
9. **内容安全两级**：L1 版本化词库每次生成必跑；L2 语境深检可降级；findings advisory 不硬阻断
10. **identity 集中授权清单**：`IdentityAuthorizationManifest` 构建期覆盖全部 Controller 映射并 fail-closed

## 15. 工程与质量体系

- **测试**：409 Java 测试文件（Testcontainers 全链 IT，本地需 JDK 25 + 2g 堆）+ Vitest + Playwright E2E（`npm run e2e:seed` 幂等种子）
- **文档门禁**：`docs/status.yaml` 机器可读状态源，CI `npm run docs:status` 校验状态与覆盖率
- **发布门禁**：released-migrations sha256 校验、MIGRATION_COMPATIBILITY_APPROVED、SOPS 密钥扫描、cosign/SPDX 框架
- **工具链**：JDK 25（`brew install openjdk@25`）、`./gradlew`、Gradle 测试堆已设 2g

## 16. 当前边界（四类，性质不同）

- **A. 纯代码缺口**：#40 搜索与报表导出；原生 APP / 微信小程序
- **B. 已完成的近期增强**：#38 合规导出、#42 门店媒体库、#43 高级创作、#44 创作追踪、#45 AI 矩阵与安全
- **C. 框架就绪、卡外部渠道**：真实 PSP（D-01，头号阻塞）、官方履约指标源、Seedance/MiniMax 真实凭据、
  真实营销归因 provider、生产 AI 金额结算
- **D. 生产基础设施**：TLS/LB、外部 HA Kafka/Temporal/MinIO、Canary、真实 Alertmanager/OTel 投递、
  灾备演练、Secret Manager 接入

## 17. 常用命令

```bash
docker compose up -d                              # 启动后端栈
docker compose --profile observability up -d      # 加可观测性栈
npm run dev:client                                # 前端 :5173
cd platform-java && ./gradlew :services:edge-bff:bootJar
npm run e2e:seed                                  # E2E seed（幂等，--reset 清空）
npm run test && npm run typecheck && npm run build
```

## 18. 权威文档指针（`docs/`）

- 草场系统技术总体设计（HLD-v0.1）—— 架构基线（§6.1/6.2 数据所有权规则出处）
- 草场产品需求文档（PRD v1.4）
- 草场Java微服务技术架构与渐进迁移方案 —— 迁移蓝图（§343 生产 Flyway Release Job）
- 草场开发进度与续接指南 —— 第四节是 backlog 唯一权威
- 生产发布与灾备运行手册 —— 迁移不可变/expand-contract 门禁出处
- ADR：D-01（PSP，部分采纳）/ D-02 / D-06 / D-07 / D-11 / D-15 / D-16 等
